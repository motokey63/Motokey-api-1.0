---
milestone: v1.5
audited: 2026-07-11T22:00:00Z
status: tech_debt
scores:
  requirements: 6/6
  phases: 3/3
  integration: 4/4
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 21-migrations-r-troactives-mise-jour-schema-sql
    items:
      - "sql/migrations/21_interventions_undocumented_columns.sql documents the niveau_preuve CHECK constraint in a comment but does not apply it in its own ALTER TABLE DDL — the CHECK only exists in schema.sql. If this migration file is ever run standalone against an older bootstrapped environment (its own stated catch-up purpose), that environment ends up missing the constraint prod actually has."
      - "sql/migrations/15_billing_foundation.sql (pre-dates this milestone) was never backported with the billing_events.created_at column discovered and patched into schema.sql during Phase 22. Re-running migration 15 alone against an environment that already has billing_events (without created_at) would not close that gap — same class of issue as the niveau_preuve item above."
  - phase: 22-v-rification-bootstrap-nettoyage-header
    items:
      - "No top-level README.md or .env.example documents the new clone -> bootstrap-fresh-schema.js -> introspect-schema.js --compare verification chain. A fresh developer has no discoverable entry point beyond reading script source or .planning/ phase history."
      - "scripts/bootstrap-fresh-schema.js error message and header comment point to .planning/phases/22-.../22-RESEARCH.md and 'plan 22-02' — these PLAN/RESEARCH files are untracked pre-archival (consistent with the v1.1-v1.4 pattern where they get force-added into milestones/vX.Y-phases/ at milestone closure). Expected to self-resolve once this milestone archives; flagged so it isn't lost."
---

# Milestone v1.5 — Résolution dérive schema.sql — Audit Report

**Audited:** 2026-07-11
**Status:** tech_debt (no blockers, no unsatisfied requirements — accumulated non-critical debt worth a look)

## Milestone Scope

**Goal:** Combler la dérive non documentée découverte en Phase 19 (colonnes/contraintes en prod sur `garages`/`clients`/`interventions`/`devis` sans fichier de migration correspondant), en identifiant l'origine de chaque ajout, avant tout nouveau feature.

**Phases:** 20 (Introspection & Corrélation d'Origine), 21 (Migrations Rétroactives & Mise à Jour schema.sql), 22 (Vérification Bootstrap & Nettoyage Header)

## Phase Verification Summary

| Phase | Status | Score | Report |
|---|---|---|---|
| 20 | passed | 7/7 must-haves | `20-VERIFICATION.md` |
| 21 | passed | 4/4 must-haves | `21-VERIFICATION.md` |
| 22 | passed | 9/9 must-haves | `22-VERIFICATION.md` |

All 3 phases individually verified passed. No missing VERIFICATION.md files (no unverified phases).

## Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | Description | Assigned Phase | Traceability (`[x]`/`[ ]`) | SUMMARY frontmatter | VERIFICATION.md status | Final Status |
|---|---|---|---|---|---|---|
| SCHEMA-02 | Colonnes non documentées identifiées (type/contraintes/nullabilité) | 20 | `[x]` | listed (20-01) | passed / SATISFIED | **satisfied** |
| SCHEMA-03 | Corrélation git de chaque colonne découverte | 20 | `[x]` | listed (20-01) | passed / SATISFIED | **satisfied** |
| SCHEMA-04 | Migrations rétroactives numérotées 20+ avec commentaire d'origine | 21 | `[x]` | listed (21-01) | passed / SATISFIED | **satisfied** |
| SCHEMA-05 | `schema.sql` mis à jour avec colonnes Gap A | 21 | `[x]` | missing on 21-02/21-04 frontmatter | passed / SATISFIED (direct evidence in VERIFICATION.md) | **satisfied** (frontmatter metadata gap, not a real gap — see note) |
| SCHEMA-06 | `billing_events` + objets migration 13 ajoutés à `schema.sql` | 21 | `[x]` | missing on 21-03/21-04 frontmatter | passed / SATISFIED (direct evidence in VERIFICATION.md) | **satisfied** (frontmatter metadata gap, not a real gap — see note) |
| SCHEMA-07 | Bootstrap propre + header nettoyé | 22 | `[x]` | listed (22-01, 22-03) | passed / SATISFIED (live re-run evidence) | **satisfied** |

**Note on SCHEMA-05/SCHEMA-06 frontmatter gap:** Per the 3-source matrix, "passed + missing SUMMARY frontmatter" formally resolves to `partial (verify manually)`. Manual verification was already performed independently by `21-VERIFICATION.md` with direct file-read evidence (schema.sql column-by-column comparison against `20-FINDINGS.md` and source migrations 13/15) — not just a claim. Treated as **satisfied**, with the missing `requirements_completed` frontmatter on `21-02-SUMMARY.md`/`21-03-SUMMARY.md`/`21-04-SUMMARY.md` noted as a bookkeeping gap only.

**No orphaned requirements.** Every REQ-ID in `.planning/REQUIREMENTS.md`'s traceability table (SCHEMA-02 through SCHEMA-07) appears in exactly one phase's VERIFICATION.md Requirements Coverage table, and none are missing from all VERIFICATIONs.

**FAIL gate check:** 0 unsatisfied requirements → milestone does NOT force `gaps_found`.

## Cross-Phase Integration

Spawned `gsd-integration-checker` against all 3 phases with the full SCHEMA-02→07 requirement set. Full findings:

**Wired (4/4 major links):**
1. `20-FINDINGS.md` → `sql/migrations/20_garages_undocumented_columns.sql` / `21_interventions_undocumented_columns.sql` / `22_devis_undocumented_columns.sql` — 39/39 columns traced verbatim (names, types, origin comments), no drift.
2. `sql/migrations/13_*.sql` + `15_*.sql` → `schema.sql` Gap B section — verbatim DDL port confirmed field-by-field.
3. `scripts/introspect-schema.js` `EXPECTED_TABLES` → all 5 Gap B objects — generic compare loop, no special-casing needed.
4. `scripts/bootstrap-fresh-schema.js` → live `schema.sql` (dynamic `fs.readFileSync`, not a stale copy) — confirmed via two independent live re-runs (22-02 execution + 22-VERIFICATION re-run).

**No orphans, no hard breaks.**

**E2E flow (schema lifecycle):** undocumented drift → findings → retroactive migration → `schema.sql` update → bootstrap verification → compare vs prod → header cleanup. Confirmed complete and mechanically working end-to-end (live-verified, not just claimed). This milestone has zero UI/application-route surface — no other E2E flow applies.

**Minor gaps found (see `tech_debt` above):** two migration files that document a constraint/column in a comment without applying it in their own DDL (functional only if run standalone against an older environment — `schema.sql` itself is unaffected and correct), and a missing onboarding doc (README/.env.example) for the new verification chain this milestone introduced.

**Resolved during audit, not a live gap:** the integration checker's report flagged `.planning/PROJECT.md`'s `## Requirements > ### Active` section as still containing a stale unchecked `SCHEMA-07` bullet. This was already corrected in a prior session step (PROJECT.md now shows `✓ SCHEMA-07` under Validated, with no residual `[ ] SCHEMA-07` under Active) — confirmed via direct `grep` re-check during this audit. Not included in `tech_debt` above since it no longer reflects current file state.

## Nyquist Compliance (Discovery Only)

| Phase | VALIDATION.md | `nyquist_compliant` | Classification |
|---|---|---|---|
| 20 | exists | `true` | COMPLIANT |
| 21 | exists | `true` | COMPLIANT |
| 22 | exists | `false` | PARTIAL |

Phase 22 flagged `nyquist_compliant: false` in its VALIDATION.md frontmatter. Discovery only per workflow rules — not auto-actioned here. If desired: `/gsd:validate-phase 22`.

## Gaps Summary

**Critical (blocking):** None.
**Requirements:** 6/6 satisfied, 0 orphaned, 0 unsatisfied.
**Integration:** 4/4 links wired, 0 broken, 0 orphaned exports.
**Flows:** 1/1 (schema lifecycle) complete and live-verified.

**Tech debt (non-blocking, listed in frontmatter above):** 4 items across Phases 21 and 22 — two are "documented but not self-sufficiently applied" constraint/column gaps in standalone migration files (schema.sql itself is correct), two are onboarding-documentation gaps for the new bootstrap-verification tooling this milestone introduced.

## Recommendation

**Proceed to `/gsd:complete-milestone 1.5`.** No blockers exist. The tech debt items are real but narrow in scope (specific migration-file self-sufficiency, missing onboarding docs) and don't affect `schema.sql`'s correctness or SCHEMA-07's actual bootstrap-verification claim. Recommend carrying them into the backlog/next milestone rather than blocking closure on them.

---

*Audited: 2026-07-11T22:00:00Z*
*Auditor: Claude (gsd audit-milestone orchestrator + gsd-integration-checker)*
