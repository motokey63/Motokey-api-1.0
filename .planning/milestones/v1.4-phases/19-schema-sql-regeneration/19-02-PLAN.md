---
phase: 19-schema-sql-regeneration
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified: [schema.sql]
autonomous: true
requirements: [SCHEMA-01]
must_haves:
  truths:
    - "schema.sql includes CREATE TABLE statements for client_device_tokens, push_send_log, and garage_users"
    - "schema.sql includes the moto maintenance-tier columns (migration 18) and the clients UNIQUE(email, garage_id) constraint (migration 19)"
    - "schema.sql's devis status constraint documents the exact live CHECK values (confirmed via pg_get_constraintdef in plan 01), replacing the stale statut_devis ENUM"
    - "schema.sql's header comment explicitly documents it is a known-partial bootstrap (does not cover the ~19 untracked live tables)"
  artifacts:
    - path: "schema.sql"
      provides: "CREATE TABLE garage_users"
      contains: "CREATE TABLE garage_users"
    - path: "schema.sql"
      provides: "CREATE TABLE client_device_tokens"
      contains: "CREATE TABLE client_device_tokens"
    - path: "schema.sql"
      provides: "CREATE TABLE push_send_log"
      contains: "CREATE TABLE push_send_log"
    - path: "schema.sql"
      provides: "motos maintenance-tier columns"
      contains: "last_maintenance_tier_notified"
    - path: "schema.sql"
      provides: "clients UNIQUE(email, garage_id)"
      contains: "clients_email_garage_id_key"
    - path: "schema.sql"
      provides: "devis.statut as TEXT + CHECK (not ENUM)"
      contains: "CHECK (statut IN"
    - path: "schema.sql"
      provides: "known-partial bootstrap header"
      contains: "BOOTSTRAP PARTIEL"
  key_links:
    - from: "schema.sql devis.statut CHECK clause"
      to: "plan 01 pg_get_constraintdef output"
      via: "verbatim copy of allowed values"
      pattern: "CHECK \\(statut IN"
---

<objective>
Regenerate `schema.sql` to reflect the narrow-scope drift from migrations 12/16/17/18/19 plus the devis.statut ENUM→CHECK correction, satisfying ROADMAP Phase 19 success criteria 1–4. This is a FAITHFUL-REPRODUCTION edit, not a refactor.

Purpose: A developer bootstrapping a fresh Supabase project from schema.sql gets the garage-side schema matching prod for migrations 1–19, with the header honestly documenting what it does NOT cover.
Output: Updated `schema.sql`.

SCOPE GUARD (NARROW — locked by Mehdi 2026-07-08):
- IN SCOPE: add tables `garage_users`, `client_device_tokens`, `push_send_log`; add motos migration-18 columns; add clients migration-19 UNIQUE; convert `devis.statut` from `statut_devis` ENUM to TEXT+CHECK; add partial-bootstrap header comment.
- OUT OF SCOPE (do NOT do): reconstructing the ~19 untracked tables (ordres_reparation/or_*, catalogue_pieces, entites_facturation/factures/factures_scannees/compteurs_documents, pdp_queue/pdp_transmissions, plans_constructeur, moto_proprietaires, users_client*, the 3 views). Do NOT restructure other `devis` columns (client snapshot fields, lignes JSONB, total_* rework). Do NOT remove or alter the existing `devis_lignes` table. Do NOT "fix" `recalc_score_moto()` (its stale formula is out of scope AND touching the anti-fraude scoring is forbidden by CLAUDE.md). Do NOT touch motokey-api.js/app.html/supabase.js/MotoKey_Client.html.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-schema-sql-regeneration/19-01-SUMMARY.md
@.planning/phases/19-schema-sql-regeneration/19-RESEARCH.md
@schema.sql
@sql/migrations/12_garage_users.sql
@sql/migrations/16_client_device_tokens.sql
@sql/migrations/17_push_send_log.sql
@sql/migrations/18_motos_maintenance_alert_state.sql
@sql/migrations/19_clients_email_garage_unique.sql

<interfaces>
<!-- schema.sql conventions that MUST be preserved (see 19-RESEARCH.md Architecture Patterns): -->
- Box-drawing section headers: `-- ═══...` (U+2550) between sections, `╔╗╚╝║` title box at top.
- NETTOYAGE block: `DROP TABLE IF EXISTS <t> CASCADE;` in FK-safe order + `DROP TYPE IF EXISTS <type> CASCADE;` list.
- TYPES ENUM block, then one CREATE TABLE per section, then TRIGGERS/functions, then INDEX, then RLS, then REALTIME, then final verification SELECT.
- Existing statut ENUM to REMOVE: line ~28 `DROP TYPE IF EXISTS statut_devis CASCADE;` and line ~40 `CREATE TYPE statut_devis AS ENUM ('brouillon','envoye','valide','annule');` and line ~189 `statut statut_devis NOT NULL DEFAULT 'brouillon',`.
- Ground truth from plan 01 SUMMARY: the verbatim devis_statut_check allowed-value list (constraint name devis_statut_check). App default is 'brouillon'.
- Migration-file DDL for the 3 new tables uses `gen_random_uuid()` (pgcrypto) — schema.sql already CREATEs the pgcrypto extension, so keep `gen_random_uuid()` as written in the migrations (do NOT rewrite to uuid_generate_v4()).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 3 new tables (garage_users, client_device_tokens, push_send_log) preserving schema.sql conventions</name>
  <files>schema.sql</files>
  <read_first>
    - schema.sql (full — to match section-header style, DROP block ordering, table section format)
    - sql/migrations/12_garage_users.sql
    - sql/migrations/16_client_device_tokens.sql
    - sql/migrations/17_push_send_log.sql
    - .planning/phases/19-schema-sql-regeneration/19-01-SUMMARY.md (RLS findings for these 3 tables)
  </read_first>
  <action>
    Add three new CREATE TABLE sections to schema.sql, each with a box-drawing header (`-- ═══...` + `-- TABLE : <name>` + `-- ═══...`) matching existing sections. Copy the DDL VERBATIM from the migration files (they were confirmed by research to match prod exactly), including their `CREATE INDEX` and `COMMENT ON` statements:

    1. `garage_users` (from migration 12): columns id/auth_user_id/garage_id/role (CHECK role IN ('PRO','MECANO'))/actif/created_at/created_by, UNIQUE(auth_user_id, garage_id); indexes idx_garage_users_garage and idx_garage_users_auth (both WHERE actif = true); both COMMENTs. Place AFTER the `clients` table section (it FKs garages + auth.users). FK target auth.users already exists in schema.sql.
    2. `client_device_tokens` (from migration 16): id/client_id (FK clients ON DELETE CASCADE)/token (UNIQUE)/platform (CHECK IN ('ios','android'))/last_used_at/created_at; index idx_client_device_tokens_client; COMMENT. Place AFTER the `clients` table (FKs clients).
    3. `push_send_log` (from migration 17): id/idempotency_key (UNIQUE)/client_id (PLAIN UUID, intentionally NO FK — preserve migration 17's explanatory comment about why)/token/sent_at/created_at; index idx_push_send_log_client; COMMENT. Keep the multi-line `-- NOTE (2026-07-02): ...` comment verbatim.

    Update the NETTOYAGE block: add `DROP TABLE IF EXISTS push_send_log CASCADE;`, `DROP TABLE IF EXISTS client_device_tokens CASCADE;`, `DROP TABLE IF EXISTS garage_users CASCADE;` at the TOP of the DROP TABLE list (they are leaf tables / have no dependents, so dropping first is FK-safe; CASCADE also self-heals).

    RLS: consult plan 01 SUMMARY. IF the pg_policies/relrowsecurity output shows RLS enabled + policies on any of these 3 tables, reproduce them verbatim in the RLS section (with `ALTER TABLE <t> ENABLE ROW LEVEL SECURITY;` + the policy). IF plan 01 confirmed NO RLS on a table, add NOTHING for it (faithful reproduction — do not invent policies).
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE garage_users" schema.sql && grep -c "CREATE TABLE client_device_tokens" schema.sql && grep -c "CREATE TABLE push_send_log" schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE garage_users" schema.sql` == 1
    - `grep -c "CREATE TABLE client_device_tokens" schema.sql` == 1
    - `grep -c "CREATE TABLE push_send_log" schema.sql` == 1
    - `grep -c "DROP TABLE IF EXISTS garage_users CASCADE" schema.sql` == 1 (and same for the other two).
    - `grep -c "idx_garage_users_auth" schema.sql` == 1; `grep -c "idx_client_device_tokens_client" schema.sql` == 1; `grep -c "idx_push_send_log_client" schema.sql` == 1.
    - `grep "NOTE (2026-07-02)" schema.sql` present (push_send_log FK-omission comment preserved).
    - `grep -c "role IN ('PRO', 'MECANO')" schema.sql` >= 1 and `grep -c "platform IN ('ios', 'android')" schema.sql` >= 1.
    - RLS additions for these tables match plan 01 SUMMARY findings exactly (present only if prod has them).
  </acceptance_criteria>
  <done>The 3 previously-absent tables now exist in schema.sql with their exact migration-file DDL, indexes, comments, DROP-block entries, and RLS reproduced faithfully per plan 01 findings.</done>
</task>

<task type="auto">
  <name>Task 2: Apply motos columns (mig 18), clients UNIQUE (mig 19), convert devis.statut ENUM→CHECK, add partial-bootstrap header</name>
  <files>schema.sql</files>
  <read_first>
    - schema.sql (motos section ~L106, clients section ~L88, devis section ~L183, NETTOYAGE ~L15-33, TYPES ENUM ~L38-45, title box ~L1-4)
    - sql/migrations/18_motos_maintenance_alert_state.sql
    - sql/migrations/19_clients_email_garage_unique.sql
    - .planning/phases/19-schema-sql-regeneration/19-01-SUMMARY.md (verbatim devis_statut_check allowed values — REQUIRED for the CHECK clause)
  </read_first>
  <action>
    Make four edits to schema.sql:

    (A) motos maintenance-tier columns (migration 18): inside the `CREATE TABLE motos (...)` block, add two columns before `created_at`:
        `last_maintenance_tier_notified    TEXT CHECK (last_maintenance_tier_notified IN ('warning', 'urgent') OR last_maintenance_tier_notified IS NULL),`
        `last_maintenance_tier_notified_at TIMESTAMPTZ,`
        Also copy migration 18's `COMMENT ON COLUMN motos.last_maintenance_tier_notified IS '...'` verbatim into the file (place it right after the motos table section, matching how other COMMENTs are placed).

    (B) clients UNIQUE(email, garage_id) (migration 19): inside the `CREATE TABLE clients (...)` block, add a table-level constraint line before the closing `)`:
        `CONSTRAINT clients_email_garage_id_key UNIQUE (email, garage_id)`
        (Add the comma on the preceding column line as needed.) This must produce the SAME named constraint as migration 19 (`clients_email_garage_id_key`).

    (C) Convert devis.statut from ENUM to TEXT+CHECK (ROADMAP criterion 3):
        - In NETTOYAGE block: DELETE the line `DROP TYPE  IF EXISTS statut_devis           CASCADE;`.
        - In TYPES ENUM block: DELETE the line `CREATE TYPE statut_devis          AS ENUM ('brouillon','envoye','valide','annule');`.
        - In the `CREATE TABLE devis` block: change `statut          statut_devis NOT NULL DEFAULT 'brouillon',` to a TEXT column with a CHECK constraint whose allowed values are COPIED VERBATIM from plan 01's captured `devis_statut_check` definition. Write it as:
          `statut          TEXT NOT NULL DEFAULT 'brouillon' CHECK (statut IN (<exact values from plan 01>)),`
          The DEFAULT stays 'brouillon' (the value supabase.js inserts for new devis, L542) — but ONLY if 'brouillon' appears in the confirmed CHECK set. If plan 01's confirmed set does NOT include 'brouillon', use whichever value the confirmed set contains that the app inserts on creation, and note the discrepancy in the SUMMARY.
        - Do NOT touch `type_ligne_devis` (still used by devis_lignes) or any other ENUM.
        - Do NOT recreate `statut_devis` anywhere. Do NOT modify any other devis column. Do NOT touch devis_lignes.

    (D) Partial-bootstrap header (ROADMAP criterion 4): immediately AFTER the top `╔══╗` title box (before the EXTENSIONS section), insert this comment block verbatim:
        ```
        -- ⚠️ BOOTSTRAP PARTIEL CONNU (Phase 19 v1.4 — 2026-07-08)
        -- Ce fichier reflète le schéma prod pour les objets des migrations 1–19 UNIQUEMENT.
        -- Il NE couvre PAS ~19 tables/vues qui existent en prod sans fichier de migration :
        --   • Ordres de réparation : ordres_reparation, or_taches, or_pieces, or_historique
        --   • Facturation/billing   : entites_facturation, factures, factures_scannees, compteurs_documents
        --   • E-invoicing (PDP)      : pdp_queue, pdp_transmissions
        --   • Catalogue pièces       : catalogue_pieces
        --   • Plans constructeur     : plans_constructeur
        --   • Propriété moto          : moto_proprietaires
        --   • Auth client séparée     : users_client, users_client_sessions, email_verifications, password_resets, auth_logs
        --   • Vues                    : v_factures_pdp, v_users_client_stats
        -- Un projet bootstrappé ici obtient le socle garage (migrations 1–19), PAS ces sous-systèmes.
        -- Parité complète 38 tables : différée (voir REQUIREMENTS.md Out of Scope, narrowed 2026-07-08).
        ```
  </action>
  <verify>
    <automated>grep -c "last_maintenance_tier_notified" schema.sql && grep -c "clients_email_garage_id_key" schema.sql && grep -c "CREATE TYPE statut_devis" schema.sql && grep -c "BOOTSTRAP PARTIEL" schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "last_maintenance_tier_notified " schema.sql` >= 1 AND `grep -c "last_maintenance_tier_notified_at" schema.sql` >= 1 (both migration-18 columns on motos).
    - `grep -c "COMMENT ON COLUMN motos.last_maintenance_tier_notified" schema.sql` == 1.
    - `grep -c "clients_email_garage_id_key UNIQUE (email, garage_id)" schema.sql` == 1.
    - `grep -c "CREATE TYPE statut_devis" schema.sql` == 0 (ENUM type definition removed).
    - `grep -c "DROP TYPE  IF EXISTS statut_devis" schema.sql` == 0 (drop line removed).
    - `grep -c "statut_devis" schema.sql` == 0 (no residual reference anywhere).
    - `grep -E "statut +TEXT NOT NULL DEFAULT 'brouillon' CHECK \(statut IN" schema.sql` matches; the IN(...) list equals plan 01's verbatim allowed values.
    - `grep -c "BOOTSTRAP PARTIEL" schema.sql` == 1 AND the header lists ordres_reparation, factures, users_client, pdp_queue.
    - `grep -c "CREATE TABLE devis_lignes" schema.sql` == 1 (UNCHANGED — devis_lignes intentionally left as-is per narrow scope).
    - `grep -c "recalc_score_moto" schema.sql` unchanged from before (scoring trigger NOT modified).
  </acceptance_criteria>
  <done>schema.sql documents migrations 18 & 19, the devis.statut CHECK matches the live constraint verbatim (ENUM removed), and the header honestly declares the known-partial scope — satisfying ROADMAP criteria 1–4.</done>
</task>

</tasks>

<verification>
- All 4 ROADMAP success criteria that concern the file contents (1–4) are grep-verifiable as PASS.
- No out-of-scope change: devis_lignes intact, other devis columns untouched, recalc_score_moto() untouched, protected files untouched, no ~19 out-of-scope tables added.
- `statut_devis` ENUM fully removed (0 references); devis.statut is TEXT+CHECK with plan-01-confirmed values.
</verification>

<success_criteria>
- schema.sql contains CREATE TABLE for the 3 previously-absent tables (criterion 1).
- schema.sql contains motos mig-18 columns + clients mig-19 UNIQUE (criterion 2).
- devis.statut is TEXT+CHECK with exact live values, ENUM gone (criterion 3).
- Header documents known-partial bootstrap (criterion 4).
- Existing schema.sql conventions preserved (section headers, DROP block, ordering).
</success_criteria>

<output>
After completion, create `.planning/phases/19-schema-sql-regeneration/19-02-SUMMARY.md`. Record the final devis.statut CHECK clause written, and note any DEFAULT-value discrepancy discovered in step (C).
</output>
