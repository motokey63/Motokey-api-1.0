---
phase: 19-schema-sql-regeneration
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified: [schema.sql]
autonomous: false
requirements: [SCHEMA-01]
must_haves:
  truths:
    - "Executing schema.sql against a fresh Supabase project produces tables/columns matching prod for all objects introduced by migrations 1-19, with no errors"
    - "The fresh bootstrapped project contains the 3 new tables + motos maintenance columns + clients UNIQUE constraint"
  artifacts:
    - path: "schema.sql"
      provides: "bootstrap-clean schema (patched if the fresh-project run surfaced any error)"
      contains: "CREATE TABLE garages"
  key_links:
    - from: "fresh Supabase project"
      to: "prod schema (migrations 1-19 objects)"
      via: "node scripts/introspect-schema.js --compare"
      pattern: "\\-\\-compare"
---

<objective>
Prove ROADMAP Phase 19 success criterion 5: executing the regenerated `schema.sql` against a genuinely empty Postgres runs with NO errors and produces the migration-1–19 objects. This cannot be automated from this environment — no local Postgres/Docker/psql/Supabase CLI exists (confirmed in 19-RESEARCH.md Environment Availability) — so it requires Mehdi to paste-run schema.sql in a fresh free-tier Supabase project. The automated diff (via the plan-01 script) then confirms the fresh project matches prod for the in-scope objects.

Purpose: Without this step, criterion 5 would be marked done without ever running against a real empty Postgres (19-RESEARCH.md Pitfall 4).
Output: A confirmed-clean bootstrap; schema.sql patched if any error surfaced.

SCOPE GUARD: The fresh project is a KNOWN-PARTIAL bootstrap. It will NOT contain the ~19 out-of-scope tables — that is expected and correct, not a failure. Success = schema.sql runs without SQL error AND the in-scope object set matches.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-schema-sql-regeneration/19-RESEARCH.md
@.planning/phases/19-schema-sql-regeneration/19-02-SUMMARY.md
@schema.sql
@scripts/introspect-schema.js
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Bootstrap schema.sql in a fresh Supabase project and report errors</name>
  <files>(none — human runs schema.sql in a new Supabase project's SQL Editor)</files>
  <read_first>
    - schema.sql (the file to paste-run)
    - .planning/phases/19-schema-sql-regeneration/19-RESEARCH.md (Pitfall 3 — DROP block re-run safety; Pitfall 4 — why this is manual)
  </read_first>
  <action>
    HUMAN ACTION. Mehdi creates a NEW throwaway free-tier Supabase project, pastes the entire regenerated `schema.sql` into its SQL Editor, and runs it, then reports whether it ran clean or the exact SQL error text, plus the new project's URL + an API key (for Task 2's automated diff). Full steps in <how-to-verify>. No file is edited by this task; schema.sql patching (if needed) happens in Task 3.
  </action>
  <what-built>
    The regenerated `schema.sql` from plan 02, ready to bootstrap a brand-new Supabase project. Supabase-specific dependencies (`auth.users`, `extensions.uuid_generate_v4()`, `pgcrypto`, `supabase_realtime` publication) exist in any Supabase project, so a fresh Supabase project is the correct target (a vanilla local Postgres would give false failures).
  </what-built>
  <how-to-verify>
    1. Create a NEW free-tier Supabase project (Dashboard → New project). Wait for it to finish provisioning. This is a throwaway test project — do NOT use the prod project rzbqbaccjyxvtlnfitrr.
    2. Open the new project → SQL Editor → New query. Paste the ENTIRE contents of `schema.sql`. Run it.
    3. Report the outcome:
       - SUCCESS: the final verification SELECT returns the table list with no red error banner.
       - FAILURE: copy the exact error text (e.g. `type "x" already exists`, `relation "y" does not exist`, `column ... does not exist`) back into the resume signal.
    4. From the new project → Project Settings → API, copy its Project URL and its `anon`/service API key (needed for Task 2's automated diff). Provide them in the resume signal. (These are throwaway-project credentials.)
  </how-to-verify>
  <verify>Fresh-project SQL Editor run of schema.sql returns the final verification SELECT with no error banner; new project URL + key provided in resume signal.</verify>
  <acceptance_criteria>
    - schema.sql executed in the fresh project with NO SQL error (the final `SELECT tablename, rowsecurity ...` returns rows).
    - The returned table list includes garages, clients, motos, interventions, devis, devis_lignes, garage_users, client_device_tokens, push_send_log (the migration-1–19 object set).
    - The fresh project's URL + an API key are provided for Task 2.
  </acceptance_criteria>
  <done>schema.sql confirmed to execute cleanly (or exact errors captured) in a genuinely empty Supabase project; fresh-project credentials handed off to Task 2.</done>
  <resume-signal>Report "clean" (with the fresh project URL + key) OR paste the exact SQL error text. If an error occurred, execution loops to Task 3 (patch) then re-runs this task.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Automated diff — fresh project vs prod for in-scope objects</name>
  <files>(none — runs existing scripts/introspect-schema.js in compare mode)</files>
  <read_first>
    - scripts/introspect-schema.js (compare-mode contract)
    - .planning/phases/19-schema-sql-regeneration/19-01-SUMMARY.md (expected in-scope object set)
  </read_first>
  <action>
    Using the fresh project URL + key provided in Task 1's resume signal, run the compare mode:
    `node scripts/introspect-schema.js --compare <FRESH_URL> <FRESH_KEY>`
    This introspects both the fresh project and prod (prod from .env) and diffs the in-scope table/column set (baseline garage tables + garage_users + client_device_tokens + push_send_log). Confirm exit code 0 (no missing in-scope tables/columns in the fresh project). Out-of-scope prod-only tables (ordres_reparation, factures, users_client, etc.) MUST NOT be reported as diffs — they are expected absent. If the compare reports missing in-scope objects, that indicates a schema.sql defect → go to Task 3.
    Do NOT print the API keys to logs.
  </action>
  <verify>
    <automated>node scripts/introspect-schema.js --compare "$FRESH_URL" "$FRESH_KEY"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/introspect-schema.js --compare <FRESH_URL> <FRESH_KEY>` exits 0.
    - Output confirms the fresh project contains all in-scope tables and columns (garage_users, client_device_tokens, push_send_log, motos.last_maintenance_tier_notified*, clients UNIQUE) matching prod.
    - No out-of-scope prod-only table is flagged as a diff.
  </acceptance_criteria>
  <done>The fresh bootstrap is confirmed to match prod for the migration-1–19 object set — ROADMAP criterion 5 satisfied by real execution + automated diff.</done>
</task>

<task type="auto">
  <name>Task 3: Patch schema.sql if the fresh-project bootstrap surfaced errors (conditional)</name>
  <files>schema.sql</files>
  <read_first>
    - schema.sql
    - .planning/phases/19-schema-sql-regeneration/19-RESEARCH.md (Pitfall 3 — DROP TYPE/DROP TABLE list may need new entries: or_statut, client_type_enum, proprietaire_type_enum, mode_acquisition_enum are used by out-of-scope tables and should NOT be added; but verify no in-scope type/table causes a re-run collision)
  </read_first>
  <action>
    ONLY perform this task if Task 1 reported a SQL error OR Task 2's diff reported missing in-scope objects. Otherwise skip (mark done, no changes).
    Diagnose the exact error from Task 1's report and fix the minimal cause in schema.sql:
    - `type "x" already exists` / `relation "y" already exists` on re-run → ensure the object has a matching `DROP ... IF EXISTS ... CASCADE` entry in the NETTOYAGE block (Pitfall 3).
    - `column ... does not exist` / ordering error → fix column/section ordering so FKs and columns resolve.
    - Do NOT introduce out-of-scope tables to satisfy an error; if an error stems from a dependency on an out-of-scope object, the correct fix is to remove that unintended dependency, not to add the out-of-scope table.
    After patching, the loop returns to Task 1 (Mehdi re-runs in the fresh project, or drops+recreates). Keep changes minimal and within the narrow scope.
  </action>
  <verify>
    <automated>node --check scripts/introspect-schema.js; grep -c "CREATE TABLE garages" schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - If Task 1 & Task 2 both passed: this task makes ZERO changes to schema.sql (git diff empty for this task).
    - If a patch was needed: the specific error from Task 1 no longer reproduces on the next fresh-project run, and the patch touched only schema.sql within narrow scope (no out-of-scope table added, `grep -c "statut_devis" schema.sql` still == 0, devis_lignes still present).
  </acceptance_criteria>
  <done>schema.sql bootstraps cleanly in a fresh Supabase project; any error found during manual verification is fixed within narrow scope, or no changes were needed.</done>
</task>

</tasks>

<verification>
- schema.sql ran in a fresh Supabase project with no SQL error (human-confirmed).
- `node scripts/introspect-schema.js --compare` exits 0 — fresh project matches prod for the migration-1–19 object set.
- Any patch stayed within narrow scope (no out-of-scope tables, ENUM still removed, devis_lignes intact).
</verification>

<success_criteria>
- ROADMAP Phase 19 criterion 5 satisfied by real execution against an empty Postgres + automated in-scope diff.
- SCHEMA-01 fully satisfied end-to-end (criteria 1–5).
</success_criteria>

<output>
After completion, create `.planning/phases/19-schema-sql-regeneration/19-03-SUMMARY.md`. Record the bootstrap outcome (clean / errors-and-fixes), the compare-mode result, and confirm all 5 ROADMAP criteria PASS.
</output>
