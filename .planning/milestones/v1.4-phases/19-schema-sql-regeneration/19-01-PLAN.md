---
phase: 19-schema-sql-regeneration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/introspect-schema.js]
autonomous: false
requirements: [SCHEMA-01]
must_haves:
  truths:
    - "The exact live CHECK constraint definition for devis.statut is captured verbatim (from pg_get_constraintdef, not guessed from application code)"
    - "A reusable Node script can introspect any Supabase project's tables/columns using only a URL + API key (no psql/CLI/new credentials)"
    - "It is known whether garage_users, client_device_tokens, push_send_log carry RLS policies in prod (to reproduce faithfully)"
  artifacts:
    - path: "scripts/introspect-schema.js"
      provides: "PostgREST OpenAPI introspection + compare mode for bootstrap verification"
      contains: "rest/v1/"
  key_links:
    - from: "scripts/introspect-schema.js"
      to: "{SUPABASE_URL}/rest/v1/?apikey=..."
      via: "Node built-in fetch, key read from .env (never echoed)"
      pattern: "process\\.env\\.SUPABASE_SECRET_KEY"
---

<objective>
Capture the ground-truth inputs required to regenerate schema.sql for the narrow-scope drift (migrations 1–19), WITHOUT guessing. Two inputs are unknown and cannot come from files alone:
1. The exact live CHECK constraint on `devis.statut` (the endpoint used for column introspection does NOT expose CHECK text — see 19-RESEARCH.md Pitfall 1). Application code uses `brouillon`/`envoye`/`accepte`/`refuse` but the phase description mentions `accepte`/`refuse`/`expire`/`converti` — these DO NOT reconcile, so neither source is trustworthy alone.
2. Whether the 3 new tables (`garage_users`, `client_device_tokens`, `push_send_log`) carry RLS policies in prod beyond what their migration files show.

Purpose: Everything else in scope (3 tables, motos columns, clients constraint) already has authored migration files that research confirmed match prod exactly — but the devis CHECK is a genuine unknown that MUST be resolved by querying Postgres system catalogs directly.
Output: A committed `scripts/introspect-schema.js` (reused for final bootstrap verification in plan 03) and a captured, verbatim `pg_get_constraintdef` result for `devis.statut`.

SCOPE GUARD (narrowed 2026-07-08 by Mehdi): NARROW scope only. Do NOT attempt full 38-table parity. The ~19 untracked tables (repair orders, billing/invoicing, parts catalogue, separate client-auth system, PDP tables, 3 views) are explicitly OUT OF SCOPE (REQUIREMENTS.md Out of Scope). This plan only gathers inputs for the named items.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-schema-sql-regeneration/19-RESEARCH.md
@.planning/REQUIREMENTS.md
@schema.sql

<interfaces>
<!-- Ground truth already established (do NOT re-guess these): -->
- devis.statut default value used by app: 'brouillon' (supabase.js L542, L873)
- devis.statut transitions used by app: 'envoye' (L580), 'accepte' (L592), 'refuse'
- Live CHECK constraint NAME: devis_statut_check (referenced in supabase.js L590 comment)
- devis_lignes: DROPPED from prod, replaced by devis.lignes JSONB — do NOT resurrect
- .env keys available: SUPABASE_URL, SUPABASE_SECRET_KEY (both already used by setup-supabase.js)
- Supabase project ref: rzbqbaccjyxvtlnfitrr.supabase.co
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create introspect-schema.js (PostgREST OpenAPI introspection + compare mode)</name>
  <files>scripts/introspect-schema.js</files>
  <read_first>
    - .planning/phases/19-schema-sql-regeneration/19-RESEARCH.md (Code Examples section — verified-working fetch snippet; Environment Availability — no psql/CLI/Docker)
    - scripts/seed-test-moto-15-uat.js (existing pattern for require('dotenv').config() + reading SUPABASE_SECRET_KEY)
    - CLAUDE.md ("Secrets : jamais en clair" rule)
  </read_first>
  <action>
    Create `scripts/introspect-schema.js` — a Node script (no new npm deps; use built-in global `fetch` and existing `dotenv`) with TWO modes.

    Default mode (`node scripts/introspect-schema.js`):
    - `require('dotenv').config();`
    - Read `const url = process.env.SUPABASE_URL;` and `const key = process.env.SUPABASE_SECRET_KEY || process.env.SUPABASE_SERVICE_KEY;`
    - `const res = await fetch(url + '/rest/v1/?apikey=' + key, { headers: { Authorization: 'Bearer ' + key } });`
    - Parse `spec.definitions` (one entry per table/view). For each table print: table name, and for each column its `type`/`format`, whether it is in the `required` array (= NOT NULL without default), and any `default`/FK note.
    - NEVER print the key itself (do not console.log `key` or the full URL-with-apikey). Print only `url` host if needed.
    - After printing, ASSERT and report PASS/FAIL for the presence of the narrow-scope objects: tables `garage_users`, `client_device_tokens`, `push_send_log` present; `motos` has columns `last_maintenance_tier_notified` and `last_maintenance_tier_notified_at`. Exit code 0 if all present, non-zero otherwise.

    Compare mode (`node scripts/introspect-schema.js --compare <FRESH_URL> <FRESH_KEY>`):
    - Introspect the fresh project (from the two CLI args) AND prod (from .env).
    - Diff the two on: set of table names, and per-table set of column names, LIMITED to the set of tables that schema.sql is expected to create (baseline garage tables + garage_users + client_device_tokens + push_send_log). Do NOT flag prod-only tables from the ~19 out-of-scope subsystem as diffs — the fresh project is a KNOWN-PARTIAL bootstrap.
    - Print any missing tables/columns in the fresh project relative to that expected set. Exit 0 if the expected set fully matches, non-zero otherwise.

    Add a top-of-file comment: `// One-off schema introspection helper (Phase 19). Uses PostgREST OpenAPI — no psql/CLI/new credentials. See .planning/phases/19-schema-sql-regeneration/19-RESEARCH.md.`
  </action>
  <verify>
    <automated>node scripts/introspect-schema.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/introspect-schema.js` exits 0 and prints table/column data for the live project.
    - Output contains lines confirming `garage_users`, `client_device_tokens`, `push_send_log` present and both `motos.last_maintenance_tier_notified*` columns present (all PASS).
    - `grep -c "process.env.SUPABASE_SECRET_KEY" scripts/introspect-schema.js` >= 1.
    - `grep -c "console.log" scripts/introspect-schema.js` shows no line that logs the raw key (manual read confirms the key is never printed).
    - `grep -c "\\-\\-compare" scripts/introspect-schema.js` >= 1 (compare mode implemented).
    - `node --check scripts/introspect-schema.js` passes.
  </acceptance_criteria>
  <done>Committed introspection script runs green against prod and confirms the 5 narrow-scope objects exist live; compare mode is implemented for plan 03's bootstrap verification.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture exact devis.statut CHECK constraint + RLS for 3 new tables (Supabase Dashboard SQL Editor)</name>
  <files>(none — read-only query, result captured into the plan SUMMARY)</files>
  <read_first>
    - .planning/phases/19-schema-sql-regeneration/19-RESEARCH.md (Code Examples — the read-only pg_catalog query block; Open Questions #2 — the unreconciled CHECK values)
  </read_first>
  <action>
    HUMAN ACTION (read-only, zero data mutation). Mehdi runs a single SQL query in the Supabase Dashboard SQL Editor of the prod project (rzbqbaccjyxvtlnfitrr) — the same mechanism used for every migration 10–19 — to recover the two facts PostgREST OpenAPI cannot expose: the verbatim `devis.statut` CHECK definition, and the RLS state of the 3 new tables. The full query and steps are in <how-to-verify>. The verbatim output is pasted back and recorded in the SUMMARY for plan 02 to consume. No file is edited by this task.
  </action>
  <what-built>
    A single READ-ONLY SQL query (zero data mutation) to be pasted into the Supabase Dashboard SQL Editor — the exact same mechanism already used for every migration 10–19. This recovers the two facts that PostgREST OpenAPI cannot expose: the verbatim `devis.statut` CHECK definition, and whether the 3 new tables have RLS policies.
  </what-built>
  <how-to-verify>
    1. Open Supabase Dashboard (project rzbqbaccjyxvtlnfitrr) → SQL Editor → New query.
    2. Paste and RUN (read-only, safe):
       ```sql
       -- (a) exact CHECK constraints for devis (resolves statut allowed values)
       SELECT conname, pg_get_constraintdef(oid) AS definition
       FROM pg_constraint
       WHERE contype = 'c'
         AND conrelid = 'public.devis'::regclass;

       -- (b) RLS policies on the 3 new tables (to reproduce faithfully in schema.sql)
       SELECT tablename, policyname, cmd, qual, with_check
       FROM pg_policies
       WHERE schemaname = 'public'
         AND tablename IN ('garage_users','client_device_tokens','push_send_log')
       ORDER BY tablename, policyname;

       -- (c) confirm RLS enabled flag on those tables
       SELECT relname, relrowsecurity
       FROM pg_class
       WHERE relnamespace = 'public'::regnamespace
         AND relname IN ('garage_users','client_device_tokens','push_send_log');
       ```
    3. Copy the FULL text output of all three queries back into the resume signal — especially the exact `pg_get_constraintdef` string for the constraint named `devis_statut_check` (e.g. `CHECK ((statut = ANY (ARRAY['brouillon'::text, ...])))`).
    4. Do NOT edit any data. Do NOT run any migration. This is read-only.
  </how-to-verify>
  <verify>Verbatim `pg_get_constraintdef` string for devis's statut CHECK is pasted into the resume signal and recorded in the plan SUMMARY; RLS output for the 3 new tables recorded.</verify>
  <acceptance_criteria>
    - The verbatim `pg_get_constraintdef` string for `devis`'s statut CHECK is recorded in the plan SUMMARY (not paraphrased) — this is the single source that plan 02 will copy into schema.sql.
    - The recorded allowed-values list includes at minimum `brouillon`, `envoye`, `accepte`, `refuse` (the values the running app writes — if the live constraint does NOT contain these, that is a red flag to surface, since prod inserts would be failing).
    - Query (b)+(c) output records whether each of the 3 new tables has RLS enabled and any policy definitions — recorded verbatim in the SUMMARY.
  </acceptance_criteria>
  <done>The verbatim devis_statut_check definition and the RLS state of the 3 new tables are recorded in the SUMMARY, unblocking plan 02.</done>
  <resume-signal>Paste the full output of queries (a), (b), (c). Confirm the devis statut CHECK definition verbatim. Then type "captured".</resume-signal>
</task>

</tasks>

<verification>
- `scripts/introspect-schema.js` committed, runs green against prod, confirms 5 narrow-scope objects present.
- The exact `devis_statut_check` definition is recorded verbatim in the SUMMARY (no guessing downstream).
- RLS state for the 3 new tables is recorded so plan 02 reproduces prod faithfully.
</verification>

<success_criteria>
- Ground-truth inputs for schema.sql regeneration are captured: (1) reusable introspection tooling, (2) verbatim devis.statut CHECK, (3) RLS state of the 3 new tables.
- No guessing: the CHECK values come from `pg_get_constraintdef`, per ROADMAP success criterion 3.
</success_criteria>

<output>
After completion, create `.planning/phases/19-schema-sql-regeneration/19-01-SUMMARY.md`. It MUST include the verbatim `devis_statut_check` definition string and the RLS findings — plan 02 depends on them.
</output>
