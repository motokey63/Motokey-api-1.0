---
phase: 18-client-login-fixture-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [setup-supabase.js]
autonomous: true
requirements: [CFIX-01]

must_haves:
  truths:
    - "Running setup-supabase.js creates (or confirms) a Supabase Auth user for sophie@email.com, mirroring the existing garage account creation pattern"
    - "The clients table row for sophie@email.com has auth_user_id populated and correctly linked to that Supabase Auth user"
    - "POST /auth/client/login with sophie@email.com / client123 returns 200 with a valid session token (no longer 401)"
    - "test-api.js no longer fails on the CLIENT login step"
  artifacts:
    - path: "setup-supabase.js"
      provides: "Idempotent creation of Sophie's Supabase Auth user + linkage of auth_user_id into the clients upsert"
      contains: "sb.auth.admin.createUser"
  key_links:
    - from: "setup-supabase.js (sb.auth.admin.createUser for sophie@email.com)"
      to: "clients.auth_user_id"
      via: "clients upsert payload"
      pattern: "auth_user_id:\\s*clientUserId"
    - from: "clients.auth_user_id"
      to: "POST /auth/client/login (SBLayer.Auth.loginClient)"
      via: "query('clients', { auth_user_id: data.user.id })"
      pattern: "auth_user_id"
---

<objective>
Fix `setup-supabase.js` so the CLIENT test fixture (`sophie@email.com` / `client123`) has a matching Supabase Auth user and a `clients` row whose `auth_user_id` is linked to it — mirroring the existing garage-account creation pattern in the same file.

Purpose: `/auth/client/login` (and `POST /auth/login` with `role: 'client'`) authenticate against Supabase Auth via `SBLayer.Auth.loginClient`, which then looks up `clients` by `auth_user_id`. Today Sophie has a `clients` row but no Auth user and no `auth_user_id`, so login returns 401 and `test-api.js` fails on its "Login client" step. This blocks all local/QA CLIENT-side testing.

Output: An updated, idempotent `setup-supabase.js` that seeds Sophie's Auth account and links it, verified end-to-end by a successful CLIENT login.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase — no exploration required. -->

Diagnosed root cause (do NOT re-diagnose): setup-supabase.js creates the garage Auth user
(garage@motokey.fr) via sb.auth.admin.createUser, then upserts a clients row for
sophie@email.com — but never creates a Supabase Auth user for Sophie, and never sets
clients.auth_user_id. loginClient authenticates against Supabase Auth, so login returns 401.

--- clients table (schema.sql lines 88-101) ---
CREATE TABLE clients (
  id               UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  auth_user_id     UUID UNIQUE REFERENCES auth.users(id) ON DELETE SET NULL,
  garage_id        UUID NOT NULL REFERENCES garages(id) ON DELETE CASCADE,
  nom TEXT NOT NULL, email TEXT, tel TEXT, ...
);
# auth_user_id already exists and is the column that must be populated for Sophie.

--- The authoritative login path (supabase.js:156-164) ---
async loginClient({ email, password }) {
  const { data, error } = await supabasePublic.auth.signInWithPassword({ email, password });
  if (error) throw new Error('Identifiants incorrects');
  const client = await query('clients', { auth_user_id: data.user.id }, { single: true });
  ...
}
# Both the client lookup AND the auth sign-in must succeed. Creating the Auth user fixes
# the sign-in; linking auth_user_id fixes the clients lookup.

--- The endpoint (motokey-api.js:2429-2448) ---
POST /auth/client/login → sbPub().auth.signInWithPassword({ email, password })
  → 401 INVALID_CREDENTIALS if no Auth user behind the email.
# test-api.js line 53-54 also exercises this via POST /auth/login {role:'client'} → loginClient.

--- The pattern to mirror (setup-supabase.js:48-85, the garage block) ---
const { data: authData, error: authErr } = await sb.auth.admin.createUser({
  email: 'garage@motokey.fr', password: 'motokey2026', email_confirm: true,
});
if (authErr && !authErr.message.includes('already registered')) throw authErr;
const userId = authData?.user?.id;   // <-- null on re-run when user already exists
if (userId) { ...upsert garages with auth_user_id: userId... }

--- IDEMPOTENCY GAP to fix as part of this work ---
On any machine already seeded once, the garage Auth user exists → createUser returns
"already registered" → authData?.user?.id is null → the entire `if (userId) {...}` block
(garage + client + moto) is skipped. So merely adding a Sophie createUser call inside that
block is NOT enough — the block never runs on re-seeded environments. The fix must resolve
existing Auth user IDs by email so the block always runs and Sophie always ends up linked.

--- Supabase JS v2 admin API available (package.json: @supabase/supabase-js ^2.45.0) ---
sb.auth.admin.createUser({ email, password, email_confirm: true }) → { data:{user}, error }
sb.auth.admin.listUsers({ page, perPage })                        → { data:{users:[...]}, error }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sophie's Supabase Auth user and link auth_user_id in setup-supabase.js</name>
  <files>setup-supabase.js</files>
  <read_first>
    - C:/motokey-api/setup-supabase.js (the file being modified — read the full test-account block, lines ~45-88)
    - C:/motokey-api/supabase.js (lines 156-173 — loginClient + createClientAccount, the linkage this fix must satisfy)
    - C:/motokey-api/schema.sql (lines 88-101 — clients table, confirms auth_user_id column exists)
  </read_first>
  <action>
    Edit `setup-supabase.js` to (a) make Auth-user resolution idempotent for both accounts and (b) create + link Sophie's CLIENT Auth user. Make these concrete changes:

    1. Add a module-level helper function (place it above `async function main()`), which resolves an existing Auth user's id by email by paging through `listUsers` (needed because `createUser` returns `null` user data when the email is "already registered"):

    ```js
    async function findAuthUserIdByEmail(email) {
      const perPage = 200;
      for (let page = 1; page <= 20; page++) {
        const { data, error } = await sb.auth.admin.listUsers({ page, perPage });
        if (error) throw error;
        const u = data.users.find(function (x) { return x.email === email; });
        if (u) return u.id;
        if (data.users.length < perPage) return null;
      }
      return null;
    }
    ```

    2. In the garage block (currently lines ~48-55), after the `sb.auth.admin.createUser({ email: 'garage@motokey.fr', ... })` call and its `if (authErr && !authErr.message.includes('already registered')) throw authErr;` guard, replace the line `const userId = authData?.user?.id;` with a resolution that falls back to the existing user when the account already exists:

    ```js
    let userId = authData?.user?.id;
    if (!userId) userId = await findAuthUserIdByEmail('garage@motokey.fr');
    ```

    Keep the existing `if (userId) { ... }` wrapper — but it will now always be entered on re-runs (previously it was skipped once the garage already existed).

    3. Inside the `if (garage) { ... }` block, BEFORE the `sb.from('clients').upsert(...)` call, create (or confirm) Sophie's Auth account, mirroring the garage pattern:

    ```js
    const { data: cliAuthData, error: cliAuthErr } = await sb.auth.admin.createUser({
      email: 'sophie@email.com',
      password: 'client123',
      email_confirm: true,
    });
    if (cliAuthErr && !cliAuthErr.message.includes('already registered')) throw cliAuthErr;
    let clientUserId = cliAuthData?.user?.id;
    if (!clientUserId) clientUserId = await findAuthUserIdByEmail('sophie@email.com');
    ```

    4. Add `auth_user_id: clientUserId,` as the first field in the existing `sb.from('clients').upsert({ ... }, { onConflict: 'email,garage_id' })` payload. Leave `onConflict: 'email,garage_id'` and all other client fields (garage_id, nom, email, tel) unchanged. The resulting upsert payload must be:

    ```js
    const { data: client } = await sb.from('clients').upsert({
      auth_user_id: clientUserId,
      garage_id: garage.id,
      nom: 'Sophie Laurent', email: 'sophie@email.com', tel: '06 10 00 00 01',
    }, { onConflict: 'email,garage_id' }).select().single();
    ```

    5. Update the success log line inside `if (client) { ... }` from `'  ✅ Garage + Client Sophie + Moto MT-07 créés'` to `'  ✅ Garage + Client Sophie (auth liée) + Moto MT-07 créés'` so re-runs visibly confirm the linkage.

    Do NOT change the buckets block, the moto upsert (`onConflict: 'vin'`), the final banner, or the password/email values. Do NOT introduce PowerShell/sed/Python edits — edit the file directly.
  </action>
  <verify>
    <automated>node --check setup-supabase.js && grep -q "findAuthUserIdByEmail" setup-supabase.js && grep -q "email: 'sophie@email.com'" setup-supabase.js && grep -q "password: 'client123'" setup-supabase.js && grep -q "auth_user_id: clientUserId" setup-supabase.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check setup-supabase.js` exits 0 (no syntax error)
    - `grep -c "sb.auth.admin.createUser" setup-supabase.js` returns 2 (garage account + Sophie account)
    - `grep -q "email: 'sophie@email.com'" setup-supabase.js` matches (Sophie Auth user creation present)
    - `grep -q "password: 'client123'" setup-supabase.js` matches (correct fixture password)
    - `grep -q "auth_user_id: clientUserId" setup-supabase.js` matches (link written into clients upsert)
    - `grep -q "onConflict: 'email,garage_id'" setup-supabase.js` still matches (client upsert conflict key unchanged)
    - `grep -q "async function findAuthUserIdByEmail" setup-supabase.js` matches (idempotent lookup helper present)
    - `grep -q "userId = await findAuthUserIdByEmail('garage@motokey.fr')" setup-supabase.js` matches (garage block now runs on re-seed)
    - `grep -q "onConflict: 'vin'" setup-supabase.js` still matches (moto upsert untouched)
  </acceptance_criteria>
  <done>
    setup-supabase.js creates/confirms an Auth user for sophie@email.com, resolves the existing user id on re-runs for both garage and client, and writes auth_user_id into the clients upsert. Syntax valid.
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed the fixture and prove CLIENT login returns 200</name>
  <files>test-api.js (verification harness; no files modified — this task runs setup-supabase.js and the API to prove login)</files>
  <read_first>
    - C:/motokey-api/setup-supabase.js (the edited file, to confirm what will run)
    - C:/motokey-api/test-api.js (lines 53-54 — the "Login client" assertion that must now pass)
    - C:/motokey-api/motokey-api.js (lines 2428-2448 — POST /auth/client/login) and (lines 602-627 — POST /auth/login role:'client')
  </read_first>
  <action>
    Run the seed script against the configured Supabase project, then prove the fixture logs in. This requires SUPABASE_URL and SUPABASE_SECRET_KEY (or SUPABASE_SERVICE_KEY) in the local `.env` (already used by setup-supabase.js via `require('dotenv').config()`), and the API running locally on port 3000 with Supabase enabled.

    Steps:
    1. Run `node setup-supabase.js`. It must exit 0 and print the line `✅ Garage + Client Sophie (auth liée) + Moto MT-07 créés`. It must NOT print a `⚠️` line mentioning an auth error for sophie@email.com. (Re-run once to confirm idempotency: a second run must also reach the same success line without throwing "already registered".)
    2. Start the API server in the background: `node motokey-api.js` (defaults to port 3000). Wait until `GET http://localhost:3000/` returns 200 before proceeding.
    3. Prove the endpoint from the roadmap success criteria: `POST http://localhost:3000/auth/client/login` with JSON body `{"email":"sophie@email.com","password":"client123"}` must return HTTP 200 and a body containing a `session` object with an `access_token`.
    4. Prove the test-suite path: `POST http://localhost:3000/auth/login` with body `{"email":"sophie@email.com","password":"client123","role":"client"}` must return HTTP 200 with `data.role === "client"` and a non-empty `data.token`.
    5. Run `node test-api.js` (with the server up) and confirm the `Login client` line shows `✅` (not `❌`) and the summary reports 0 errors on that step.
    6. Stop the background server when done.

    If SUPABASE env vars are not present locally, do NOT fake the result — report the blocker in the SUMMARY (the fix in Task 1 is still complete and grep-verified); functional proof then falls to the next environment that has credentials.
  </action>
  <verify>
    <automated>node setup-supabase.js && (node motokey-api.js & SRV=$!; sleep 3; CODE=$(curl -s -o /dev/null -w '%{http_code}' -X POST localhost:3000/auth/client/login -H 'Content-Type: application/json' -d '{"email":"sophie@email.com","password":"client123"}'); echo "client-login-http=$CODE"; curl -s -X POST localhost:3000/auth/login -H 'Content-Type: application/json' -d '{"email":"sophie@email.com","password":"client123","role":"client"}' | grep -q '"role":"client"' && echo LOGIN_OK; node test-api.js; kill $SRV; test "$CODE" = "200")</automated>
  </verify>
  <acceptance_criteria>
    - `node setup-supabase.js` exits 0 and stdout contains `Client Sophie (auth liée)` with no `⚠️` line referencing sophie@email.com
    - `curl -s -o /dev/null -w '%{http_code}' -X POST localhost:3000/auth/client/login -d '{"email":"sophie@email.com","password":"client123"}'` prints `200` (previously `401`)
    - The `/auth/client/login` response body contains `"session"` with a non-empty `access_token`
    - `curl -s -X POST localhost:3000/auth/login -d '{"email":"sophie@email.com","password":"client123","role":"client"}'` response contains `"role":"client"` and a non-empty `token`
    - `node test-api.js` output contains `✅ Login client` (not `❌ Login client`)
    - Running `node setup-supabase.js` a second time also reaches the success line without throwing (idempotent)
  </acceptance_criteria>
  <done>
    The CLIENT fixture logs in end-to-end: POST /auth/client/login returns 200 with a valid session, POST /auth/login role:client returns 200, and test-api.js passes its "Login client" step. The seed script is idempotent across re-runs.
  </done>
</task>

</tasks>

<verification>
- Static: `node --check setup-supabase.js` passes; grep confirms Sophie's createUser + `auth_user_id: clientUserId` in the clients upsert + the `findAuthUserIdByEmail` helper.
- Functional: after `node setup-supabase.js`, `POST /auth/client/login` (sophie@email.com / client123) returns 200 with a session; `POST /auth/login` role:client returns 200; `test-api.js` "Login client" step passes.
- Idempotency: a second `node setup-supabase.js` run still reaches the Sophie success line and does not throw on "already registered".
</verification>

<success_criteria>
All four ROADMAP Phase 18 success criteria are TRUE:
1. setup-supabase.js creates (or confirms) a Supabase Auth user for sophie@email.com, mirroring the garage pattern.
2. The clients row for sophie@email.com has auth_user_id populated and linked to that Auth user.
3. POST /auth/client/login with sophie@email.com / client123 returns 200 with a valid session token (no longer 401).
4. test-api.js no longer fails on the CLIENT login step.
</success_criteria>

<output>
After completion, create `.planning/phases/18-client-login-fixture-fix/18-01-SUMMARY.md`
</output>
