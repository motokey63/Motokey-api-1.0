---
phase: 20-introspection-corr-lation-d-origine
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md
autonomous: false
requirements: [SCHEMA-02, SCHEMA-03]

must_haves:
  truths:
    - "Every undocumented column on garages/clients/interventions/devis has EXACT type from information_schema.columns (numeric precision/scale, text length) — not just the OpenAPI-collapsed type"
    - "Every explicit CHECK/UNIQUE/FK constraint on the 4 tables is captured verbatim from pg_constraint (or confirmed 'none') — closing Pitfall 4"
    - "The 9 ghost columns carry Mehdi's confirm-or-correct answer on their intended origin/feature"
    - "The final artifact accounts for all undocumented columns on all 4 tables — Phase 21 can write retroactive migrations from it without re-querying prod or re-running git"
  artifacts:
    - path: ".planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md"
      provides: "Finalized findings: EXACT metadata + constraints + Mehdi-confirmed ghost origins"
      contains: "information_schema"
  key_links:
    - from: "Dashboard SQL query output"
      to: "20-FINDINGS.md EXACT cells"
      via: "verbatim paste of information_schema.columns + pg_constraint rows"
      pattern: "numeric_precision|pg_get_constraintdef|character_maximum_length"
---

<objective>
Finalize the Phase 20 findings by capturing the two things that require Mehdi and cannot be obtained autonomously: (1) exact Postgres catalog metadata (numeric precision/scale, text length, CHECK/UNIQUE/FK constraint text) via `information_schema.columns` + `pg_constraint`, which the PostgREST OpenAPI endpoint cannot report; and (2) Mehdi's confirm-or-correct on the 9 ghost columns whose origin git cannot recover.

Purpose: Close SCHEMA-02 ("type EXACT, contraintes") and SCHEMA-03 ("origine documentée") to a level Phase 21 can build retroactive migrations from without re-discovery.
Output: `.planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md` (finalized — TBD EXACT cells filled, ghost origins confirmed).

INVESTIGATION phase — writes NO code, NO schema.sql, NO migration file. Only the findings artifact is edited. This plan is NON-autonomous: both tasks are checkpoints requiring Mehdi.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-introspection-corr-lation-d-origine/20-RESEARCH.md
@.planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Capture exact catalog metadata (SCHEMA-02) — Mehdi runs 3 read-only queries</name>
  <files>.planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md</files>
  <read_first>
    - .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md (the EXACT cells you will fill from the query output)
    - .planning/phases/20-introspection-corr-lation-d-origine/20-RESEARCH.md ("Code Examples" — the ready-to-run SQL; "Common Pitfalls" 4 — why OpenAPI silence ≠ no constraint; "Open Questions" 2 — the DISTINCT probe)
  </read_first>
  <action>
    Present the 3 read-only SQL queries below to Mehdi (in French) to run in Supabase Dashboard →
    SQL Editor against PROD (rzbqbaccjyxvtlnfitrr), then wait for him to paste the output. No
    direct-connection credential exists in .env, so this follows the Phase 19 plan 01 precedent
    (short Dashboard pastes are reliable). Do NOT print any secret; these are read-only.

    Once Mehdi pastes the output, edit 20-FINDINGS.md:
      - Fill EVERY TBD "EXACT" cell with the exact data_type + NUMERIC(precision,scale) /
        VARCHAR(length) / TEXT + is_nullable + column_default for each undocumented column on all
        4 tables (from Requête 1).
      - Add a "Constraints (pg_constraint)" subsection recording every CHECK/UNIQUE/FK from
        Requête 2 verbatim. For any target column with NO explicit constraint, write "aucune
        contrainte explicite (confirmé pg_constraint)" — never leave it inferred from OpenAPI silence.
      - Record Requête 3's distinct values under the garages ghost columns as origin evidence
        (e.g. all rows type='pro' → "colonne jamais divergée du défaut en prod").
    Use Write/Edit only on the artifact — no PowerShell/sed/awk.
  </action>
  <what-built>
    Plan 01 produced 20-FINDINGS.md with OpenAPI baseline types but the EXACT (pg catalog) cells
    are still TBD, because PostgREST cannot report numeric precision/scale, text length, or CHECK
    constraint text. These read-only catalog queries close that gap.
  </what-built>
  <how-to-verify>
    Requêtes à exécuter dans Supabase Dashboard → SQL Editor (projet PROD rzbqbaccjyxvtlnfitrr),
    lecture seule, puis coller le résultat :

    -- Requête 1 : type/longueur/précision/nullabilité/défaut exacts
    SELECT table_name, column_name, data_type,
           character_maximum_length, numeric_precision, numeric_scale,
           is_nullable, column_default
    FROM information_schema.columns
    WHERE table_schema = 'public'
      AND table_name IN ('garages','clients','interventions','devis')
    ORDER BY table_name, ordinal_position;

    -- Requête 2 : toutes contraintes CHECK/UNIQUE/FK sur les 4 tables (clôt Pitfall 4)
    SELECT conrelid::regclass AS table_name, conname, contype,
           pg_get_constraintdef(oid) AS definition
    FROM pg_constraint
    WHERE connamespace = 'public'::regnamespace
      AND conrelid::regclass::text IN ('garages','clients','interventions','devis')
    ORDER BY 1, 2;

    -- Requête 3 : valeurs distinctes des colonnes fantômes garages (indice d'origine/intention)
    SELECT DISTINCT type, marque_officielle, actif FROM garages;
    SELECT DISTINCT ville, cp FROM garages WHERE ville IS NOT NULL OR cp IS NOT NULL;
  </how-to-verify>
  <resume-signal>Colle la sortie des 3 requêtes (ou tape "aucune ligne" pour la requête 3 si vide), puis je remplis les cellules EXACT.</resume-signal>
  <verify>
    <automated>grep -qE "numeric_precision|NUMERIC\([0-9]|character_maximum_length|VARCHAR\([0-9]|pg_constraint|information_schema" .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - No "EXACT" cell in 20-FINDINGS.md still reads TBD for any undocumented column on any of the 4 tables.
    - The artifact contains a "Constraints (pg_constraint)" subsection with verbatim `pg_get_constraintdef` output OR an explicit "aucune contrainte explicite (confirmé pg_constraint)" per column.
    - Numeric columns (e.g. devis.total_ht, devis.total_tva, devis.remise_montant) show an exact NUMERIC(precision,scale), not a bare "numeric".
    - Requête 3's distinct garages values are recorded as origin evidence for the garages ghost columns.
    - grep finds "information_schema" and "pg_constraint" (proof the catalog source was used, not OpenAPI).
  </acceptance_criteria>
  <done>Every undocumented column on all 4 tables has exact type/precision/length/nullability/default and its constraint status confirmed from pg_constraint — SCHEMA-02 satisfied at "exact" fidelity.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ghost-column origin confirmation (SCHEMA-03) — Mehdi confirms or corrects</name>
  <files>.planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md</files>
  <read_first>
    - .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md ("Ghost columns — pending Mehdi confirmation" list from plan 01, plus Requête 3 evidence from Task 1)
    - .planning/phases/20-introspection-corr-lation-d-origine/20-RESEARCH.md ("Open Questions" 1 & 2 — the semantic-inference hypotheses to present)
  </read_first>
  <action>
    Present the 9 ghost columns and the research's INFERRED intent to Mehdi (in French — see the
    itemised list in how-to-verify). For each column ask: "confirmes-tu cette origine, corriges-tu,
    ou est-ce oublié/inconnu ?". Then edit 20-FINDINGS.md: add a "SCHEMA-03 — origine confirmée par
    Mehdi" section with one verdict line per ghost column, each being exactly one of:
    "CONFIRMÉ : <intention>" / "CORRIGÉ : <vraie origine>" / "INCONNU/OUBLIÉ : origine
    indéterminée, colonne non utilisée par le code actuel". An "INCONNU" answer is an ACCEPTABLE
    final verdict (per 20-RESEARCH.md State-of-the-Art row 3) — Phase 21 will still capture the
    column faithfully (SCHEMA-02) with an "origine indéterminée" migration comment. Finally, add a
    completeness-gate line asserting every undocumented column on all 4 tables now has BOTH an EXACT
    metadata row AND an origin verdict, and remove/resolve any residual "TBD" so `grep -c TBD` is 0.
    Edit only the artifact — no PowerShell/sed/awk.
  </action>
  <what-built>
    Plan 01 confirmed 9 columns are ghost columns (zero code trail, verified 3 ways) and recorded
    only LOW-confidence semantic guesses for their origin. git cannot recover their true origin —
    only Mehdi can. This checkpoint resolves SCHEMA-03 for those 9 columns with real confidence.
  </what-built>
  <how-to-verify>
    Présente à Mehdi les 9 colonnes fantômes et l'intention INFÉRÉE, une réponse par colonne :

    garages (5) :
      - ville, cp        → hypothèse : découpage d'adresse structuré préparé mais jamais câblé
      - type             → hypothèse : distinction 'pro' vs 'concession' pour le gating tier VERT,
                           jamais utilisée (le RBAC réel passe par app_metadata.role)
      - marque_officielle→ hypothèse : marque qu'une concession représente officiellement
      - actif            → hypothèse : flag soft-delete/désactivation garage jamais câblé
    interventions (4) :
      - niveau_preuve    → hypothèse : défaut 'declare' colle au vocabulaire anti-fraude
                           (facture/visuel/declare) — scaffoldé pour la pondération, jamais lu
      - facture_id (FK factures_scannees) → hypothèse : lien OCR facture→intervention, jamais câblé
      - photo_url        → hypothèse : preuve photo par intervention, jamais câblé
      - operation_code   → hypothèse : lien vers plan_entretien.code_operation, jamais câblé
  </how-to-verify>
  <resume-signal>Pour chaque colonne fantôme, réponds "confirmé", "corrigé : ..." ou "inconnu" — puis je clôture l'artefact.</resume-signal>
  <verify>
    <automated>grep -qE "confirm|CONFIRM|INCONNU|CORRIG|indétermin" .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md && grep -qiE "origine confirmée|Mehdi" .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - A "SCHEMA-03 — origine confirmée par Mehdi" section exists with one verdict line per each of the 9 ghost columns.
    - Each ghost-column verdict is one of: CONFIRMÉ / CORRIGÉ / INCONNU-OUBLIÉ (no ghost column left with only a plan-01 LOW-confidence guess).
    - A final completeness gate line asserts all undocumented columns on all 4 tables have EXACT metadata + an origin verdict.
    - `grep -c TBD 20-FINDINGS.md` returns 0.
  </acceptance_criteria>
  <done>All 9 ghost columns carry Mehdi's confirmed/corrected/undetermined origin, the artifact has zero remaining TBDs, and Phase 21 can consume it without re-discovery.</done>
</task>

</tasks>

<verification>
- Every undocumented column on all 4 tables has exact catalog metadata (precision/length) + constraint status from pg_constraint.
- All 9 ghost columns carry a Mehdi verdict (confirmed/corrected/undetermined).
- `grep -c TBD 20-FINDINGS.md` == 0 (no deferred cells remain).
- No code/schema.sql/migration file modified — only the findings artifact.
</verification>

<success_criteria>
- SCHEMA-02 satisfied at "exact" fidelity: type/precision/length/nullability/constraint captured from the Postgres catalog for every undocumented column.
- SCHEMA-03 satisfied: every undocumented column has a documented origin — commit, legacy migration file, Mehdi-confirmed intent, or an explicit "undetermined ghost" verdict.
- 20-FINDINGS.md is a complete, durable artifact Phase 21 consumes without re-querying prod or re-running git.
</success_criteria>

<output>
After completion, create `.planning/phases/20-introspection-corr-lation-d-origine/20-02-SUMMARY.md`.
</output>
