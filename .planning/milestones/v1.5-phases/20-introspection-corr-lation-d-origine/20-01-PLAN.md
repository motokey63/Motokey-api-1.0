---
phase: 20-introspection-corr-lation-d-origine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md
autonomous: true
requirements: [SCHEMA-02, SCHEMA-03]

must_haves:
  truths:
    - "20-FINDINGS.md exists and lists every undocumented column on garages, clients, interventions, devis — none of the 4 tables omitted"
    - "clients' 5 undocumented columns are recorded as ALREADY resolved by migrations/04-rbac-migration.sql (commit c66ad69) — no re-investigation"
    - "Each garages/interventions/devis undocumented column has a baseline row (type/nullable/default/FK from PostgREST OpenAPI) AND an origin verdict (commit hash+date, OR explicit 'ghost column — no code trail', OR 'code-catch-up commit, true DB origin earlier/unknown')"
  artifacts:
    - path: ".planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md"
      provides: "Durable per-column findings artifact (baseline metadata + git origin) consumable by Phase 21 without re-discovery"
      min_lines: 80
      contains: "migrations/04-rbac-migration.sql"
  key_links:
    - from: "20-FINDINGS.md per-column rows"
      to: "Phase 21 retroactive migrations (SCHEMA-04)"
      via: "each row carries table + column + type + nullable + constraint + origin verdict"
      pattern: "ghost column|code-catch-up|c66ad69"
---

<objective>
Produce the durable findings artifact for Phase 20 by doing everything that can be done autonomously (no Mehdi, no direct Postgres credential): capture a fresh PostgREST-OpenAPI baseline for the 4 target tables, cross-reference the legacy `migrations/` directory to resolve `clients` for free, and run the complete `git log -S` origin sweep across every undocumented column with disambiguation.

Purpose: SCHEMA-02 (baseline type/nullable/default/FK) and SCHEMA-03 (git origin correlation) for all columns reachable without a live catalog query — leaving only exact precision/length/CHECK and Mehdi's ghost-column confirmation for plan 02.
Output: `.planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md` (created).

This is an INVESTIGATION phase. This plan writes NO application code, NO schema.sql change, NO migration file. It only creates the findings document.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-introspection-corr-lation-d-origine/20-RESEARCH.md

<interfaces>
<!-- schema.sql BASELINE columns for the 4 target tables (already documented). -->
<!-- An "undocumented" column = present in live prod (via introspect-schema.js) -->
<!-- but ABSENT from these lists. Diff prod against these to derive the definitive set. -->

garages (schema.sql L89-116) — DOCUMENTED columns:
  id, auth_user_id, nom, email, siret, tel, adresse, logo_url, taux_std, taux_spec,
  tva, plan, plan_expire_at, sms_active, qr_prefix, mecano_session_timeout_minutes,
  stripe_customer_id, stripe_subscription_id, plan_code, subscription_status,
  subscription_current_period_end, grace_period_ends_at, motos_limit, users_limit,
  created_at, updated_at
  → Research-expected UNDOCUMENTED (5): ville, cp, type, marque_officielle, actif

clients (schema.sql L142-158) — DOCUMENTED columns:
  id, auth_user_id, garage_id, nom, email, tel, adresse, nb_interventions,
  client_depuis, notes, is_pro, limite_motos_gratuites, created_at, updated_at
  → Research-expected UNDOCUMENTED (5): client_type, raison_sociale, siret,
    tva_intracom, adresse_facturation  (ALL resolved by migrations/04-rbac-migration.sql)

interventions (schema.sql L266-288) — DOCUMENTED columns:
  id, moto_id, garage_id, technicien_id, devis_id, type, titre, description, km,
  date_intervention, montant_ht, montant_ttc, score_confiance, qr_code,
  signature_hash, facture_url, facture_ocr, notifie_client, notifie_at,
  created_at, updated_at
  → Research-expected UNDOCUMENTED (4): niveau_preuve, facture_id, operation_code, photo_url

devis (schema.sql L315-339) — DOCUMENTED columns:
  id, moto_id, garage_id, technicien_id, numero, statut, remise_type, remise_pct,
  remise_note, tva, total_mo_ht, total_pieces_ht, remise_lignes, sous_total_ht,
  remise_globale, base_ht, tva_montant, total_ttc, valide_at, expire_at, pdf_url,
  created_at, updated_at
  → Research-expected UNDOCUMENTED (~24): entite_facturation_id, date_acceptation,
    date_refus, client_nom, client_adresse, client_cp, client_ville, client_email,
    client_tel, client_siret, client_tva, moto_label, moto_vin, moto_km, lignes,
    total_ht, total_tva, remise_montant, date_creation, date_validite, date_envoi,
    or_id, notes, cree_par, client_id
  ⚠️ DO NOT list total_ttc / remise_pct / remise_type / remise_note as undocumented —
     they are in the baseline above. Only total_ht / total_tva / remise_montant are new.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh baseline + legacy-migration cross-reference — create 20-FINDINGS.md</name>
  <files>.planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md</files>
  <read_first>
    - .planning/phases/20-introspection-corr-lation-d-origine/20-RESEARCH.md (Summary, "Preliminary Findings — Column-by-Column", "Architecture Patterns" Pass 0/Pass 1)
    - scripts/introspect-schema.js (existing PostgREST OpenAPI introspection — how to run it)
    - schema.sql lines 89-158 and 266-339 (the 4 target tables' current documented columns — already extracted in the <interfaces> block above)
    - migrations/04-rbac-migration.sql (the file that resolves ALL 5 clients columns — read it in full)
  </read_first>
  <action>
    Step A — Refresh the live baseline (catches any drift since the 2026-07-09 research):
    Run `node scripts/introspect-schema.js` and capture its output. For each of the 4 target
    tables (garages, clients, interventions, devis), extract the live column list. The definitive
    UNDOCUMENTED set per table = live columns MINUS the DOCUMENTED lists in the <interfaces> block
    above. If the live set differs from the research-expected set in <interfaces>, use the LIVE set
    as authoritative and note the delta in the artifact.

    Step B — Pass 0 legacy cross-reference (mandatory — Phase 19 skipped this):
    Read migrations/04-rbac-migration.sql in full. Confirm it adds exactly these 5 columns to
    `clients`: client_type (ENUM client_type_enum, NOT NULL DEFAULT 'particulier'), raison_sociale
    (text), siret (text, UNIQUE index idx_clients_siret WHERE siret IS NOT NULL), tva_intracom
    (text), adresse_facturation (text) — plus CONSTRAINT clients_pro_requirements
    CHECK (client_type = 'particulier' OR (client_type = 'pro' AND raison_sociale IS NOT NULL AND
    siret IS NOT NULL)). Also run `ls migrations/ sql/migrations/` and confirm no OTHER file in
    either directory adds any of the undocumented garages/interventions/devis columns (research
    found none — verify, do not assume). Record the origin commit for the clients cluster as
    c66ad69 (2026-04-14, "feat(L4): migration RBAC — client_type + colonnes pro/particulier") —
    verify with `git log --oneline -S"client_type" -- migrations/04-rbac-migration.sql | tail -1`.

    Step C — Create the findings artifact
    `.planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md` with this structure:
      - Header: phase, date, source (fresh introspection date), a completeness checklist
        listing the 4 tables with their undocumented column COUNT.
      - Section "clients — RESOLVED (legacy migration)": a table with all 5 columns, their exact
        type/nullable/default/constraint copied verbatim from migrations/04-rbac-migration.sql,
        and origin = migrations/04-rbac-migration.sql @ c66ad69. Mark clients as needing NO further
        Postgres/git work.
      - Section "garages — undocumented (baseline)", "interventions — undocumented (baseline)",
        "devis — undocumented (baseline)": one table each, one row per undocumented column, columns:
        `column | type (OpenAPI) | nullable | default | FK | EXACT (pg catalog — plan 02) | origin (plan 01 Task 2)`.
        Fill the OpenAPI type/nullable/default/FK columns NOW from Step A's introspection output.
        Leave the "EXACT" and "origin" cells as `TBD` placeholders (filled by Task 2 / plan 02).
      - Copy the research's Preliminary Findings type/nullable/default values as the seed, but
        OVERRIDE any that differ from the fresh Step A introspection.
    Write the file with the Write tool (no PowerShell/sed/awk). Communicate any human-facing note in French.
  </action>
  <verify>
    <automated>test -f .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md && grep -q "migrations/04-rbac-migration.sql" .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md && grep -q "c66ad69" .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md && grep -qE "ville|marque_officielle" .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md && grep -qE "niveau_preuve|operation_code" .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md && grep -qE "entite_facturation_id|lignes" .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `20-FINDINGS.md` exists in the phase directory.
    - grep finds all 5 clients columns (client_type, raison_sociale, siret, tva_intracom, adresse_facturation) with the string "migrations/04-rbac-migration.sql" and "c66ad69".
    - grep finds "clients_pro_requirements" (the CHECK constraint) recorded verbatim.
    - grep finds all 5 garages undocumented columns (ville, cp, type, marque_officielle, actif) each with a type/nullable/default cell.
    - grep finds all 4 interventions undocumented columns (niveau_preuve, facture_id, operation_code, photo_url).
    - grep finds the devis undocumented columns including entite_facturation_id, lignes, total_ht, total_tva, remise_montant (and NOT a false claim that total_ttc/remise_pct are undocumented).
    - A completeness checklist near the top states the undocumented column count per table (garages/clients/interventions/devis).
  </acceptance_criteria>
  <done>20-FINDINGS.md created with clients fully resolved and a baseline (type/nullable/default/FK) row for every undocumented garages/interventions/devis column, EXACT + origin cells left as TBD.</done>
</task>

<task type="auto">
  <name>Task 2: Full git-log-S origin sweep (SCHEMA-03) with disambiguation</name>
  <files>.planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md</files>
  <read_first>
    - .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md (the artifact Task 1 created — you are filling its "origin" cells)
    - .planning/phases/20-introspection-corr-lation-d-origine/20-RESEARCH.md ("Common Pitfalls" 2 & 3, "Architecture Patterns" Pass 2, "Preliminary Findings" origin columns)
    - supabase.js lines 186 (Garages.update allowlist) and 397-408 (Interventions.create payload) — to confirm the ghost-column verdict architecturally, not just from grep
  </read_first>
  <action>
    For EVERY undocumented column recorded in 20-FINDINGS.md on garages, interventions, and devis
    (clients is already resolved — skip it), determine an origin verdict.

    Per-column recipe (from 20-RESEARCH.md Code Examples):
      1. `git log --oneline -S"<column_name>" -- . | tail -1`   (oldest commit touching the literal)
      2. If a hit: `git show -s --format="%h %ad %s" --date=short <sha>` for date+message, and
         `git show <sha> -- <file>` to read the diff.
      3. DISAMBIGUATION (Pitfall 3): column names ville, cp, type, actif, photo_url, notes,
         total_ht are reused across garages/clients/catalogue_pieces/garage_users/motos/devis.
         A raw hit does NOT prove it is the TARGET table's column. Open the diff and confirm the
         surrounding code references the target table (`.from('garages')`, `INSERT INTO garages`,
         `insert('interventions', {...})`, an `ALTER TABLE <target>`). If the only hits are for a
         DIFFERENT table's same-named column, treat as "no trail for the target column".
      4. Classify each column into exactly one origin verdict:
         - COMMIT ORIGIN: a real introducing commit found and confirmed for the target table →
           record "hash | date | message".
         - GHOST COLUMN: zero hits for the target column across full history → record
           "ghost column — no code trail (verified via git log -S + allowlist inspection)".
           Confirm architecturally: garages ghost cols (ville/cp/type/marque_officielle/actif) are
           NOT in Garages.update()'s allowlist (supabase.js L186:
           ['nom','tel','adresse','siret','taux_std','taux_spec','tva','sms_active','mecano_session_timeout_minutes']);
           interventions ghost cols (niveau_preuve/facture_id/operation_code/photo_url) are NOT in
           Interventions.create()'s literal payload (supabase.js L397-408). Cite these line refs.
         - CODE-CATCH-UP (Pitfall 2): hit exists but the commit message says "aligner/align",
           "fix column name", "real/live schema", "rewrite against real" (e.g. f2d7d9a 2026-05-11,
           b29d4f5 2026-07-04) → record "earliest code awareness: <hash/date>; true DB origin
           earlier/unknown (undocumented Dashboard ALTER)". Do NOT list the catch-up commit as the origin.
    Prioritise the ~9 devis columns the research did NOT individually isolate: notes, cree_par,
    client_id, plus any of moto_label/moto_vin/moto_km/or_id/date_validite/date_envoi/remise_montant
    not yet pinned — run the recipe for each and record the verdict.
    Update every "origin" cell in 20-FINDINGS.md from TBD to one of the three verdicts. Add a short
    "Ghost columns — pending Mehdi confirmation (plan 02)" list enumerating exactly the 9 ghost
    columns (garages: ville/cp/type/marque_officielle/actif; interventions:
    niveau_preuve/facture_id/operation_code/photo_url) so plan 02's checkpoint has a ready list.
    Use the Write/Edit tools only — no PowerShell/sed/awk on the artifact.
  </action>
  <verify>
    <automated>grep -qE "ghost column" .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md && grep -qE "code awareness|Dashboard" .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md && grep -q "supabase.js" .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - No "origin" cell in 20-FINDINGS.md still contains the literal "TBD" for the origin column (only EXACT-column TBDs owned by plan 02 remain).
    - The string "ghost column" appears and is attached to exactly the 9 expected columns (garages x5 + interventions x4).
    - The ghost-column rows cite supabase.js line references (L186 allowlist and/or L397-408 payload).
    - At least one devis column origin is recorded as a "code awareness / true DB origin earlier" verdict (Pitfall 2 applied), NOT as a plain origin.
    - A dedicated "Ghost columns — pending Mehdi confirmation" list enumerates the 9 columns for plan 02.
    - Every undocumented garages/interventions/devis column has a non-empty origin verdict.
  </acceptance_criteria>
  <done>Every undocumented garages/interventions/devis column in 20-FINDINGS.md carries one of three origin verdicts (commit origin / ghost column / code-catch-up), the 9 ghost columns are enumerated for plan 02, and the code-catch-up caveat is applied to the devis cluster.</done>
</task>

</tasks>

<verification>
- `20-FINDINGS.md` exists and covers all 4 tables (garages/clients/interventions/devis) — completeness checklist present.
- clients fully resolved from migrations/04-rbac-migration.sql (no TBD anywhere for clients).
- Every garages/interventions/devis undocumented column has an OpenAPI baseline row + an origin verdict.
- EXACT (pg catalog) cells remain TBD — intentionally deferred to plan 02.
- No application code, schema.sql, or migration file was modified (only the findings artifact).
</verification>

<success_criteria>
- SCHEMA-02 (partial): baseline type/nullable/default/FK captured for every undocumented column; clients exact metadata resolved from the legacy migration.
- SCHEMA-03: every undocumented column has a documented origin verdict (commit / ghost / code-catch-up), with disambiguation applied.
- The artifact is durable and hands plan 02 a precise remaining scope (EXACT metadata + 9 ghost confirmations).
</success_criteria>

<output>
After completion, create `.planning/phases/20-introspection-corr-lation-d-origine/20-01-SUMMARY.md`.
</output>
