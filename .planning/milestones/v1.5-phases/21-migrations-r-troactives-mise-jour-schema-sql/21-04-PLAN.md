---
phase: 21-migrations-r-troactives-mise-jour-schema-sql
plan: 04
type: execute
wave: 3
depends_on: ["21-01", "21-02", "21-03"]
files_modified: []
autonomous: true
requirements: [SCHEMA-05, SCHEMA-06]
must_haves:
  truths:
    - "An automatic comparison confirms schema.sql has no remaining undocumented Gap A column vs 20-FINDINGS.md"
    - "Gap B tables/view in schema.sql are confirmed faithful copies of migrations 13/15"
    - "No FK to an out-of-scope table (entites_facturation, factures_scannees) leaked into schema.sql"
    - "The stale devis columns are confirmed removed"
  artifacts:
    - path: ".planning/phases/21-migrations-r-troactives-mise-jour-schema-sql/21-04-SUMMARY.md"
      provides: "Verification report proving success criterion 4 (no remaining undocumented object)"
      contains: "Gap A"
  key_links:
    - from: "schema.sql"
      to: "20-FINDINGS.md (Gap A) + migrations 13/15 (Gap B)"
      via: "grep-based presence diff + live introspection sanity check"
      pattern: "SCHEMA-05|SCHEMA-06"
---

<objective>
Final phase-gate verification. Prove success criterion 4: after plans 21-01/02/03, schema.sql shows no remaining undocumented column or object for Gap A or Gap B. Verification is textual/manual (grep-based presence checks + diff-against-source-file) plus a live prod introspection sanity check — NO local Postgres/psql exists in this environment, so full live-bootstrap SQL syntax validation is explicitly Phase 22's job (SCHEMA-07). This plan does not claim to have validated SQL syntax by execution.

Purpose: close SCHEMA-05 + SCHEMA-06 by confirming completeness and catching any transcription drift before Phase 22.
Output: `21-04-SUMMARY.md` recording the pass/fail of each check.

Depends on 21-01 (migration files), 21-02 (Gap A in schema.sql), 21-03 (Gap B in schema.sql).
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md
@sql/migrations/13_liaison_client_moto.sql
@sql/migrations/15_billing_foundation.sql
@schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Gap A completeness in schema.sql vs 20-FINDINGS.md</name>
  <files>.planning/phases/21-migrations-r-troactives-mise-jour-schema-sql/21-04-SUMMARY.md (read-only verification of schema.sql)</files>
  <read_first>
    - .planning/phases/20-introspection-corr-lation-d-origine/20-FINDINGS.md (the authoritative column lists for garages 5 / clients 5 / interventions 4 / devis 25 + the stale-10 list)
    - schema.sql (garages, clients, interventions, devis CREATE TABLE blocks as edited by 21-02)
  </read_first>
  <action>
Perform a column-by-column presence diff for each of the 4 Gap A tables. For each table, extract the relevant CREATE TABLE block and confirm every expected column name appears exactly once, with the expected type/nullable/default. Use these checks:

1. **garages (5):** confirm `ville`, `cp`, `type`, `marque_officielle`, `actif` present. Confirm `type` is `NOT NULL DEFAULT 'pro'` and `actif` is `BOOLEAN NOT NULL DEFAULT true`.
2. **clients (5 + type + constraint + index):** confirm `client_type`, `raison_sociale`, `siret`, `tva_intracom`, `adresse_facturation`; `client_type_enum` created AND dropped in NETTOYAGE; `clients_pro_requirements` CHECK; `idx_clients_siret` unique index; 5 `COMMENT ON COLUMN clients.` lines.
3. **interventions (4):** confirm `niveau_preuve` (with CHECK), `facture_id` (NO REFERENCES), `operation_code`, `photo_url`.
4. **devis (25 added, 10 removed):** confirm all 25 Gap A columns present (`client_nom`, `client_adresse`, `client_cp`, `client_ville`, `client_email`, `client_tel`, `client_siret`, `client_tva`, `client_id`, `moto_label`, `moto_vin`, `moto_km`, `lignes`, `total_ht`, `total_tva`, `remise_montant`, `date_creation`, `date_validite`, `date_envoi`, `date_acceptation`, `date_refus`, `or_id`, `notes`, `cree_par`, `entite_facturation_id`). Confirm the 10 stale columns are GONE from the devis block: `technicien_id`, `total_mo_ht`, `total_pieces_ht`, `remise_lignes`, `sous_total_ht`, `remise_globale`, `base_ht`, `tva_montant`, `valide_at`, `expire_at`.
5. **FK landmine:** `grep -n "REFERENCES factures_scannees\|REFERENCES entites_facturation" schema.sql` must return ZERO.

Record each check's result (PASS/FAIL with the grep evidence) in the SUMMARY. If any FAIL, list the exact missing/extra column so a follow-up gap-closure plan can fix it.
  </action>
  <verify>
    <automated>grep -c "REFERENCES factures_scannees\|REFERENCES entites_facturation" schema.sql; grep -n "client_type\|clients_pro_requirements\|idx_clients_siret\|entite_facturation_id\|niveau_preuve" schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "REFERENCES factures_scannees\|REFERENCES entites_facturation" schema.sql` returns 0
    - All 5 garages + 5 clients + 4 interventions + 25 devis Gap A column names are found in schema.sql (documented as a checklist in the SUMMARY)
    - None of the 10 stale devis column names remain inside the devis CREATE TABLE block
    - `clients_pro_requirements`, `idx_clients_siret`, and `client_type_enum` (create + drop) are all present
    - The SUMMARY contains an explicit PASS/FAIL line per table plus the FK-landmine check
  </acceptance_criteria>
  <done>Every Gap A column is confirmed present in schema.sql with correct constraints; stale columns confirmed removed; no out-of-scope FK leaked; results recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Gap B faithful-copy + NETTOYAGE + live prod sanity check</name>
  <files>.planning/phases/21-migrations-r-troactives-mise-jour-schema-sql/21-04-SUMMARY.md (read-only verification of schema.sql)</files>
  <read_first>
    - sql/migrations/13_liaison_client_moto.sql and sql/migrations/15_billing_foundation.sql (the verbatim DDL sources)
    - schema.sql (Gap B section + NETTOYAGE + TYPES + RLS blocks as edited by 21-03)
    - scripts/introspect-schema.js (the live introspection tool — note EXPECTED_TABLES does NOT include Gap B tables; the default run introspects ALL prod tables and prints them, which is enough for a sanity confirmation without editing the script this phase)
  </read_first>
  <action>
1. **Gap B verbatim-copy diff.** For each of `billing_events`, `motos_proprietaires_historique`, `liaisons_client_garage`, `reclamations_moto`, and the `v_motos_avec_proprietaire` view, compare the schema.sql block against the source migration file block field-by-field. Confirm: same columns, same types, same NOT NULL/DEFAULT, same CHECK (`historique_coherence`), same indexes, same view SELECT. Confirm NO backfill `INSERT`/`UPDATE` statements were copied.
2. **Enum + NETTOYAGE idempotency.** Confirm `mode_acquisition_enum` is declared with all 8 values and dropped in NETTOYAGE; confirm all 4 Gap B tables + the view have `DROP TABLE/VIEW ... CASCADE` entries positioned before their referenced parent tables. This is the "re-running schema.sql twice does not error" guarantee.
3. **Live prod sanity check.** Run `node scripts/introspect-schema.js` (default mode; requires .env; ~2s). This confirms prod hasn't drifted further since Phase 20's snapshot and prints all prod tables/columns. Cross-check that the Gap B table names appear in prod's output (they should — they were created by migrations 13/15). This is a sanity check, NOT a schema.sql validator. Do NOT claim it validated schema.sql's SQL syntax — it did not.
4. **Scope note.** Explicitly record in the SUMMARY that full live-bootstrap SQL syntax validation (instantiating schema.sql against a fresh Supabase project) is deferred to Phase 22 / SCHEMA-07, consistent with Phase 19's precedent (its verification report scored "no local psql available" as non-blocking).

Record each check PASS/FAIL with evidence in the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/introspect-schema.js; grep -c "DROP TABLE IF EXISTS billing_events\|DROP TABLE IF EXISTS reclamations_moto\|DROP TABLE IF EXISTS liaisons_client_garage\|DROP TABLE IF EXISTS motos_proprietaires_historique\|DROP VIEW  IF EXISTS v_motos_avec_proprietaire" schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DROP TABLE IF EXISTS billing_events\|DROP TABLE IF EXISTS reclamations_moto\|DROP TABLE IF EXISTS liaisons_client_garage\|DROP TABLE IF EXISTS motos_proprietaires_historique" schema.sql` returns 4, plus the view DROP present
    - `grep -n "CREATE TYPE mode_acquisition_enum" schema.sql` shows 8 enum values; `grep -n "DROP TYPE  IF EXISTS mode_acquisition_enum" schema.sql` present
    - `node scripts/introspect-schema.js` exits 0 (narrow-scope assertions still PASS — confirms prod reachable and unchanged) and its output lists billing_events / motos_proprietaires_historique / liaisons_client_garage / reclamations_moto among prod tables
    - `grep -n "INSERT INTO\|Backfill" schema.sql` shows no Gap B backfill statements leaked
    - The SUMMARY records the deferral of live-bootstrap syntax validation to Phase 22 and does NOT claim SQL syntax was validated by execution
  </acceptance_criteria>
  <done>Gap B DDL confirmed a faithful copy of migrations 13/15; NETTOYAGE idempotency confirmed; live prod introspection ran clean; Phase 22 deferral documented.</done>
</task>

</tasks>

<verification>
- Success criterion 4 met: no remaining undocumented Gap A column (checklist PASS) and no missing Gap B object (checklist PASS) in schema.sql
- No out-of-scope FK in schema.sql
- `node scripts/introspect-schema.js` confirms prod reachable and unchanged since Phase 20
- Live-bootstrap syntax validation explicitly deferred to Phase 22 (not claimed here)
</verification>

<success_criteria>
- SCHEMA-05 + SCHEMA-06 completeness confirmed by automatic/textual comparison
- Any residual gap is surfaced (with the exact column/object) rather than silently passed
</success_criteria>

<output>
After completion, create `.planning/phases/21-migrations-r-troactives-mise-jour-schema-sql/21-04-SUMMARY.md` with the per-check PASS/FAIL verification report.
</output>
