---
phase: 22-v-rification-bootstrap-nettoyage-header
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified: []
autonomous: false
requirements: [SCHEMA-07]

user_setup:
  - service: supabase
    why: "A brand-new throwaway Supabase project is required to bootstrap-test schema.sql against a genuinely empty Postgres. Project creation cannot be automated from this environment (needs an org-level Dashboard/Management-API token not in .env)."
    env_vars:
      - name: FRESH_DB_URL
        source: "New Supabase project -> Project Settings -> Database -> Connection string -> 'URI' tab (direct Postgres connection string)"
      - name: FRESH_REST_URL
        source: "New Supabase project -> Project Settings -> API -> Project URL"
      - name: FRESH_ANON_KEY
        source: "New Supabase project -> Project Settings -> API -> Project API keys -> anon/public key"
    dashboard_config:
      - task: "Create ONE new free-tier Supabase project (NOT prod rzbqbaccjyxvtlnfitrr). Wait for provisioning to finish. Do NOT run any SQL in its public schema before the executor runs schema.sql."
        location: "Supabase Dashboard -> New project"

must_haves:
  truths:
    - "schema.sql executes against a genuinely empty fresh Postgres with zero SQL errors (prints SCHEMA_BOOTSTRAP_OK)"
    - "An automated compare confirms the fresh bootstrap matches prod for all Gap A columns and all 5 Gap B objects"
    - "v_motos_avec_proprietaire exists structurally in the fresh project (via information_schema.views if PostgREST does not expose it)"
  artifacts: []
  key_links:
    - from: "scripts/bootstrap-fresh-schema.js"
      to: "fresh Supabase Postgres"
      via: "FRESH_DB_URL from .env"
      pattern: "SCHEMA_BOOTSTRAP_OK"
    - from: "scripts/introspect-schema.js --compare"
      to: "fresh Supabase REST API"
      via: "FRESH_REST_URL + FRESH_ANON_KEY"
      pattern: "billing_events"
---

<objective>
Prove SCHEMA-07's core claim: `schema.sql` actually executes cleanly against a genuinely empty
Postgres and the resulting schema matches prod for every Gap A / Gap B object. This is the one
thing every prior phase explicitly deferred (21-VERIFICATION.md: "Full SQL syntax validation via a
fresh Supabase bootstrap was not run — explicitly deferred to Phase 22"). Grep-based structural
checks are NOT proof the SQL parses/executes — this plan runs it for real.

The blocking constraint: creating the fresh Supabase project and retrieving its connection string
is an unavoidable HUMAN ACTION (Mehdi, in the Dashboard). It cannot be automated from this
environment. So this plan opens with a human-action checkpoint, then the executor runs the two
pre-built scripts (from 22-01) against the credentials Mehdi supplies.

Purpose: Close SCHEMA-07 criteria 1 and 2 (clean bootstrap + automated compare) with real
execution evidence, not inference.
Output: A passing bootstrap run (SCHEMA_BOOTSTRAP_OK) and a passing compare (exit 0, all 18
expected objects present). No files modified — this is verification.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-v-rification-bootstrap-nettoyage-header/22-RESEARCH.md
@.planning/phases/22-v-rification-bootstrap-nettoyage-header/22-01-SUMMARY.md

<interfaces>
<!-- Scripts authored in 22-01, consumed here: -->
<!-- scripts/bootstrap-fresh-schema.js  — reads process.env.FRESH_DB_URL, prints SCHEMA_BOOTSTRAP_OK -->
<!-- scripts/introspect-schema.js --compare <FRESH_REST_URL> <FRESH_ANON_KEY> — diffs fresh vs prod for all 18 EXPECTED_TABLES -->

<!-- Fallback view-existence check (Pitfall 4 / Open Question 1) — run over the SAME pg connection: -->
```sql
SELECT table_name FROM information_schema.views
WHERE table_schema = 'public' AND table_name = 'v_motos_avec_proprietaire';
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Mehdi crée un projet Supabase neuf et fournit ses accès</name>
  <what-built>
    Les scripts de vérification sont prêts (plan 22-01) : `scripts/bootstrap-fresh-schema.js` et
    `scripts/introspect-schema.js --compare`. Il manque uniquement un projet Supabase neuf et jetable
    contre lequel exécuter `schema.sql` — création impossible à automatiser depuis cet environnement
    (pas de token org-level dans `.env`).
  </what-built>
  <how-to-verify>
    Mehdi, dans le Dashboard Supabase (https://supabase.com/dashboard) :
    1. Crée UN nouveau projet free-tier. **PAS** le projet prod `rzbqbaccjyxvtlnfitrr`. Choisis un
       nom jetable (ex. `motokey-schema-check-22`). Attends la fin du provisioning.
    2. **N'exécute AUCUN SQL** dans le schéma `public` de ce projet avant que l'exécuteur ait lancé
       `schema.sql` (le projet doit être vraiment vierge — Pitfall 5 du research).
    3. Récupère 3 valeurs et ajoute-les au fichier `.env` local (jamais dans le chat, jamais dans un
       commit — `.env` est déjà gitignoré) :
       - `FRESH_DB_URL=` → Project Settings → Database → Connection string → onglet **URI**
         (chaîne de connexion Postgres directe). Si Supabase ne propose qu'une URL "pooler", elle
         convient aussi.
       - `FRESH_REST_URL=` → Project Settings → API → **Project URL**
       - `FRESH_ANON_KEY=` → Project Settings → API → Project API keys → clé **anon/public**
         (on utilise la clé anon, pas la service key, pour minimiser l'exposition d'un secret dans
         une commande de compare).
    4. Confirme quand c'est fait.

    Note sécurité : ne colle jamais `FRESH_DB_URL` ni les clés en clair dans la conversation —
    seulement dans `.env`. Les scripts ne loggent que le host, jamais la chaîne complète.
  </how-to-verify>
  <resume-signal>Réponds "fait" (les 3 variables sont dans .env) ou signale un blocage.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Bootstrap schema.sql contre le projet neuf</name>
  <files>(aucun — exécution de vérification)</files>
  <read_first>
    - scripts/bootstrap-fresh-schema.js (confirm it reads FRESH_DB_URL and has the prod guard, before running)
    - 22-RESEARCH.md — "Architecture Patterns" (steps 3-5) and Pitfall 1 (worktree staleness) / Pitfall 2 (pg missing)
  </read_first>
  <action>
    First, guard against known pitfalls from 22-RESEARCH.md:
    - Worktree staleness (Pitfall 1): run `git log --oneline -3` and confirm HEAD includes Phase 21's
      work (commit `725407f` or later; `8a519f7` is the current expected tip). If schema.sql is
      missing `billing_events` (grep it), run `git merge --ff-only master` before proceeding.
    - pg present (Pitfall 2): `node -e "require('pg')"` — if it errors, `npm install pg --no-save`.

    Then run the committed bootstrap runner:
    `node scripts/bootstrap-fresh-schema.js`

    Expected output: `SCHEMA_BOOTSTRAP_OK` and exit 0.

    Optional (do NOT spend time on it): Mehdi may first try pasting schema.sql into the fresh
    project's Dashboard SQL Editor — but per Phase 19 this truncated 3× at random points on a
    smaller file (834 lines now). If any truncation sign appears, abandon immediately and use this
    pg script — that IS the primary path, not a fallback.

    On failure (`SCHEMA_BOOTSTRAP_FAILED: <postgres error>`): the pg driver surfaces the exact
    Postgres error message + position. Diagnose it (most likely class would be a
    `relation/type "x" does not exist` ordering bug — though 22-RESEARCH.md verified DDL ordering is
    sound this session, so a clean run is expected). Patch `schema.sql` minimally, then re-run —
    schema.sql's top NETTOYAGE `DROP ... CASCADE` block makes re-running safe against the same fresh
    project without recreating it. If schema.sql is patched, note it for plan 22-03 (header must
    still reflect a verified-clean state). Do NOT print the connection string at any point.
  </action>
  <verify>
    <automated>node scripts/bootstrap-fresh-schema.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/bootstrap-fresh-schema.js` prints `SCHEMA_BOOTSTRAP_OK` and exits 0
    - No `SCHEMA_BOOTSTRAP_FAILED` in output
    - Command output does not contain the raw `FRESH_DB_URL` connection string (host-only logging)
    - `git log --oneline -3` shows HEAD at Phase 21 tip or later (schema.sql contains `billing_events`)
  </acceptance_criteria>
  <done>schema.sql executed against a genuinely empty fresh Postgres with zero SQL errors — SCHEMA-07 criterion 1 satisfied with real execution evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Compare automatique fresh vs prod + vérif structurelle de la vue</name>
  <files>(aucun — exécution de vérification)</files>
  <read_first>
    - scripts/introspect-schema.js (confirm EXPECTED_TABLES now has the 18 entries from 22-01, and how --compare reports pass/fail)
    - 22-RESEARCH.md — Pitfall 3 (EXPECTED_TABLES) and Pitfall 4 / Open Question 1 (view exposure via PostgREST)
  </read_first>
  <action>
    Run the compare between the freshly-bootstrapped project and prod:
    `node scripts/introspect-schema.js --compare "$FRESH_REST_URL" "$FRESH_ANON_KEY"`
    (Pass the values from .env via shell variables so the anon key isn't hand-typed into the command
    literally; e.g. source them from .env or use `node -e` to read them. Never echo the key.)

    Expected: exit 0, and the report shows all 18 EXPECTED_TABLES (13 baseline + 5 Gap B) present in
    the fresh project matching prod, with no missing Gap A columns on garages/clients/interventions/devis.

    View caveat (MEDIUM confidence — Pitfall 4 / Open Question 1): `v_motos_avec_proprietaire` is a
    VIEW. PostgREST may or may not list it in its OpenAPI `definitions` depending on grants. If the
    compare reports the view as missing from the fresh project's introspection output, do NOT
    conclude schema.sql failed — instead confirm the view exists STRUCTURALLY via a direct query over
    the same pg connection used in Task 2. Write a tiny throwaway check (scratchpad, not committed) or
    extend a one-off `node -e` using pg + FRESH_DB_URL that runs:
    `SELECT table_name FROM information_schema.views WHERE table_schema='public' AND table_name='v_motos_avec_proprietaire';`
    If it returns one row, the view was created without error → SCHEMA-07's existence requirement is
    met (PostgREST REST-visibility is a secondary/informational concern, not what SCHEMA-07 asks).
    Record which path confirmed the view (PostgREST compare vs information_schema fallback) in the
    SUMMARY.

    Also re-run the default-mode prod sanity check to catch any mid-phase live drift:
    `node scripts/introspect-schema.js`
  </action>
  <verify>
    <automated>node scripts/introspect-schema.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/introspect-schema.js --compare "$FRESH_REST_URL" "$FRESH_ANON_KEY"` exits 0
    - The compare report lists all 4 Gap B tables (billing_events, motos_proprietaires_historique, liaisons_client_garage, reclamations_moto) as present in the fresh project
    - `v_motos_avec_proprietaire` confirmed to EXIST in the fresh project — either via the compare output OR via the `information_schema.views` fallback returning exactly 1 row
    - No missing Gap A columns reported on garages/clients/interventions/devis
    - `node scripts/introspect-schema.js` (default prod mode) still exits 0
    - The anon key does not appear literally in any committed file or the SUMMARY
  </acceptance_criteria>
  <done>Automated compare confirms fresh bootstrap matches prod for all Gap A columns and all 5 Gap B objects (view existence confirmed structurally) — SCHEMA-07 criterion 2 satisfied.</done>
</task>

</tasks>

<verification>
- Bootstrap prints SCHEMA_BOOTSTRAP_OK (criterion 1).
- Compare exits 0 with all 18 objects present; view existence confirmed (criterion 2).
- Default-mode prod introspection still green (no mid-phase drift).
- No secrets printed in chat, logs, or committed files.
</verification>

<success_criteria>
- SCHEMA-07 criteria 1 and 2 satisfied with real execution evidence (not grep inference).
- Any schema.sql patch needed during bootstrap is captured for 22-03's header rewrite.
</success_criteria>

<output>
After completion, create `.planning/phases/22-v-rification-bootstrap-nettoyage-header/22-02-SUMMARY.md`
(record: bootstrap result, compare result, how the view was confirmed, any schema.sql patch applied).
</output>
