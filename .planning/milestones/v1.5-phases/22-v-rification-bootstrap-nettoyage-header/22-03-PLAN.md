---
phase: 22-v-rification-bootstrap-nettoyage-header
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - schema.sql
  - .planning/PROJECT.md
autonomous: true
requirements: [SCHEMA-07]

must_haves:
  truths:
    - "schema.sql's header no longer describes Gap A or Gap B as unresolved/non-covered/TODO"
    - "schema.sql's header still documents the ~19 out-of-scope tables (OR, billing/factures, catalogue, PDP, client-auth) as a separate, still-real scope boundary"
    - "PROJECT.md Known Gaps no longer lists the stale schema.sql header bullet as an open cleanup item"
  artifacts:
    - path: "schema.sql"
      provides: "Header rewritten: Gap A/B marked résolu+vérifié, out-of-scope boundary preserved"
      contains: "RÉSOLU"
    - path: ".planning/PROJECT.md"
      provides: "Known Gaps section updated to reflect Gap A/B closure"
      contains: "SCHEMA-07"
  key_links:
    - from: "schema.sql header"
      to: "sql/migrations/20_garages_undocumented_columns.sql"
      via: "resolved-see-X pointer for Gap A origin detail"
      pattern: "20_garages_undocumented_columns"
---

<objective>
Documentation catch-up, the final step of SCHEMA-07. Now that 22-02 has PROVEN schema.sql
bootstraps clean and matches prod, rewrite the stale header comment block in `schema.sql` (which
still describes Gap A and Gap B as unresolved) and update `PROJECT.md`'s Known Gaps to drop the
schema.sql-header-staleness bullet.

Critical scoping constraint (22-RESEARCH.md "Header Rewrite Guidance" + anti-pattern #3): this
rewrite closes ONLY the Gap A / Gap B claims. It must NOT claim full 38-table parity — the ~19
out-of-scope tables (OR system, billing/factures, catalogue pièces, PDP e-invoicing, separate
client-auth) are still genuinely absent from schema.sql and remain intentionally out of scope per
REQUIREMENTS.md. That broader partial-bootstrap boundary (header block 1) stays.

Purpose: Close SCHEMA-07 criteria 3 (header no longer lists Gap A/B unresolved) and 4 (PROJECT.md
reflects closure). This must run AFTER 22-02 because the new header text asserts "vérifié bootstrap
Phase 22" — that assertion is only true once 22-02 passes.
Output: Rewritten schema.sql header + updated PROJECT.md Known Gaps.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-v-rification-bootstrap-nettoyage-header/22-RESEARCH.md
@.planning/phases/22-v-rification-bootstrap-nettoyage-header/22-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Réécrire l'en-tête de schema.sql (Gap A/B résolus, hors-scope préservé)</name>
  <files>schema.sql</files>
  <read_first>
    - schema.sql (read lines 1-45 — you must see the current 3-block header exactly as-is before editing; line numbers of blocks may have shifted if 22-02 applied a patch)
    - .planning/phases/22-v-rification-bootstrap-nettoyage-header/22-02-SUMMARY.md (check whether 22-02 applied any schema.sql patch that affects wording)
    - 22-RESEARCH.md — "Header Rewrite Guidance" section (the authoritative spec for what to keep/remove/replace)
  </read_first>
  <action>
    The header has 3 comment blocks. Apply exactly these changes; do NOT touch anything below the
    header (the `-- EXTENSIONS` / `CREATE EXTENSION` line and everything after stays untouched).

    BLOCK 1 — the "⚠️ BOOTSTRAP PARTIEL CONNU" opening line + the ~19 out-of-scope table list
    (currently lines 6-18). KEEP the whole out-of-scope list unchanged (it is still true and
    separately tracked). ONLY change the opening line's phase/date parenthetical.
    Replace this exact line:
    `-- ⚠️ BOOTSTRAP PARTIEL CONNU (Phase 19 v1.4 — 2026-07-08, patché 2026-07-09)`
    with:
    `-- ⚠️ BOOTSTRAP PARTIEL CONNU (Phase 19 v1.4 — 2026-07-08 ; Gap A + Gap B résolus et vérifiés Phases 21-22 v1.5 — 2026-07-10)`
    Leave lines 7-18 (the "Ce fichier reflète..." through "Parité complète 38 tables : différée..."
    block listing ordres_reparation, entites_facturation, pdp_queue, users_client, etc.) EXACTLY as-is.

    BLOCK 2 — the "GAP CONNU SUPPLÉMENTAIRE" Gap B block (currently lines 20-26, from
    `-- GAP CONNU SUPPLÉMENTAIRE` through `--     (Les colonnes motos/clients de la migration 13 elle-même SONT incluses ci-dessous.)`).
    REMOVE it entirely and replace with exactly:
    ```
    -- Gap B (objets des migrations 13/15) — RÉSOLU en Phase 21 (SCHEMA-06), vérifié par bootstrap
    -- propre + comparaison automatique vs prod en Phase 22 (SCHEMA-07) :
    --   • billing_events (migration 15) + motos_proprietaires_historique, liaisons_client_garage,
    --     reclamations_moto et la vue v_motos_avec_proprietaire (migration 13) sont créés ci-dessous.
    ```

    BLOCK 3 — the "DÉRIVE NON DOCUMENTÉE DÉCOUVERTE" Gap A block (currently lines 28-40, from
    `-- DÉRIVE NON DOCUMENTÉE DÉCOUVERTE` through `-- ... À traiter dans une phase future.`).
    REMOVE it entirely — including the "Non couvert ici — nécessiterait une recherche dédiée..."
    framing — and replace with exactly:
    ```
    -- Gap A (39 colonnes prod sans fichier de migration, sur garages/clients/interventions/devis) —
    -- RÉSOLU en Phase 21 (SCHEMA-04/05), vérifié bootstrap Phase 22 (SCHEMA-07) :
    --   • Chaque colonne porte un commentaire d'origine inline dans son bloc CREATE TABLE ci-dessous.
    --   • Détail origine/verdict : sql/migrations/20_garages_undocumented_columns.sql,
    --     sql/migrations/21_interventions_undocumented_columns.sql,
    --     sql/migrations/22_devis_undocumented_columns.sql (+ .planning .../20-FINDINGS.md).
    ```

    After editing, there must be NO remaining occurrence of the phrases "Non couvert ici" or
    "non corrigés car hors du périmètre" anywhere in schema.sql — those are the exact stale-framing
    strings success criterion 3 checks for.
  </action>
  <verify>
    <automated>grep -c "Non couvert ici\|non corrigés car hors du périmètre" schema.sql; test $(grep -c "Non couvert ici\|non corrigés car hors du périmètre" schema.sql) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Non couvert ici" schema.sql` returns 0 matches
    - `grep -n "non corrigés car hors du périmètre" schema.sql` returns 0 matches
    - schema.sql header contains `Gap A` followed by `RÉSOLU` (Gap A marked resolved)
    - schema.sql header contains `Gap B` followed by `RÉSOLU` (Gap B marked resolved)
    - schema.sql header contains `SCHEMA-07`
    - schema.sql header still contains `ordres_reparation` and `entites_facturation` and `pdp_queue` (block 1 out-of-scope list preserved)
    - schema.sql header still contains `Parité complète 38 tables` (out-of-scope boundary preserved)
    - schema.sql header references `20_garages_undocumented_columns.sql`
    - `CREATE EXTENSION IF NOT EXISTS "uuid-ossp"` still present (body untouched)
  </acceptance_criteria>
  <done>Header no longer frames Gap A/B as unresolved/TODO; out-of-scope ~19-table boundary preserved; SQL body unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Mettre à jour PROJECT.md Known Gaps (fermeture dérive)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (read the "### Known Gaps (carried into next milestone)" section, ~lines 38-43 — you must see the current schema.sql bullet verbatim before editing)
  </read_first>
  <action>
    In `.planning/PROJECT.md`, under "### Known Gaps (carried into next milestone)", the schema.sql
    header bullet (currently line ~43) is now resolved. Replace this exact bullet:
    `- **\`schema.sql\` — en-tête known-partial-bootstrap obsolète** : Gap A et Gap B sont désormais entièrement résolus dans le corps de \`schema.sql\` (Phase 21), mais le commentaire d'en-tête (L6-40) décrit encore l'état d'avant — les décrit comme non résolus. Nettoyage cosmétique non-bloquant, prévu Phase 22 (SCHEMA-07) avec la vérification bootstrap.`

    with a resolved line (moves it from open-gap to closed status):
    `- ~~**\`schema.sql\` — en-tête known-partial-bootstrap obsolète**~~ → **RÉSOLU (v1.5 Phase 22, 2026-07-10, SCHEMA-07)** : bootstrap propre vérifié contre un projet Supabase neuf (connexion Postgres directe, comparaison automatique vs prod), et l'en-tête de \`schema.sql\` réécrit — Gap A et Gap B ne sont plus listés comme non résolus. La dérive non documentée découverte en Phase 19 est entièrement close (SCHEMA-02→07). Reste hors scope, distinct : parité complète des ~19 tables (OR, billing/factures, catalogue, PDP, auth client) — voir en-tête \`schema.sql\` bloc 1 et REQUIREMENTS.md Out of Scope.`

    Keep the other Known Gaps bullets (Phase 8 / BILL-06, MSTORE-02) UNCHANGED — those remain open
    and blocked on external human actions, unrelated to this phase.
  </action>
  <verify>
    <automated>grep -c "RÉSOLU (v1.5 Phase 22" .planning/PROJECT.md; test $(grep -c "RÉSOLU (v1.5 Phase 22" .planning/PROJECT.md) -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` Known Gaps section contains `RÉSOLU (v1.5 Phase 22`
    - `.planning/PROJECT.md` contains `SCHEMA-07`
    - The stale phrasing `mais le commentaire d'en-tête (L6-40) décrit encore l'état d'avant` no longer appears (grep returns 0)
    - Phase 8 / BILL-06 bullet still present (grep `BILL-06` in PROJECT.md ≥ 1)
    - MSTORE-02 bullet still present (grep `MSTORE-02` in PROJECT.md ≥ 1)
  </acceptance_criteria>
  <done>PROJECT.md Known Gaps reflects Gap A/B closure via SCHEMA-07; unrelated external gaps preserved.</done>
</task>

</tasks>

<verification>
- `grep "Non couvert ici\|non corrigés car hors du périmètre" schema.sql` → 0 matches (criterion 3).
- schema.sql header still lists the ~19 out-of-scope tables (boundary preserved).
- PROJECT.md Known Gaps shows Gap A/B resolved, BILL-06 + MSTORE-02 untouched (criterion 4).
</verification>

<success_criteria>
- SCHEMA-07 criterion 3: header no longer lists Gap A/B as unresolved.
- SCHEMA-07 criterion 4: PROJECT.md Known Gaps reflects the closure.
- No false full-parity claim introduced; out-of-scope boundary intact.
</success_criteria>

<output>
After completion, create `.planning/phases/22-v-rification-bootstrap-nettoyage-header/22-03-SUMMARY.md`
</output>
