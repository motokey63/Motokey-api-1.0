---
phase: 15-feature-parity-screens
plan: 06
type: execute
wave: 3
depends_on: [15-01, 15-02, 15-03, 15-04]
files_modified:
  - mobile-app/app/(app)/(tabs)/devis/index.tsx
autonomous: true
requirements: [MPARITY-02, MPARITY-05]
must_haves:
  truths:
    - "The user sees their devis list with statut label/color and line items (MPARITY-02)"
    - "An 'envoye' devis can be accepted or refused, each behind a confirm dialog, updating the list (MPARITY-02)"
    - "When the network is unreachable, the devis list shows cached data + a 'dernière mise à jour' banner (MPARITY-05)"
    - "An empty devis list shows the passive EmptyState (no CTA)"
  artifacts:
    - path: "mobile-app/app/(app)/(tabs)/devis/index.tsx"
      provides: "Devis list tab: fetch+cache, inline expand, accept/refuse with confirm"
      contains: "parseDevisList"
  key_links:
    - from: "mobile-app/app/(app)/(tabs)/devis/index.tsx"
      to: "POST /devis/:id/valider and /devis/:id/refuser"
      via: "apiPost after Alert.alert confirm, then reload"
      pattern: "/valider"
---

<objective>
Build the Devis tab: a flat list screen porting `MotoKey_Client.html`'s `loadClientDevis()` + `acceptDevis()` + `refuseDevis()` — devis cards with statut pill, line-item breakdown, total TTC, and inline Accepter/Refuser actions (each behind a native confirm dialog), plus the read-only offline cache. Per RESEARCH Open Question 2, devis stays a flat inline-expand list (no detail push), since each list item already carries its full line data.

Purpose: Delivers MPARITY-02 (view + accept/refuse devis) and MPARITY-05 (devis-list offline cache).
Output: `devis/index.tsx`.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-feature-parity-screens/15-RESEARCH.md
@.planning/phases/15-feature-parity-screens/15-UI-SPEC.md
@.planning/phases/15-feature-parity-screens/15-CONTEXT.md

<interfaces>
<!-- Contracts from Waves 1-2. Use directly. -->

Data (mobile-app/lib/api.ts + hooks/useAuth.ts):
```typescript
import { apiGet, apiPost, errMsg } from '../../../../lib/api';
const { getValidAccessToken } = useAuth();   // '../../../../hooks/useAuth'
// POST /devis/:id/valider  (body null) → accept
// POST /devis/:id/refuser  (body null) → refuse
```

Wave 1 logic (mobile-app/lib/devisDisplay.ts):
```typescript
devisStatutLabel(s): string; devisStatutColor(s): string; parseDevisList(data): Devis[];
interface Devis { id; numero?; statut; total_ttc?; created_at?; valide_at?; refuse_at?; motos?; devis_lignes?; lignes? }
interface DevisLigne { description?; desc?; quantite?; prix_unitaire? }
```
Wave 1 logic (mobile-app/lib/motoDisplay.ts): `fmtDate(d?): string;`
Wave 1 logic (mobile-app/lib/cache.ts): `getCached/setCached/shouldServeCache/fmtCacheTimestamp/CACHE_KEY_DEVIS`

Wave 2 components: `<StatutBadge label color />`, `<EmptyState icon heading body />`, `<OfflineBanner updatedAt />`. Existing `<Button>` for Valider/Refuser.

Confirm dialogs (RN core — port of the web client's confirm()):
```typescript
import { Alert } from 'react-native';
Alert.alert('Refuser ce devis ?', 'Cette action est définitive et sera visible par le garage.',
  [{ text: 'Annuler', style: 'cancel' }, { text: 'Refuser', style: 'destructive', onPress: () => doRefuse(id) }]);
```

Backend: GET /devis → data.devis (brouillon already server-filtered for CLIENT). Each devis carries devis_lignes/lignes inline. total_ttc is a number.

UI-SPEC copy (verbatim):
- Devis empty heading: "Aucun devis pour l'instant"
- Devis empty body: "Vos devis apparaîtront ici dès qu'un garage vous en envoie un." (no CTA)
- Refuser confirm: title "Refuser ce devis ?" / body "Cette action est définitive et sera visible par le garage." / buttons "Annuler" + "Refuser" (destructive).
- Accepter confirm: title "Valider ce devis ?" / body "Le garage sera notifié de votre acceptation." / buttons "Annuler" + "Valider".
- Statut labels/colors come from devisStatutLabel/devisStatutColor (do NOT hardcode).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Devis list tab (fetch + cache + inline cards + accept/refuse with confirm)</name>
  <files>mobile-app/app/(app)/(tabs)/devis/index.tsx</files>
  <read_first>
    - MotoKey_Client.html lines 943-1021 (loadClientDevis card markup: numero, date+moto, statut pill, line-item table, total TTC, actions; acceptDevis/refuseDevis confirm+post+reload)
    - mobile-app/lib/devisDisplay.ts (label/color/parse) + motoDisplay.ts (fmtDate)
    - mobile-app/lib/cache.ts (CACHE_KEY_DEVIS + shouldServeCache)
    - mobile-app/components/StatutBadge.tsx, EmptyState.tsx, OfflineBanner.tsx, Button.tsx
    - .planning/phases/15-feature-parity-screens/15-RESEARCH.md Open Question 2 (flat inline list) + Pattern 3 (cache-on-failure)
    - .planning/phases/15-feature-parity-screens/15-UI-SPEC.md (Devis statut colors; card2 for nested line-item surface; destructive confirmation copy)
  </read_first>
  <action>
    Create mobile-app/app/(app)/(tabs)/devis/index.tsx — functional component, `<FlatList>` + `<RefreshControl>` + offline cache (same Pattern 3 shape as motos list).
    Load flow (port loadClientDevis + RESEARCH Pattern 3):
    1. `const token = await getValidAccessToken();`
    2. `const res = await apiGet('/devis', token);`
    3. If `res.ok`: `const list = parseDevisList(res.data); await setCached(CACHE_KEY_DEVIS, list); setDevis(list); setStaleSince(null);`
    4. Else if `shouldServeCache(res.status)`: `const cached = await getCached<Devis[]>(CACHE_KEY_DEVIS); if (cached) { setDevis(cached.data); setStaleSince(cached.updatedAt); } else setError(errMsg(res.data));`
    5. Else: `setError(errMsg(res.data));`
    Card rendering (per devis, port renderer):
    - Header row: `numero || id` (fontSize 15/'700'), sub line `fmtDate(created_at) + ' · ' + motoInfo` where motoInfo = `dv.motos ? dv.motos.marque + ' ' + dv.motos.modele + (dv.motos.plaque ? ' — ' + dv.motos.plaque : '') : '—'` (colors.tx3, fontSize 12), and a right-aligned `<StatutBadge label={devisStatutLabel(dv.statut)} color={devisStatutColor(dv.statut)} />`.
    - Line items: `const lignes = dv.devis_lignes || dv.lignes || [];` render each as a row `description||desc` (colors.tx2) + `{quantite||1} × {prix_unitaire?.toFixed(2) ?? '—'} €` right-aligned, on a colors.card2 nested surface. If no lignes: "Aucune ligne." (colors.tx3, fontSize 13).
    - Total: `Total TTC : {typeof total_ttc === 'number' ? total_ttc.toFixed(2) + ' €' : '—'}` (fontWeight '700', top border colors.border).
    - Actions: only when `dv.statut === 'envoye'`: a row with `<Button title="Valider" onPress={() => confirmAccept(dv.id)} />` and a destructive-styled "Refuser" button `onPress={() => confirmRefuse(dv.id)}` — use the same fallback pattern as `RevokeGarageModal` in 15-04-PLAN.md (`Button` has no destructive variant): a `<Pressable>` styled with `backgroundColor colors.rd`, white bold text, `borderRadius 10`, `paddingVertical 14`, matching `Button`'s primary metrics. Otherwise, if valide_at/refuse_at, show "Décision le {fmtDate(valide_at || refuse_at)}" (colors.tx3, fontSize 12).
    Actions logic:
    - `confirmAccept(id)`: `Alert.alert('Valider ce devis ?', 'Le garage sera notifié de votre acceptation.', [{text:'Annuler',style:'cancel'},{text:'Valider',onPress:() => doAccept(id)}]);`
    - `doAccept(id)`: `const t = await getValidAccessToken(); const { ok, data } = await apiPost('/devis/' + id + '/valider', null, t); if (ok) { showToast('Devis accepté.', 'success'); load(); } else showToast(errMsg(data), 'error');`
    - `confirmRefuse(id)`: `Alert.alert('Refuser ce devis ?', 'Cette action est définitive et sera visible par le garage.', [{text:'Annuler',style:'cancel'},{text:'Refuser',style:'destructive',onPress:() => doRefuse(id)}]);`
    - `doRefuse(id)`: POST '/devis/' + id + '/refuser', on ok `showToast('Devis refusé.', 'info'); load();` else error toast.
    - Import `showToast` from '../../../../components/Toast'.
    Empty (list empty, not error): `<EmptyState icon="📋" heading="Aucun devis pour l'instant" body="Vos devis apparaîtront ici dès qu'un garage vous en envoie un." />` (no CTA).
    Offline banner when `staleSince != null`. Loading → ActivityIndicator colors.acc.
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-app/app/(app)/(tabs)/devis/index.tsx` contains `parseDevisList` and `devisStatutLabel` and `devisStatutColor`
    - contains `apiPost('/devis/' + id + '/valider'` and `apiPost('/devis/' + id + '/refuser'` (or equivalent template producing `/valider` and `/refuser`)
    - contains `Alert.alert` with both `'Refuser ce devis ?'` and `'Valider ce devis ?'`
    - contains `setCached(CACHE_KEY_DEVIS` and `shouldServeCache(res.status)`
    - contains the verbatim empty-state heading `Aucun devis pour l'instant`
    - contains `<OfflineBanner` and `<StatutBadge`
    - `cd mobile-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Devis list renders cards with statut pill + line items + total, accept/refuse behind confirm dialogs reload the list, offline cache shows banner on status 0; tsc clean.</done>
</task>

</tasks>

<verification>
- `cd mobile-app && npx tsc --noEmit` exits 0
- `cd mobile-app && npm test` green
- Manual (phase checkpoint): open Devis tab → cards with statut/line items; accept an 'envoye' devis (confirm → statut "Validé", actions gone); refuse another; airplane mode → cached devis + timestamp banner
</verification>

<success_criteria>
MPARITY-02 (view + accept/refuse devis with confirm) and MPARITY-05 (devis-list offline cache) are delivered, ported 1:1 from the web client's devis screen.
</success_criteria>

<output>
After completion, create `.planning/phases/15-feature-parity-screens/15-06-SUMMARY.md`.
</output>
