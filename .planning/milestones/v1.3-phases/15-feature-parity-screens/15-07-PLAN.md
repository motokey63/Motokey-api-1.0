---
phase: 15-feature-parity-screens
plan: 07
type: execute
wave: 3
depends_on: [15-02, 15-03, 15-04]
files_modified:
  - mobile-app/app/(app)/(tabs)/motos/add.tsx
  - mobile-app/app/(app)/(tabs)/motos/claim.tsx
autonomous: true
requirements: [MPARITY-04]
must_haves:
  truths:
    - "The user can add a moto manually via a form (marque/modèle/plaque/VIN/année/km/mode), respecting the plan limit (MPARITY-04)"
    - "When the plan limit is reached, the 'Passer Pro' CTA block shows and the form is hidden"
    - "The user can claim an orphan moto with VIN + plaque only; photo upload is disabled (D-02)"
    - "Both forms validate required fields client-side before POST and show inline errors"
  artifacts:
    - path: "mobile-app/app/(app)/(tabs)/motos/add.tsx"
      provides: "Ajouter une moto form + plan-limit check + Passer Pro CTA"
      contains: "parseLimite"
    - path: "mobile-app/app/(app)/(tabs)/motos/claim.tsx"
      provides: "Réclamer une moto form (VIN+plaque), disabled-photo notice"
      contains: "buildClaimPayload"
  key_links:
    - from: "mobile-app/app/(app)/(tabs)/motos/add.tsx"
      to: "GET /client/limite-motos + POST /client/motos"
      via: "parseLimite gates the form; buildAddMotoPayload posts"
      pattern: "buildAddMotoPayload"
    - from: "mobile-app/app/(app)/(tabs)/motos/claim.tsx"
      to: "POST /client/reclamations"
      via: "buildClaimPayload with pending_manual_verification"
      pattern: "pending_manual_verification"
---

<objective>
Build the two garage/moto-linkage form screens reached from the Motos tab menu (D-05): "Ajouter une moto" (free-form add respecting the plan-limit + "Passer Pro" CTA) and "Réclamer une moto" (claim an orphan moto with VIN + plaque only, photo disabled per D-02). Ports `MotoKey_Client.html`'s `renderAddMotoTab`/`submitAddMoto` and `renderClaimTab`/`submitClaim`.

Purpose: Delivers the add + claim halves of MPARITY-04 (the list halves — réclamations + garages — are plan 15-08).
Output: `motos/add.tsx` + `motos/claim.tsx`.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-feature-parity-screens/15-RESEARCH.md
@.planning/phases/15-feature-parity-screens/15-UI-SPEC.md
@.planning/phases/15-feature-parity-screens/15-CONTEXT.md

<interfaces>
<!-- Contracts from Waves 1-2. Use directly. -->

Data (mobile-app/lib/api.ts + hooks/useAuth.ts):
```typescript
import { apiGet, apiPost, errMsg } from '../../../../lib/api';
const { getValidAccessToken } = useAuth();  // '../../../../hooks/useAuth'
```

Wave 1 logic (mobile-app/lib/garageLiaison.ts):
```typescript
parseLimite(data): LimiteMotos;                 // { count, limite, can_add, cta_pro }
validateAddMoto(f): string | null;              // null = valid
buildAddMotoPayload(f): { marque, modele, plaque, vin, annee, km, mode_acquisition };
validateClaim(vin, plaque): string | null;
buildClaimPayload(vin, plaque): { vin_fourni, plaque_fournie, carte_grise_photo_url: 'pending_manual_verification' };
interface AddMotoForm { marque; modele; plaque; vin; annee?; km?; mode_acquisition }
```

Wave 2 components: existing `<TextField value onChangeText placeholder? label? keyboardType? />`, `<Button title onPress loading? />`. showToast from '../../../../components/Toast'.

expo-router: `import { router, Stack } from 'expo-router';` — after success `router.replace('/(app)/(tabs)/motos')` (add) or `router.replace('/(app)/(tabs)/motos/reclamations')` (claim). Set header title via `<Stack.Screen options={{ title: '...' }} />`.

Backend:
- GET /client/limite-motos → { count, limite, can_add, cta_pro }
- POST /client/motos (body buildAddMotoPayload) → 402 when limit hit
- POST /client/reclamations (body buildClaimPayload) → creates réclamation

UI-SPEC / CONTEXT copy:
- Add-moto mode_acquisition options (verbatim from web client select): achat_neuf "Achat neuf", achat_occasion "Achat occasion", don "Don", heritage "Héritage", cession_perso "Cession personnelle", reprise_garage "Reprise par garage", mise_en_stock "Mise en stock", inconnu "Non précisé".
- Passer Pro block: direct port of the web client copy (crown 👑 + "Passez Pro pour plus de motos" + "Limite de {limite} moto(s) atteinte (plan Standard). Le plan Pro vous permet d'en gérer jusqu'à 10." + disabled "Passer au plan Pro (bientôt)" button).
- Claim disabled-photo notice (D-02): "Upload désactivé temporairement — contactez votre garage pour finaliser la réclamation." shown in place of any file input.
- 402 on add → inline error "Limite atteinte — passez Pro."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter une moto (add.tsx) — plan-limit check + form + Passer Pro CTA</name>
  <files>mobile-app/app/(app)/(tabs)/motos/add.tsx</files>
  <read_first>
    - MotoKey_Client.html lines 1127-1172 (renderAddMotoTab: limite fetch, ctaHtml block, form fields, mode select; submitAddMoto: required validation, payload, 402 handling)
    - mobile-app/lib/garageLiaison.ts (parseLimite/validateAddMoto/buildAddMotoPayload)
    - mobile-app/components/TextField.tsx + Button.tsx
    - .planning/phases/15-feature-parity-screens/15-CONTEXT.md D-01 (add-moto in scope) + Claude's Discretion (Passer Pro copy = direct port)
    - .planning/phases/15-feature-parity-screens/15-UI-SPEC.md (form spacing sm/md; Label 13/600; accent reserved for primary CTA)
  </read_first>
  <action>
    Create mobile-app/app/(app)/(tabs)/motos/add.tsx — `<Stack.Screen options={{ title: 'Ajouter une moto' }} />`, wrapped in `<ScrollView>` (bg colors.bg, padding 16).
    On mount: `const token = await getValidAccessToken(); const res = await apiGet('/client/limite-motos', token); const lim = parseLimite(res.ok ? res.data : null);`
    Render `{lim.count} / {lim.limite} utilisée(s)` sub line.
    If `!lim.can_add`: render the "Passer Pro" CTA block (direct port): a card with 👑, heading "Passez Pro pour plus de motos", body `` `Limite de ${lim.limite} moto${lim.limite>1?'s':''} atteinte (plan Standard). Le plan Pro vous permet d'en gérer jusqu'à 10.` ``, and a DISABLED `<Button>` (or disabled Pressable) titled "Passer au plan Pro (bientôt)". Do NOT render the form.
    If `lim.can_add`: render the form with `<TextField>` for each field, holding local state `{ marque, modele, plaque, vin, annee, km, mode_acquisition }` (mode_acquisition default 'achat_neuf'):
    - marque (label "Marque *", placeholder "Yamaha"), modele ("Modèle *", "MT-07"), annee ("Année", "2021", keyboardType 'numeric'), km ("Kilométrage", "0", 'numeric'), plaque ("Plaque *", "AB-123-CD"), vin ("VIN *", "17 caractères").
    - mode_acquisition: a simple selector (since no Picker dependency is guaranteed, render a row of selectable ghost buttons or a native `Picker` only if `@react-native-picker/picker` resolves — otherwise a horizontal set of Pressable chips) over the 8 options listed in <interfaces>. Default 'achat_neuf'. Keep it simple; label "Mode d'acquisition".
    - Inline error `<Text style={{color: colors.rd}}>` bound to an `err` state, hidden when empty.
    - Submit `<Button title="Enregistrer ma moto" loading={submitting} onPress={submit} />`.
    submit():
    - `const e = validateAddMoto(form); if (e) { setErr(e); return; }`
    - `setSubmitting(true); const t = await getValidAccessToken(); const { ok, data, status } = await apiPost('/client/motos', buildAddMotoPayload(form), t); setSubmitting(false);`
    - `if (!ok) { setErr(status === 402 ? 'Limite atteinte — passez Pro.' : errMsg(data)); return; }`
    - `showToast('Moto ajoutée !', 'success'); router.replace('/(app)/(tabs)/motos');`
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-app/app/(app)/(tabs)/motos/add.tsx` contains `parseLimite` and `validateAddMoto` and `buildAddMotoPayload`
    - contains `apiPost('/client/motos'` and the 402 branch string `Limite atteinte — passez Pro.`
    - contains the Passer Pro copy `Passez Pro pour plus de motos` and the emoji `👑`
    - contains a conditional on `lim.can_add` that hides the form when false
    - contains `router.replace('/(app)/(tabs)/motos')` after success
    - `cd mobile-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Add-moto form gated by the plan limit, Passer Pro CTA on limit, client-side validation + 402 handling, success returns to the list; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Réclamer une moto (claim.tsx) — VIN+plaque form, disabled photo</name>
  <files>mobile-app/app/(app)/(tabs)/motos/claim.tsx</files>
  <read_first>
    - MotoKey_Client.html lines 1174-1223 (renderClaimTab: VIN/plaque inputs, disabled-photo uploadHtml branch; submitClaim: validation, carte_grise_photo_url:'pending_manual_verification', post, redirect)
    - mobile-app/lib/garageLiaison.ts (validateClaim/buildClaimPayload)
    - mobile-app/components/TextField.tsx + Button.tsx
    - .planning/phases/15-feature-parity-screens/15-CONTEXT.md D-02 (photo DISABLED — VIN+plaque only)
    - .planning/phases/15-feature-parity-screens/15-UI-SPEC.md (Claim disabled-photo notice copy)
  </read_first>
  <action>
    Create mobile-app/app/(app)/(tabs)/motos/claim.tsx — `<Stack.Screen options={{ title: 'Réclamer une moto' }} />`, `<ScrollView>` bg colors.bg padding 16.
    Intro copy (colors.tx2, fontSize 13): "Moto déjà référencée dans MotoKey ? Soumettez votre réclamation — le garage vérifiera vos documents."
    Form with local state `{ vin, plaque }`:
    - `<TextField label="VIN *" placeholder="17 caractères" value={vin} onChangeText={setVin} autoCapitalize="characters" />`
    - `<TextField label="Plaque *" placeholder="AB-123-CD" value={plaque} onChangeText={setPlaque} autoCapitalize="characters" />`
    - Disabled-photo block (D-02): a card/box (bg colors.bg, border colors.border, padding 12) with label "Photo carte grise" and body text "Upload désactivé temporairement — contactez votre garage pour finaliser la réclamation." (colors.tx2, fontSize 13). NO file/camera input.
    - Inline `err` `<Text style={{color: colors.rd}}>`.
    - `<Button title="Soumettre la réclamation" loading={submitting} onPress={submit} />`.
    submit():
    - `const e = validateClaim(vin, plaque); if (e) { setErr(e); return; }`
    - `setSubmitting(true); const t = await getValidAccessToken(); const { ok, data } = await apiPost('/client/reclamations', buildClaimPayload(vin, plaque), t); setSubmitting(false);`
    - `if (!ok) { setErr(errMsg(data)); return; }`
    - `showToast('Réclamation soumise — en attente de validation du garage', 'success'); router.replace('/(app)/(tabs)/motos/reclamations');`
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-app/app/(app)/(tabs)/motos/claim.tsx` contains `validateClaim` and `buildClaimPayload`
    - contains the disabled-photo copy `Upload désactivé temporairement` and no `capture=` / no file-picker import
    - contains `apiPost('/client/reclamations'`
    - contains `router.replace('/(app)/(tabs)/motos/reclamations')` after success
    - the string `pending_manual_verification` is NOT hardcoded in the screen (it comes from buildClaimPayload)
    - `cd mobile-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Claim form collects VIN+plaque only, shows the disabled-photo notice, validates, posts via buildClaimPayload, success routes to réclamations; tsc clean.</done>
</task>

</tasks>

<verification>
- `cd mobile-app && npx tsc --noEmit` exits 0
- `cd mobile-app && npm test` green
- Manual (phase checkpoint): from Motos menu → Ajouter → fill form → moto appears in list; hit limit → Passer Pro block shows; Réclamer → VIN+plaque → réclamation created (no photo UI)
</verification>

<success_criteria>
The add-moto and claim-moto halves of MPARITY-04 are delivered: manual add respecting plan limit + Passer Pro CTA, and VIN+plaque claim with photo disabled per D-02.
</success_criteria>

<output>
After completion, create `.planning/phases/15-feature-parity-screens/15-07-SUMMARY.md`.
</output>
