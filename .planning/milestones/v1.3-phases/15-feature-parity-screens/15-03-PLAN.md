---
phase: 15-feature-parity-screens
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile-app/app/(app)/(tabs)/_layout.tsx
  - mobile-app/app/(app)/(tabs)/motos/_layout.tsx
  - mobile-app/app/(app)/(tabs)/devis/_layout.tsx
  - mobile-app/app/(app)/(tabs)/compte.tsx
  - mobile-app/app/_layout.tsx
  - mobile-app/app/(app)/home.tsx
autonomous: true
requirements: [MPARITY-01]
must_haves:
  truths:
    - "After login the app lands on a bottom tab bar with Motos / Devis / Compte tabs (D-03)"
    - "The Motos tab hosts a nested Stack so list→detail→sub-flows can push (D-04/D-05)"
    - "The old placeholder Home is gone and no code still points at /(app)/home"
    - "Compte tab shows email + logout at Phase 14 parity (D-06)"
  artifacts:
    - path: "mobile-app/app/(app)/(tabs)/_layout.tsx"
      provides: "bottom Tabs navigator (Motos/Devis/Compte) with emoji icons"
      contains: "Tabs"
    - path: "mobile-app/app/(app)/(tabs)/motos/_layout.tsx"
      provides: "nested Stack inside the Motos tab"
      contains: "Stack"
    - path: "mobile-app/app/(app)/(tabs)/compte.tsx"
      provides: "minimal account screen (email + logout)"
      contains: "logout"
  key_links:
    - from: "mobile-app/app/_layout.tsx"
      to: "/(app)/(tabs)/motos"
      via: "router.replace default authenticated route"
      pattern: "\\(tabs\\)/motos"
---

<objective>
Restructure `app/(app)/` from the Phase 14 placeholder Home into the Phase 15 navigation shell: a 3-tab bottom bar (Motos / Devis / Compte, D-03), a nested Stack inside the Motos tab (D-04/D-05), a minimal Compte tab (D-06), and an updated root-redirect target (RESEARCH Pitfall 4). Screen bodies come in later plans — this plan builds the empty skeleton those screens drop into.

Purpose: Structural foundation. Every Phase 15 screen (15-05/06/07/08) is a file under `(tabs)/motos/` or `(tabs)/devis/`; those routes only render inside the navigators this plan creates. Landing this early (Wave 1) means later plans just add screen files, never touch navigation wiring.
Output: Tabs layout + Motos stack layout + Devis stack layout + Compte tab; home.tsx deleted; root redirect updated.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-feature-parity-screens/15-RESEARCH.md
@.planning/phases/15-feature-parity-screens/15-UI-SPEC.md

<interfaces>
<!-- Existing files the executor edits/reads. -->

mobile-app/app/_layout.tsx (RootNav) currently redirects authenticated users:
```tsx
} else if (status === 'authenticated' && inAuthGroup) {
  router.replace('/(app)/home');   // ← MUST become '/(app)/(tabs)/motos'
}
```

mobile-app/app/(app)/_layout.tsx (KEEP AS-IS — do not modify):
```tsx
export default function AppLayout() { return <Stack screenOptions={{ headerShown: false }} />; }
```
This Stack wraps the new (tabs) group automatically (file-based routing).

mobile-app/app/(app)/home.tsx (DELETE — its email+logout body becomes compte.tsx):
```tsx
const { email, logout } = useAuth();
// <Logo/>, <Text>Bienvenue {email}</Text>, <Button title="Se déconnecter" onPress={()=>logout()}/>
```

From mobile-app/hooks/useAuth.ts: `useAuth()` returns `{ status, email, logout, ... }`.
From mobile-app/theme/colors.ts: `colors.acc` (#ff6b00) is the active-tab tint (per UI-SPEC).
expo-router exports: `import { Tabs } from 'expo-router'` and `import { Stack } from 'expo-router'`.

Icon policy (UI-SPEC + RESEARCH Pitfall 2): use EMOJI in a <Text>, NOT @expo/vector-icons
(which does not resolve from mobile-app's own node_modules). Motos=🏍️, Devis=📋, Compte=👤.
Font policy: system default (fontWeight only), no fontFamily on any new text.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Tabs shell + nested Motos/Devis stacks + Compte tab</name>
  <files>mobile-app/app/(app)/(tabs)/_layout.tsx, mobile-app/app/(app)/(tabs)/motos/_layout.tsx, mobile-app/app/(app)/(tabs)/devis/_layout.tsx, mobile-app/app/(app)/(tabs)/compte.tsx</files>
  <read_first>
    - mobile-app/app/(app)/_layout.tsx (the parent Stack that wraps (tabs) — leave unchanged)
    - mobile-app/app/(app)/home.tsx (source content to port into compte.tsx)
    - mobile-app/theme/colors.ts (colors.acc for active tint, colors.card for tab bar bg, colors.tx2 inactive)
    - mobile-app/components/Button.tsx + mobile-app/components/Logo.tsx (reused verbatim in compte.tsx)
    - .planning/phases/15-feature-parity-screens/15-RESEARCH.md Pattern 1 (Tabs + nested Stack file structure) and Pattern 2 (emoji-as-icon example)
  </read_first>
  <action>
    Create mobile-app/app/(app)/(tabs)/_layout.tsx:
    - `import { Tabs } from 'expo-router';` `import { Text } from 'react-native';` `import { colors } from '../../../theme/colors';`
    - Return `<Tabs screenOptions={{ headerShown: false, tabBarActiveTintColor: colors.acc, tabBarInactiveTintColor: colors.tx2, tabBarStyle: { backgroundColor: colors.card } }}>` with three `<Tabs.Screen>`:
      - `name="motos"` options `{ title: 'Motos', tabBarIcon: () => <Text style={{ fontSize: 20 }}>🏍️</Text> }`
      - `name="devis"` options `{ title: 'Devis', tabBarIcon: () => <Text style={{ fontSize: 20 }}>📋</Text> }`
      - `name="compte"` options `{ title: 'Compte', tabBarIcon: () => <Text style={{ fontSize: 20 }}>👤</Text> }`
    Create mobile-app/app/(app)/(tabs)/motos/_layout.tsx:
    - `import { Stack } from 'expo-router';` default export returning `<Stack screenOptions={{ headerShown: true, headerTintColor: colors.tx, headerStyle: { backgroundColor: colors.card } }} />` (import colors from '../../../../theme/colors'). Do NOT enumerate Stack.Screen children — expo-router auto-registers the index/[id]/add/claim/reclamations/garages files that later plans add.
    Create mobile-app/app/(app)/(tabs)/devis/_layout.tsx:
    - Same minimal `<Stack screenOptions={{ headerShown: false }} />` default export (devis is a single flat screen per RESEARCH Open Question 2 — inline accept/refuse, no drill-down; a header-less stack keeps the tab title from the Tabs layer).
    Create mobile-app/app/(app)/(tabs)/compte.tsx by porting mobile-app/app/(app)/home.tsx verbatim (D-06): `useAuth()` → `{ email, logout }`, render `<Logo/>`, `<Text>Bienvenue {email ?? ''}</Text>`, `<Button title="Se déconnecter" onPress={() => logout()} />`, same StyleSheet (bg=colors.bg, centered, padding 24). Fix relative import depths for the new location (`../../../hooks/useAuth`, `../../../components/Logo`, `../../../components/Button`, `../../../theme/colors`).
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-app/app/(app)/(tabs)/_layout.tsx` contains `Tabs` and `name="motos"` and `name="devis"` and `name="compte"`
    - `mobile-app/app/(app)/(tabs)/_layout.tsx` contains `tabBarActiveTintColor: colors.acc` and the emoji `🏍️`
    - `mobile-app/app/(app)/(tabs)/motos/_layout.tsx` contains `Stack`
    - `mobile-app/app/(app)/(tabs)/devis/_layout.tsx` contains `Stack`
    - `mobile-app/app/(app)/(tabs)/compte.tsx` contains `logout` and `Bienvenue`
    - No file under `mobile-app/app/(app)/(tabs)/` imports from `@expo/vector-icons`
    - `cd mobile-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Tab bar + nested Motos stack + flat Devis stack + Compte tab exist; emoji icons only; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete placeholder Home and update the root redirect target</name>
  <files>mobile-app/app/_layout.tsx, mobile-app/app/(app)/home.tsx (deleted)</files>
  <read_first>
    - mobile-app/app/_layout.tsx (the RootNav redirect that must change from /(app)/home to /(app)/(tabs)/motos)
    - mobile-app/app/(app)/home.tsx (confirm its content is now fully covered by compte.tsx before deleting)
  </read_first>
  <action>
    In mobile-app/app/_layout.tsx, change the authenticated redirect target from `router.replace('/(app)/home')` to `router.replace('/(app)/(tabs)/motos')` (RESEARCH Pitfall 4 — Motos is the default landing tab). Leave the unauthenticated `router.replace('/(auth)/login')` branch untouched.
    Delete mobile-app/app/(app)/home.tsx (its email+logout body now lives in compte.tsx).
    Grep the whole mobile-app tree for any remaining `'/(app)/home'` or `(app)/home` string literals (tests, other screens) and update each to `/(app)/(tabs)/motos`.
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-app/app/_layout.tsx` contains `router.replace('/(app)/(tabs)/motos')`
    - `mobile-app/app/_layout.tsx` does NOT contain `'/(app)/home'`
    - `mobile-app/app/(app)/home.tsx` does not exist (file deleted)
    - grep for `(app)/home` across `mobile-app/app` and `mobile-app/lib` returns no matches
    - `cd mobile-app && npx tsc --noEmit` exits 0
    - `cd mobile-app && npm test` exits 0 (full suite still green after restructure)
  </acceptance_criteria>
  <done>home.tsx removed, no dangling references, root redirect points at the Motos tab, full test suite green.</done>
</task>

</tasks>

<verification>
- `cd mobile-app && npx tsc --noEmit` exits 0
- `cd mobile-app && npm test` exits 0
- Navigation shell renders a 3-tab bar (verified structurally by tsc + route file presence; full on-device tab render is covered by the phase human-verification checkpoint)
</verification>

<success_criteria>
The app has a Motos/Devis/Compte bottom tab bar with a nested Motos stack, the old Home placeholder is gone, and post-login navigation lands on the Motos tab — a clean skeleton for the screen plans to fill.
</success_criteria>

<output>
After completion, create `.planning/phases/15-feature-parity-screens/15-03-SUMMARY.md`.
</output>
