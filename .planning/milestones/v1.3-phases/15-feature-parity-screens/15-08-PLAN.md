---
phase: 15-feature-parity-screens
plan: 08
type: execute
wave: 3
depends_on: [15-02, 15-03, 15-04]
files_modified:
  - mobile-app/app/(app)/(tabs)/motos/reclamations.tsx
  - mobile-app/app/(app)/(tabs)/motos/garages.tsx
autonomous: true
requirements: [MPARITY-04]
must_haves:
  truths:
    - "The user sees their réclamations list with statut badge (en_attente/accepte/refuse/litige) (MPARITY-04)"
    - "The user sees their linked garages with Actif/Quitté status (MPARITY-04)"
    - "The user can leave an active garage via a modal showing the verbatim legal notice + optional motif, then the status flips to Quitté (MPARITY-04)"
    - "Empty réclamations/garages lists show the correct EmptyState copy"
  artifacts:
    - path: "mobile-app/app/(app)/(tabs)/motos/reclamations.tsx"
      provides: "Réclamations list with statut badges"
      contains: "parseReclamations"
    - path: "mobile-app/app/(app)/(tabs)/motos/garages.tsx"
      provides: "Garages list + RevokeGarageModal wiring (DELETE)"
      contains: "RevokeGarageModal"
  key_links:
    - from: "mobile-app/app/(app)/(tabs)/motos/garages.tsx"
      to: "DELETE /client/garages/:id"
      via: "apiFetch('DELETE', ...) with { motif } after modal confirm"
      pattern: "'DELETE'"
---

<objective>
Build the two garage/moto-linkage list screens reached from the Motos tab menu (D-05): "Mes réclamations" (list of claim requests with statut badges) and "Mes garages" (linked garages with Actif/Quitté status + the leave-garage flow via RevokeGarageModal). Ports `MotoKey_Client.html`'s `loadClientReclamationsTab`, `loadClientGaragesTab`, and `openRevokeModal`/`submitRevoke`.

Purpose: Delivers the réclamations + garages (leave/révocation) halves of MPARITY-04.
Output: `motos/reclamations.tsx` + `motos/garages.tsx`.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-feature-parity-screens/15-RESEARCH.md
@.planning/phases/15-feature-parity-screens/15-UI-SPEC.md
@.planning/phases/15-feature-parity-screens/15-CONTEXT.md

<interfaces>
<!-- Contracts from Waves 1-2. Use directly. -->

Data (mobile-app/lib/api.ts + hooks/useAuth.ts):
```typescript
import { apiGet, apiFetch, errMsg } from '../../../../lib/api';
const { getValidAccessToken } = useAuth();  // '../../../../hooks/useAuth'
// Leave garage: apiFetch('DELETE', '/client/garages/' + id, { motif }, token)
```

Wave 1 logic (mobile-app/lib/garageLiaison.ts):
```typescript
parseReclamations(data): Reclamation[];   // unwraps real envelope: data.data.reclamations || data.reclamations || []
parseGarages(data): GarageLink[];          // unwraps real envelope: data.data.garages || data.garages || []
interface Reclamation { id; statut; vin_fourni?; date_creation?; motif_refus?; motos?:{marque?;modele?;plaque?} }
interface GarageLink { id; statut; date_creation?; garages?:{nom?;adresse?;tel?} }
```
Wave 1 logic (mobile-app/lib/devisDisplay.ts): `reclamationStatutLabel(s); reclamationStatutColor(s);`

Wave 2 components: `<StatutBadge label color />`, `<EmptyState icon heading body ctaTitle? onCta? />`, `<RevokeGarageModal visible garageName loading? onCancel onConfirm />`, `<Button>`. showToast from '../../../../components/Toast'.

expo-router: `import { router, Stack } from 'expo-router';`

Backend:
- GET /client/reclamations → data.reclamations
- GET /client/garages → data.garages (each item: g.statut === 'actif'|..., g.garages nested object)
- DELETE /client/garages/:id body { motif } → success; already-revoked returns 409. apiFetch's `ok` (HTTP-status based) is authoritative (no need to check data.success separately).

UI-SPEC copy (verbatim):
- Réclamations empty heading: "Aucune réclamation en cours" / body: "Réclamez une moto déjà enregistrée par un garage pour l'associer à votre compte." / CTA: "Réclamer une moto"
- Garages empty heading: "Aucun garage lié" / body: "Vos garages partenaires apparaîtront ici une fois une moto liée ou réclamée." (no CTA)
- Réclamation statut labels: en_attente "En attente", accepte "Acceptée", refuse "Refusée", litige "Litige" (from reclamationStatutLabel).
- Garage statut badge: g.statut==='actif' → "Actif" (colors.gn), else "Quitté" (colors.tx3).
- Leave-garage success toast: "Vous avez quitté le garage. Historique conservé 10 ans."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mes réclamations (reclamations.tsx) — list with statut badges</name>
  <files>mobile-app/app/(app)/(tabs)/motos/reclamations.tsx</files>
  <read_first>
    - MotoKey_Client.html lines 1225-1247 (loadClientReclamationsTab: fetch, empty state, per-item card with moto identity, vin_fourni, date, motif_refus, statut badge)
    - mobile-app/lib/garageLiaison.ts (parseReclamations) + devisDisplay.ts (reclamationStatutLabel/Color)
    - mobile-app/components/StatutBadge.tsx, EmptyState.tsx
    - .planning/phases/15-feature-parity-screens/15-CONTEXT.md Specifics (réclamation labels) + D-05
    - .planning/phases/15-feature-parity-screens/15-UI-SPEC.md (Réclamations empty copy; réclamation statut colors)
  </read_first>
  <action>
    Create mobile-app/app/(app)/(tabs)/motos/reclamations.tsx — `<Stack.Screen options={{ title: 'Mes réclamations' }} />`. This screen is ONLINE-ONLY (D-09 — no cache).
    Load: `const token = await getValidAccessToken(); const res = await apiGet('/client/reclamations', token);` If `!res.ok` show error state (icon ⚠️ + errMsg + "Réessayer" Button). Else `const list = parseReclamations(res.data);`
    Empty: `<EmptyState icon="📋" heading="Aucune réclamation en cours" body="Réclamez une moto déjà enregistrée par un garage pour l'associer à votre compte." ctaTitle="Réclamer une moto" onCta={() => router.push('/(app)/(tabs)/motos/claim')} />`
    List (`<FlatList>` or map inside ScrollView): per réclamation card (bg colors.card, radius 12, padding 16, marginBottom 12):
    - moto line: `{r.motos?.marque ?? ''} {r.motos?.modele ?? ''} — {r.motos?.plaque ?? '—'}` (fontSize 14/'700')
    - `VIN fourni : {r.vin_fourni ?? '—'}` (colors.tx2, fontSize 12)
    - `Soumis le {r.date_creation ? new Date(r.date_creation).toLocaleDateString('fr-FR') : '—'}` (colors.tx2, fontSize 12)
    - if `r.motif_refus`: `Motif refus : {r.motif_refus}` (colors.rd, fontSize 12)
    - right-aligned `<StatutBadge label={reclamationStatutLabel(r.statut)} color={reclamationStatutColor(r.statut)} />`
    Loading → ActivityIndicator colors.acc.
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-app/app/(app)/(tabs)/motos/reclamations.tsx` contains `parseReclamations` and `reclamationStatutLabel` and `reclamationStatutColor`
    - contains the verbatim empty heading `Aucune réclamation en cours` and the CTA `Réclamer une moto`
    - contains `router.push('/(app)/(tabs)/motos/claim')`
    - contains `VIN fourni :`
    - `cd mobile-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Réclamations list renders cards with statut badges + empty-state CTA; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Mes garages (garages.tsx) — list + leave-garage via RevokeGarageModal</name>
  <files>mobile-app/app/(app)/(tabs)/motos/garages.tsx</files>
  <read_first>
    - MotoKey_Client.html lines 1249-1296 (loadClientGaragesTab card markup + Actif/Quitté badge + Quitter button; openRevokeModal/submitRevoke DELETE flow)
    - MotoKey_Client.html lines 355-380 (revoke modal markup + legal notice — already ported into RevokeGarageModal in 15-04)
    - mobile-app/lib/garageLiaison.ts (parseGarages)
    - mobile-app/components/RevokeGarageModal.tsx (from 15-04) + StatutBadge.tsx + EmptyState.tsx
    - .planning/phases/15-feature-parity-screens/15-UI-SPEC.md (Garages empty copy; Actif/Quitté colors; leave-garage success toast)
    - mobile-app/lib/api.ts (apiFetch supports 'DELETE' with a body)
  </read_first>
  <action>
    Create mobile-app/app/(app)/(tabs)/motos/garages.tsx — `<Stack.Screen options={{ title: 'Mes garages' }} />`. ONLINE-ONLY (D-09).
    Load: `const token = await getValidAccessToken(); const res = await apiGet('/client/garages', token);` If `!res.ok` show error state + Réessayer. Else `const list = parseGarages(res.data);`
    Empty: `<EmptyState icon="🔧" heading="Aucun garage lié" body="Vos garages partenaires apparaîtront ici une fois une moto liée ou réclamée." />` (no CTA).
    List, per garage link (g): card (bg colors.card, radius 12, padding 16, marginBottom 12):
    - `const garage = g.garages || {}; const isActive = g.statut === 'actif';`
    - garage.nom (fontSize 15/'700'), optional garage.adresse + garage.tel (colors.tx2, fontSize 13), `Lié depuis le {g.date_creation ? new Date(g.date_creation).toLocaleDateString('fr-FR') : '—'}` (colors.tx3, fontSize 12)
    - right badge: `<StatutBadge label={isActive ? 'Actif' : 'Quitté'} color={isActive ? colors.gn : colors.tx3} />`
    - if `isActive`: a destructive-styled "Quitter ce garage" button `onPress={() => openRevoke(g.id, garage.nom || 'ce garage')}` — use the same fallback pattern as `RevokeGarageModal` in 15-04-PLAN.md (`Button` has no destructive variant): a `<Pressable>` styled with `backgroundColor colors.rd`, white bold text, `borderRadius 10`, `paddingVertical 14`, matching `Button`'s primary metrics.
    Revoke state + modal:
    - `const [revokeTarget, setRevokeTarget] = useState<{ id: string; name: string } | null>(null); const [revoking, setRevoking] = useState(false);`
    - `openRevoke(id, name)` → `setRevokeTarget({ id, name })`.
    - Render `<RevokeGarageModal visible={!!revokeTarget} garageName={revokeTarget?.name ?? ''} loading={revoking} onCancel={() => setRevokeTarget(null)} onConfirm={doRevoke} />`.
    - `doRevoke(motif: string | null)`: `if (!revokeTarget) return; setRevoking(true); const t = await getValidAccessToken(); const { ok, data } = await apiFetch('DELETE', '/client/garages/' + revokeTarget.id, { motif }, t); setRevoking(false); setRevokeTarget(null); if (ok) { showToast('Vous avez quitté le garage. Historique conservé 10 ans.', 'info'); load(); } else showToast(errMsg(data), 'error');`
    Loading → ActivityIndicator colors.acc.
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-app/app/(app)/(tabs)/motos/garages.tsx` contains `parseGarages` and `RevokeGarageModal`
    - contains `apiFetch('DELETE', '/client/garages/'` (leave-garage DELETE with motif body)
    - contains `g.statut === 'actif'` (or an `isActive` derived from it) and the labels `'Actif'` / `'Quitté'`
    - contains the success toast `Vous avez quitté le garage. Historique conservé 10 ans.`
    - contains the verbatim empty heading `Aucun garage lié`
    - `cd mobile-app && npx tsc --noEmit` exits 0
    - `cd mobile-app && npm test` exits 0
  </acceptance_criteria>
  <done>Garages list renders Actif/Quitté cards, leave-garage opens the legal-notice modal and DELETEs on confirm, status flips after reload; tsc + full test suite clean.</done>
</task>

</tasks>

<verification>
- `cd mobile-app && npx tsc --noEmit` exits 0
- `cd mobile-app && npm test` green (full suite — final Wave 3 merge gate)
- Manual (phase checkpoint): from Motos menu → Mes réclamations shows claims with badges; Mes garages → Quitter opens modal with legal notice → confirm → status "Quitté"
</verification>

<success_criteria>
The réclamations-list and garages-list (leave/révocation) halves of MPARITY-04 are delivered, including the verbatim legal-notice revoke modal and the DELETE flow.
</success_criteria>

<output>
After completion, create `.planning/phases/15-feature-parity-screens/15-08-SUMMARY.md`.
</output>
