---
phase: 15-feature-parity-screens
plan: 04
type: execute
wave: 2
depends_on: [15-01, 15-02]
files_modified:
  - mobile-app/components/ScoreBadge.tsx
  - mobile-app/components/StatutBadge.tsx
  - mobile-app/components/EmptyState.tsx
  - mobile-app/components/OfflineBanner.tsx
  - mobile-app/components/MotoListCard.tsx
  - mobile-app/components/RevokeGarageModal.tsx
autonomous: true
requirements: [MPARITY-01, MPARITY-02, MPARITY-04, MPARITY-05]
must_haves:
  truths:
    - "A moto's score renders inside a couleur-coded badge (vert/bleu/jaune/rouge)"
    - "Devis/réclamation/garage statut renders as a colored pill"
    - "Empty lists show an emoji + heading + body + optional CTA, not bare 'No data'"
    - "The offline banner shows a formatted 'Dernière mise à jour' timestamp"
    - "The revoke-garage modal shows the verbatim legal notice + optional motif input"
  artifacts:
    - path: "mobile-app/components/ScoreBadge.tsx"
      provides: "score/100 + couleur pill, reused on list + fiche"
      contains: "couleurColor"
    - path: "mobile-app/components/EmptyState.tsx"
      provides: "emoji + heading + body + optional primary/ghost CTA"
      contains: "EmptyState"
    - path: "mobile-app/components/OfflineBanner.tsx"
      provides: "'Dernière mise à jour : {timestamp}' banner"
      contains: "Dernière mise à jour"
    - path: "mobile-app/components/RevokeGarageModal.tsx"
      provides: "Modal with legal notice + motif TextField + Annuler/Quitter"
      contains: "art. L110-4"
  key_links:
    - from: "mobile-app/components/ScoreBadge.tsx"
      to: "lib/motoDisplay.ts"
      via: "couleurColor(couleur_dossier)"
      pattern: "couleurColor"
    - from: "mobile-app/components/OfflineBanner.tsx"
      to: "lib/cache.ts"
      via: "fmtCacheTimestamp(updatedAt)"
      pattern: "fmtCacheTimestamp"
---

<objective>
Build the six shared presentational components the Phase 15 screens compose from, wired to the Wave 1 logic modules and the UI-SPEC design contract (colors, spacing, typography, copy). No screens or data fetching here — pure, reusable UI.

Purpose: Interface-first. The motos/devis/liaison screen plans (15-05/06/07/08) import these components instead of re-implementing badges/empty-states/modals inline, guaranteeing visual consistency and satisfying the UI-SPEC's "reuse verbatim, do not fork variants" rule.
Output: ScoreBadge, StatutBadge, EmptyState, OfflineBanner, MotoListCard, RevokeGarageModal under `mobile-app/components/`.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-feature-parity-screens/15-UI-SPEC.md
@.planning/phases/15-feature-parity-screens/15-CONTEXT.md

<interfaces>
<!-- Wave 1 modules (from 15-01, 15-02) this plan imports. Use these signatures directly. -->

From mobile-app/lib/motoDisplay.ts:
```typescript
export function couleurColor(couleurDossier?: string): string; // → hex, defaults colors.rd
export function fmtDate(d?: string | null): string;
```
From mobile-app/lib/motoParse.ts:
```typescript
export interface Moto { id:string; marque:string; modele:string; annee?:number; km?:number; plaque:string; score?:number; couleur_dossier?:string; /* ... */ }
```
From mobile-app/lib/cache.ts:
```typescript
export function fmtCacheTimestamp(ms: number): string; // 'DD/MM à HHhMM'
```

Existing Phase 14 kit (reuse verbatim, do not fork):
- mobile-app/components/Button.tsx → `<Button title onPress loading? variant?='primary'|'ghost' disabled? />`
- mobile-app/components/TextField.tsx → `<TextField value onChangeText placeholder? label? ... />`
- mobile-app/theme/colors.ts → colors.{bg,card,card2,border,tx,tx2,tx3,acc,gn,bl,yw,rd}

UI-SPEC design tokens (MUST hold):
- Spacing multiples of 4: xs4 sm8 md16 lg24 xl32.
- Typography: Body 14/400, Label 13/600, Heading 18/600, Display 22/600. Only weights 400 and 600 in new text. No fontFamily.
- Color 60/30/10: bg dominant, card surfaces, acc accent (CTAs/active tab/focus/ghost-link ONLY — never a status badge). rd destructive.
- Status colors are data-driven (couleurColor / statut colors), never colors.acc-substituted.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScoreBadge + StatutBadge (couleur/score pill + generic statut pill)</name>
  <files>mobile-app/components/ScoreBadge.tsx, mobile-app/components/StatutBadge.tsx</files>
  <read_first>
    - mobile-app/lib/motoDisplay.ts (couleurColor)
    - mobile-app/theme/colors.ts (badge bg/border tokens)
    - .planning/phases/15-feature-parity-screens/15-UI-SPEC.md (Status Semantic Colors table; Typography Label 13/600)
    - MotoKey_Client.html lines 616-620 (score-badge markup — score-num + "Score MotoKey" label)
  </read_first>
  <action>
    Create mobile-app/components/ScoreBadge.tsx:
    - Props: `{ score?: number; couleurDossier?: string; size?: 'sm' | 'lg' }` (default 'sm').
    - Render a right-aligned badge: the number `{score ?? '—'}` in a bold (fontWeight '600') colored text where color = `couleurColor(couleurDossier)`; below it the label "Score MotoKey" in colors.tx3, fontSize 12 (Label role). 'lg' variant bumps the number to fontSize 28, 'sm' to 20.
    - Import `{ couleurColor }` from '../lib/motoDisplay', `{ colors }` from '../theme/colors'.
    Create mobile-app/components/StatutBadge.tsx:
    - Props: `{ label: string; color: string }`.
    - Render a pill `<View>` with `<Text>{label}</Text>`: paddingVertical 4, paddingHorizontal 10, borderRadius 999, backgroundColor = color at ~15% opacity (use the color with alpha, e.g. append hex '26' for ~15% — pass a solid `color` and set `backgroundColor: color + '26'`), text color = `color`, fontSize 12 (Label), fontWeight '600'.
    - No import from motoDisplay needed — caller passes the resolved color (devisStatutColor/reclamationStatutColor/couleurColor).
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-app/components/ScoreBadge.tsx` contains `couleurColor` and `Score MotoKey`
    - `mobile-app/components/ScoreBadge.tsx` imports from `../lib/motoDisplay`
    - `mobile-app/components/StatutBadge.tsx` contains `label` and `borderRadius: 999`
    - Neither component contains `fontFamily`
    - `cd mobile-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ScoreBadge renders couleur-coded score; StatutBadge renders a colored pill; both hold the UI-SPEC typography; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: EmptyState + OfflineBanner</name>
  <files>mobile-app/components/EmptyState.tsx, mobile-app/components/OfflineBanner.tsx</files>
  <read_first>
    - mobile-app/components/Button.tsx (primary + ghost variants for the CTAs)
    - mobile-app/lib/cache.ts (fmtCacheTimestamp)
    - mobile-app/theme/colors.ts
    - .planning/phases/15-feature-parity-screens/15-UI-SPEC.md (Copywriting Contract empty-state copy; Offline banner copy "Dernière mise à jour : {ex. 03/07 à 14h32}")
    - .planning/phases/15-feature-parity-screens/15-CONTEXT.md Specifics (empty-state tone: icon + short sentence + CTA)
  </read_first>
  <action>
    Create mobile-app/components/EmptyState.tsx:
    - Props: `{ icon: string; heading: string; body: string; ctaTitle?: string; onCta?: () => void; secondaryTitle?: string; onSecondary?: () => void }`.
    - Layout: centered column, lg (24) vertical padding, 2xl (48) top margin. `<Text style={{fontSize:32}}>{icon}</Text>` (emoji), heading (fontSize 18, fontWeight '600', colors.tx), body (fontSize 14, fontWeight '400', colors.tx2, textAlign center, marginTop 8). If `ctaTitle` render `<Button title={ctaTitle} onPress={onCta} />` (marginTop 16). If `secondaryTitle` render `<Button variant="ghost" title={secondaryTitle} onPress={onSecondary} />`.
    - Import Button from './Button', colors from '../theme/colors'.
    Create mobile-app/components/OfflineBanner.tsx:
    - Props: `{ updatedAt: number }`.
    - Render a top banner (not a toast): backgroundColor colors.card2, borderBottomWidth 1, borderColor colors.border, paddingVertical 8, paddingHorizontal 16. `<Text>` (fontSize 13, fontWeight '600', colors.tx2): `` `Dernière mise à jour : ${fmtCacheTimestamp(updatedAt)}` ``.
    - Import `{ fmtCacheTimestamp }` from '../lib/cache', colors from '../theme/colors'.
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-app/components/EmptyState.tsx` contains `ctaTitle` and `secondaryTitle` and imports `./Button`
    - `mobile-app/components/OfflineBanner.tsx` contains `Dernière mise à jour :` and `fmtCacheTimestamp`
    - `mobile-app/components/OfflineBanner.tsx` imports from `../lib/cache`
    - Neither component contains `fontFamily`
    - `cd mobile-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>EmptyState (icon+heading+body+optional CTAs) and OfflineBanner (formatted timestamp) exist and hold UI-SPEC copy/tokens; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: MotoListCard + RevokeGarageModal</name>
  <files>mobile-app/components/MotoListCard.tsx, mobile-app/components/RevokeGarageModal.tsx</files>
  <read_first>
    - mobile-app/components/ScoreBadge.tsx (composed into MotoListCard — created in Task 1 of this plan)
    - mobile-app/components/TextField.tsx (motif input in the modal)
    - mobile-app/components/Button.tsx (Annuler ghost + Quitter destructive)
    - mobile-app/lib/motoParse.ts (Moto interface)
    - MotoKey_Client.html lines 663-672 (moto-header identity: marque modele annee, km, plaque)
    - MotoKey_Client.html lines 355-380 + 1271-1296 (revoke modal markup + submitRevoke flow)
    - .planning/phases/15-feature-parity-screens/15-UI-SPEC.md (Destructive confirmation — Quitter un garage: verbatim legal notice + "Quitter le garage" red button)
  </read_first>
  <action>
    Create mobile-app/components/MotoListCard.tsx (D-04 lightweight row — identity + score + couleur only, NO inline expansion):
    - Props: `{ moto: Moto; onPress: () => void }`.
    - Render a `<Pressable onPress={onPress}>` card (backgroundColor colors.card, borderRadius 12, padding 16 (md), marginBottom 12, flexDirection row, justifyContent space-between, alignItems center). Left column: marque+modele (fontSize 18/'600', colors.tx) with annee appended in colors.tx2/'400'; km line `{moto.km ? moto.km.toLocaleString('fr-FR') + ' km' : ''}` (fontSize 13, colors.tx2); plaque (fontSize 13, colors.tx3). Right: `<ScoreBadge score={moto.score} couleurDossier={moto.couleur_dossier} size="sm" />`.
    - Import ScoreBadge from './ScoreBadge', Moto from '../lib/motoParse', colors.
    Create mobile-app/components/RevokeGarageModal.tsx:
    - Props: `{ visible: boolean; garageName: string; loading?: boolean; onCancel: () => void; onConfirm: (motif: string | null) => void }`.
    - Use RN core `<Modal transparent visible={visible} animationType="fade" onRequestClose={onCancel}>`. Overlay: flex center, semi-transparent backdrop (rgba(0,0,0,0.4)). Card: colors.card, borderRadius 12, padding 24 (lg), maxWidth 340.
    - Title (fontSize 18/'600'): `` `Quitter ${garageName} ?` `` (or "Quitter ce garage ?" if garageName empty).
    - Legal notice body VERBATIM (do not paraphrase), fontSize 13, colors.tx2: "Information légale (art. L110-4 Code de commerce) : votre historique d'entretien sera conservé par le garage pendant 10 ans après révocation. Vous perdrez uniquement l'accès en lecture à ces données."
    - Local state `const [motif, setMotif] = useState('')`; `<TextField value={motif} onChangeText={setMotif} placeholder="Motif (facultatif)" />`.
    - Buttons row: `<Button variant="ghost" title="Annuler" onPress={onCancel} />` and a destructive "Quitter le garage" button — reuse `<Button>` but render its container/text in colors.rd (if Button has no destructive variant, wrap a `<Pressable>` styled with backgroundColor colors.rd, white bold text, borderRadius 10, paddingVertical 14 — matching Button's primary metrics). onPress → `onConfirm(motif.trim() || null)`. Pass `loading` to disable while submitting.
    - Import TextField, Button, colors, useState.
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-app/components/MotoListCard.tsx` contains `ScoreBadge` and `onPress` and imports `Moto` from `../lib/motoParse`
    - `mobile-app/components/RevokeGarageModal.tsx` contains the verbatim substring `art. L110-4 Code de commerce`
    - `mobile-app/components/RevokeGarageModal.tsx` contains `Motif (facultatif)` and `Quitter le garage` and uses RN `Modal`
    - `mobile-app/components/RevokeGarageModal.tsx` calls `onConfirm(motif.trim() || null)`
    - Neither component contains `fontFamily`
    - `cd mobile-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MotoListCard (lightweight row with ScoreBadge) and RevokeGarageModal (verbatim legal notice + motif input + red confirm) exist; tsc clean.</done>
</task>

</tasks>

<verification>
- `cd mobile-app && npx tsc --noEmit` exits 0
- `cd mobile-app && npm test` still green (no logic modules broken)
- grep confirms no `@expo/vector-icons` and no `fontFamily` in any new component
</verification>

<success_criteria>
Six reusable components exist, each wired to the Wave 1 logic modules and holding the UI-SPEC color/spacing/typography/copy contract, ready for the screen plans to compose.
</success_criteria>

<output>
After completion, create `.planning/phases/15-feature-parity-screens/15-04-SUMMARY.md`.
</output>
