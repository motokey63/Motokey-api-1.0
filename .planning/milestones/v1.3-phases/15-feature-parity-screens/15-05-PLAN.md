---
phase: 15-feature-parity-screens
plan: 05
type: execute
wave: 3
depends_on: [15-01, 15-02, 15-03, 15-04]
files_modified:
  - mobile-app/app/(app)/(tabs)/motos/index.tsx
  - mobile-app/app/(app)/(tabs)/motos/[id].tsx
autonomous: true
requirements: [MPARITY-01, MPARITY-03, MPARITY-05]
must_haves:
  truths:
    - "The user sees a list of their motos, each with couleur-coded score (MPARITY-01)"
    - "Tapping a moto pushes to a Fiche Moto detail with historique interventions (MPARITY-03)"
    - "The Fiche Moto plan-d'entretien section is hidden (not errored) when the alertes endpoint 403s for CLIENT"
    - "When the network is unreachable, the motos list shows cached data + a 'dernière mise à jour' banner (MPARITY-05)"
    - "An empty motos list shows the EmptyState with Ajouter/Réclamer CTAs"
  artifacts:
    - path: "mobile-app/app/(app)/(tabs)/motos/index.tsx"
      provides: "Motos list tab: fetch+enrich+cache, lightweight cards, secondary-flow menu (D-05)"
      contains: "MotoListCard"
    - path: "mobile-app/app/(app)/(tabs)/motos/[id].tsx"
      provides: "Fiche Moto detail: historique + plan d'entretien (403-hidden) + pneus"
      contains: "parseAlertes"
  key_links:
    - from: "mobile-app/app/(app)/(tabs)/motos/index.tsx"
      to: "GET /motos + per-moto /interventions + /entretien/alertes"
      via: "apiGet with getValidAccessToken, enriched then setCached"
      pattern: "setCached\\(CACHE_KEY_MOTOS"
    - from: "mobile-app/app/(app)/(tabs)/motos/[id].tsx"
      to: "plan d'entretien section"
      via: "parseAlertes → null hides the section"
      pattern: "parseAlertes"
---

<objective>
Build the Motos tab: a list screen (lightweight couleur/score cards, pull-to-refresh, read-only offline cache, empty-state CTAs, and a menu to reach the secondary garage/moto flows) and a Fiche Moto detail screen (historique interventions + plan d'entretien with 403-hides-section discipline + pneumatiques). Ports `MotoKey_Client.html`'s `loadMotos()` + `renderMotoCard()` into two Expo Router screens (D-04 splits the web client's inline card into list→detail).

Purpose: Delivers MPARITY-01 (list with couleur/score), MPARITY-03 (historique), and MPARITY-05 (offline cache for the motos list).
Output: `motos/index.tsx` + `motos/[id].tsx`.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-feature-parity-screens/15-RESEARCH.md
@.planning/phases/15-feature-parity-screens/15-UI-SPEC.md
@.planning/phases/15-feature-parity-screens/15-CONTEXT.md

<interfaces>
<!-- Contracts from Waves 1-2 (plans 15-01/02/03/04) + existing Phase 14 API. Use directly. -->

Data fetching (mobile-app/lib/api.ts + hooks/useAuth.ts):
```typescript
import { apiGet } from '../../../../lib/api';
const { getValidAccessToken } = useAuth();      // from '../../../../hooks/useAuth'
const token = await getValidAccessToken();       // string | null — call before each fetch
// apiGet(path, token) → { ok, status, data }
```

Wave 1 logic (import from '../../../../lib/...'):
```typescript
// motoParse.ts
parseMotosList(data): Moto[]; parseInterventions(res): Intervention[]; parseAlertes(res): Alerte[]|null; fmtStatut(s?): string;
interface Moto { id; marque; modele; annee?; km?; plaque; score?; couleur_dossier?; pneu_av?; pneu_ar?; garage?; garage_nom?; garage_tel?; interventions?; alertes? }
// motoDisplay.ts
couleurColor(c?): string; fmtDate(d?): string;
// cache.ts
getCached<T>(key); setCached<T>(key,data); shouldServeCache(status): boolean; fmtCacheTimestamp(ms); CACHE_KEY_MOTOS;
```

Wave 2 components (import from '../../../../components/...'):
```typescript
<MotoListCard moto onPress />
<ScoreBadge score couleurDossier size='lg' />
<StatutBadge label color />        // for plan-d'entretien alerte statut pills (fmtStatut + couleur)
<EmptyState icon heading body ctaTitle? onCta? secondaryTitle? onSecondary? />
<OfflineBanner updatedAt />
```

Backend (motokey-api.js, CLIENT-accessible unless noted):
- GET /motos → data.motos (or bare array). Has score + couleur_dossier columns.
- GET /motos/:id/interventions → data.interventions (CLIENT branch exists).
- GET /motos/:id/entretien/alertes → **403 for CLIENT** (MECANO-gated). parseAlertes returns null → hide section. NOT an error. (RESEARCH Pitfall 1)
- Response envelope: `{ success, data:{...} }`.

expo-router navigation:
```typescript
import { router, useLocalSearchParams, Stack } from 'expo-router';
router.push('/(app)/(tabs)/motos/' + moto.id);   // to detail
router.push('/(app)/(tabs)/motos/add');           // secondary flows (routes from 15-07/08)
const { id } = useLocalSearchParams<{ id: string }>();
// Set the stack header title per screen with <Stack.Screen options={{ title: '...' }} />
```

UI-SPEC copy (verbatim):
- Motos empty heading: "Aucune moto pour l'instant"
- Motos empty body: "Ajoutez votre première moto ou réclamez-en une déjà enregistrée chez un garage partenaire."
- CTAs: "Ajouter ma moto" (primary) + "Réclamer une moto" (ghost)
- Interventions empty (inline, in fiche): "Aucune intervention enregistrée pour cette moto."
- Network error (fiche, online-only): "Serveur inaccessible — l'API est-elle démarrée ?" + "Réessayer" button.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Motos list tab (fetch + enrich + cache + empty state + secondary-flow menu)</name>
  <files>mobile-app/app/(app)/(tabs)/motos/index.tsx</files>
  <read_first>
    - MotoKey_Client.html lines 691-729 (loadMotos: GET /motos, per-moto parallel interventions+alertes enrich, empty state, error state)
    - mobile-app/lib/api.ts + mobile-app/hooks/useAuth.ts (apiGet + getValidAccessToken)
    - mobile-app/lib/cache.ts (getCached/setCached/shouldServeCache/CACHE_KEY_MOTOS)
    - mobile-app/lib/motoParse.ts (parseMotosList/parseInterventions/parseAlertes)
    - mobile-app/components/MotoListCard.tsx, EmptyState.tsx, OfflineBanner.tsx (from 15-04)
    - .planning/phases/15-feature-parity-screens/15-RESEARCH.md Pattern 3 (cache-on-failure code example) + Pitfall 3 (status===0 gate)
    - .planning/phases/15-feature-parity-screens/15-CONTEXT.md D-04 (lightweight list), D-05 (secondary flows behind a menu), D-09 (cache scope)
  </read_first>
  <action>
    Create mobile-app/app/(app)/(tabs)/motos/index.tsx — a functional component using useState + useCallback + useEffect (or useFocusEffect from expo-router) and a `<FlatList>` with `<RefreshControl>`.
    Load flow (port loadMotos + RESEARCH Pattern 3):
    1. `const token = await getValidAccessToken();`
    2. `const res = await apiGet('/motos', token);`
    3. If `res.ok`: `const motos = parseMotosList(res.data);` then enrich each moto in parallel with `Promise.all(motos.map(...))`: `const [ivRes, alRes] = await Promise.all([apiGet('/motos/'+m.id+'/interventions', token), apiGet('/motos/'+m.id+'/entretien/alertes', token)]); return { ...m, interventions: parseInterventions(ivRes), alertes: parseAlertes(alRes) };` Then `await setCached(CACHE_KEY_MOTOS, enriched); setMotos(enriched); setStaleSince(null);`
    4. Else if `shouldServeCache(res.status)`: `const cached = await getCached<Moto[]>(CACHE_KEY_MOTOS); if (cached) { setMotos(cached.data); setStaleSince(cached.updatedAt); } else setError(errMsg(res.data));`
    5. Else (real 401/403/500): `setError(errMsg(res.data));`
    Rendering:
    - Loading: `<ActivityIndicator color={colors.acc} />` centered.
    - If `staleSince != null`: render `<OfflineBanner updatedAt={staleSince} />` above the list.
    - Empty (motos.length === 0, not error): `<EmptyState icon="🏍️" heading="Aucune moto pour l'instant" body="Ajoutez votre première moto ou réclamez-en une déjà enregistrée chez un garage partenaire." ctaTitle="Ajouter ma moto" onCta={() => router.push('/(app)/(tabs)/motos/add')} secondaryTitle="Réclamer une moto" onSecondary={() => router.push('/(app)/(tabs)/motos/claim')} />`
    - List: `<FlatList data={motos} keyExtractor={m => m.id} renderItem={({item}) => <MotoListCard moto={item} onPress={() => router.push('/(app)/(tabs)/motos/' + item.id)} />} refreshControl={<RefreshControl refreshing={refreshing} onRefresh={load} />} />`
    - Secondary-flow access (D-05): a header row / menu with ghost `<Button>`s or a `<Stack.Screen options={{ headerRight: ... }}>` linking to `/(app)/(tabs)/motos/add`, `/claim`, `/reclamations`, `/garages`. Simplest acceptable: a horizontal row of ghost buttons above the list ("Ajouter", "Réclamer", "Mes réclamations", "Mes garages") each doing router.push to the matching route. Set the stack title to "Mes motos" via `<Stack.Screen options={{ title: 'Mes motos' }} />`.
    - Import errMsg from '../../../../lib/api', colors, router from 'expo-router'.
    Note: pass the enriched moto (with interventions/alertes already fetched) into navigation is NOT required — the detail screen re-fetches by id (D-09: fiche detail is online-only). The list cache stores the enriched array for the offline list render only.
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-app/app/(app)/(tabs)/motos/index.tsx` contains `MotoListCard` and `FlatList` and `RefreshControl`
    - contains `setCached(CACHE_KEY_MOTOS` and `shouldServeCache(res.status)` (network-failure gate, not `!res.ok`)
    - contains `parseInterventions` and `parseAlertes` in the enrich step
    - contains `router.push('/(app)/(tabs)/motos/add')` and `'/(app)/(tabs)/motos/claim'`
    - contains the verbatim empty-state heading `Aucune moto pour l'instant`
    - contains `<OfflineBanner` referencing the stale timestamp state
    - `cd mobile-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Motos list fetches+enriches+caches, renders lightweight cards, shows offline banner on status 0, empty-state CTAs, and links to secondary flows; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fiche Moto detail (historique + plan d'entretien 403-hidden + pneus)</name>
  <files>mobile-app/app/(app)/(tabs)/motos/[id].tsx</files>
  <read_first>
    - MotoKey_Client.html lines 609-689 (renderMotoCard: identity, garage de référence, historique interventions, plan d'entretien conditional, pneumatiques)
    - mobile-app/lib/motoParse.ts (parseInterventions/parseAlertes/fmtStatut) + motoDisplay.ts (fmtDate/couleurColor)
    - mobile-app/components/ScoreBadge.tsx + StatutBadge.tsx (from 15-04)
    - .planning/phases/15-feature-parity-screens/15-RESEARCH.md Pitfall 1 (alertes 403 → section masquée, NOT error)
    - .planning/phases/15-feature-parity-screens/15-UI-SPEC.md (Heading 18/600 section titles; xl 32 gap between Fiche Moto sections; interventions empty inline copy; hidden-section rule)
  </read_first>
  <action>
    Create mobile-app/app/(app)/(tabs)/motos/[id].tsx — reads `const { id } = useLocalSearchParams<{ id: string }>();`. This screen is ONLINE-ONLY (D-09 — no cache fallback).
    Load flow:
    1. `const token = await getValidAccessToken();`
    2. Fetch the moto + its enrichments: `const [motoRes, ivRes, alRes] = await Promise.all([apiGet('/motos/' + id, token), apiGet('/motos/' + id + '/interventions', token), apiGet('/motos/' + id + '/entretien/alertes', token)]);`
    3. If `!motoRes.ok`: show error state — centered EmptyState-style with icon "⚠️", the message `errMsg(motoRes.data)` (falls back to "Serveur inaccessible — l'API est-elle démarrée ?"), and a "Réessayer" `<Button>` re-running load.
    4. Else: `const moto = motoRes.data?.moto || motoRes.data; const interventions = parseInterventions(ivRes); const alertes = parseAlertes(alRes);` (alertes may be null → 403).
    Rendering inside a `<ScrollView>` (bg colors.bg, padding 16), sections separated by xl (32) gaps, each section title fontSize 18/'600':
    - Header: marque+modele (fontSize 18/'600') with annee in colors.tx2/'400'; km + plaque lines; `<ScoreBadge score={moto.score} couleurDossier={moto.couleur_dossier} size="lg" />`. Set `<Stack.Screen options={{ title: moto.marque + ' ' + moto.modele }} />`.
    - "Garage de référence": garageName = `moto.garage?.nom || moto.garage_nom || '—'`, optional tel.
    - "Historique · {n} intervention(s)": for each intervention render title (fontWeight '600'), meta line `fmtDate(i.date_intervention) + (i.km ? ' · ' + i.km.toLocaleString('fr-FR') + ' km' : '') + (i.technicien_nom ? ' · ' + i.technicien_nom : '')` (colors.tx2, fontSize 13), and a small couleur dot using couleurColor(i.type). If interventions.length === 0 render the inline text "Aucune intervention enregistrée pour cette moto." (colors.tx3, fontSize 13) — NOT a full EmptyState.
    - Plan d'entretien: **only if `alertes && alertes.length > 0`**. If `alertes === null` (403) OR empty, OMIT the entire section — render nothing, no error, no empty message (RESEARCH Pitfall 1 / UI-SPEC hidden-section rule). When rendered, each alerte: `<StatutBadge label={fmtStatut(a.statut)} color={...} />` + `a.nom || a.nom_operation` + `a.km_prochain ? 'Prévu à ' + Number(a.km_prochain).toLocaleString('fr-FR') + ' km' : ''`.
    - Pneumatiques: only if `moto.pneu_av || moto.pneu_ar`. Show "Avant" / "Arrière" labels + values.
    - Import apiGet/errMsg from '../../../../lib/api', useAuth, colors, Stack/useLocalSearchParams from 'expo-router'.
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-app/app/(app)/(tabs)/motos/[id].tsx` contains `useLocalSearchParams` and `parseAlertes` and `parseInterventions`
    - contains a conditional that renders the plan-d'entretien section only when `alertes` is non-null and non-empty (e.g. `alertes && alertes.length > 0`)
    - contains the inline interventions-empty copy `Aucune intervention enregistrée pour cette moto.`
    - contains `pneu_av` and `pneu_ar` conditional rendering
    - contains `<ScoreBadge` with `size="lg"`
    - does NOT reference `CACHE_KEY` (fiche is online-only, D-09)
    - `cd mobile-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Fiche Moto shows historique + pneus, hides plan d'entretien cleanly on 403/empty, and surfaces a real error+retry on network failure; tsc clean.</done>
</task>

</tasks>

<verification>
- `cd mobile-app && npx tsc --noEmit` exits 0
- `cd mobile-app && npm test` green (logic modules unaffected)
- Manual (phase checkpoint): open Motos tab → cards show couleur score; tap → Fiche Moto with interventions; plan section absent for CLIENT (403) without error; airplane mode → cached list + timestamp banner
</verification>

<success_criteria>
MPARITY-01 (list with couleur/score), MPARITY-03 (historique + fiche), and MPARITY-05 (motos-list offline cache) are delivered; the plan-d'entretien 403-hides-section discipline matches the web client exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/15-feature-parity-screens/15-05-SUMMARY.md`.
</output>
