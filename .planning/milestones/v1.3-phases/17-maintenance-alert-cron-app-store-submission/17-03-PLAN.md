---
phase: 17-maintenance-alert-cron-app-store-submission
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile-app/app.json
  - mobile-app/eas.json
  - mobile-app/package.json
  - mobile-app/package-lock.json
  - .planning/phases/17-maintenance-alert-cron-app-store-submission/store-content/privacy-manifest-content.md
  - .planning/phases/17-maintenance-alert-cron-app-store-submission/store-content/data-safety-content.md
autonomous: true
requirements: [MSTORE-01]

must_haves:
  truths:
    - "app.json declares com.motokey.app for both ios.bundleIdentifier and android.package"
    - "app.json declares ios.privacyManifests required-reason API entries covering the app's actual API usage"
    - "eas.json exists with development (developmentClient + internal distribution), preview, and production build profiles"
    - "expo-dev-client is a dependency so a development build profile can compile"
    - "Apple App Privacy and Google Data Safety answers are written from the real data inventory, ready to paste once accounts exist"
  artifacts:
    - path: "mobile-app/app.json"
      provides: "Store identifiers + privacy manifest declarations"
      contains: "com.motokey.app"
    - path: "mobile-app/eas.json"
      provides: "EAS build profiles"
      contains: "development"
    - path: ".planning/phases/17-maintenance-alert-cron-app-store-submission/store-content/privacy-manifest-content.md"
      provides: "Apple App Privacy nutrition-label answers"
    - path: ".planning/phases/17-maintenance-alert-cron-app-store-submission/store-content/data-safety-content.md"
      provides: "Google Play Data Safety form answers"
  key_links:
    - from: "mobile-app/eas.json"
      to: "mobile-app/package.json expo-dev-client"
      via: "development profile developmentClient:true requires the package"
      pattern: "developmentClient"
---

<objective>
Make the mobile app store-submission-ready in code and content (MSTORE-01), without touching any paid-account-gated step: set the bundle/package identifiers, declare Apple privacy manifests, add EAS build profiles + the dev-client dependency, and write the Apple App Privacy + Google Data Safety answers from the app's real data inventory.

Purpose: Everything that does NOT require an Apple/Google/Expo account is completed now, so Plan 04's human checkpoints only cover the genuinely account-gated actions.
Output: Updated `app.json`, a new `eas.json`, `expo-dev-client` installed, and two ready-to-paste compliance content docs.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-maintenance-alert-cron-app-store-submission/17-CONTEXT.md
@.planning/phases/17-maintenance-alert-cron-app-store-submission/17-RESEARCH.md
@mobile-app/AGENTS.md

<interfaces>
<!-- Current app.json (the file being edited). Add fields; keep everything already present. -->

From mobile-app/app.json (current — note: NO bundleIdentifier, NO android.package, NO extra.eas.projectId, typedRoutes already true):
```json
{ "expo": {
  "name": "MotoKey", "slug": "motokey", "version": "1.0.0", "scheme": "motokey",
  "ios": { "icon": "./assets/expo.icon" },
  "android": { "adaptiveIcon": {...}, "predictiveBackGestureEnabled": false },
  "plugins": ["expo-router", ["expo-splash-screen", {...}], "expo-secure-store", "expo-font", "expo-notifications"],
  "experiments": { "typedRoutes": true, "reactCompiler": true }
}}
```

Real data inventory (from 17-RESEARCH.md — the app collects ONLY these):
- Auth email (login/register)
- Moto data: VIN, plaque, km, année, marque, modèle
- Client contact fields: nom, email, tél (from add.tsx)
- Expo push token
- NO camera/photos, NO location, NO payment data (billing stays web/Stripe), NO analytics/tracking SDKs.

D-03 (locked): bundle ID / package = `com.motokey.app` for BOTH platforms.
Expo SDK is ~54 (do NOT bump). `extra.eas.projectId` is intentionally NOT set here — it is populated by `eas init` in Plan 04.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: app.json — identifiers + Apple privacy manifests</name>
  <files>mobile-app/app.json</files>
  <read_first>
    - mobile-app/app.json (full current file — preserve all existing keys)
    - 17-RESEARCH.md "State of the Art" table (privacy manifest aggregation) + "Open Questions #... privacy" + Tertiary sources note (exact reason codes are LOW confidence — declare the standard set, verify via prebuild in Plan 04)
    - mobile-app/AGENTS.md
  </read_first>
  <action>
    Edit `mobile-app/app.json`, keeping every existing key. Add:
    - `ios.bundleIdentifier`: `"com.motokey.app"` (inside the existing `ios` object, next to `icon`).
    - `android.package`: `"com.motokey.app"` (inside the existing `android` object).
    - `ios.privacyManifests.NSPrivacyAccessedAPITypes` declaring the standard required-reason APIs for an Expo/RN app that uses AsyncStorage + file access + expo-device/expo-secure-store:
    ```json
    "privacyManifests": {
      "NSPrivacyAccessedAPITypes": [
        { "NSPrivacyAccessedAPIType": "NSPrivacyAccessedAPICategoryUserDefaults", "NSPrivacyAccessedAPITypeReasons": ["CA92.1"] },
        { "NSPrivacyAccessedAPIType": "NSPrivacyAccessedAPICategoryFileTimestamp", "NSPrivacyAccessedAPITypeReasons": ["C617.1"] },
        { "NSPrivacyAccessedAPIType": "NSPrivacyAccessedAPICategorySystemBootTime", "NSPrivacyAccessedAPITypeReasons": ["35F9.1"] },
        { "NSPrivacyAccessedAPIType": "NSPrivacyAccessedAPICategoryDiskSpace", "NSPrivacyAccessedAPITypeReasons": ["E174.1"] }
      ]
    }
    ```
    Do NOT set `extra.eas.projectId` (Plan 04's `eas init` writes it). Do NOT set `android.googleServicesFile` yet (added in Plan 04 once the Firebase `google-services.json` exists). Do NOT bump the Expo SDK or change `version`.
    Add a top-level note in the SUMMARY that these reason codes are the standard RN/Expo set and must be reconciled against the actual generated `PrivacyInfo.xcprivacy` from Plan 04's build/prebuild (research flagged the exact codes LOW confidence).
  </action>
  <verify>
    <automated>cd mobile-app && node -e "const j=require('./app.json'); if(j.expo.ios.bundleIdentifier!=='com.motokey.app')process.exit(1); if(j.expo.android.package!=='com.motokey.app')process.exit(1); if(!j.expo.ios.privacyManifests.NSPrivacyAccessedAPITypes.length)process.exit(1); console.log('app.json ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The node verify command above prints "app.json ok" (valid JSON, both identifiers = com.motokey.app, privacyManifests non-empty)
    - grep `"bundleIdentifier": "com.motokey.app"` mobile-app/app.json returns a match
    - grep `"package": "com.motokey.app"` mobile-app/app.json returns a match
    - grep `NSPrivacyAccessedAPICategoryUserDefaults` mobile-app/app.json returns a match
    - grep -c `projectId` mobile-app/app.json returns 0 (left for eas init in Plan 04)
    - grep `"typedRoutes": true` mobile-app/app.json still returns a match (existing keys preserved)
  </acceptance_criteria>
  <done>app.json carries both com.motokey.app identifiers and the standard Apple privacy manifest declarations, with all pre-existing config intact and projectId deliberately absent.</done>
</task>

<task type="auto">
  <name>Task 2: eas.json build profiles + expo-dev-client dependency</name>
  <files>mobile-app/eas.json, mobile-app/package.json, mobile-app/package-lock.json</files>
  <read_first>
    - mobile-app/package.json (current dependencies + jest config — confirm expo-dev-client is absent, confirm SDK ~54)
    - 17-RESEARCH.md "Standard Stack → Supporting" (expo-dev-client via `npx expo install`) and "Architecture Patterns → Recommended Project Structure"
    - mobile-app/AGENTS.md (read the versioned SDK 54 EAS docs before writing eas.json)
  </read_first>
  <action>
    (a) From `mobile-app/`, add the dev-client with the SDK-pinned installer so the version matches SDK 54: `npx expo install expo-dev-client` (use `--legacy-peer-deps` if npm errors on peer deps, matching this repo's established install pattern). This updates `package.json` + `package-lock.json`.

    (b) Create `mobile-app/eas.json` with three profiles. The `development` profile MUST have `developmentClient: true` and `distribution: "internal"` (so the D-06 Android dev build is directly installable). Include `preview` (internal) and a `production` placeholder:
    ```json
    {
      "cli": { "version": ">= 12.0.0", "appVersionSource": "remote" },
      "build": {
        "development": { "developmentClient": true, "distribution": "internal", "android": { "buildType": "apk" } },
        "preview": { "distribution": "internal", "android": { "buildType": "apk" } },
        "production": { "autoIncrement": true }
      },
      "submit": { "production": {} }
    }
    ```
    Do NOT run `eas build`/`eas login`/`eas init` here — those are Plan 04 human checkpoints. Verify the exact `cli.version`/schema keys against the SDK-54 EAS docs before finalizing (do not trust training-data defaults per AGENTS.md).
  </action>
  <verify>
    <automated>cd mobile-app && node -e "const e=require('./eas.json'); if(!e.build.development.developmentClient)process.exit(1); if(e.build.development.distribution!=='internal')process.exit(1); const p=require('./package.json'); if(!(p.dependencies['expo-dev-client']))process.exit(1); console.log('eas ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The node verify command above prints "eas ok" (development profile has developmentClient:true + internal distribution, expo-dev-client is in dependencies)
    - grep `developmentClient` mobile-app/eas.json returns a match
    - grep `"distribution": "internal"` mobile-app/eas.json returns a match
    - grep `expo-dev-client` mobile-app/package.json returns a match
    - `cd mobile-app && npx tsc --noEmit` exits 0 (adding the dependency did not break the type project)
  </acceptance_criteria>
  <done>eas.json defines an installable Android development profile, and expo-dev-client is installed at the SDK-54-compatible version — the app can be built with a development profile in Plan 04.</done>
</task>

<task type="auto">
  <name>Task 3: Apple App Privacy + Google Data Safety content docs</name>
  <files>.planning/phases/17-maintenance-alert-cron-app-store-submission/store-content/privacy-manifest-content.md, .planning/phases/17-maintenance-alert-cron-app-store-submission/store-content/data-safety-content.md</files>
  <read_first>
    - 17-RESEARCH.md "Summary" (real data inventory), "Don't Hand-Roll" (platform fixed taxonomies), "Common Pitfalls #2/#3", REQUIREMENTS.md "Out of Scope" (no camera/location/payment/tracking)
    - .planning/phases/17-maintenance-alert-cron-app-store-submission/17-CONTEXT.md (D-02 — content written now, submission parked)
  </read_first>
  <action>
    Create `.planning/phases/17-maintenance-alert-cron-app-store-submission/store-content/privacy-manifest-content.md` and `.../store-content/data-safety-content.md`.

    Both must map ONLY the real data inventory (no invented categories):
    - Email address (account auth) — collected, linked to identity, NOT used for tracking.
    - Contact info: name, email, phone (nom/email/tel from add.tsx) — collected, linked, not tracking.
    - Vehicle/other data: VIN, plaque, km, année, marque, modèle — "Other user content"/product interaction.
    - Device push token — used for push notifications (functionality), not tracking.
    - Explicitly declare NO: camera/photos, location, financial/payment data (billing is web/Stripe), advertising/analytics SDKs, third-party data sharing.

    `privacy-manifest-content.md` = Apple App Store Connect "App Privacy" questionnaire answers, structured in Apple's category vocabulary (Contact Info → Email Address / Name / Phone Number; Identifiers → Device ID/push token; User Content). For each: Data Types collected, "Used to Track You?" = No, "Linked to You?" = Yes, purpose (App Functionality). Add a header note: this is the App Store Connect nutrition-label content (distinct from the technical PrivacyInfo.xcprivacy in app.json), submittable only once an Apple Developer account exists (D-02 parked).

    `data-safety-content.md` = Google Play Console "Data safety" form answers in Google's category vocabulary (Personal info → Name, Email address, Phone number; App activity; Device or other IDs). For each: collected? yes; shared? no; processed ephemerally? no; required or optional; encrypted in transit? yes; user can request deletion? yes (via account). Add a header note: submittable only once a Google Play Console account exists (D-02 parked).

    Do NOT write free-form prose privacy-policy text — use the platforms' structured category answers (Don't Hand-Roll).
  </action>
  <verify>
    <automated>test -f ".planning/phases/17-maintenance-alert-cron-app-store-submission/store-content/privacy-manifest-content.md" && test -f ".planning/phases/17-maintenance-alert-cron-app-store-submission/store-content/data-safety-content.md" && echo docs-ok</automated>
  </verify>
  <acceptance_criteria>
    - Both content files exist (test command prints "docs-ok")
    - grep -i `push token` privacy-manifest-content.md returns a match
    - grep -i `Used to Track You` privacy-manifest-content.md returns a match (Apple vocabulary present)
    - grep -i `Data safety` data-safety-content.md returns a match (Google vocabulary present)
    - grep -i `camera` data-safety-content.md returns a match (explicitly declaring NO camera)
    - grep -i `Stripe` data-safety-content.md OR privacy-manifest-content.md returns a match (payment handled off-app noted)
  </acceptance_criteria>
  <done>Both compliance docs enumerate exactly the app's real data collection in each platform's own category taxonomy, explicitly excluding camera/location/payment/tracking, ready to paste once accounts exist.</done>
</task>

</tasks>

<verification>
- `cd mobile-app && npx tsc --noEmit` green after the dependency add.
- app.json: both identifiers = com.motokey.app, privacyManifests present, projectId absent, existing keys intact.
- eas.json: installable Android development profile.
- Two content docs present with the correct platform vocabularies and the real (minimal) data inventory.
- Actual manifest/content submission is PARKED (D-02) — no account-gated action attempted here.
</verification>

<success_criteria>
- MSTORE-01 code + content groundwork is complete and account-free: the app is configured for a build and the two compliance questionnaires are answered from the true data inventory.
</success_criteria>

<output>
After completion, create `.planning/phases/17-maintenance-alert-cron-app-store-submission/17-03-SUMMARY.md`.
</output>
