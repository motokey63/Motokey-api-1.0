---
phase: 17-maintenance-alert-cron-app-store-submission
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile-app/hooks/useNotificationObserver.ts
  - mobile-app/hooks/__tests__/useNotificationObserver.test.ts
autonomous: true
requirements: [MPUSH-04]

must_haves:
  truths:
    - "A notification whose data is { type: 'moto_entretien', motoId } maps to the Fiche Moto route object"
    - "The existing devis_recu mapping and null-fallback behaviour are unchanged"
    - "The moto route is returned as a { pathname, params } object (not an interpolated string) so typed-routes resolves the [id] segment"
  artifacts:
    - path: "mobile-app/hooks/useNotificationObserver.ts"
      provides: "Extended mapNotificationDataToRoute with the moto_entretien branch + widened NotificationRoute type"
      contains: "moto_entretien"
    - path: "mobile-app/hooks/__tests__/useNotificationObserver.test.ts"
      provides: "Unit tests covering the new moto_entretien → {pathname, params} mapping"
      contains: "moto_entretien"
  key_links:
    - from: "mobile-app/hooks/useNotificationObserver.ts"
      to: "mobile-app/app/(app)/(tabs)/motos/[id].tsx"
      via: "router.push({ pathname: '/(app)/(tabs)/motos/[id]', params: { id } })"
      pattern: "motos/\\[id\\]"
---

<objective>
Ship MPUSH-04's mobile half: extend the notification-tap router so a maintenance-alert push (`data.type === 'moto_entretien'`) deep-links straight to the concerned moto's Fiche Moto, reusing the Phase 16 observer mechanism already wired for devis.

Purpose: "Tap the reminder → land on the moto" (ROADMAP SC-2) with zero new navigation infrastructure — one new branch in one pure function, plus its tests.
Output: A widened `NotificationRoute` type, a new `moto_entretien` branch in `mapNotificationDataToRoute`, and unit tests locking the `{pathname, params}` object shape.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-maintenance-alert-cron-app-store-submission/17-CONTEXT.md
@.planning/phases/17-maintenance-alert-cron-app-store-submission/17-RESEARCH.md
@mobile-app/AGENTS.md

<interfaces>
<!-- Current state of the file being edited — extend, do not rewrite. -->

From mobile-app/hooks/useNotificationObserver.ts (current):
```ts
export type NotificationRoute = '/(app)/(tabs)/devis';

export function mapNotificationDataToRoute(data: any): NotificationRoute | null {
  if (data && data.type === 'devis_recu') return '/(app)/(tabs)/devis';
  return null;
}
// redirect() already calls: if (route) router.push(route as any);  — accepts the object shape unmodified.
```

The Motos tab's own existing router.push (mobile-app/app/(app)/(tabs)/motos/index.tsx, the working pattern to mirror):
```ts
router.push({ pathname: '/(app)/(tabs)/motos/[id]', params: { id: item.id } });
```

The backend push payload (from Plan 01's service) sends: `data: { type: 'moto_entretien', motoId: moto.id }`.

Existing tests (mobile-app/hooks/__tests__/useNotificationObserver.test.ts) already mock expo-notifications + expo-router and test mapNotificationDataToRoute as a pure function.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend mapNotificationDataToRoute for moto_entretien + tests</name>
  <files>mobile-app/hooks/useNotificationObserver.ts, mobile-app/hooks/__tests__/useNotificationObserver.test.ts</files>
  <read_first>
    - mobile-app/hooks/useNotificationObserver.ts (full current file — the type + function being extended, and the redirect() call site that must keep working)
    - mobile-app/hooks/__tests__/useNotificationObserver.test.ts (existing test structure to extend, not replace)
    - mobile-app/app/(app)/(tabs)/motos/index.tsx (locate the existing `router.push({ pathname: '/(app)/(tabs)/motos/[id]', params: { id } })` call — copy this literal shape verbatim; Pitfall 5)
    - mobile-app/AGENTS.md (Expo HAS CHANGED — verify the typed-routes Href object shape against the installed SDK if in doubt)
  </read_first>
  <behavior>
    - Test: mapNotificationDataToRoute({ type: 'moto_entretien', motoId: 'abc' }) deep-equals { pathname: '/(app)/(tabs)/motos/[id]', params: { id: 'abc' } }
    - Test: mapNotificationDataToRoute({ type: 'moto_entretien' }) (missing motoId) returns null
    - Test: a numeric motoId is coerced to a string in params.id (motoId: 42 → params.id === '42')
    - Existing tests still pass: devis_recu → '/(app)/(tabs)/devis', unknown/undefined/{} → null
  </behavior>
  <action>
    In `mobile-app/hooks/useNotificationObserver.ts`, widen the exported type to a union that includes the moto object shape, matching the literal used by motos/index.tsx:
    ```ts
    export type NotificationRoute =
      | '/(app)/(tabs)/devis'
      | { pathname: '/(app)/(tabs)/motos/[id]'; params: { id: string } };
    ```
    Add the new branch to `mapNotificationDataToRoute`, keeping the existing `devis_recu` branch and the final `return null` unchanged:
    ```ts
    if (data && data.type === 'moto_entretien' && data.motoId) {
      return { pathname: '/(app)/(tabs)/motos/[id]', params: { id: String(data.motoId) } };
    }
    ```
    Do NOT change the `redirect()` function — its `router.push(route as any)` already accepts this object shape. Do NOT return an interpolated string like `'/(app)/(tabs)/motos/' + motoId` (Pitfall 5: typed-routes will not match the dynamic `[id]` segment).

    In `mobile-app/hooks/__tests__/useNotificationObserver.test.ts`, add tests for the four behaviors above using `toEqual` for the object shape (not `toBe`), leaving the existing four tests intact.
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit && npx jest hooks/useNotificationObserver</automated>
  </verify>
  <acceptance_criteria>
    - `cd mobile-app && npx tsc --noEmit` exits 0 (no `as any` cast added to mapNotificationDataToRoute's return)
    - `cd mobile-app && npx jest hooks/useNotificationObserver` passes with the new tests
    - grep `moto_entretien` mobile-app/hooks/useNotificationObserver.ts returns a match
    - grep `pathname: '/(app)/(tabs)/motos/\[id\]'` mobile-app/hooks/useNotificationObserver.ts returns a match
    - grep `String(data.motoId)` mobile-app/hooks/useNotificationObserver.ts returns a match
    - grep `moto_entretien` mobile-app/hooks/__tests__/useNotificationObserver.test.ts returns a match
    - grep -c `router.push` diff of useNotificationObserver.ts shows the redirect() call is unchanged (still exactly one push call site)
  </acceptance_criteria>
  <done>The pure mapper returns the correct Fiche Moto route object for moto_entretien pushes, coerces motoId to string, guards on a missing motoId, and preserves all prior devis behaviour — all under green tsc + jest.</done>
</task>

</tasks>

<verification>
- `cd mobile-app && npx tsc --noEmit && npx jest hooks/useNotificationObserver` green.
- The redirect()/observer hook is untouched (only the pure mapper + type changed) — no risk to the mounting boundary Phase 16 established.
- On-device tap-to-navigate verification is deferred to Plan 04 (needs a real EAS build + a real maintenance push from Plan 01's cron).
</verification>

<success_criteria>
- A maintenance-alert notification carries `type: 'moto_entretien'` + `motoId`, and tapping it resolves to the correct moto's Fiche Moto via the typed-routes object shape (unit-verified here; on-device-verified in Plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/17-maintenance-alert-cron-app-store-submission/17-02-SUMMARY.md`.
</output>
