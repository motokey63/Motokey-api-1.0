---
phase: 17-maintenance-alert-cron-app-store-submission
plan: 04
type: execute
wave: 2
depends_on: ["17-01", "17-02", "17-03"]
files_modified:
  - mobile-app/app.json
  - mobile-app/google-services.json
autonomous: false
requirements: [MPUSH-04, MSTORE-01, MSTORE-02]
user_setup:
  - service: expo
    why: "eas login / eas init / eas build require a free Expo account (none exists yet)"
    dashboard_config:
      - task: "Create a free account at expo.dev/signup"
        location: "https://expo.dev/signup"
  - service: firebase
    why: "Real Android push delivery needs FCM V1 credentials (Google deprecated legacy FCM)"
    dashboard_config:
      - task: "Create a free Firebase project, add an Android app (package com.motokey.app), download google-services.json, generate an FCM V1 service-account key and upload it to EAS credentials"
        location: "https://console.firebase.google.com"
  - service: supabase
    why: "Cron reads/writes the migration-18 tier columns"
    dashboard_config:
      - task: "Run sql/migrations/18_motos_maintenance_alert_state.sql"
        location: "Supabase Dashboard (rzbqbaccjyxvtlnfitrr) > SQL Editor"
  - service: railway
    why: "Cron endpoint authenticates against CRON_SECRET"
    env_vars:
      - name: CRON_SECRET
        source: "Set the same secret value here as in the GitHub Actions secret (Railway Dashboard env vars)"
  - service: github
    why: "Scheduled workflow needs the secret to call the endpoint"
    dashboard_config:
      - task: "Add CRON_SECRET repository Actions secret (same value as Railway)"
        location: "GitHub repo > Settings > Secrets and variables > Actions"

must_haves:
  truths:
    - "A real Android EAS development build installs on a device and registers a real push token (closes the Phase 13/16 SC-1 deferral)"
    - "POST /cron/maintenance-alerts is 401 without the header and 200 with the correct X-Cron-Secret against Railway prod"
    - "A seeded over-threshold moto produces a visible maintenance push on the device; tapping it opens that moto's Fiche Moto"
    - "The generated iOS privacy manifest matches the app's real API usage (MSTORE-01)"
    - "MSTORE-02 (TestFlight / Play internal track submission) is explicitly recorded as parked, not attempted"
  artifacts:
    - path: "mobile-app/app.json"
      provides: "extra.eas.projectId (from eas init) + android.googleServicesFile reference"
      contains: "projectId"
  key_links:
    - from: "GitHub Actions secret + Railway CRON_SECRET"
      to: "POST /cron/maintenance-alerts"
      via: "matching X-Cron-Secret header"
      pattern: "CRON_SECRET"
---

<objective>
Turn the Wave 1 code into a proven live loop: produce a real Android EAS development build (closing the long-deferred SC-1 real-device push), enable the cron in prod (migration + secret + deploy), then verify end-to-end that an over-threshold moto pushes a maintenance reminder that deep-links to its Fiche Moto — and inspect the generated Apple privacy manifest for MSTORE-01. MSTORE-02's actual store submission stays parked per D-02.

Purpose: This is the phase's proof gate. Everything before it is code; this is where MPUSH-04 becomes observable on a device and MSTORE-01 is validated against a real build.
Output: A populated `extra.eas.projectId`, a committed `google-services.json` + `android.googleServicesFile` reference, an installed dev build, and a confirmed E2E run.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-maintenance-alert-cron-app-store-submission/17-CONTEXT.md
@.planning/phases/17-maintenance-alert-cron-app-store-submission/17-RESEARCH.md
@.planning/phases/17-maintenance-alert-cron-app-store-submission/17-VALIDATION.md
@.planning/phases/16-push-wiring-end-to-end/16-04-SUMMARY.md
@mobile-app/AGENTS.md

<interfaces>
<!-- What Wave 1 produced, that this plan wires live. -->
- Plan 01: services/maintenanceAlertService.js (runMaintenanceAlertCron), POST /cron/maintenance-alerts (X-Cron-Secret), sql/migrations/18_motos_maintenance_alert_state.sql, .github/workflows/maintenance-alerts.yml, scripts/seed-test-maintenance-cron.js, scripts/test-maintenance-cron.js
- Plan 02: mapNotificationDataToRoute now routes { type:'moto_entretien', motoId } → Fiche Moto
- Plan 03: app.json has com.motokey.app identifiers + ios.privacyManifests; eas.json has an installable Android `development` profile; expo-dev-client installed; store-content/*.md written

Prod URL: https://motokey11-production.up.railway.app  (NEVER motokey-api-10)
Test fixtures: scripts/seed-test-maintenance-cron.js seeds VIN MAINT-CRON-WARN-0001 (warning) + MAINT-CRON-URG-0001 (urgent), client-owned by test@motokey.fr.

Research constraints for this plan:
- Default the one dev build to `--platform android` (Pitfall 3 — iOS real-device build is blocked on the not-yet-created paid Apple account).
- Real Android delivery needs the Firebase/FCM V1 credentials (Pitfall 2 — otherwise tickets succeed but no banner appears).
- Do NOT run `eas submit` — MSTORE-02 is parked (Pitfall 4).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Expo account + eas login/init</name>
  <files>mobile-app/app.json</files>
  <read_first>
    - mobile-app/AGENTS.md (read the versioned SDK 54 EAS docs before running commands)
    - 17-RESEARCH.md "Standard Stack" (eas-cli install), "Environment Availability" (eas-cli + Expo account both missing → free setup), "Open Questions #2" (Firebase/FCM V1 is a SEPARATE checkpoint from eas login/init — that is Task 2, not this task)
    - mobile-app/app.json (projectId currently absent — eas init will add extra.eas.projectId)
  </read_first>
  <action>
    This checkpoint covers ONLY the Expo-console account flow (Firebase is deliberately isolated into Task 2 — different external console, easier failure isolation per RESEARCH Open Questions #2). The executor may first AUTOMATE the non-gated part: `npm install --global eas-cli` (or use `npx eas-cli`) and confirm with `eas --version`. Everything below is account/browser-gated — present it as a human runbook, then pause for the resume signal. All commands run from `mobile-app/`:
    1. Create a free Expo account at https://expo.dev/signup (Mehdi has none yet).
    2. `eas login` (interactive browser/credentials).
    3. `eas init` — accept creating the project; it writes `extra.eas.projectId` into `mobile-app/app.json`.
    Do NOT set up Firebase or run `eas build` here — those are Task 2.
  </action>
  <verify>
    <automated>cd mobile-app && node -e "const j=require('./app.json'); if(!(j.expo.extra&&j.expo.extra.eas&&j.expo.extra.eas.projectId))process.exit(1); console.log('expo-init ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints "expo-init ok" (extra.eas.projectId now present in app.json)
    - grep `projectId` mobile-app/app.json returns a match
    - The Expo account exists and `eas login` succeeded (executor confirmed via resume signal)
  </acceptance_criteria>
  <done>A free Expo account exists, `eas login` succeeded, and `eas init` populated `extra.eas.projectId` in mobile-app/app.json.</done>
  <resume-signal>Type "logged-in" with the eas projectId that eas init wrote, or describe where the Expo signup / login / init failed.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Firebase project + FCM V1 credentials + Android dev build</name>
  <files>mobile-app/app.json, mobile-app/google-services.json</files>
  <read_first>
    - mobile-app/AGENTS.md (versioned SDK 54 EAS/FCM docs before running commands)
    - 17-RESEARCH.md "Common Pitfalls #2" (FCM V1 credential gap silently swallows Android push), "#3" (keep the build --platform android — iOS real-device build is blocked on the paid Apple account), "State of the Art" (FCM legacy → V1 migration), "Environment Availability" (Firebase project + FCM V1 service account missing → free setup)
    - mobile-app/app.json (extra.eas.projectId now present from Task 1; the `android` object needs `googleServicesFile` added once google-services.json exists)
  </read_first>
  <action>
    This checkpoint is the Firebase-console + build flow, kept separate from Task 1's Expo flow so a silent Pitfall-2 FCM credential gap is not conflated with an EAS build issue. Requires Task 1's projectId to already be in app.json. All commands run from `mobile-app/`:
    1. Create a free Firebase project at https://console.firebase.google.com, add an Android app with package `com.motokey.app`, and download `google-services.json` into `mobile-app/`.
    2. Generate an FCM V1 service-account private key and upload it in EAS (Expo dashboard → project → Credentials → Android → FCM V1, or `eas credentials`). Pitfall 2: without this the push silently never arrives even though tickets succeed.
    3. Add `"googleServicesFile": "./google-services.json"` inside the `android` object in `app.json` (executor can make this edit once the file is present).
    4. Build: `eas build --profile development --platform android` (Pitfall 3: keep it Android — an iOS real-device build needs the paid Apple account that does not exist).
    5. Install the resulting dev build on a real Android device and open it once; it should register a real Expo push token (the Compte tab retry entry point from Phase 16 can confirm registration).
    Do NOT run `eas submit` (MSTORE-02 parked).
  </action>
  <verify>
    <automated>cd mobile-app && node -e "const j=require('./app.json'); if(j.expo.android.googleServicesFile!=='./google-services.json')process.exit(1); require('fs').accessSync('./google-services.json'); console.log('fcm-build wiring ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints "fcm-build wiring ok" (googleServicesFile referenced in app.json.android AND google-services.json exists on disk)
    - `mobile-app/google-services.json` exists and the FCM V1 service-account key was uploaded to EAS (confirmed via resume signal)
    - The EAS Android development build completed (build URL reported) and launched + registered a real push token on a real device
  </acceptance_criteria>
  <done>An Android EAS dev build (with FCM V1 credentials wired) exists and runs on-device with a real push token; app.json carries the google-services.json reference.</done>
  <resume-signal>Type "built" with the EAS build URL (and confirm FCM V1 key uploaded), or describe where it failed (e.g. build signing vs. silent FCM delivery — Pitfall 2 vs 3).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Enable the cron live (migration + secret + deploy)</name>
  <files>sql/migrations/18_motos_maintenance_alert_state.sql, .github/workflows/maintenance-alerts.yml</files>
  <read_first>
    - sql/migrations/18_motos_maintenance_alert_state.sql (the SQL to run)
    - motokey-api.js POST /cron/maintenance-alerts route (confirm it reads process.env.CRON_SECRET)
    - .github/workflows/maintenance-alerts.yml (confirm it references secrets.CRON_SECRET + the motokey11 prod URL)
    - CLAUDE.md (secrets discipline — never paste the secret value in a command; use the dashboards)
  </read_first>
  <action>
    The cron code, migration file, and workflow (Plan 01) are inert until applied/deployed. The executor may prepare/confirm the git push of the already-committed Wave 1 backend code; the rest are human dashboard actions. Human runbook:
    1. In Supabase Dashboard (project rzbqbaccjyxvtlnfitrr) → SQL Editor, run `sql/migrations/18_motos_maintenance_alert_state.sql`. Confirm the two columns now exist on `motos`.
    2. Generate a strong random secret. Add it as env var `CRON_SECRET` in the Railway Dashboard for service motokey1.1 (never paste it into a shell command — CLAUDE.md secrets rule).
    3. Add the same value as repository Actions secret `CRON_SECRET` (GitHub → Settings → Secrets and variables → Actions).
    4. Ensure the Wave 1 backend commits are pushed to `origin/master` so Railway redeploys with the cron endpoint (Phase 16 hit a bug where unpushed commits meant prod served stale code — confirm the deploy picks up POST /cron/maintenance-alerts).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" -X POST https://motokey11-production.up.railway.app/cron/maintenance-alerts</automated>
  </verify>
  <acceptance_criteria>
    - The curl above returns 401 (endpoint deployed and rejecting header-less calls — proves the code is live, not a 404)
    - Migration 18 applied: `motos.last_maintenance_tier_notified` and `last_maintenance_tier_notified_at` columns exist in prod (confirmed via Supabase Dashboard)
    - CRON_SECRET is set as a Railway env var AND a GitHub Actions repo secret (same value)
  </acceptance_criteria>
  <done>Cron endpoint is deployed live and secret-gated; migration 18 applied; the daily workflow has the secret it needs.</done>
  <resume-signal>Type "live" once migration + secret + deploy are done (and the curl returns 401 not 404), or describe the blocker.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-end MPUSH-04 + MSTORE-01 verification</name>
  <files>mobile-app/app.json</files>
  <read_first>
    - scripts/seed-test-maintenance-cron.js (fixtures + moto ids it prints)
    - scripts/test-maintenance-cron.js (local harness)
    - 17-VALIDATION.md "Manual-Only Verifications" + "Phase gate"
    - 17-CONTEXT.md D-02 (MSTORE-02 parked)
  </read_first>
  <action>
    With the dev build on-device (Task 2) and the cron live (Task 3), run the full loop and inspect the privacy manifest. Report each step's outcome:
    1. Seed: `node scripts/seed-test-maintenance-cron.js` (creates the WARNING + URGENT client-owned motos for test@motokey.fr, resets their notified tier to NULL). Note the printed moto ids.
    2. Auth-rejection: `curl -i -X POST https://motokey11-production.up.railway.app/cron/maintenance-alerts` (no header) → expect 401; wrong header value → 401.
    3. Successful run: `curl -X POST https://motokey11-production.up.railway.app/cron/maintenance-alerts -H "X-Cron-Secret: <secret>"` → 200, body reports scanned/notified counts (≥ 2 notified on first post-seed run).
    4. Idempotent re-run: immediately re-run the same authorized curl → 200 with 0 new notifications for the already-notified tiers (no spam).
    5. On-device: confirm the maintenance push banner(s) arrived on the Android dev build; tap one → confirm it opens the correct moto's Fiche Moto (Plan 02's moto_entretien deep link).
    6. MSTORE-01: inspect the iOS privacy manifest generated by the build (EAS artifacts, or `npx expo prebuild --platform ios` locally → `ios/**/PrivacyInfo.xcprivacy`). Reconcile the actual required-reason API entries against app.json's declarations from Plan 03; edit app.json if the generated manifest reveals missing/extra reason codes (codes were LOW-confidence in research).
    7. MSTORE-02: confirm it stays PARKED — do NOT run `eas submit`. Record as a known gap (Phase 8/BILL-06 pattern), blocked on Mehdi creating the paid Apple + Google accounts.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" -X POST https://motokey11-production.up.railway.app/cron/maintenance-alerts -H "X-Cron-Secret: wrong-value-should-401"</automated>
  </verify>
  <acceptance_criteria>
    - Header-less and wrong-header curl both return HTTP 401 (verify command above returns 401)
    - Authorized curl returns 200 and reports ≥ 1 notified on the first post-seed run
    - Immediate authorized re-run returns 200 with 0 additional notifications for unchanged tiers
    - A maintenance push banner was visible on the Android dev build device
    - Tapping the maintenance notification navigated to the correct moto's Fiche Moto
    - The generated PrivacyInfo.xcprivacy was inspected and app.json's privacyManifests reconciled against it (MSTORE-01)
    - SUMMARY records MSTORE-02 as parked with no `eas submit` attempted
  </acceptance_criteria>
  <done>MPUSH-04 proven on-device with no re-run spam, the deep link lands on the right moto, MSTORE-01 validated against the real manifest, and MSTORE-02 recorded as a parked known gap.</done>
  <resume-signal>Type "verified" with each step's result, or describe failures (e.g. no banner → check FCM V1 credentials per Pitfall 2).</resume-signal>
</task>

</tasks>

<verification>
- Cron auth: 401 without/with-wrong header, 200 with correct header, idempotent re-run (0 new) — against Railway prod.
- On-device: maintenance push received + tap → Fiche Moto navigation confirmed on the Android EAS dev build (closes SC-1 + MPUSH-04 SC-2).
- MSTORE-01: generated privacy manifest inspected and reconciled.
- MSTORE-02: parked, no submission attempted.
</verification>

<success_criteria>
- MPUSH-04 is fully proven on a real device with no spam on re-run.
- SC-1 (real device push token + delivery), deferred since Phase 13, is closed via the EAS dev build.
- MSTORE-01 validated against a real build; MSTORE-02 explicitly parked as a known gap.
</success_criteria>

<output>
After completion, create `.planning/phases/17-maintenance-alert-cron-app-store-submission/17-04-SUMMARY.md` (record the parked MSTORE-02 gap and the projectId/build URL).
</output>
