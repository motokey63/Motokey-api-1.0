---
phase: 16-push-wiring-end-to-end
plan: 04
type: execute
wave: 3
depends_on: ["16-01", "16-03"]
files_modified: []
autonomous: false
requirements: [MPUSH-01, MPUSH-02, MPUSH-03, MPUSH-05]

must_haves:
  truths:
    - "A curl-driven smoke test confirms the full backend chain works: seeded brouillon devis -> POST /devis/:id/envoyer -> statut becomes envoye -> a push send is attempted (console.log fallback visible if PUSH_ENABLED=false) -> a second call to PUT /devis/:id on the same devis is rejected with 400 INVALID_STATUS"
    - "The full mobile jest suite (Phase 14 through Phase 16-02/16-03) is green and tsc --noEmit is clean"
    - "A human confirms on-device: the soft-ask screen appears full-screen exactly once after login, the Compte tab retrigger works, and tapping a locally scheduled devis_recu test notification navigates to the Devis tab"
  artifacts: []
  key_links: []
---

<objective>
Close the loop on Phase 16: run the automated backend + mobile checks that are within this environment's reach, then hand off to Mehdi for the on-device human verification that automation cannot perform (visual soft-ask sequencing, OS-prompt timing, real tap navigation). Explicitly reconfirm and carry forward the scope boundary already agreed for this phase: real remote push delivery (MPUSH-02's actual token registration against a live Expo push token, and MPUSH-03's actual notification arriving on a device) requires a development build via EAS, which does not exist yet in this repo (no `eas.json`, no `projectId`, no logged-in Expo account) — Mehdi explicitly chose to defer EAS project setup to Phase 17 (2026-07-04 planning decision), mirroring the SC-1 deferral pattern already used across Phases 13→15. This plan's checkpoint does NOT block on that — it verifies everything that IS achievable today (all code, backend curl smoke test, and MPUSH-05's tap-to-navigate logic via a local test notification) and explicitly carries the rest forward as a known, already-agreed-to gap.

Purpose: prove the code built in 16-01/16-02/16-03 actually works together, within the verification boundary this phase's research and user decision already established.
Output: a green automated smoke pass + a completed human-verify checkpoint, with any real discrepancies surfaced for a possible gap-closure plan.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-push-wiring-end-to-end/16-CONTEXT.md
@.planning/phases/16-push-wiring-end-to-end/16-RESEARCH.md
@.planning/phases/16-push-wiring-end-to-end/16-VALIDATION.md
@.planning/phases/16-push-wiring-end-to-end/16-01-SUMMARY.md
@.planning/phases/16-push-wiring-end-to-end/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated backend curl smoke test + full mobile suite</name>
  <files>(none — verification only, no source files modified)</files>
  <read_first>
    - scripts/seed-test-devis-16-uat.js (Plan 16-01 — produces the brouillon devis id to use below)
    - scripts/test-push.js (Phase 13 harness — existing curl-adjacent convention for manual push verification)
  </read_first>
  <action>
    1. Seed the fixture (idempotent, safe to re-run):
       ```bash
       node scripts/seed-test-devis-16-uat.js
       ```
       Note the printed devis `id`.

    2. Authenticate as the garage test account and capture the access token:
       ```bash
       curl -s -X POST https://motokey11-production.up.railway.app/auth/login \
         -H "Content-Type: application/json" \
         -d '{"email":"garage@motokey.fr","password":"motokey2026"}'
       ```
       Extract `access_token` from the JSON response (may be top-level or nested under `data`/`session` per this repo's dual response-shape convention — inspect the raw output).

    3. Call the new endpoint against the seeded devis id (replace `<DEVIS_ID>` and `<TOKEN>`):
       ```bash
       curl -s -X POST https://motokey11-production.up.railway.app/devis/<DEVIS_ID>/envoyer \
         -H "Authorization: Bearer <TOKEN>"
       ```
       Expect `{"success":true,"data":{"devis":{...,"statut":"envoye",...}},"message":"Devis envoyé au client"}`.

    4. Confirm the statut lock (D-03) by attempting an edit on the now-`envoye` devis:
       ```bash
       curl -s -X PUT https://motokey11-production.up.railway.app/devis/<DEVIS_ID> \
         -H "Authorization: Bearer <TOKEN>" -H "Content-Type: application/json" \
         -d '{"entete":{},"lignes":[]}'
       ```
       Expect `{"success":false,"error":{"message":"Devis déjà envoyé — créez un nouveau devis pour modifier","code":"INVALID_STATUS"}}` with HTTP 400.

    5. Confirm idempotency (re-calling `/envoyer` on the now-`envoye` devis must NOT fire a second push):
       ```bash
       curl -s -X POST https://motokey11-production.up.railway.app/devis/<DEVIS_ID>/envoyer \
         -H "Authorization: Bearer <TOKEN>"
       ```
       Expect a `400`/`INVALID_STATUS` failure (the devis is no longer `brouillon`), confirming `sendPush` cannot be re-triggered for the same devis.

    6. Run the full mobile jest suite + typecheck:
       ```bash
       cd mobile-app && npx jest && npx tsc --noEmit
       ```

    7. Syntax-check the backend one more time (defense-in-depth, cheap):
       ```bash
       node --check motokey-api.js
       ```

    Record the actual curl response bodies from steps 3-5 in the SUMMARY (not just "it worked") — this is the load-bearing evidence for MPUSH-03's backend half.
  </action>
  <verify>
    <automated>node --check motokey-api.js && cd mobile-app && npx jest && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Step 3's curl response contains `"statut":"envoye"`
    - Step 4's curl response contains `"INVALID_STATUS"` and no line/entête change is reflected on re-fetch
    - Step 5's curl response contains `"INVALID_STATUS"` (proves no double-send path exists)
    - `cd mobile-app && npx jest` exits 0 (all suites, including 16-02/16-03's new tests)
    - `cd mobile-app && npx tsc --noEmit` exits 0
    - `node --check motokey-api.js` exits 0
  </acceptance_criteria>
  <done>Backend chain (statut transition + push-attempt + lock-on-send) proven live against production Supabase via curl; full mobile suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human E2E verification of the soft-ask flow, Compte retrigger, and MPUSH-05 tap-to-navigate</name>
  <action>Human runs the mobile app (Expo Go or current dev workflow) against the live API and confirms the 8 checks in how-to-verify below. No code changes; this task is a blocking acceptance gate.</action>
  <what-built>
    Everything achievable without a development build: (1) the backend "Envoyer au client" action + push trigger (proven by Task 1's curl smoke test), (2) the mobile soft-ask screen shown once after login with a Compte-tab retrigger, and (3) notification-tap-to-Devis-tab navigation, exercisable today via a `__DEV__`-only locally scheduled test notification (unaffected by Expo Go's SDK 53+ remote-push removal).

    Explicitly NOT built or verified in this phase (per the 2026-07-04 planning decision to defer EAS project setup to Phase 17, mirroring the Phase 13 SC-1 precedent): real Expo push token retrieval on a real device, and a real remote push notification actually arriving. Both require a development build (EAS Build), which requires an Expo account login and `eas init` — neither exists in this repo yet. `registerForPushAsync`/`retryRegistrationIfGranted` are written and unit-tested to fail silently and safely when no EAS `projectId` is configured (exactly today's state), so nothing will crash — the OS permission prompt and `POST /client/device-tokens` call simply won't produce a real token until Phase 17's EAS setup lands.
  </what-built>
  <how-to-verify>
    On your device, in Expo Go (or however you currently run the mobile app):
    1. If you've logged in before, log out first (Compte tab -> Se déconnecter) so the soft-ask flag can be re-tested from a clean state — OR clear the app's storage / reinstall if you want a true fresh-install pass.
    2. Log back in. Confirm: a full-screen, MotoKey-branded "Restez informé" screen appears BEFORE anything else (before any OS permission dialog, before the tabs shell).
    3. Tap "Activer les notifications". Confirm you land on the Motos tab afterward (the OS permission dialog may or may not appear depending on your device/Expo Go's current push support — either way, the app must not crash and must proceed to the tabs).
    4. Navigate to the Compte tab. Confirm a "Se déconnecter", an "Activer les notifications", and (in dev builds) a "Tester notification (dev)" button are all present.
    5. Tap "Activer les notifications" again from Compte. Confirm the soft-ask screen reappears (D-06 retrigger).
    6. Tap "Plus tard" this time. Confirm you land back on the Motos tab without any error.
    7. From Compte, tap "Tester notification (dev)". Wait ~2 seconds for the local notification banner to appear, then tap it. Confirm the app navigates to the Devis tab.
    8. Log out. Confirm no crash and you land back on the login screen (the push-token deregister call happens silently in the background regardless of whether a real token was ever registered).
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 steps behaved as described, or describe which step failed and what you observed instead.</resume-signal>
</task>

</tasks>

<verification>
Task 1's automated commands must all exit 0 before the checkpoint is presented. The checkpoint itself is the final phase gate — a "described issue" response should be triaged into a `/gsd:plan-phase 16 --gaps` follow-up rather than blocking phase completion outright, unless the issue indicates a genuine regression in existing (Phase 14/15) functionality.
</verification>

<success_criteria>
- Backend curl smoke test passes with recorded evidence (statut transition, lock-on-send, no double-send)
- Full mobile jest suite green, tsc clean
- Human confirms the 8-step on-device pass
- MPUSH-01, MPUSH-02 (code + backend proven, real-device token deferred), MPUSH-03 (code + backend proven, real-device delivery deferred), MPUSH-05 (fully proven via local test notification) all have a clear, honest verification status recorded
</success_criteria>

<output>
After completion, create `.planning/phases/16-push-wiring-end-to-end/16-04-SUMMARY.md`, explicitly recording: (a) the curl response bodies from Task 1, (b) the human checkpoint's outcome verbatim, (c) a restated carry-forward note that real on-device push token registration/delivery (MPUSH-02/03) remains deferred to Phase 17's EAS setup — this is not a new gap, it is the same scope boundary agreed during Phase 16 planning.
</output>
