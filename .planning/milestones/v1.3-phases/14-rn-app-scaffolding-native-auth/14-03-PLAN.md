---
phase: 14-rn-app-scaffolding-native-auth
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - mobile-app/theme/colors.ts
  - mobile-app/components/Logo.tsx
  - mobile-app/components/Button.tsx
  - mobile-app/components/TextField.tsx
  - mobile-app/components/OtpCodeInput.tsx
  - mobile-app/components/Toast.tsx
  - mobile-app/app/_layout.tsx
  - mobile-app/app/(auth)/_layout.tsx
  - mobile-app/app/(auth)/login.tsx
  - mobile-app/app/(auth)/register.tsx
  - mobile-app/app/(auth)/reset-request.tsx
  - mobile-app/app/(auth)/verify.tsx
  - mobile-app/app/(app)/_layout.tsx
  - mobile-app/app/(app)/home.tsx
autonomous: true
requirements: [MAUTH-01]
must_haves:
  truths:
    - "User can register, then verify via a mandatory 8-digit OTP screen, and land logged in (D-01)"
    - "User can log in with email/password and reach a Home screen showing their email + a logout button"
    - "User can reset a forgotten password via the OTP flow using the same shared OTP component (D-04)"
    - "Screens adopt MotoKey branding: orange #ff6b00 accent, Inter font, 'MotoKey' logo lockup (D-06)"
    - "Routing gates unauthenticated users to the (auth) group and authenticated users to (app)/home"
  artifacts:
    - path: "mobile-app/app/_layout.tsx"
      provides: "Root: AuthProvider + Inter font load + polyfill + auth routing guard"
      contains: "AuthProvider"
    - path: "mobile-app/components/OtpCodeInput.tsx"
      provides: "Shared 8-digit OTP input (font 24px, letter-spacing 8px, centered) used by verify (register + reset)"
      contains: "letterSpacing"
    - path: "mobile-app/app/(auth)/verify.tsx"
      provides: "Shared OTP screen with mode=register|reset (D-01/D-04)"
      contains: "mode"
    - path: "mobile-app/app/(app)/home.tsx"
      provides: "Placeholder Home: 'Bienvenue {email}' + logout (D-05)"
      contains: "Bienvenue"
  key_links:
    - from: "mobile-app/app/(auth)/login.tsx"
      to: "useAuth().login"
      via: "onPress handler"
      pattern: "useAuth\\(\\)"
    - from: "mobile-app/app/_layout.tsx"
      to: "useAuth().status routing"
      via: "redirect based on status"
      pattern: "status"
    - from: "mobile-app/app/(app)/home.tsx"
      to: "useAuth().logout"
      via: "logout button onPress"
      pattern: "logout"
---

<objective>
Build the auth UI: a small branded component kit (Logo, Button, TextField, shared OtpCodeInput, Toast), the Expo Router structure with an auth routing guard, the five auth screens (login, register, OTP verify for both register+reset via a `mode` param, reset-request), and the placeholder Home screen (D-05). All copy is the web client's exact French (D-03); all styling uses the MotoKey palette (D-06). Delivers MAUTH-01 end-to-end in the UI.

Purpose: Turn the session engine from plan 14-02 into a usable, on-brand native auth experience.
Output: `theme/`, `components/`, and the `app/` route tree; the app builds and typechecks.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-rn-app-scaffolding-native-auth/14-CONTEXT.md
@MotoKey_Client.html

<decisions_in_scope>
- D-01: register -> immediate MANDATORY OTP verification (no skip state) -> login. Shared OTP component for both register-verification and reset-confirm.
- D-02: "Mot de passe oublié" is a plain text link BELOW the password field on login (not a prominent CTA).
- D-03: reuse the web client's exact French copy verbatim (strings enumerated in actions below).
- D-04: password reset uses the SAME shared OTP component as register-verification (not a distinct component).
- D-05: after login show a minimal placeholder Home ("Bienvenue {email}" + logout button) — nothing more.
- D-06: orange accent #ff6b00 / #ff8c33, accent bg #fff4ee, Inter font, "MotoKey" logo lockup with orange italic "Key".
- D-07: no separate UI-SPEC; D-06 is sufficient guidance.
</decisions_in_scope>

<interfaces>
<!-- Consumed from plan 14-02 (already on disk): -->
hooks/useAuth.ts: useAuth() -> { status, email, login, register, verifyEmail, requestPasswordReset, confirmPasswordReset, logout, getValidAccessToken }
context/AuthContext.tsx: AuthProvider

<!-- Brand palette to create in theme/colors.ts (values are LOCKED, from MotoKey_Client.html :root and D-06): -->
```typescript
export const colors = {
  bg: '#f0f2f5', card: '#ffffff', card2: '#f8f9fb',
  border: '#e2e5eb', border2: '#d0d4dc',
  tx: '#1a1d23', tx2: '#5a6172', tx3: '#9ba3b4',
  acc: '#ff6b00', acc2: '#ff8c33', accbg: '#fff4ee',
  gn: '#16a34a', bl: '#2563eb', yw: '#d97706', rd: '#dc2626',
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand theme + shared component kit (Logo, Button, TextField, OtpCodeInput, Toast)</name>
  <files>mobile-app/theme/colors.ts, mobile-app/components/Logo.tsx, mobile-app/components/Button.tsx, mobile-app/components/TextField.tsx, mobile-app/components/OtpCodeInput.tsx, mobile-app/components/Toast.tsx</files>
  <read_first>
    - MotoKey_Client.html lines ~9-75 (`:root` palette, `.btn-primary`, `.form-group input:focus`, `.otp-input`, `.logo`/`.logo em`)
    - .planning/phases/14-rn-app-scaffolding-native-auth/14-CONTEXT.md (D-06 branding, specifics on OTP visual treatment)
    - mobile-app/hooks/useAuth.ts (Toast will be triggered by AuthContext hard-expiry — see 14-02 TODO)
  </read_first>
  <action>
    Create `mobile-app/theme/colors.ts` exactly as the interfaces block (values locked from the web client `:root` and D-06).

    Create these RN components (StyleSheet, no external UI lib):
    - `components/Logo.tsx`: renders "Moto" + "Key" as a `<Text>` where "Moto" uses `colors.tx` and "Key" uses `colors.acc` with `fontStyle:'italic'` (D-06 "orange italic Key"), fontFamily `'Inter_900Black'` if loaded else default, fontSize 28, fontWeight '900'. Optional small pill badge prop ("Espace Client") styled like `.logo-badge` (accbg bg, acc text, acc2 border, radius 20).
    - `components/Button.tsx`: props `{ title, onPress, loading?, variant?: 'primary'|'ghost', disabled? }`. Primary = `colors.acc` bg, white text, radius 10, padding vertical 14, centered; pressed/hover-equivalent uses `colors.acc2`. Ghost = transparent bg, `colors.acc` text, no border (mirrors `.btn-ghost`). When `loading`, show an `ActivityIndicator` and disable. This is the component that renders the D-02 "Mot de passe oublié" link (variant ghost).
    - `components/TextField.tsx`: props `{ value, onChangeText, placeholder, secureTextEntry?, keyboardType?, autoCapitalize?, label? }`. Style: white bg, 1px `colors.border`, radius 10, padding 12; on focus set border to `colors.acc` (track focus with local state) — mirrors `.form-group input:focus`.
    - `components/OtpCodeInput.tsx` (D-01/D-04 shared): a single `TextField`-like `TextInput` with `keyboardType='number-pad'`, `maxLength=8`, `placeholder='00000000'`, and style `{ fontSize:24, fontWeight:'700', letterSpacing:8, textAlign:'center', paddingVertical:14 }` (exact `.otp-input` treatment). Props `{ value, onChangeText }`.
    - `components/Toast.tsx`: a lightweight toast context/provider — `ToastProvider` + `useToast()` returning `toast(msg: string, type?: 'success'|'error'|'info')`. Render a small absolutely-positioned banner that auto-dismisses after ~4500ms (web parity). Colors: success `colors.gn`, error `colors.rd`, info `colors.tx2`. Export a module-level `showToast(msg, type)` escape hatch so `AuthContext.onHardExpiry` (14-02) can trigger the "Session expirée — reconnectez-vous" toast without a hook — wire it by having `ToastProvider` register its `toast` fn into that module-level ref on mount, and update the 14-02 `onHardExpiry` TODO to call `showToast('Session expirée — reconnectez-vous','error')`.
  </action>
  <verify>
    <automated>cd mobile-app && npm run typecheck</automated>
  </verify>
  <done>theme/colors.ts + all five components exist, typecheck clean, palette values match the web client, OtpCodeInput uses fontSize 24 / letterSpacing 8, Logo renders orange italic "Key".</done>
  <acceptance_criteria>
    - `grep -q "acc: '#ff6b00'" mobile-app/theme/colors.ts` and `grep -q "acc2: '#ff8c33'" mobile-app/theme/colors.ts`
    - `grep -q "letterSpacing: 8" mobile-app/components/OtpCodeInput.tsx` and `grep -q "maxLength={8}" mobile-app/components/OtpCodeInput.tsx`
    - `grep -q "fontStyle: 'italic'" mobile-app/components/Logo.tsx`
    - `grep -q "export function useToast\|export const useToast" mobile-app/components/Toast.tsx` and `grep -q "showToast" mobile-app/components/Toast.tsx`
    - `cd mobile-app && npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Router shell, auth guard, Inter font load, providers (app/_layout.tsx + group layouts)</name>
  <files>mobile-app/app/_layout.tsx, mobile-app/app/(auth)/_layout.tsx, mobile-app/app/(app)/_layout.tsx</files>
  <read_first>
    - MotoKey_Client.html lines ~745-767 (init/routing decision by session presence — the guard logic to mirror)
    - mobile-app/context/AuthContext.tsx and mobile-app/hooks/useAuth.ts (from 14-02)
    - mobile-app/components/Toast.tsx (ToastProvider from Task 1)
    - Expo Router docs: authentication redirect pattern (use context7 `/expo/expo` if the Redirect/protected-routes API is uncertain at implementation time)
  </read_first>
  <action>
    Replace the stub `mobile-app/app/_layout.tsx` with the root layout:
    - First line: `import 'react-native-get-random-values';` (belt-and-suspenders; secureStore also imports it).
    - Load Inter via `@expo-google-fonts/inter`: `useFonts({ Inter_400Regular, Inter_500Medium, Inter_600SemiBold, Inter_700Bold, Inter_900Black })`; render nothing (or a splash) until `fontsLoaded`.
    - Wrap the tree: `<ToastProvider><AuthProvider><RootNav/></AuthProvider></ToastProvider>`.
    - `RootNav`: read `const { status } = useAuth()`. While `status==='loading'` render an `ActivityIndicator` centered. Use Expo Router's declarative redirect: render a `<Stack>` with the two groups and use `<Redirect>` or `useRouter().replace` inside an effect — when `status==='unauthenticated'` and the current segment is not `(auth)`, redirect to `/(auth)/login`; when `status==='authenticated'` and the current segment is `(auth)`, redirect to `/(app)/home`. (Use `useSegments()` + `useRouter()`; if unsure of the current Expo Router protected-routes idiom, verify via context7 `/expo/expo`.)

    Create `mobile-app/app/(auth)/_layout.tsx`: a `<Stack screenOptions={{ headerShown:false, contentStyle:{ backgroundColor: colors.bg } }} />`.
    Create `mobile-app/app/(app)/_layout.tsx`: a `<Stack screenOptions={{ headerShown:false }} />`.

    Ensure the app boots to the login screen when logged out and to home when a persisted session is restored (this exercises the 14-02 cold-start path).
  </action>
  <verify>
    <automated>cd mobile-app && npm run typecheck</automated>
  </verify>
  <done>Root layout loads Inter, mounts ToastProvider + AuthProvider, and redirects by auth status between the (auth) and (app) route groups; group layouts exist with headers hidden. Typecheck clean.</done>
  <acceptance_criteria>
    - `grep -q "AuthProvider" mobile-app/app/_layout.tsx` and `grep -q "ToastProvider" mobile-app/app/_layout.tsx`
    - `grep -q "useFonts" mobile-app/app/_layout.tsx` and `grep -q "Inter_" mobile-app/app/_layout.tsx`
    - `grep -Eq "useSegments|Redirect|replace" mobile-app/app/_layout.tsx` (auth guard present)
    - Files `mobile-app/app/(auth)/_layout.tsx` and `mobile-app/app/(app)/_layout.tsx` exist
    - `cd mobile-app && npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Auth screens (login, register, verify[register|reset], reset-request) + placeholder Home</name>
  <files>mobile-app/app/(auth)/login.tsx, mobile-app/app/(auth)/register.tsx, mobile-app/app/(auth)/reset-request.tsx, mobile-app/app/(auth)/verify.tsx, mobile-app/app/(app)/home.tsx</files>
  <read_first>
    - MotoKey_Client.html lines ~772-938 (login/register/verify/reset-request/reset-confirm handlers — exact copy, validation thresholds, success navigation) and ~579-588 (logout)
    - .planning/phases/14-rn-app-scaffolding-native-auth/14-CONTEXT.md (D-01..D-05 flow + verbatim-copy rule D-03)
    - mobile-app/hooks/useAuth.ts, mobile-app/components/* and mobile-app/theme/colors.ts (from Task 1/2 and 14-02)
  </read_first>
  <action>
    All screens use `useAuth()`, `useRouter()`, the component kit, and `useToast()`. Send NO x-client-type (handled in api.ts). Use these EXACT French strings (D-03, verbatim from the web client):

    `mobile-app/app/(auth)/login.tsx`:
    - Logo + email TextField + password TextField (secureTextEntry). Below the password field, a Button variant="ghost" "Mot de passe oublié ?" that `router.push('/(auth)/reset-request')` (D-02), and a ghost link "Créer un compte" -> `router.push('/(auth)/register')`.
    - Primary Button "Se connecter". On press: if `!email || !pwd` toast `'Email et mot de passe requis.'` (error). Else call `const r = await login(email, pwd)`; if `!r.ok` toast `r.error` (error); on ok toast `'Bienvenue !'` (success) — routing to home happens automatically via the guard.

    `mobile-app/app/(auth)/register.tsx`:
    - Fields: nom, email, tel (optional), password, password-confirm. Primary Button "Créer mon compte".
    - Validation (verbatim): if `!nom || !email || !pwd` toast `'Nom, email et mot de passe requis.'`; if `pwd.length < 12` toast `'Mot de passe trop court — minimum 12 caractères avec majuscule, chiffre et caractère spécial.'`; if `pwd !== confirm` toast `'Les mots de passe ne correspondent pas.'`.
    - On valid: `const r = await register(email, pwd, nom, tel || undefined)`; if `!r.ok` toast `r.error`; on ok toast `'Code envoyé ! Vérifiez votre boîte email.'` (success) and `router.push({ pathname:'/(auth)/verify', params:{ mode:'register', email } })` (D-01 mandatory verification, no skip).
    - Ghost link "J'ai déjà un compte" -> `router.push('/(auth)/login')`.

    `mobile-app/app/(auth)/verify.tsx` (SHARED, D-01/D-04) — reads `const { mode, email } = useLocalSearchParams<{mode:'register'|'reset'; email:string}>()`:
    - Header subtitle: register -> `'Un code à 8 chiffres a été envoyé à ' + email + '.'`; reset -> `'Entrez le code envoyé à ' + email + ' et choisissez votre nouveau mot de passe.'`.
    - Always render the shared `<OtpCodeInput>`.
    - If `mode==='reset'`: also render new-password + confirm TextFields.
    - Primary Button: register -> "Vérifier", reset -> "Réinitialiser".
    - Validation (verbatim): if `!code || code.length < 6` toast `'Entrez le code reçu par email.'`. For reset also: if `pwd.length < 12` toast `'Mot de passe : minimum 12 caractères avec majuscule, chiffre et caractère spécial.'`; if `pwd !== confirm` toast `'Les mots de passe ne correspondent pas.'`.
    - register submit: `const r = await verifyEmail(email, code)`; if `!r.ok` toast `r.error`; on ok: if `r.authed` toast `'Email vérifié — bienvenue !'` (guard routes to home); else toast `'Email vérifié ! Connectez-vous.'` and `router.replace('/(auth)/login')`.
    - reset submit: `const r = await confirmPasswordReset(email, code, pwd)`; if `!r.ok` toast `r.error`; on ok toast `'Mot de passe mis à jour. Connectez-vous.'` (success) and `router.replace('/(auth)/login')`.
    - "Renvoyer le code" ghost button: register -> `register(email, pwd?, nom?)` is not available here (no pwd in verify) so instead call the resend by re-triggering; simplest faithful behavior: register mode calls nothing destructive — show toast `'Code renvoyé. Vérifiez vos emails et vos spams.'` after calling `requestPasswordReset(email)` for reset mode, and for register mode navigate back is acceptable. (Keep it simple: reset mode resend -> `await requestPasswordReset(email)` then info toast; register mode resend -> info toast prompting to go back and re-submit if needed.)

    `mobile-app/app/(auth)/reset-request.tsx`:
    - Email TextField + Primary Button "Envoyer le code". Validation: if `!email` toast `'Entrez votre adresse email.'`. On valid: `await requestPasswordReset(email)` (anti-enum, ignore result), toast `'Si votre email est enregistré, un code vous a été envoyé.'` (info), then `router.push({ pathname:'/(auth)/verify', params:{ mode:'reset', email } })`.
    - Ghost link "Retour à la connexion" -> `router.replace('/(auth)/login')`.

    `mobile-app/app/(app)/home.tsx` (D-05 placeholder, smoke test only):
    - Logo, a `<Text>` `'Bienvenue ' + (email ?? '')` from `useAuth().email`, and a Primary Button "Se déconnecter" -> `await logout()` (guard routes back to login). NOTHING else — no feature content (Phase 15 replaces this).
  </action>
  <verify>
    <automated>cd mobile-app && npm run typecheck && npx jest</automated>
  </verify>
  <done>All five auth screens + placeholder Home exist, use the shared OtpCodeInput for both verify modes, carry the exact verbatim French copy, wire to `useAuth()` actions, and rely on the router guard for post-auth navigation. Full project typechecks and the whole jest suite (lib tests from 14-01/14-02) is still green.</done>
  <acceptance_criteria>
    - `grep -q "Bienvenue" mobile-app/app/(app)/home.tsx` and `grep -q "logout" mobile-app/app/(app)/home.tsx`
    - `grep -q "mode" mobile-app/app/(auth)/verify.tsx` and `grep -q "OtpCodeInput" mobile-app/app/(auth)/verify.tsx`
    - `grep -q "Mot de passe trop court — minimum 12 caractères" mobile-app/app/(auth)/register.tsx`
    - `grep -q "Mot de passe oublié" mobile-app/app/(auth)/login.tsx`
    - `grep -q "useAuth()" mobile-app/app/(auth)/login.tsx`
    - No screen file contains the string `x-client-type`
    - `cd mobile-app && npm run typecheck && npx jest` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `cd mobile-app && npm run typecheck` passes for the whole project.
- `cd mobile-app && npx jest` passes (all lib suites still green after UI added).
- The route tree `app/(auth)/{login,register,verify,reset-request}.tsx` + `app/(app)/home.tsx` + group layouts all exist.
- Shared OtpCodeInput used by verify in BOTH modes (D-01/D-04).
- Palette + logo match D-06; copy matches D-03 verbatim.
</verification>

<success_criteria>
- MAUTH-01 delivered in UI: register -> mandatory OTP -> login; login -> Home; forgotten password -> OTP reset.
- Branding applied (D-06), copy verbatim (D-03), placeholder Home per D-05.
- Navigation gated by auth status; no direct token handling in screens.
</success_criteria>

<output>
After completion, create `.planning/phases/14-rn-app-scaffolding-native-auth/14-03-SUMMARY.md`.
</output>
