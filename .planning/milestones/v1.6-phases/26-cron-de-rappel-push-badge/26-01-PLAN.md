---
phase: 26-cron-de-rappel-push-badge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/migrations/24_consommables_rappel_state.sql
  - schema.sql
  - tests/test-consommable-rappel-cron.js
autonomous: true
requirements: [GAUGE-03, GAUGE-04]
must_haves:
  truths:
    - "Les colonnes consommables.dernier_rappel_envoye_at, consommables.dernier_rappel_km et photos_consommables.km_a_la_photo existent dans la migration 24 ET dans schema.sql (parité)"
    - "Le squelette de test tourne et sort avec le code 0 même sans aucune assertion remplie"
  artifacts:
    - path: "sql/migrations/24_consommables_rappel_state.sql"
      provides: "3 colonnes d'état de rappel + km à la photo (D-04, D-07)"
      contains: "ADD COLUMN dernier_rappel_envoye_at"
    - path: "schema.sql"
      provides: "Parité bootstrap avec migration 24"
      contains: "dernier_rappel_envoye_at"
    - path: "tests/test-consommable-rappel-cron.js"
      provides: "Squelette de test Wave 0 (skeleton-then-fill)"
      contains: "process.exit"
  key_links:
    - from: "sql/migrations/24_consommables_rappel_state.sql"
      to: "schema.sql"
      via: "même DDL hand-appended au même commit"
      pattern: "dernier_rappel_envoye_at"
---

<objective>
Poser les fondations DB et test de la Phase 26 : la migration 24 qui ajoute les 3 colonnes d'état de rappel (D-04, D-07), la mise à jour de parité dans `schema.sql`, et le squelette de test `tests/test-consommable-rappel-cron.js` (Wave 0, exit 0) que les plans 26-02/26-03 rempliront.

Purpose: Aucun code de service/endpoint ne peut fonctionner sans ces colonnes ; discipline v1.6 (migration + schema.sql au même commit) imposée dès le premier plan.
Output: 1 fichier migration, `schema.sql` mis à jour, 1 fichier de test squelette.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-cron-de-rappel-push-badge/26-CONTEXT.md
@.planning/phases/26-cron-de-rappel-push-badge/26-RESEARCH.md
@.planning/phases/26-cron-de-rappel-push-badge/26-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 24 (colonnes rappel + km_a_la_photo) + parité schema.sql</name>
  <read_first>
    - sql/migrations/23_consommables_km.sql (schéma actuel des 2 tables, dernier fichier de migration existant)
    - sql/migrations/18_motos_maintenance_alert_state.sql (pattern de migration légère d'ajout de colonnes d'état de notification — modèle à suivre)
    - schema.sql lignes 559-584 (définitions inline CREATE TABLE consommables + photos_consommables où insérer les colonnes)
  </read_first>
  <action>
    Créer `sql/migrations/24_consommables_rappel_state.sql` avec EXACTEMENT ce DDL (le numéro 24 est confirmé libre — dernier fichier = 23) :

    ```sql
    -- Migration 24 : Colonnes consommables/photos_consommables — état "dernier rappel envoyé"
    -- + référence km à la photo (GAUGE-03/04, D-04/D-07)
    -- À appliquer manuellement via Supabase Dashboard > SQL Editor

    ALTER TABLE consommables
      ADD COLUMN dernier_rappel_envoye_at TIMESTAMPTZ,
      ADD COLUMN dernier_rappel_km        INTEGER;

    ALTER TABLE photos_consommables
      ADD COLUMN km_a_la_photo INTEGER;

    COMMENT ON COLUMN consommables.dernier_rappel_envoye_at IS
      'Horodatage du dernier rappel photo envoye pour ce consommable (NULL = jamais notifie ou rearme depuis la derniere photo). Mis a jour par /cron/rappels-photo-consommables ; remis a NULL par PhotosConsommables.insert() (D-05).';
    COMMENT ON COLUMN consommables.dernier_rappel_km IS
      'motos.km au moment du dernier rappel envoye pour ce consommable — informatif, rearme avec dernier_rappel_envoye_at.';
    COMMENT ON COLUMN photos_consommables.km_a_la_photo IS
      'motos.km au moment de l upload de cette photo — reference de depart pour le calcul km parcouru depuis la derniere photo (D-07).';
    ```

    Puis, dans `schema.sql`, ajouter les mêmes colonnes DIRECTEMENT dans les définitions inline CREATE TABLE (discipline v1.6 : schema.sql doit refléter la migration au même commit) :
    - Dans `CREATE TABLE consommables (...)` (commence ligne 559) : après la ligne `reference         TEXT,` (ligne 569) et avant `created_at`, insérer :
      `  dernier_rappel_envoye_at TIMESTAMPTZ,`
      `  dernier_rappel_km        INTEGER,`
    - Dans `CREATE TABLE photos_consommables (...)` (commence ligne 575) : après la ligne `analyse_status    TEXT,` (ligne 582) et avant `created_at`, insérer :
      `  km_a_la_photo     INTEGER,`

    NE PAS toucher aux triggers km, aux CHECK, ni aux autres tables. Édition directe native (jamais PowerShell/sed).
  </action>
  <verify>
    <automated>grep -c "dernier_rappel_envoye_at" C:/motokey-api/sql/migrations/24_consommables_rappel_state.sql && grep -c "dernier_rappel_envoye_at" C:/motokey-api/schema.sql && grep -c "km_a_la_photo" C:/motokey-api/schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `sql/migrations/24_consommables_rappel_state.sql` existe et contient `ADD COLUMN dernier_rappel_envoye_at`, `ADD COLUMN dernier_rappel_km`, `ADD COLUMN km_a_la_photo`
    - `schema.sql` contient `dernier_rappel_envoye_at` ET `dernier_rappel_km` dans le bloc `CREATE TABLE consommables`
    - `schema.sql` contient `km_a_la_photo` dans le bloc `CREATE TABLE photos_consommables`
    - Aucun `CREATE TRIGGER` ni modification des tables `releves_km`/`motos` ajoutés (D-05 n'est PAS un trigger — voir plan 26-02)
  </acceptance_criteria>
  <done>Migration 24 créée avec les 3 colonnes ; schema.sql reflète les mêmes 3 colonnes inline ; parité vérifiable par grep.</done>
</task>

<task type="auto">
  <name>Task 2: Squelette de test Wave 0 (tests/test-consommable-rappel-cron.js)</name>
  <read_first>
    - tests/test-km-photos-cloudinary.js (pattern de harnais exact du repo : http brut, helpers `check()`/`request()`, credentials seed, comptage OK/KO, exit code — à répliquer)
    - .planning/phases/26-cron-de-rappel-push-badge/26-VALIDATION.md (section Wave 0 Requirements + Per-Task Verification Map)
  </read_first>
  <action>
    Créer `tests/test-consommable-rappel-cron.js` en répliquant le squelette de `tests/test-km-photos-cloudinary.js` (raw `http`, aucun framework), avec :
    - En-tête de commentaire indiquant : "SQUELETTE (Wave 0, plan 26-01) — les assertions GAUGE-03/GAUGE-04 sont remplies par 26-02 (unitaires purs) et 26-03 (intégration HTTP). Doit sortir exit 0 même vide."
    - Les mêmes helpers : `let OK=0, KO=0;`, `function check(label, cond, detail)`, un helper `request(method, path, {token, json})` (copier depuis le fichier modèle ; le multipart n'est pas requis ici, JSON suffit).
    - Les credentials seed connus : `client: { email:'sophie@email.com', password:'client123' }` et `garage: { email:'garage@motokey.fr', password:'motokey2026' }`.
    - Constante `const CRON_SECRET = process.env.CRON_SECRET || null;` (les tests d'intégration cron de 26-03 seront skippables si absent).
    - 3 sections stub vides, chacune un `console.log` de titre : `[UNIT] fonction pure de retard (26-02)`, `[GAUGE-03] cron push + idempotence + reset D-05 (26-03)`, `[GAUGE-04] champ calculé rappel_photo_en_retard (26-03)`.
    - Une fonction `async function run()` qui appelle les sections puis affiche `console.log(\`\nRESULTAT: ${OK} OK / ${KO} KO\`)` et `process.exit(KO === 0 ? 0 : 1);`.
    - `run().catch(e => { console.error(e); process.exit(1); });`

    Le fichier ne DOIT dépendre d'aucune colonne migration ni du serveur pour cette étape squelette : les sections sont vides, donc `run()` sort 0.
  </action>
  <verify>
    <automated>node C:/motokey-api/tests/test-consommable-rappel-cron.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test-consommable-rappel-cron.js` existe et `node tests/test-consommable-rappel-cron.js` sort avec exit code 0 (sans serveur lancé)
    - Le fichier contient `function check(`, `process.exit(`, `CRON_SECRET`, et les 3 titres de section `[UNIT]`, `[GAUGE-03]`, `[GAUGE-04]`
    - Le fichier NE lance aucune erreur non catchée (harnais `run().catch` présent)
  </acceptance_criteria>
  <done>Le squelette de test tourne isolément et sort 0 ; structure prête à être remplie par 26-02/26-03.</done>
</task>

</tasks>

<verification>
- `grep dernier_rappel_envoye_at sql/migrations/24_*.sql schema.sql` retourne des hits dans les deux
- `node tests/test-consommable-rappel-cron.js` sort exit 0
- `node --check` implicite : aucun JS modifié dans ce plan hors le test (qui tourne)
</verification>

<success_criteria>
Migration 24 + parité schema.sql posées ; squelette de test Wave 0 vert (exit 0). Aucun trigger DB ajouté (D-05 reste JS, plan 26-02).
</success_criteria>

<output>
After completion, create `.planning/phases/26-cron-de-rappel-push-badge/26-01-SUMMARY.md`
</output>
