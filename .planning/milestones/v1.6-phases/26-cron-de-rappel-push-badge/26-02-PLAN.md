---
phase: 26-cron-de-rappel-push-badge
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - services/consommableRappelService.js
  - supabase.js
  - tests/test-consommable-rappel-cron.js
autonomous: true
requirements: [GAUGE-03, GAUGE-04]
must_haves:
  truths:
    - "Une fonction pure isConsommableEnRetard(conso, motoKm, latestPhoto) répond true/false selon la grille de seuils par type (D-01), le premier des deux seuils (km OU mois) franchi déclenchant le retard"
    - "Un consommable sans référence exploitable (ni km_montage/date_montage ni photo) est exclu (retourne false), jamais une exception (D-08)"
    - "PhotosConsommables.insert() capture km_a_la_photo et remet dernier_rappel_envoye_at/dernier_rappel_km à NULL sur le consommable lié (D-05, en JS et non via trigger)"
    - "Motos.list() et Motos.getById() exposent un champ calculé rappel_photo_en_retard (bool) sans nouvelle colonne DB, réutilisant la MÊME fonction pure que le cron (GAUGE-04)"
  artifacts:
    - path: "services/consommableRappelService.js"
      provides: "SEUILS (D-01), isConsommableEnRetard, moisEcoules, runConsommableRappelCron"
      exports: ["SEUILS", "isConsommableEnRetard", "moisEcoules", "runConsommableRappelCron"]
      min_lines: 60
    - path: "supabase.js"
      provides: "D-05 reset + km_a_la_photo dans PhotosConsommables.insert ; champ rappel_photo_en_retard dans Motos.list/getById"
      contains: "rappel_photo_en_retard"
  key_links:
    - from: "supabase.js Motos.list/getById"
      to: "services/consommableRappelService.isConsommableEnRetard"
      via: "lazy require (évite le cycle supabase<->service)"
      pattern: "require\\('./services/consommableRappelService'\\)"
    - from: "supabase.js PhotosConsommables.insert"
      to: "consommables.dernier_rappel_envoye_at/dernier_rappel_km"
      via: ".update({...null}).eq('id', consommable_id) après insert (D-05)"
      pattern: "dernier_rappel_envoye_at: null"
---

<objective>
Créer le service `consommableRappelService.js` (grille de seuils D-01, fonction de retard pure réutilisable, runner du cron) et étendre `supabase.js` : capture `km_a_la_photo` + reset D-05 dans `PhotosConsommables.insert()`, et champ calculé `rappel_photo_en_retard` sur `Motos.list()`/`getById()` (GAUGE-04). La logique de seuils vit une seule fois (le service) et est réutilisée par le cron ET l'exposition badge — jamais dupliquée.

Purpose: Poser le cœur métier (détection binaire de retard) et sa réutilisation backend, avant l'endpoint HTTP (26-03).
Output: 1 nouveau service, `supabase.js` étendu, tests unitaires purs (offline) ajoutés au fichier de test.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-cron-de-rappel-push-badge/26-CONTEXT.md
@.planning/phases/26-cron-de-rappel-push-badge/26-RESEARCH.md

<interfaces>
<!-- Signatures existantes que l'exécuteur doit réutiliser sans les recréer -->

services/maintenanceAlertService.js — structure cron à répliquer (binaire au lieu de 3 rangs) :
  runMaintenanceAlertCron() → { scanned, notified, details }
  - lookup motos via SBLayer.supabase.from('motos').select(...)
  - per-moto try/catch autour du calcul, push { moto_id, error } dans details + continue (Pitfall 5)
  - idempotencyKey = `maintenance-alert:${moto.id}:${worst}:${YYYY-MM-DD}`

services/pushService.js :
  async function sendPush(clientId, payload, idempotencyKey) → { sent: N, results } | { sent: 0 } | { error }
  - fan-out multi-device + dédup interne via push_send_log ; NE JAMAIS appeler expo-server-sdk directement
  - payload = { title, body, data: { type, motoId } }

supabase.js Consommables :
  Consommables.listByMoto(moto_id) → [{ id, moto_id, type_consommable, km_montage, date_montage, reference, dernier_rappel_envoye_at, dernier_rappel_km, ... }]

supabase.js PhotosConsommables (à étendre) :
  PhotosConsommables.insert({ moto_id, consommable_id, type_consommable, photo_url, analyse_ia, analyse_status }) → row
  PhotosConsommables.listByConsommable(consommable_id) → rows triés created_at DESC (la première = la plus récente)

supabase.js Motos.list(garage_id, filters) (lignes 235-269) — précédent UX-02 `alerte_entretien` : champ calculé à l'affichage, aucune colonne DB, batch-fetch par ids.
supabase.js Motos.getById(id, garage_id) (lignes 271-279) — retourne 1 moto.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: services/consommableRappelService.js (seuils D-01, fonction pure, runner cron)</name>
  <read_first>
    - services/maintenanceAlertService.js (structure complète à adapter — lookup, per-moto try/catch+continue, push conditionnel, persistance, summary)
    - services/pushService.js (signature exacte sendPush, dédup interne)
    - .planning/phases/26-cron-de-rappel-push-badge/26-RESEARCH.md (section "Pure lateness-calculation function" + Patterns 1/2/3 + Pitfalls 1/3/4/5)
    - .planning/phases/26-cron-de-rappel-push-badge/26-CONTEXT.md (D-01 à D-08)
  </read_first>
  <behavior>
    - isConsommableEnRetard : type inconnu (absent de SEUILS) → false (défensif)
    - isConsommableEnRetard : refKm = latestPhoto.km_a_la_photo si photo, sinon conso.km_montage ; refDate = latestPhoto.created_at si photo, sinon conso.date_montage
    - isConsommableEnRetard : refKm null ET refDate absent → false (D-08 exclusion, jamais throw)
    - isConsommableEnRetard : (motoKm - refKm) >= seuil.km → true ; moisEcoules(refDate, now) >= seuil.mois → true ; le premier des deux suffit
    - moisEcoules(from, to) = (to.getFullYear()-from.getFullYear())*12 + (to.getMonth()-from.getMonth()) — arithmétique calendaire, PAS division par 30 jours (Pitfall 4)
    - Exemple pneu_ar (seuil km 2500) : km_montage 10000, motoKm 12600, pas de photo → true ; motoKm 12000 → false
  </behavior>
  <action>
    Créer `services/consommableRappelService.js`. Commencer par `'use strict';` puis :
    `const SBLayer = require('../supabase');` et `const pushService = require('./pushService');`

    1) Grille de seuils D-01 EXACTE (map en dur, aucune migration/table) :
    ```javascript
    const SEUILS = {
      pneu_av:{km:3000,mois:6}, pneu_ar:{km:2500,mois:6}, chaine:{km:3000,mois:6},
      plaquettes_av:{km:3000,mois:6}, plaquettes_ar:{km:4500,mois:6},
      disque_av:{km:8000,mois:12}, disque_ar:{km:8000,mois:12},
      huile_moteur:{km:5000,mois:6}, liquide_frein:{km:6000,mois:12},
    };
    const LABELS = { pneu_av:'Pneu avant', pneu_ar:'Pneu arriere', chaine:'Chaine',
      plaquettes_av:'Plaquettes avant', plaquettes_ar:'Plaquettes arriere',
      disque_av:'Disque avant', disque_ar:'Disque arriere',
      huile_moteur:'Huile moteur', liquide_frein:'Liquide de frein' };
    ```

    2) Fonctions PURES (aucun accès DB) :
    ```javascript
    function moisEcoules(from, to) {
      return (to.getFullYear()-from.getFullYear())*12 + (to.getMonth()-from.getMonth());
    }
    function isConsommableEnRetard(conso, motoKm, latestPhoto) {
      const seuil = SEUILS[conso.type_consommable];
      if (!seuil) return false;
      const refKm  = latestPhoto ? latestPhoto.km_a_la_photo : conso.km_montage;
      const refDate = latestPhoto ? latestPhoto.created_at : conso.date_montage;
      if (refKm == null && !refDate) return false; // D-08
      const kmDepasse  = refKm != null && motoKm != null && (motoKm - refKm) >= seuil.km;
      const moisDepasse = refDate && moisEcoules(new Date(refDate), new Date()) >= seuil.mois;
      return !!(kmDepasse || moisDepasse);
    }
    ```

    3) Helper interne `async function _consommablesEnRetardPourMoto(moto)` : charge `SBLayer.Consommables.listByMoto(moto.id)`, et pour chaque conso récupère la photo la plus récente via `SBLayer.PhotosConsommables.listByConsommable(conso.id)` (rows[0] = plus récente ou null), applique `isConsommableEnRetard(conso, moto.km, latestPhoto)`. Retourne le tableau des consos en retard (objets consommable complets).

    4) `async function runConsommableRappelCron()` (adapter maintenanceAlertService, mais binaire) :
    - Lookup UNIQUEMENT les motos client (le badge garage GAUGE-04 est calculé au read-time, pas par le cron — voir Open Question 2 du RESEARCH) :
      `SBLayer.supabase.from('motos').select('id, client_id, km, marque, modele, proprietaire_type').eq('proprietaire_type','client').not('client_id','is',null)`
      (NE PAS copier le filtre Phase 17 tel quel sans le `proprietaire_type='client'` — Pitfall 1.)
    - Pour chaque moto, DANS un `try/catch` scopé par moto (Pitfall 5 : sur erreur, `details.push({moto_id, error:e.message}); continue;`) :
      - `const lateAll = await _consommablesEnRetardPourMoto(moto);`
      - `const newlyLate = lateAll.filter(c => c.dernier_rappel_envoye_at == null);` (D-03 : ne renotifie pas un consommable déjà notifié)
      - Si `newlyLate.length === 0` : `details.push({moto_id: moto.id, late: lateAll.map(c=>c.type_consommable), notified:false}); continue;`
      - Construire le push groupé (D-02, UN seul push listant TOUS les consos actuellement en retard) :
        `const types = lateAll.map(c=>c.type_consommable).sort();`
        `const liste = types.map(t=>LABELS[t]||t).join(', ');`
        `const title = 'Photos d entretien a faire';`
        `const body = \`Votre \${moto.marque||'moto'} \${moto.modele||''} : \${types.length} consommable(s) a photographier (\${liste}).\`.replace(/\s+/g,' ').trim();`
      - idempotencyKey DOIT varier avec le contenu (Pitfall 3) :
        `const idempotencyKey = \`rappel-photo:\${moto.id}:\${types.join('+')}:\${new Date().toISOString().slice(0,10)}\`;`
      - `const pushResult = await pushService.sendPush(moto.client_id, { title, body, data:{ type:'moto_entretien', motoId: moto.id } }, idempotencyKey); notified++;`
      - Persister l'état SUR CHAQUE consommable de `newlyLate` (D-04, colonnes sur consommables, PAS sur motos — Anti-Pattern) :
        `for (const c of newlyLate) { await SBLayer.supabase.from('consommables').update({ dernier_rappel_envoye_at: new Date().toISOString(), dernier_rappel_km: moto.km ?? null }).eq('id', c.id); }`
      - `details.push({ moto_id: moto.id, late: types, notified: true, pushResult });`
    - Retourner `{ scanned: (motos||[]).length, notified, details }` et `console.log` un résumé (miroir Phase 17).

    5) Exports : `module.exports = { SEUILS, LABELS, isConsommableEnRetard, moisEcoules, runConsommableRappelCron };`

    Ajouter au fichier `tests/test-consommable-rappel-cron.js` (section `[UNIT]`) des assertions PURES (aucun serveur/DB) via `require('../services/consommableRappelService')` :
    - `check('pneu_ar retard km', isConsommableEnRetard({type_consommable:'pneu_ar',km_montage:10000,date_montage:null}, 12600, null) === true)`
    - `check('pneu_ar pas retard', isConsommableEnRetard({type_consommable:'pneu_ar',km_montage:10000,date_montage:null}, 12000, null) === false)`
    - `check('D-08 aucune ref exclu', isConsommableEnRetard({type_consommable:'chaine',km_montage:null,date_montage:null}, 99999, null) === false)`
    - `check('type inconnu false', isConsommableEnRetard({type_consommable:'inconnu',km_montage:0}, 99999, null) === false)`
    - `check('moisEcoules calendaire', moisEcoules(new Date('2026-01-15'), new Date('2026-07-15')) === 6)`
    - `check('photo plus recente prime km_montage', isConsommableEnRetard({type_consommable:'huile_moteur',km_montage:0}, 4000, {km_a_la_photo:1000,created_at:'2026-07-01'}) === false)` (ref = photo 1000, 4000-1000=3000 < 5000)
  </action>
  <verify>
    <automated>node --check C:/motokey-api/services/consommableRappelService.js && node C:/motokey-api/tests/test-consommable-rappel-cron.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `services/consommableRappelService.js` exporte `SEUILS`, `isConsommableEnRetard`, `moisEcoules`, `runConsommableRappelCron` (grep `module.exports` liste les 4)
    - Fichier contient la grille exacte : `pneu_ar:{km:2500` et `disque_av:{km:8000` et `liquide_frein:{km:6000`
    - Fichier contient `.eq('proprietaire_type','client')` (Pitfall 1) et `types.join('+')` dans l'idempotencyKey (Pitfall 3)
    - Fichier contient `data:{ type:'moto_entretien'` (payload cohérent Phase 17) et un `try {` ... `continue;` par moto (Pitfall 5)
    - `node --check services/consommableRappelService.js` passe
    - `node tests/test-consommable-rappel-cron.js` sort exit 0 avec les 6 assertions `[UNIT]` vertes (sans serveur ni DB)
    - Aucun appel direct à `expo-server-sdk` (grep ne trouve pas `expo-server-sdk` dans le fichier)
  </acceptance_criteria>
  <done>Service créé avec fonction pure de retard réutilisable + runner cron binaire ; 6 tests unitaires purs verts offline.</done>
</task>

<task type="auto">
  <name>Task 2: supabase.js — D-05 reset + km_a_la_photo + champ rappel_photo_en_retard</name>
  <read_first>
    - supabase.js lignes 235-279 (Motos.list + Motos.getById — précédent alerte_entretien à répliquer)
    - supabase.js lignes 1365-1396 (PhotosConsommables.insert + listByConsommable actuels)
    - services/consommableRappelService.js (fonction isConsommableEnRetard à réutiliser — créée Task 1)
    - .planning/phases/26-cron-de-rappel-push-badge/26-RESEARCH.md (Pattern 4 + Pitfall 2 : D-05 en JS, pas trigger)
  </read_first>
  <action>
    Éditer `supabase.js` (édition directe native, jamais PowerShell/sed) :

    A) `PhotosConsommables.insert(...)` (ligne ~1370) — étendre pour accepter et persister `km_a_la_photo`, ET appliquer le reset D-05 :
    - Changer la signature destructurée pour inclure `km_a_la_photo` :
      `async insert({ moto_id, consommable_id, type_consommable, photo_url, analyse_ia, analyse_status, km_a_la_photo }) {`
    - Ajouter au `payload` : `km_a_la_photo: (km_a_la_photo !== undefined && km_a_la_photo !== null && km_a_la_photo !== '') ? parseInt(km_a_la_photo) : null,`
    - APRÈS l'INSERT réussi (après `if (error) throw ...`, avant `return data;`), ajouter le reset D-05 (en JS, PAS un trigger DB — Pitfall 2) :
      ```javascript
      // D-05 : toute nouvelle photo rearme le rappel du consommable lie (reset a NULL)
      if (consommable_id) {
        const { error: rErr } = await supabase.from('consommables')
          .update({ dernier_rappel_envoye_at: null, dernier_rappel_km: null })
          .eq('id', consommable_id);
        if (rErr) console.warn('[PhotosConsommables.insert] reset D-05 echoue:', rErr.message); // non bloquant
      }
      ```

    B) `Motos.list(...)` (lignes 235-269) — ajouter un champ calculé `rappel_photo_en_retard` (bool) + `consommables_en_retard` (array de type_consommable), au read-time, aucune colonne DB (précédent `alerte_entretien`), réutilisant la fonction pure du service via LAZY require (évite le cycle supabase<->service — le require en tête de fichier créerait une dépendance circulaire) :
    - En tête de la méthode `list`, après avoir `rows`, batch-fetch pour tous les `ids` :
      `const { isConsommableEnRetard } = require('./services/consommableRappelService');`
      - `consommables` : `supabase.from('consommables').select('*').in('moto_id', ids)` → grouper par moto_id.
      - photos récentes : `supabase.from('photos_consommables').select('consommable_id, km_a_la_photo, created_at').in('moto_id', ids).order('created_at', { ascending:false })` → pour chaque consommable_id, garder la PREMIÈRE rencontrée (= la plus récente grâce au tri desc) dans un map `latestPhotoByConso`.
    - Dans le `.map(m => ...)` final, calculer :
      ```javascript
      const consos = consosByMoto[m.id] || [];
      const enRetard = consos.filter(c => isConsommableEnRetard(c, m.km, latestPhotoByConso[c.id] || null));
      return { ...m, pct_max_usage: pctMax, alerte_entretien: ops.length>0 && pctMax>=80,
               rappel_photo_en_retard: enRetard.length > 0,
               consommables_en_retard: enRetard.map(c => c.type_consommable) };
      ```
      (Garder `pct_max_usage`/`alerte_entretien` existants intacts.)

    C) `Motos.getById(id, garage_id)` (lignes 271-279) — enrichir de la même façon pour une moto unique :
    - Après avoir chargé `data`, si `data` non null : charger `SBLayer`-style `Consommables.listByMoto(id)` (helper existant) et pour chaque conso sa photo la plus récente via `PhotosConsommables.listByConsommable(conso.id)` (rows[0] ou null), appliquer `isConsommableEnRetard(conso, data.km, latestPhoto)`, et retourner `{ ...data, rappel_photo_en_retard: enRetard.length>0, consommables_en_retard: enRetard.map(c=>c.type_consommable) }`. Utiliser le même lazy `require('./services/consommableRappelService')`.

    NE PAS modifier la pondération anti-fraude ni les helpers km. NE PAS créer de trigger DB.
  </action>
  <verify>
    <automated>node --check C:/motokey-api/supabase.js && grep -c "rappel_photo_en_retard" C:/motokey-api/supabase.js && grep -c "dernier_rappel_envoye_at: null" C:/motokey-api/supabase.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check supabase.js` passe
    - `PhotosConsommables.insert` contient `km_a_la_photo` dans la signature ET dans le payload
    - `supabase.js` contient `dernier_rappel_envoye_at: null` (reset D-05) dans `PhotosConsommables.insert` (grep >= 1)
    - `Motos.list` ET `Motos.getById` retournent `rappel_photo_en_retard` (grep `rappel_photo_en_retard` >= 2 occurrences)
    - Le champ est calculé via `require('./services/consommableRappelService')` en lazy require (pas de require en tête de fichier — grep confirme l'absence d'un require top-level du service ligne < 20)
    - Aucun `CREATE TRIGGER` ajouté à supabase.js (D-05 est en JS)
  </acceptance_criteria>
  <done>PhotosConsommables.insert capture km_a_la_photo + reset D-05 ; Motos.list/getById exposent rappel_photo_en_retard via la fonction pure partagée ; node --check vert.</done>
</task>

</tasks>

<verification>
- `node --check services/consommableRappelService.js` et `node --check supabase.js` passent
- `node tests/test-consommable-rappel-cron.js` sort exit 0 (6 assertions unitaires vertes)
- grep confirme réutilisation (pas duplication) de la grille D-01 : `SEUILS` défini uniquement dans le service
</verification>

<success_criteria>
Le cœur métier (détection binaire de retard par type, D-01) existe comme fonction pure unique, réutilisée par le futur cron ET par l'exposition badge GAUGE-04. D-05 (reset à la photo) et D-07 (km_a_la_photo) câblés en JS dans supabase.js.
</success_criteria>

<output>
After completion, create `.planning/phases/26-cron-de-rappel-push-badge/26-02-SUMMARY.md`
</output>
