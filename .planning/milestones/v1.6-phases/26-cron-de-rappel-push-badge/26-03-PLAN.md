---
phase: 26-cron-de-rappel-push-badge
plan: 03
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - motokey-api.js
  - tests/test-consommable-rappel-cron.js
autonomous: true
requirements: [GAUGE-03, GAUGE-04]
must_haves:
  truths:
    - "POST /cron/rappels-photo-consommables est authentifié par X-Cron-Secret/CRON_SECRET (pas de JWT), renvoie 401 sans secret valide et 200 { scanned, notified, details } avec"
    - "Le handler upload photo consommable passe km_a_la_photo = motos.km au moment de l'upload à PhotosConsommables.insert (D-07)"
    - "Le fichier de test couvre GAUGE-03 (push groupé + idempotence + reset D-05) et GAUGE-04 (champ rappel_photo_en_retard), skippable proprement si CRON_SECRET/colonnes absents"
  artifacts:
    - path: "motokey-api.js"
      provides: "endpoint cron + capture km_a_la_photo"
      contains: "/cron/rappels-photo-consommables"
    - path: "tests/test-consommable-rappel-cron.js"
      provides: "assertions d'intégration GAUGE-03/GAUGE-04"
      contains: "rappels-photo-consommables"
  key_links:
    - from: "motokey-api.js POST /cron/rappels-photo-consommables"
      to: "consommableRappelService.runConsommableRappelCron"
      via: "require + appel dans le bloc endpoint (miroir /cron/maintenance-alerts)"
      pattern: "runConsommableRappelCron"
    - from: "motokey-api.js handlePhotoConsommable"
      to: "PhotosConsommables.insert km_a_la_photo"
      via: "kmActuel passé à l'insert (D-07)"
      pattern: "km_a_la_photo"
---

<objective>
Exposer le cron via `POST /cron/rappels-photo-consommables` dans `motokey-api.js` (pattern `X-Cron-Secret` verbatim de `/cron/maintenance-alerts`), câbler la capture `km_a_la_photo` dans le handler d'upload photo existant (D-07), et remplir les assertions d'intégration GAUGE-03/GAUGE-04 du fichier de test.

Purpose: Rendre le cron déclenchable par un scheduler externe et vérifiable de bout en bout ; connecter la référence km à la photo pour que le calcul "depuis dernière photo" soit correct.
Output: `motokey-api.js` étendu (endpoint + 1 ligne handler) ; assertions d'intégration ajoutées au test.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-cron-de-rappel-push-badge/26-CONTEXT.md
@.planning/phases/26-cron-de-rappel-push-badge/26-RESEARCH.md

<interfaces>
motokey-api.js lignes 749-762 — endpoint /cron/maintenance-alerts (structure X-Cron-Secret à répliquer VERBATIM) :
```javascript
if ((p = M('POST', '/cron/maintenance-alerts')) !== null) {
  const secret = req.headers['x-cron-secret'];
  if (!process.env.CRON_SECRET || secret !== process.env.CRON_SECRET) {
    return fail(res, 'Non autorisé', 401, 'UNAUTHORIZED');
  }
  try {
    const result = await maintenanceAlertService.runMaintenanceAlertCron();
    return ok(res, result, 'Cron entretien exécuté');
  } catch (e) {
    console.error('[cron] maintenance-alerts échoué:', e.message);
    return fail(res, e.message, 500, 'CRON_ERROR');
  }
}
```
motokey-api.js ligne 82 — `const maintenanceAlertService = require('./services/maintenanceAlertService');` (ajouter le require du nouveau service à côté).
motokey-api.js lignes 566-571 — handlePhotoConsommable : `kmActuel = r.moto ? (r.moto.km ?? null) : null;` déjà disponible ; `PhotosConsommables.insert({...})` appelé ligne 571.
services/consommableRappelService.js — `runConsommableRappelCron()` → { scanned, notified, details } (créé plan 26-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint cron + capture km_a_la_photo dans motokey-api.js</name>
  <read_first>
    - motokey-api.js lignes 78-90 (bloc des require services — où ajouter le require)
    - motokey-api.js lignes 749-762 (endpoint /cron/maintenance-alerts — modèle exact)
    - motokey-api.js lignes 530-577 (handler handlePhotoConsommable — où passer km_a_la_photo)
    - .planning/phases/26-cron-de-rappel-push-badge/26-RESEARCH.md (Pattern 3 : endpoint cron ; note "requireRole() NON requis — exception X-Cron-Secret établie")
  </read_first>
  <action>
    Éditer `motokey-api.js` (édition directe native, jamais PowerShell/sed) :

    A) Ajouter le require du nouveau service juste après la ligne 82 (`const maintenanceAlertService = require('./services/maintenanceAlertService');`) :
    `const consommableRappelService = require('./services/consommableRappelService');`

    B) Ajouter le nouvel endpoint IMMÉDIATEMENT après le bloc `/cron/maintenance-alerts` (après la ligne 762, avant le commentaire `/* ROOT */`), en répliquant la structure verbatim (même auth X-Cron-Secret, PAS de requireRole — c'est l'exception établie pour les crons) :
    ```javascript
    if ((p = M('POST', '/cron/rappels-photo-consommables')) !== null) {
      const secret = req.headers['x-cron-secret'];
      if (!process.env.CRON_SECRET || secret !== process.env.CRON_SECRET) {
        return fail(res, 'Non autorisé', 401, 'UNAUTHORIZED');
      }
      try {
        const result = await consommableRappelService.runConsommableRappelCron();
        return ok(res, result, 'Cron rappels photo consommables exécuté');
      } catch (e) {
        console.error('[cron] rappels-photo-consommables échoué:', e.message);
        return fail(res, e.message, 500, 'CRON_ERROR');
      }
    }
    ```

    C) Dans `handlePhotoConsommable` (ligne ~571), passer `km_a_la_photo` à l'insert (D-07 : capture `motos.km` au moment de l'upload — `kmActuel` est déjà calculé ligne 567) :
    - Modifier l'appel `SBLayer.PhotosConsommables.insert({ moto_id: motoId, consommable_id: conso.id, type_consommable: type, photo_url: secure_url, analyse_ia: analyse, analyse_status: analyse.analyse_status })` pour ajouter `, km_a_la_photo: kmActuel` dans l'objet.

    NE PAS toucher aux autres endpoints, ni au body() interceptor, ni à la pondération anti-fraude.
  </action>
  <verify>
    <automated>node --check C:/motokey-api/motokey-api.js && grep -c "rappels-photo-consommables" C:/motokey-api/motokey-api.js && grep -c "km_a_la_photo: kmActuel" C:/motokey-api/motokey-api.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check motokey-api.js` passe
    - `motokey-api.js` contient `const consommableRappelService = require('./services/consommableRappelService');`
    - `motokey-api.js` contient `M('POST', '/cron/rappels-photo-consommables')` ET `runConsommableRappelCron()`
    - Le nouvel endpoint contient `req.headers['x-cron-secret']` et `process.env.CRON_SECRET` (pas de requireRole)
    - `handlePhotoConsommable` passe `km_a_la_photo: kmActuel` à `PhotosConsommables.insert`
  </acceptance_criteria>
  <done>Endpoint cron ajouté (auth X-Cron-Secret) + km_a_la_photo câblé dans l'upload ; node --check vert.</done>
</task>

<task type="auto">
  <name>Task 2: Assertions d'intégration GAUGE-03/GAUGE-04 (tests/test-consommable-rappel-cron.js)</name>
  <read_first>
    - tests/test-km-photos-cloudinary.js (pattern login client/garage, request multipart, skip gracieux — modèle)
    - tests/test-consommable-rappel-cron.js (squelette + assertions unitaires déjà présentes)
    - motokey-api.js lignes 749-777 (les 2 endpoints cron pour connaître la forme de réponse ok/fail)
    - .planning/phases/26-cron-de-rappel-push-badge/26-VALIDATION.md (Per-Task Verification Map — les 4 comportements à couvrir)
  </read_first>
  <action>
    Compléter `tests/test-consommable-rappel-cron.js`. Lancer le serveur (`PUSH_ENABLED` non défini → push en mode dev console-log, pas d'Expo réel). Remplir 2 sections. TOUTE assertion qui dépend des colonnes migration 24 OU de `CRON_SECRET` doit être SKIPPABLE proprement (log `⏭️  skip: <raison>`, ne pas incrémenter KO) — même discipline que les assertions Cloudinary skippables de Phase 25. Le fichier doit sortir exit 0 tant qu'aucune assertion active n'échoue.

    Helpers à ajouter (mirroir test-km-photos-cloudinary.js) : `login(creds)` (POST /auth/client/login pour le client, /auth/login pour le garage) retournant le token ; `cron(secretHeader)` = `request('POST', '/cron/rappels-photo-consommables', { headers:{ 'x-cron-secret': secretHeader } })` (adapter `request` pour accepter des headers custom si besoin).

    Section `[GAUGE-03]` — auth + cron + idempotence + reset D-05 :
    1. Auth : `cron('mauvais-secret')` → attendu HTTP 401. (toujours actif, ne dépend pas des colonnes)
    2. Si `CRON_SECRET` absent de l'env du test → skip le reste de la section avec `⏭️  skip: CRON_SECRET non défini`.
    3. `cron(CRON_SECRET)` → HTTP 200, corps avec `data.scanned` (number) et `data.notified` (number). check la forme.
    4. Scénario retard (skippable si l'insert/colonnes échouent — détecter une erreur mentionnant `km_a_la_photo`/`dernier_rappel` ou PGRST column, alors skip) :
       - login client sophie, GET ses motos, choisir une moto ; via l'API existante, poser un consommable avec un km_montage bas (ex: PATCH/POST consommables `chaine` km_montage = motoKm - 5000) pour forcer le dépassement du seuil 3000.
       - Premier `cron(CRON_SECRET)` : attendu `notified >= 1` (ou, si l'environnement ne permet pas de garantir l'isolation, vérifier que `details` contient la moto avec `notified:true`).
       - Deuxième `cron(CRON_SECRET)` immédiat : attendu que CETTE moto ne soit PAS re-notifiée (idempotence D-03 — `dernier_rappel_envoye_at` désormais non NULL) — chercher la moto dans `details` avec `notified:false`.
       - Reset D-05 : uploader une nouvelle photo du consommable `chaine` (POST /motos/:id/photos-consommables, multipart — SKIP proprement si Cloudinary renvoie 503, c'est le comportement attendu sans credentials), puis un troisième `cron(CRON_SECRET)` : la moto redevient notifiable (`notified:true`). Si l'upload est 503 (Cloudinary non configuré), skip cette sous-assertion avec `⏭️  skip: Cloudinary non configuré (reset D-05 non testable end-to-end ici)`.
    5. Vérifier que le push est parti en mode dev : le corps de réponse `details[].pushResult` doit avoir `sent >= 1` OU chaque device en `{ dev:true }` (selon tokens seed). Skippable si aucun token device pour le client (`sent:0`).

    Section `[GAUGE-04]` — champ calculé sans push :
    6. login garage, GET /motos (liste garage) → chaque moto de la réponse porte le champ `rappel_photo_en_retard` (booléen présent, typeof === 'boolean') et `consommables_en_retard` (array). check la présence du champ sur au moins une moto. Skippable si la liste est vide.
    7. GET une moto garage précise (getById via l'endpoint fiche moto) → même champ présent.

    Terminer : le récapitulatif `RESULTAT: OK/KO` et `process.exit(KO===0?0:1)` déjà en place (26-01) restent valides.
  </action>
  <verify>
    <automated>node --check C:/motokey-api/tests/test-consommable-rappel-cron.js; echo "syntax_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check tests/test-consommable-rappel-cron.js` passe
    - Le fichier contient `rappels-photo-consommables`, `rappel_photo_en_retard`, et au moins un `⏭️  skip` (motif skippable)
    - La section GAUGE-03 assert un 401 sur mauvais secret ET un 200 avec `scanned`/`notified` sur bon secret
    - La section GAUGE-03 contient une assertion d'idempotence (deuxième cron → `notified:false` pour la moto) et une tentative de reset D-05 (upload photo → troisième cron notifiable), skippable si Cloudinary 503
    - La section GAUGE-04 vérifie la présence de `rappel_photo_en_retard` (typeof boolean) dans la réponse liste garage
    - Aucune assertion ne dépend d'un vrai envoi Expo (assert sur forme de retour `sent`/`dev`, pas sur livraison réelle)
  </acceptance_criteria>
  <done>Assertions d'intégration GAUGE-03/GAUGE-04 écrites, skippables proprement ; node --check vert. Passage réel au vert = gate 26-04 après application migration.</done>
</task>

</tasks>

<verification>
- `node --check motokey-api.js` et `node --check tests/test-consommable-rappel-cron.js` passent
- grep `rappels-photo-consommables` présent dans motokey-api.js ET tests
- L'endpoint 401 sans secret est asserté (ne dépend d'aucune colonne migration)
</verification>

<success_criteria>
Le cron est déclenchable par HTTP (X-Cron-Secret), km_a_la_photo est capturé à l'upload, et le fichier de test couvre GAUGE-03 (push groupé + idempotence + reset) et GAUGE-04 (champ calculé), avec skips propres tant que la migration/Cloudinary/CRON_SECRET ne sont pas en place.
</success_criteria>

<output>
After completion, create `.planning/phases/26-cron-de-rappel-push-badge/26-03-SUMMARY.md`
</output>
