---
phase: 25-endpoints-backend-km-photos-remplacement-compteur-cloudinary
plan: 04
type: execute
wave: 3
depends_on: ["25-01", "25-03"]
files_modified:
  - motokey-api.js
  - tests/test-km-photos-cloudinary.js
autonomous: true
requirements: [CONSO-01]

must_haves:
  truths:
    - "Un MECANO+ peut PATCH un consommable unitaire d'une moto de son garage (km_montage/date_montage/reference) → 200 (CONSO-01)"
    - "Un MECANO+ peut POST un tableau de consommables (jusqu'aux 9 types) en une requête → 200, chaque type upserté (CONSO-01)"
    - "Un type_consommable hors des 9 types canoniques → 400 VALIDATION_ERROR (validé via TYPES_CONSOMMABLES, pas une erreur Postgres brute)"
    - "Un CLIENT reçoit 403 sur les deux endpoints de saisie consommables (MECANO+ only)"
    - "Les deux endpoints délèguent à Consommables.upsert() — aucune logique métier dupliquée (D-04)"
  artifacts:
    - path: "motokey-api.js"
      provides: "PATCH /motos/:id/consommables/:type + POST /motos/:id/consommables"
      contains: "consommables"
  key_links:
    - from: "PATCH /motos/:id/consommables/:type"
      to: "Consommables.upsert"
      via: "SBLayer.Consommables.upsert(motoId, {type_consommable, km_montage, date_montage, reference})"
      pattern: "Consommables.upsert"
    - from: "validation type"
      to: "TYPES_CONSOMMABLES"
      via: "SBLayer.TYPES_CONSOMMABLES.includes(type)"
      pattern: "TYPES_CONSOMMABLES"
---

<objective>
Livrer les deux endpoints de saisie consommables (CONSO-01, D-04) : un endpoint unitaire (PATCH par type, usage courant du mécano) et un endpoint bulk (POST tableau, setup initial d'une moto). Les deux réservés MECANO+, validés contre `TYPES_CONSOMMABLES`, délégant à `Consommables.upsert()`.

Purpose: Le mécano renseigne km_montage/date_montage/référence des consommables — données de base des jauges Phase 27/28.
Output: 2 routes JSON dans motokey-api.js + assertions CONSO-01.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-CONTEXT.md
@.planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-RESEARCH.md

<interfaces>
Consommables.upsert (supabase.js L1314-1334) :
```javascript
await SBLayer.Consommables.upsert(moto_id, { type_consommable, km_montage, date_montage, reference })
// upsert on-conflict (moto_id, type_consommable) ; km_montage parsé en int ou null ; renvoie la ligne
// ne valide PAS type_consommable (laisse la contrainte CHECK DB) → valider côté endpoint via TYPES_CONSOMMABLES
```
TYPES_CONSOMMABLES (supabase.js, ajouté en 25-01) : les 9 types exacts, exporté sur SBLayer.
resolveMotoForCtx(ctx, motoId, a) (motokey-api.js, ajouté en 25-03) : renvoie {moto, garage_id, acteur_type, acteur_id} ou null. Sa branche MECANO+ vérifie moto.garage_id === garageId.
RBAC : `requireRole(ctx,'MECANO')`. Helpers réponse ok/fail. Pattern d'auth ctx : voir motokey-api.js L770-775.
Routeur : ces endpoints sont JSON (pas de photo) → routes normales `M('PATCH',...)` / `M('POST',...)`, le corps est dans `b`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PATCH /motos/:id/consommables/:type (unitaire, MECANO+)</name>
  <files>motokey-api.js, tests/test-km-photos-cloudinary.js</files>
  <read_first>
    - motokey-api.js L770-816 (pattern ctx + resolveMotoForCtx en 25-03) ; ok/fail L368-373 ; zone routes /motos
    - supabase.js L1314-1334 (Consommables.upsert)
    - tests/test-km-photos-cloudinary.js (section CONSO-01 stub)
  </read_first>
  <behavior>
    - MECANO+ du garage PATCH un type valide avec km_montage → 200, ligne upsertée
    - type hors 9 canoniques → 400 VALIDATION_ERROR (avant tout appel DB)
    - CLIENT → 403 FORBIDDEN_ROLE
    - moto d'un autre garage → 404 NOT_FOUND
    - non authentifié → 401
  </behavior>
  <action>
    Ajouter la route JSON dans la zone /motos de motokey-api.js :
    ```javascript
    if ((p = M('PATCH','/motos/:id/consommables/:type')) !== null) {
      const a = authSilent(req);
      if (!a && !req.ctx) return fail(res,'Non authentifié',401,'UNAUTHORIZED');
      const ctx = req.ctx || (SBLayer ? await rbac.inferLegacyRole(a.id, SBLayer) : {role:'CONCESSION',level:4,user_id:null,email:null,client_type:null});
      if (!rbac.requireRole(ctx,'MECANO')) return fail(res,'Permission refusée — MECANO minimum requis',403,'FORBIDDEN_ROLE');
      if (!SBLayer.TYPES_CONSOMMABLES.includes(p.type)) return fail(res, 'type_consommable invalide', 400, 'VALIDATION_ERROR');
      const r = await resolveMotoForCtx(ctx, p.id, a);
      if (!r) return fail(res,'Moto non trouvée',404,'NOT_FOUND');
      try {
        const row = await SBLayer.Consommables.upsert(p.id, { type_consommable: p.type, km_montage: b.km_montage, date_montage: b.date_montage, reference: b.reference });
        return ok(res, { consommable: row }, 'Consommable enregistré');
      } catch (e) { return fail(res, e.message, 500, 'SERVER_ERROR'); }
    }
    ```
    Éditer via l'outil Edit (jamais PowerShell/sed).

    Remplir la SECTION CONSO-01 (partie unitaire) du test : login garage → PATCH /motos/:id/consommables/chaine {km_montage:12000} → check 200 ; PATCH /motos/:id/consommables/invalide_xyz → check 400 VALIDATION_ERROR ; login client → PATCH → check 403.
  </action>
  <verify>
    <automated>node --check motokey-api.js && node -e "const fs=require('fs'); const s=fs.readFileSync('motokey-api.js','utf8'); ['M(\'PATCH\',\'/motos/:id/consommables/:type\')','TYPES_CONSOMMABLES.includes','Consommables.upsert(p.id'].forEach(x=>{if(!s.includes(x))throw new Error('manquant: '+x)}); const t=fs.readFileSync('tests/test-km-photos-cloudinary.js','utf8'); if(!/consommables\/chaine|VALIDATION_ERROR/.test(t))throw new Error('assertions CONSO-01 unit non remplies'); console.log('OK CONSO-01 unit');"</automated>
  </verify>
  <acceptance_criteria>
    - motokey-api.js contains la route `M('PATCH','/motos/:id/consommables/:type')`
    - motokey-api.js valide le type via `TYPES_CONSOMMABLES.includes` AVANT tout appel DB, sinon 400 VALIDATION_ERROR
    - motokey-api.js gate `requireRole(ctx,'MECANO')` (CLIENT → 403)
    - motokey-api.js délègue à `Consommables.upsert(p.id, ...)`
    - tests/test-km-photos-cloudinary.js : assertions 200, 400 (type invalide), 403 (CLIENT)
    - `node --check motokey-api.js` exits 0
  </acceptance_criteria>
  <done>PATCH unitaire consommable fonctionne : MECANO+ upsert un type valide, type invalide → 400, CLIENT → 403.</done>
</task>

<task type="auto">
  <name>Task 2: POST /motos/:id/consommables (bulk, MECANO+)</name>
  <files>motokey-api.js, tests/test-km-photos-cloudinary.js</files>
  <read_first>
    - motokey-api.js (route unitaire de la Tâche 1 comme modèle direct)
    - supabase.js L1314-1334 (Consommables.upsert)
    - tests/test-km-photos-cloudinary.js (section CONSO-01 bulk)
  </read_first>
  <action>
    Ajouter la route bulk JSON, réutilisant exactement le même gating/validation que la Tâche 1, en bouclant côté backend (D-04, aucune logique dupliquée — toujours Consommables.upsert) :
    ```javascript
    if ((p = M('POST','/motos/:id/consommables')) !== null) {
      const a = authSilent(req);
      if (!a && !req.ctx) return fail(res,'Non authentifié',401,'UNAUTHORIZED');
      const ctx = req.ctx || (SBLayer ? await rbac.inferLegacyRole(a.id, SBLayer) : {role:'CONCESSION',level:4,user_id:null,email:null,client_type:null});
      if (!rbac.requireRole(ctx,'MECANO')) return fail(res,'Permission refusée — MECANO minimum requis',403,'FORBIDDEN_ROLE');
      const items = Array.isArray(b.consommables) ? b.consommables : (Array.isArray(b) ? b : null);
      if (!items || !items.length) return fail(res, 'tableau consommables requis', 400, 'VALIDATION_ERROR');
      const bad = items.find(it => !it || !SBLayer.TYPES_CONSOMMABLES.includes(it.type_consommable));
      if (bad) return fail(res, 'type_consommable invalide dans le tableau: '+(bad&&bad.type_consommable), 400, 'VALIDATION_ERROR');
      const r = await resolveMotoForCtx(ctx, p.id, a);
      if (!r) return fail(res,'Moto non trouvée',404,'NOT_FOUND');
      try {
        const rows = [];
        for (const it of items) { rows.push(await SBLayer.Consommables.upsert(p.id, { type_consommable: it.type_consommable, km_montage: it.km_montage, date_montage: it.date_montage, reference: it.reference })); }
        return ok(res, { consommables: rows }, rows.length+' consommables enregistrés', 201);
      } catch (e) { return fail(res, e.message, 500, 'SERVER_ERROR'); }
    }
    ```
    IMPORTANT : valider TOUS les types AVANT la première écriture (pas de upsert partiel si un type est invalide). Éditer via l'outil Edit.

    Remplir la SECTION CONSO-01 (partie bulk) : login garage → POST /motos/:id/consommables {consommables:[{type_consommable:'pneu_av',km_montage:0},{type_consommable:'pneu_ar',km_montage:0}]} → check 200/201 et longueur du tableau renvoyé ; POST avec un type invalide dans le tableau → check 400.
  </action>
  <verify>
    <automated>node --check motokey-api.js && node -e "const fs=require('fs'); const s=fs.readFileSync('motokey-api.js','utf8'); ['M(\'POST\',\'/motos/:id/consommables\')','for (const it of items)','TYPES_CONSOMMABLES.includes(it.type_consommable)'].forEach(x=>{if(!s.includes(x))throw new Error('manquant: '+x)}); console.log('OK CONSO-01 bulk');"</automated>
  </verify>
  <acceptance_criteria>
    - motokey-api.js contains la route `M('POST','/motos/:id/consommables')`
    - Le handler valide chaque type via TYPES_CONSOMMABLES AVANT toute écriture (400 si un seul invalide)
    - Le handler boucle sur `Consommables.upsert` (aucune logique métier dupliquée — D-04)
    - Gate `requireRole(ctx,'MECANO')`
    - tests/test-km-photos-cloudinary.js : assertion bulk 200/201 + assertion type invalide 400
    - `node --check motokey-api.js` exits 0 ; slice CONSO-01 verte (serveur up)
  </acceptance_criteria>
  <done>POST bulk consommables fonctionne : MECANO+ upsert plusieurs types en une requête, validation stricte pré-écriture, délégation à Consommables.upsert.</done>
</task>

</tasks>

<verification>
- `node --check motokey-api.js` OK.
- Avec serveur up : section CONSO-01 (unit + bulk) de `tests/test-km-photos-cloudinary.js` verte.
- Type invalide → 400 propre (pas d'erreur Postgres brute).
</verification>

<success_criteria>
- PATCH unitaire + POST bulk livrés, MECANO+ only, validés via TYPES_CONSOMMABLES, délégant à Consommables.upsert (D-04).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-04-SUMMARY.md`
</output>
