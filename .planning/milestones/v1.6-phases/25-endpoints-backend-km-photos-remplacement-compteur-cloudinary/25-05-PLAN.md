---
phase: 25-endpoints-backend-km-photos-remplacement-compteur-cloudinary
plan: 05
type: execute
wave: 4
depends_on: ["25-01", "25-03", "25-04"]
files_modified:
  - motokey-api.js
  - tests/test-km-photos-cloudinary.js
autonomous: true
requirements: [CONSO-03, CLOUD-01]
user_setup:
  - service: cloudinary
    why: "L'assertion CLOUD-01 (round-trip réel) et l'usage prod exigent les credentials — absents en local/Railway au moment du plan (note #11 planning)"
    env_vars:
      - name: CLOUDINARY_CLOUD_NAME
        source: "Cloudinary Dashboard → Settings → Account"
      - name: CLOUDINARY_API_KEY
        source: "Cloudinary Dashboard → Settings → API Keys"
      - name: CLOUDINARY_API_SECRET
        source: "Cloudinary Dashboard → Settings → API Keys"
    dashboard_config:
      - task: "Provisionner les 3 vars en local (.env) puis sur Railway (service motokey1.1) pour activer CLOUD-01 en prod"
        location: "https://cloudinary.com + Railway Dashboard"

must_haves:
  truths:
    - "Un CLIENT (sa moto) ou un membre garage (MECANO+) peut uploader une photo de consommable en multipart → 200, photo historisée (CONSO-03)"
    - "L'ordre est : gate+ownership → multer(taille/MIME) → upload Cloudinary → (D-05) auto-création de la ligne consommable si absente → analyzePhoto → PhotosConsommables.insert"
    - "Si aucune ligne consommable n'existe pour le type, elle est auto-créée via Consommables.upsert(moto_id,{type_consommable,km_montage:null}) AVANT le lien consommable_id (D-05) — jamais consommable_id NULL sur la photo"
    - "photo_url stocké = secure_url réel renvoyé par Cloudinary ; sans credentials l'endpoint renvoie 503 (D-02), jamais un placeholder"
    - "L'analyse (stub) est attachée : analyse_ia (contrat pct_usure/etat/confiance/analyse_status/engine) + analyse_status"
    - "L'assertion CLOUD-01 round-trip réel passe si CLOUDINARY_* présent, sinon SKIP avec warning explicite (jamais silencieux, jamais échec de suite)"
  artifacts:
    - path: "motokey-api.js"
      provides: "POST /motos/:id/photos-consommables (multipart) + intercept pré-body()"
      contains: "photos-consommables"
  key_links:
    - from: "POST /motos/:id/photos-consommables"
      to: "cloudinaryService.uploadPhoto → analyzePhoto → PhotosConsommables.insert"
      via: "pipeline upload → analyse → persistance"
      pattern: "PhotosConsommables.insert"
    - from: "handler photo (D-05)"
      to: "Consommables.upsert"
      via: "auto-création ligne consommable si absente avant le lien consommable_id"
      pattern: "Consommables.upsert"
---

<objective>
Livrer l'endpoint d'upload de photo consommable (CONSO-03) — le premier endpoint multipart à chaîne complète : upload Cloudinary réel (CLOUD-01) → analyse stub → persistance historisée, avec auto-création de la ligne consommable si le mécano n'a pas encore fait sa saisie (D-05). Compléter les assertions CONSO-03 + CLOUD-01 (round-trip réel skippable) et documenter la dépendance credentials.

Purpose: Boucle produit clé — une photo devient une preuve stockée + une analyse d'usure, socle des jauges Phase 27/28.
Output: 1 route multipart + intercept pré-body() + assertions CONSO-03/CLOUD-01 + note credentials.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-CONTEXT.md
@.planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-RESEARCH.md
@.planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-VALIDATION.md

<interfaces>
Intercept multipart AVANT body() (motokey-api.js, pattern posé en 25-03, précédent /stripe/webhook L517-552) — ajouter un 3e intercept :
```javascript
if (method === 'POST' && _ct.startsWith('multipart/form-data') && /^\/motos\/[^/]+\/photos-consommables$/.test(pathname)) {
  req.ctx = await rbac.extractRoleFromRequest(req, SBLayer);
  return handlePhotoConsommable(req, res, pathname.split('/')[2]);
}
```
runMulter(req,res) → req.file {buffer,mimetype,size} + req.body (champs texte). (posé en 25-03)
resolveMotoForCtx(ctx, motoId, a) → {moto, garage_id, acteur_type, acteur_id} | null. (posé en 25-03)
cloudinaryService.uploadPhoto(buffer,{folder}) → {secure_url,...} | throw {statusCode,code}. (25-01)
TYPES_CONSOMMABLES (SBLayer, 25-01).

analyzePhoto (services/visionAnalysisService.js — contrat verrouillé Phase 24) :
```javascript
await analyzePhoto({ photoUrl, consommableId, typeConsommable, kmActuel, kmMontage })
// → { pct_usure:int0-100, etat:'bon'|'moyen'|'usé'|'critique', confiance:int0-100, analyse_status:'ok'|'incertain', engine:'stub' }
```
Consommables (supabase.js) : `upsert(moto_id,{type_consommable,km_montage,date_montage,reference})`, `listByMoto(moto_id)`.
PhotosConsommables.insert (supabase.js L1355) :
```javascript
await SBLayer.PhotosConsommables.insert({ moto_id, consommable_id, type_consommable, photo_url, analyse_ia, analyse_status })
// photo_url REQUIS (⇒ upload Cloudinary AVANT) ; consommable_id nullable au schéma mais D-05 impose non-null
```
Erreurs multer : `multer.MulterError` code 'LIMIT_FILE_SIZE' → 400 FILE_TOO_LARGE ; file undefined (MIME refusé/champ manquant) → 400.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POST /motos/:id/photos-consommables (multipart, upload→analyse→persistance, D-05)</name>
  <files>motokey-api.js</files>
  <read_first>
    - motokey-api.js (intercepts multipart + runMulter + resolveMotoForCtx posés en 25-03 ; body() L554 ; ok/fail L368-373 ; require multer/cloudinaryService)
    - services/visionAnalysisService.js L1-40 (contrat analyzePhoto verrouillé)
    - supabase.js L1314-1381 (Consommables.upsert/listByMoto + PhotosConsommables.insert)
    - .planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-RESEARCH.md ("Pattern 3" route usage + Anti-Patterns + Pitfalls)
  </read_first>
  <action>
    Éditer motokey-api.js via l'outil Edit (jamais PowerShell/sed — règle CLAUDE.md).

    1) Ajouter `const { analyzePhoto } = require('./services/visionAnalysisService');` en haut (si pas déjà requis).

    2) Ajouter le 3e intercept multipart AVANT `let b = await body(req);` (à côté des 2 intercepts km de 25-03) :
    ```javascript
    if (method === 'POST' && _ct.startsWith('multipart/form-data') && /^\/motos\/[^/]+\/photos-consommables$/.test(pathname)) {
      req.ctx = await rbac.extractRoleFromRequest(req, SBLayer);
      return handlePhotoConsommable(req, res, pathname.split('/')[2]);
    }
    ```

    3) Écrire `async function handlePhotoConsommable(req, res, motoId)` avec cet ORDRE STRICT (RESEARCH.md "Order of operations") :
    - (a) ctx : `const a = authSilent(req); if (!a && !req.ctx) return fail(res,'Non authentifié',401,'UNAUTHORIZED'); const ctx = req.ctx || (SBLayer ? await rbac.inferLegacyRole(a.id, SBLayer) : {role:'CONCESSION',level:4,user_id:null,email:null,client_type:null});`
    - (b) multer parse (valide taille/MIME AVANT Cloudinary — D-03) :
      ```javascript
      let file;
      try { file = await runMulter(req, res); }
      catch (e) { if (e instanceof multer.MulterError && e.code === 'LIMIT_FILE_SIZE') return fail(res,'Photo trop volumineuse (max 5 Mo)',400,'FILE_TOO_LARGE'); return fail(res, e.message, 400, 'UPLOAD_PARSE_ERROR'); }
      if (!file) return fail(res, 'Photo requise (champ multipart "photo", JPEG/PNG/WebP)', 400, 'VALIDATION_ERROR');
      ```
    - champs texte : `const type = (req.body && req.body.type_consommable) || null; if (!type || !SBLayer.TYPES_CONSOMMABLES.includes(type)) return fail(res,'type_consommable invalide',400,'VALIDATION_ERROR');`
    - ownership : `const r = await resolveMotoForCtx(ctx, motoId, a); if (!r) return fail(res,'Moto non trouvée',404,'NOT_FOUND');`
    - (c) upload Cloudinary → secure_url :
      ```javascript
      let secure_url;
      try { const up = await cloudinaryService.uploadPhoto(file.buffer, { folder: 'motokey/consommables/'+motoId }); secure_url = up.secure_url; }
      catch (e) { return fail(res, e.message, e.statusCode || 500, e.code || 'UPLOAD_ERROR'); } // D-02: 503 si non configuré, jamais placeholder
      ```
    - (d) D-05 : trouver/auto-créer la ligne consommable pour ce type AVANT de lier consommable_id :
      ```javascript
      let conso = (await SBLayer.Consommables.listByMoto(motoId)).find(c => c.type_consommable === type);
      if (!conso) { conso = await SBLayer.Consommables.upsert(motoId, { type_consommable: type, km_montage: null }); }
      ```
    - (e) analyse stub (synchrone) :
      ```javascript
      const kmActuel = r.moto ? (r.moto.km ?? null) : null;
      const analyse = await analyzePhoto({ photoUrl: secure_url, consommableId: conso.id, typeConsommable: type, kmActuel, kmMontage: conso.km_montage ?? null });
      ```
    - (f) persistance :
      ```javascript
      const photo = await SBLayer.PhotosConsommables.insert({ moto_id: motoId, consommable_id: conso.id, type_consommable: type, photo_url: secure_url, analyse_ia: analyse, analyse_status: analyse.analyse_status });
      ```
    - (g) réponse : `return ok(res, { photo, analyse, consommable: conso }, 'Photo consommable enregistrée', 201);`
    - Enrober d'un try/catch global → `fail(res, e.message, 500, 'SERVER_ERROR')`.

    Vérifier `node --check motokey-api.js`.
  </action>
  <verify>
    <automated>node --check motokey-api.js && node -e "const fs=require('fs'); const s=fs.readFileSync('motokey-api.js','utf8'); ['async function handlePhotoConsommable','/motos/[^/]+/photos-consommables','uploadPhoto(file.buffer','analyzePhoto({','PhotosConsommables.insert','Consommables.upsert(motoId, { type_consommable: type, km_montage: null'].forEach(x=>{if(!s.includes(x))throw new Error('manquant: '+x)}); const iU=s.indexOf('uploadPhoto(file.buffer'), iA=s.indexOf('analyzePhoto({'), iP=s.indexOf('PhotosConsommables.insert'); if(!(iU<iA&&iA<iP))throw new Error('ordre upload→analyse→insert non respecté'); console.log('OK CONSO-03 pipeline + D-05');"</automated>
  </verify>
  <acceptance_criteria>
    - motokey-api.js contains `async function handlePhotoConsommable` et l'intercept multipart `photos-consommables` (placé AVANT `let b = await body(req)`)
    - Le pipeline respecte l'ordre : `uploadPhoto(file.buffer` AVANT `analyzePhoto(` AVANT `PhotosConsommables.insert` (vérifié par index)
    - D-05 : auto-création via `Consommables.upsert(motoId, { type_consommable: type, km_montage: null` quand la ligne est absente ; `consommable_id: conso.id` jamais null
    - multer valide taille/MIME avant Cloudinary (LIMIT_FILE_SIZE → 400 FILE_TOO_LARGE)
    - Erreur Cloudinary non configurée propagée en 503 (e.statusCode) — jamais un placeholder (D-02)
    - `node --check motokey-api.js` exits 0
  </acceptance_criteria>
  <done>L'upload photo consommable est complet : gate/ownership → multer → Cloudinary → auto-création consommable (D-05) → analyse stub → persistance historisée, 503 si Cloudinary absent.</done>
</task>

<task type="auto">
  <name>Task 2: Assertions CONSO-03 + CLOUD-01 (round-trip skippable) + note credentials</name>
  <files>tests/test-km-photos-cloudinary.js, motokey-api.js</files>
  <read_first>
    - tests/test-km-photos-cloudinary.js (sections CONSO-03 + CLOUD-01 stub, helper multipart + garde CLOUDINARY_* de 25-02)
    - motokey-api.js (handlePhotoConsommable de la Tâche 1 — forme de réponse {photo, analyse, consommable})
    - .planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <action>
    Remplir la SECTION CONSO-03 du harnais (utilise la fixture `tests/fixtures/sample.jpg` + helper multipart de 25-02) :
    - login garage → POST multipart /motos/:id/photos-consommables (champ `photo` = fixture, `type_consommable` = 'chaine' sur une moto SANS ligne consommable préalable pour ce type) :
      - Si CLOUDINARY_* absent : check que la réponse est **503 CLOUDINARY_NOT_CONFIGURED** (prouve D-02 : pas de placeholder) — c'est un PASS attendu en local sans creds.
      - Si CLOUDINARY_* présent (garde `cloudReady`) : check 200/201 + présence `data.photo.photo_url` (URL https cloudinary) + `data.photo.consommable_id` non null (D-05) + `data.analyse.engine === 'stub'` + `data.analyse.analyse_status` ∈ {ok,incertain}.
    - login client (sa moto) → POST multipart → même logique (autorisé CONSO-03).
    - check type invalide → 400 ; check fichier manquant → 400.

    Remplir la SECTION CLOUD-01 (garde `cloudReady` déjà posée en 25-02) :
    - Si `cloudReady` : assertion que le `photo_url` renvoyé commence par `https://res.cloudinary.com/` (round-trip réel réussi) — réutiliser la réponse de la section CONSO-03 ou refaire un upload dédié.
    - Sinon : `console.warn('⚠️  [CLOUD-01] SKIP round-trip réel — CLOUDINARY_* absent (à provisionner par Mehdi). NON silencieux — voir 25-05-SUMMARY.');` (déjà amorcé en 25-02, confirmer qu'il reste explicite).

    Dans le 25-05-SUMMARY.md (créé à la fin), documenter explicitement (note #11) :
    - Cloudinary n'est PAS configuré localement ni sur Railway au moment de l'exécution — dépendance externe.
    - Manual-Only (VALIDATION.md) : (1) vérifier l'image sur le dashboard Cloudinary dossier `motokey/consommables/...` après provisionnement ; (2) poser les 3 env vars Railway (service motokey1.1) puis re-rejouer le test contre l'URL déployée.
    Ne PAS bloquer la complétion de la phase sur la présence des credentials (autonomous — pas de checkpoint).
  </action>
  <verify>
    <automated>node --check tests/test-km-photos-cloudinary.js && node -e "const fs=require('fs'); const t=fs.readFileSync('tests/test-km-photos-cloudinary.js','utf8'); ['photos-consommables','sample.jpg','type_consommable','cloudReady'].forEach(x=>{if(!t.includes(x))throw new Error('manquant: '+x)}); if(!/CLOUDINARY_NOT_CONFIGURED|503/.test(t))throw new Error('assertion D-02 (503 sans creds) absente'); if(!/res\\.cloudinary\\.com/.test(t))throw new Error('assertion round-trip CLOUD-01 absente'); console.log('OK CONSO-03 + CLOUD-01 assertions');"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test-km-photos-cloudinary.js section CONSO-03 : POST multipart avec `sample.jpg` + `type_consommable`, assertion 503 (sans creds, D-02) OU 200 avec photo_url + consommable_id non null + analyse.engine='stub' (avec creds)
    - Section CONSO-03 : assertion type invalide → 400, fichier manquant → 400
    - Section CLOUD-01 : assertion `res.cloudinary.com` sous garde `cloudReady`, sinon warning SKIP explicite
    - `node --check tests/test-km-photos-cloudinary.js` exits 0
    - 25-05-SUMMARY.md documente la dépendance credentials + les 2 vérifications manuelles (dashboard + Railway env)
  </acceptance_criteria>
  <done>Les assertions CONSO-03 + CLOUD-01 sont en place (round-trip réel skippable non silencieusement, D-02 prouvé par le 503), la dépendance credentials est documentée sans bloquer la phase.</done>
</task>

</tasks>

<verification>
- `node --check motokey-api.js` + `node --check tests/test-km-photos-cloudinary.js` OK.
- Avec serveur up et SANS creds Cloudinary : la suite complète tourne, CONSO-03 constate le 503 (D-02), CLOUD-01 SKIP avec warning.
- Avec creds : photo_url https cloudinary, consommable_id non null (D-05), analyse stub attachée.
- Suite complète : `node test-api.js && node tests/test-or-e2e.js && node tests/test-client-device-tokens.js && node tests/test-km-photos-cloudinary.js`.
</verification>

<success_criteria>
- CONSO-03 : upload photo CLIENT/garage → Cloudinary réel → analyse stub → historisation, D-05 auto-création.
- CLOUD-01 : stockage réel (secure_url), 503 sans creds (jamais placeholder), round-trip vérifiable, dépendance credentials documentée.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-05-SUMMARY.md`
</output>
