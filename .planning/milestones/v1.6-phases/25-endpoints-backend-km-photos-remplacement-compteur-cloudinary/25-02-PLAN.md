---
phase: 25-endpoints-backend-km-photos-remplacement-compteur-cloudinary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/sample.jpg
  - tests/test-km-photos-cloudinary.js
autonomous: true
requirements: [KM-02, KM-03, CONSO-01, CONSO-03, CLOUD-01]

must_haves:
  truths:
    - "tests/test-km-photos-cloudinary.js existe et couvre KM-02/KM-03/CONSO-01/CONSO-03/CLOUD-01 (sections stub prêtes à remplir)"
    - "Le script suit le style ad-hoc du repo (tests/test-or-e2e.js) : requêtes http brutes contre un serveur local + sortie ✅/❌, aucun jest/mocha"
    - "L'assertion CLOUD-01 (round-trip Cloudinary réel) est skippable avec un warning explicite quand CLOUDINARY_* est absent — jamais un skip silencieux ni un échec de suite"
    - "Une image fixture JPEG valide <5Mo existe sous tests/fixtures/ pour les requêtes multipart"
  artifacts:
    - path: "tests/test-km-photos-cloudinary.js"
      provides: "Harnais d'intégration HTTP pour les 5 requirements"
      min_lines: 60
    - path: "tests/fixtures/sample.jpg"
      provides: "Image multipart de test (<5Mo, JPEG valide)"
  key_links:
    - from: "tests/test-km-photos-cloudinary.js"
      to: "http://localhost:3000"
      via: "requêtes http brutes contre un serveur local en cours"
      pattern: "localhost"
---

<objective>
Créer le harnais de test d'intégration Phase 25 (Wave 0 de VALIDATION.md) et son image fixture, AVANT que les endpoints n'existent. Le script est un squelette exécutable avec sections stub par requirement, que les plans 25-03/04/05 rempliront au fur et à mesure.

Purpose: Nyquist — chaque endpoint aura une commande `<automated>` prête. Le repo n'a aucun framework de test ; on suit la convention ad-hoc (`tests/test-or-e2e.js`).
Output: `tests/test-km-photos-cloudinary.js` + `tests/fixtures/sample.jpg`.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-VALIDATION.md
@.planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-RESEARCH.md

<interfaces>
Style de référence (tests/test-or-e2e.js) : `'use strict'` ; helper `check(label, cond, detail)` qui incrémente OK/KO et log ✅/❌ ; `async function run()` ; `process.exit(KO>0?1:0)` en fin.

Serveur : `http://localhost:3000` (PORT défaut dans motokey-api.js). Le script suppose un `node motokey-api.js` déjà lancé dans un autre terminal ; s'il n'est pas joignable, afficher un message clair et exit non-zéro.

Auth : obtenir des tokens via `POST /auth/login` (chemin Supabase renvoie un token de session + rbac_role). Credentials seed connus (voir motokey-api.js root endpoint) : garage `garage@motokey.fr / motokey2026`, client `sophie@email.com / client123`. Pour un token MECANO/PRO distinct, réutiliser le login garage (rbac_role CONCESSION ⇒ PRO+ satisfait) ; noter en commentaire que des tokens de rôles fins pourront être ajoutés quand les comptes seed existeront.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter l'image fixture multipart</name>
  <files>tests/fixtures/sample.jpg</files>
  <read_first>
    - tests/test-or-e2e.js (confirmer qu'aucun test existant ne fait d'upload — la fixture est nouvelle)
  </read_first>
  <action>
    Créer un JPEG valide minimal (<5Mo, bien < la limite D-03) sous `tests/fixtures/sample.jpg`. Générer via Node (pas de binaire externe) :
    écrire un petit JPEG valide. Approche simple : générer un buffer JPEG minimal valide programmatiquement, ex. un script Node one-shot qui écrit les octets d'un JPEG 1x1 (en-tête SOI FFD8, segment APP0/JFIF, quantization/huffman minimal, EOI FFD9) — OU télécharger n'importe quelle petite image n'est pas permis hors repo. Le plus fiable : écrire un buffer JPEG 1x1 connu-valide via `fs.writeFileSync('tests/fixtures/sample.jpg', Buffer.from('<hex JPEG 1x1 valide>','hex'))`.
    Vérifier que le fichier commence par les octets magiques JPEG `FF D8 FF` et se termine par `FF D9`, et que sa taille est >0 et <5Mo.
    Créer le dossier `tests/fixtures/` s'il n'existe pas.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const b=fs.readFileSync('tests/fixtures/sample.jpg'); if(b.length===0||b.length>5*1024*1024) throw new Error('taille invalide: '+b.length); if(!(b[0]===0xFF&&b[1]===0xD8&&b[2]===0xFF)) throw new Error('pas un JPEG (magic bytes)'); if(!(b[b.length-2]===0xFF&&b[b.length-1]===0xD9)) throw new Error('JPEG sans marqueur EOI'); console.log('OK JPEG '+b.length+' bytes');"</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/sample.jpg existe
    - Les 3 premiers octets sont FF D8 FF (magic JPEG) et les 2 derniers FF D9 (EOI)
    - Taille > 0 et < 5 Mo
  </acceptance_criteria>
  <done>Une image JPEG valide <5Mo est disponible comme fixture multipart.</done>
</task>

<task type="auto">
  <name>Task 2: Créer le squelette tests/test-km-photos-cloudinary.js</name>
  <files>tests/test-km-photos-cloudinary.js</files>
  <read_first>
    - tests/test-or-e2e.js (style complet : 'use strict', helper check(), run(), sortie ✅/❌, process.exit)
    - .planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-VALIDATION.md (Per-Task Verification Map — les 5 requirements à couvrir)
  </read_first>
  <action>
    Créer `tests/test-km-photos-cloudinary.js`, script Node ad-hoc (aucun framework), style `tests/test-or-e2e.js` :
    - `'use strict';` en tête.
    - Helper `check(label, cond, detail='')` incrémentant `OK`/`KO` global et loggant `✅`/`❌`.
    - Helper `request(method, path, { token, json, multipart })` faisant une requête `http` brute vers `http://localhost:3000${path}`, header `Authorization: Bearer ${token}` si fourni, renvoyant `{ status, body }` (body JSON parsé). Pour `multipart`, construire un corps `multipart/form-data` avec la fixture `tests/fixtures/sample.jpg` sous le champ `photo` (frontière boundary manuelle — pas de dépendance form-data ; ou utiliser un helper de construction de boundary inline).
    - Helper `login(email, password)` → `POST /auth/login`, retourne le token de session.
    - `async function run()` avec des SECTIONS clairement délimitées par des commentaires, une par requirement, chacune stub pour l'instant (log `⏭️  [KM-02] à implémenter en 25-03` etc.) :
      - `// ─── KM-03 (relevé km normal) ───` (rempli en 25-03)
      - `// ─── KM-02 (remplacement compteur) ───` (rempli en 25-03)
      - `// ─── CONSO-01 (saisie consommables) ───` (rempli en 25-04)
      - `// ─── CONSO-03 (photo consommable) ───` (rempli en 25-05)
      - `// ─── CLOUD-01 (round-trip Cloudinary réel) ───` : dès maintenant, poser la garde de skip — `const cloudReady = !!(process.env.CLOUDINARY_CLOUD_NAME && process.env.CLOUDINARY_API_KEY && process.env.CLOUDINARY_API_SECRET); if(!cloudReady){ console.warn('⚠️  [CLOUD-01] SKIP round-trip réel — CLOUDINARY_* absent en local (à provisionner par Mehdi). NON silencieux.'); }` — le reste rempli en 25-05.
    - Au démarrage de `run()`, faire un ping (ex. `GET /` ) ; si le serveur n'est pas joignable → `console.error('❌ Serveur non joignable sur :3000 — lance `node motokey-api.js`')` puis `process.exit(1)`.
    - En fin : `console.log(`\n${OK} OK / ${KO} KO`); process.exit(KO>0?1:0);`.
    Le script doit tourner (exit 0) même avec toutes les sections en stub, tant que le serveur répond.
  </action>
  <verify>
    <automated>node --check tests/test-km-photos-cloudinary.js && node -e "const fs=require('fs'); const s=fs.readFileSync('tests/test-km-photos-cloudinary.js','utf8'); ['KM-03','KM-02','CONSO-01','CONSO-03','CLOUD-01'].forEach(r=>{if(!s.includes(r))throw new Error('section manquante: '+r)}); if(!s.includes('CLOUDINARY_CLOUD_NAME'))throw new Error('garde de skip CLOUD-01 absente'); if(!/localhost/.test(s))throw new Error('pas de cible localhost'); console.log('OK squelette 5 sections + garde skip');"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check tests/test-km-photos-cloudinary.js` exits 0
    - Le fichier contient les 5 marqueurs de section : KM-03, KM-02, CONSO-01, CONSO-03, CLOUD-01
    - Le fichier contient la garde `CLOUDINARY_CLOUD_NAME` (skip non silencieux CLOUD-01)
    - Le fichier contient un helper `check(` et une cible `localhost:3000`
    - Le fichier contient un helper de requête multipart utilisant `tests/fixtures/sample.jpg`
  </acceptance_criteria>
  <done>Le harnais existe, syntaxiquement valide, avec 5 sections stub + garde de skip CLOUD-01, prêt à être rempli par 25-03/04/05.</done>
</task>

</tasks>

<verification>
- `node --check tests/test-km-photos-cloudinary.js` OK.
- La fixture JPEG est valide (<5Mo, magic bytes).
- Les 5 sections de requirement + la garde skip CLOUD-01 sont présentes.
</verification>

<success_criteria>
- Harnais exécutable (exit 0 en stub avec serveur up), 5 sections couvrant KM-02/KM-03/CONSO-01/CONSO-03/CLOUD-01.
- Fixture image valide disponible.
- CLOUD-01 round-trip skippable avec warning explicite (jamais silencieux).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-02-SUMMARY.md`
</output>
