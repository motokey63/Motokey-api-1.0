---
phase: 27-ui-web-garage-client-jauges-retrait-pneus-legacy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/test-consommables-jauges.js
autonomous: true
requirements: [GAUGE-01, GAUGE-02, CONSO-04]
must_haves:
  truths:
    - "Running `node scripts/test-consommables-jauges.js --case=jauge-generale-logic` exercises the real pure weakest-link function once it exists"
    - "The script exposes exactly 5 named cases: jauge-generale-logic, endpoint-shape, frontend-structure, migration, dead-code-removed"
    - "Structural cases run with no DB/network; live cases (endpoint-shape, migration) skip gracefully when server/FRESH_DB_URL is absent instead of hard-failing"
  artifacts:
    - path: "scripts/test-consommables-jauges.js"
      provides: "Nyquist per-task verification harness for Phase 27 (GAUGE-01/02, CONSO-04)"
      contains: "--case="
      min_lines: 120
  key_links:
    - from: "scripts/test-consommables-jauges.js"
      to: "services/jaugeConsommables.js"
      via: "require of computeJaugeGenerale in jauge-generale-logic case"
      pattern: "require.*jaugeConsommables"
---

<objective>
Create the Wave 0 test harness `scripts/test-consommables-jauges.js` that every downstream Phase 27 task references as its automated `<verify>` command. This is mandated by 27-VALIDATION.md before any implementation task can claim Nyquist coverage.

Purpose: Give plans 27-02/27-03/27-04 a real, fast, mostly-DB-free feedback loop (grep-based structural checks + one pure-function unit check + two optional live checks).
Output: One new hand-rolled Node test script following the `assert()` / `--case=` convention of `scripts/test-consommables-crud.js`.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-ui-web-garage-client-jauges-retrait-pneus-legacy/27-VALIDATION.md
@.planning/phases/27-ui-web-garage-client-jauges-retrait-pneus-legacy/27-RESEARCH.md

# Model the new script on this existing one (assert/--case= convention, FRESH_DB_URL guard):
@scripts/test-consommables-crud.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/test-consommables-jauges.js with 5 cases (RED baseline)</name>
  <read_first>
    - scripts/test-consommables-crud.js (copy the assert(), CASE_FILTER, PASS/FAIL counter, exit-code, and FRESH_DB_URL host-only-logging conventions verbatim — never log the connection string in clear)
    - .planning/phases/27-ui-web-garage-client-jauges-retrait-pneus-legacy/27-VALIDATION.md (the Per-Task Verification Map defines the exact 5 case names)
  </read_first>
  <action>
Create `scripts/test-consommables-jauges.js`. Reuse verbatim from `scripts/test-consommables-crud.js`: the `'use strict'` header, `let OK=0; let KO=0;`, the `assert(caseName,label,condition,detail)` helper, the `CASE_FILTER` parser (`--case=`), a final summary print, and `process.exit(KO>0?1:0)`. Each case block must run only when `CASE_FILTER===null || CASE_FILTER===<name>`.

Implement exactly these 5 cases. Because implementation lands in later waves, these cases are expected to FAIL (RED) when run now — that is correct.

1. `jauge-generale-logic` (pure, NO DB, NO network):
   `const { computeJaugeGenerale } = require('../services/jaugeConsommables');` inside a `try/catch` so a missing module reports one clean FAIL rather than crashing. Assert:
   - `computeJaugeGenerale([])` returns `null` (D-04: no data → null).
   - Given items `[{has_data:false,pct_usure:null},{has_data:false,pct_usure:null}]`, returns `null` (D-03/D-04: "Non renseigné" excluded, so effectively no data → null).
   - Given `[{type_consommable:'chaine',has_data:true,pct_usure:20,etat:'bon'},{type_consommable:'pneu_av',has_data:true,pct_usure:70,etat:'usé'},{type_consommable:'huile_moteur',has_data:false,pct_usure:null,etat:null}]`, returns the object with `pct_usure===70` and `type_consommable==='pneu_av'` (max pct_usure among has_data, "Non renseigné" excluded — NOT the average which would be 45).
   - Given `[{has_data:true,pct_usure:10,etat:'bon'},{has_data:true,pct_usure:90,etat:'critique'}]`, the returned object has `etat==='critique'` (proves worst-state = max pct_usure, never a mean).

2. `endpoint-shape` (hybrid — structural always runs; live only if reachable):
   - Structural: read `motokey-api.js` as text, assert it contains the literal route registration `'/motos/:id/consommables'` AND that the same handler references `resolveMotoForCtx` (grep both substrings; assert their nearest occurrences are within ~1500 chars of each other, or simply assert both present).
   - Live (optional): if `process.env.JAUGES_TEST_BASE_URL` and `process.env.JAUGES_TEST_TOKEN` and `process.env.JAUGES_TEST_MOTO_ID` are all set, `fetch` `GET {BASE}/motos/{MOTO_ID}/consommables` with `Authorization: Bearer {TOKEN}`, assert HTTP 200 and body has `consommables` (array of length 9) and a `jauge_generale` key. If any of those env vars is missing, print `SKIP endpoint-shape (live) — set JAUGES_TEST_BASE_URL/TOKEN/MOTO_ID to enable` and do NOT increment KO.

3. `frontend-structure` (grep, NO DB):
   Read `app.html` and `MotoKey_Client.html` as text. Assert:
   - `app.html` contains `function loadConsommables` and `function renderConsommables` and the tab literal `{id:'consommables'`.
   - `app.html` does NOT contain the legacy km-usure heuristic `kmParcourus >= 8000` (the false-precision pattern must not survive).
   - `MotoKey_Client.html` contains `function uploadConsoPhoto` and a jauges marker `Consommables` (section title) and does NOT contain the identifier `pneusHtml`.

4. `migration` (live pg-direct against `FRESH_DB_URL`, skip if absent):
   If `process.env.FRESH_DB_URL` is unset, print `SKIP migration — FRESH_DB_URL not set` and do NOT increment KO (same guard as test-consommables-crud.js; never log the connection string, only the parsed host). If set: connect with `pg`, run the contents of `sql/migrations/25_migrate_pneus_to_consommables.sql` (read the file; if it does not exist yet, one clean FAIL), then assert a moto row seeded with `pneu_av`/`pneu_ar`/`pneu_km_montage` produces 2 `consommables` rows (`type_consommable IN ('pneu_av','pneu_ar')`) with matching `reference` and `km_montage`; run the SQL a SECOND time and assert the row count is unchanged (idempotent ON CONFLICT). Use a `proprietaire_type='garage'` fixture moto (RLS/CHECK requirement per STATE.md Phase 23).

5. `dead-code-removed` (grep, NO DB):
   Read `app.html` and `CLAUDE.md` as text. Assert `app.html` does NOT contain any of: `function renderPneus`, `function loadPneus`, `changerMotoPneus`, `section === 'pneus'`, `pneus:'Pneus'`. Assert `CLAUDE.md` no longer contains the stale claim that `renderPneus()` "a été supprimée" as a false statement — i.e. assert `CLAUDE.md` contains the new substring `Consommables` in the Pneus-section region (the correction lands in 27-03).

End with the standard summary line and `process.exit(KO>0?1:0)`.
  </action>
  <verify>
    <automated>node --check scripts/test-consommables-jauges.js</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/test-consommables-jauges.js` exists and `node --check scripts/test-consommables-jauges.js` exits 0
    - `grep -c "\-\-case=" scripts/test-consommables-jauges.js` is >= 1 and the file contains all five literals: `jauge-generale-logic`, `endpoint-shape`, `frontend-structure`, `migration`, `dead-code-removed`
    - File contains `require('../services/jaugeConsommables')`
    - File contains `computeJaugeGenerale`
    - Running `node scripts/test-consommables-jauges.js --case=frontend-structure` executes and exits with code 1 (RED baseline — implementation not yet present), printing at least one `FAIL frontend-structure` line
    - File contains `FRESH_DB_URL` guarded by an unset-check that prints `SKIP` (no clear-text connection string logged — assert file does NOT contain `console.log(process.env.FRESH_DB_URL`)
  </acceptance_criteria>
  <done>Test harness parses, exposes all 5 cases, structural/pure cases run offline, live cases skip cleanly, and the RED baseline is demonstrable.</done>
</task>

</tasks>

<verification>
- `node --check scripts/test-consommables-jauges.js` passes.
- `node scripts/test-consommables-jauges.js --case=frontend-structure` runs and reports FAIL (expected pre-implementation).
- No secret/connection-string is logged in clear text anywhere in the script.
</verification>

<success_criteria>
The Wave 0 harness exists with 5 cases matching 27-VALIDATION.md, is syntactically valid, runs offline for structural/pure cases, and skips live cases gracefully. Downstream plans 27-02/03/04 can reference its `--case=` commands as automated verifies.
</success_criteria>

<output>
After completion, create `.planning/phases/27-ui-web-garage-client-jauges-retrait-pneus-legacy/27-01-SUMMARY.md`.
</output>
