---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.html
  - motokey-api.js
autonomous: true
must_haves:
  truths:
    - "L'utilisateur peut se connecter avec garage@motokey.fr / motokey2026"
    - "Le tableau de bord affiche la liste des motos avec barre de recherche"
    - "L'utilisateur peut creer une moto via le formulaire Fiche moto"
    - "L'utilisateur peut ajouter une intervention via la section Entretien"
    - "La section Pneus affiche les infos pneus de chaque moto"
    - "La section Devis permet de creer un devis"
    - "L'application fonctionne sur Railway via le HTML embarque dans motokey-api.js"
  artifacts:
    - path: "app.html"
      provides: "Application MotoKey complete standalone"
      min_lines: 400
    - path: "motokey-api.js"
      provides: "Serveur API avec HTML embarque a jour"
      contains: "const _APP_HTML"
  key_links:
    - from: "app.html (JS inline)"
      to: "/auth/login"
      via: "fetch POST avec email+password"
      pattern: "fetch.*auth/login"
    - from: "app.html (JS inline)"
      to: "/motos"
      via: "fetch GET avec Authorization Bearer token"
      pattern: "fetch.*motos"
    - from: "app.html (JS inline)"
      to: "/motos/:id/interventions"
      via: "fetch POST pour ajouter intervention"
      pattern: "fetch.*interventions"
    - from: "motokey-api.js"
      to: "app.html"
      via: "const _APP_HTML = JSON.stringify du fichier"
      pattern: "_APP_HTML"
---

<objective>
Remplacer le squelette app.html par l'application MotoKey complete avec 5 sections fonctionnelles, connexion API, theme clair f0f2f5. Puis re-embarquer le HTML dans motokey-api.js et pousser sur Railway.

Purpose: Avoir une application web fonctionnelle servie directement par le serveur Node.js sur Railway.
Output: app.html complet + motokey-api.js mis a jour + git push
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

API credentials de test: garage@motokey.fr / motokey2026
API response format: { success: true, message: '...', data: {...}, timestamp: '...' }
Token: retourne dans data.token apres POST /auth/login { email, password, role: 'garage' }
Auth header: Authorization: Bearer {token}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ecrire app.html — application MotoKey complete</name>
  <files>C:/motokey-api/app.html</files>
  <action>
Ecrire un fichier HTML standalone complet (CSS inline dans style, JS inline dans script, pas de dependances externes sauf Google Fonts Inter optionnel) avec:

**Theme clair:**
- --bg: #f0f2f5 (fond principal)
- --bg2: #ffffff (cartes, surfaces)
- --text: #1a1d23 (texte)
- --border: #e2e5eb (bordures)
- --primary: #2563eb (bleu actions)
- --danger: #ef4444 (rouge)
- --success: #22c55e (vert)
- Font: system-ui, -apple-system, sans-serif

**Structure HTML:**
1. Ecran de login (visible par defaut) — formulaire email + password, bouton "Connexion", erreur affichee si echec
2. Barre de navigation horizontale (masquee avant login) — 5 boutons: Tableau de bord, Fiche moto, Entretien, Pneus, Devis
3. Zone contenu #main (masquee avant login)

**Section 1 — Tableau de bord (dashboard):**
- Barre de recherche en haut (filtre les cartes par marque/modele/plaque)
- Grille de cartes motos (display: grid, 3 colonnes desktop, 1 mobile)
- Chaque carte affiche: marque+modele, plaque, km, score MotoKey (pastille couleur), client_nom
- Clic sur une carte → navigue vers Fiche moto de cette moto
- Charge les motos via GET /motos (header Authorization: Bearer {token})
- Response shape: data.motos = [{id, marque, modele, plaque, km, score, couleur_dossier, client_nom, ...}]

**Section 2 — Fiche moto:**
- Si moto selectionnee: affiche detail (marque, modele, annee, plaque, vin, km, score, client)
- Sinon: formulaire creation moto avec champs: marque, modele, annee, plaque, vin, km, client_nom, client_email, client_tel
- Bouton "Enregistrer" → POST /motos avec body JSON (Authorization Bearer)
- Champs requis par l'API: marque, modele, plaque, vin
- Apres creation: recharger la liste dashboard, afficher confirmation

**Section 3 — Entretien:**
- Selecteur de moto (dropdown peuple depuis la liste chargee)
- Quand moto selectionnee: charger GET /motos/{id}/entretien → affiche le plan d'entretien (data.plan = [{nom, km_interval, km_derniere, statut, ...}])
- Formulaire ajout intervention: type (select: vert/bleu/jaune/rouge), titre, description, km, technicien, montant_ht
- Bouton "Ajouter" → POST /motos/{id}/interventions avec body JSON
- Champs requis: type, titre

**Section 4 — Pneus:**
- Selecteur de moto (dropdown)
- Quand moto selectionnee: charger GET /motos/{id} → affiche data.moto.pneu_actuel, pneu_km_montage
- Calculer km parcourus depuis montage = moto.km - moto.pneu_km_montage
- Afficher alerte si > 8000 km (usure estimee)
- Afficher tableau simple: pneu monte, km montage, km parcourus, statut

**Section 5 — Devis:**
- Liste des devis: GET /devis → data (array de devis)
- Formulaire creation: moto_id (dropdown), lignes (designation, qte, prix_ht), remise
- Bouton "Creer devis" → POST /devis
- Afficher liste avec reference, date, total

**JavaScript — API helper:**
```javascript
const API = window.location.origin;
let TOKEN = '';

async function api(path, method='GET', body=null) {
  const opts = { method, headers: { 'Content-Type': 'application/json' } };
  if (TOKEN) opts.headers['Authorization'] = 'Bearer ' + TOKEN;
  if (body) opts.body = JSON.stringify(body);
  const r = await fetch(API + path, opts);
  const j = await r.json();
  if (!j.success) throw new Error(j.error?.message || 'Erreur');
  return j.data;
}
```

**Login flow:**
1. POST /auth/login avec { email, password, role: 'garage' }
2. Stocker TOKEN = data.token
3. Masquer login, afficher nav + main
4. Charger dashboard automatiquement

**Style general:**
- Border-radius: 12px sur cartes, 8px sur inputs/boutons
- Box-shadow subtil sur les cartes: 0 1px 3px rgba(0,0,0,0.06)
- Padding: 24px zone principale
- Responsive: media query max-width 768px → 1 colonne
- Transitions douces sur hover (0.15s)
- Navigation: bouton actif avec background primary + texte blanc
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('C:/motokey-api/app.html','utf8'); const checks=['auth/login','motos','interventions','/devis','f0f2f5','pneu','Bearer','Tableau de bord','Fiche moto','Entretien','Pneus','Devis']; const ok=checks.every(c=>h.includes(c)); console.log(ok?'PASS: all sections present':'FAIL: missing '+checks.filter(c=>!h.includes(c)).join(', ')); process.exit(ok?0:1)"</automated>
  </verify>
  <done>app.html contient les 5 sections, le login, l'API helper avec Bearer auth, le theme f0f2f5, et toutes les fonctions saveMoto/saveInter connectees</done>
</task>

<task type="auto">
  <name>Task 2: Embarquer HTML dans motokey-api.js et git push</name>
  <files>C:/motokey-api/motokey-api.js</files>
  <action>
1. Lire app.html, generer la string JSON.stringify du contenu
2. Dans motokey-api.js, remplacer la ligne 379 (const _APP_HTML = "...";) par:
   const _APP_HTML = {JSON.stringify(contenu_app_html)};
   La ligne suivante (function getAppHTML() { return _APP_HTML; }) doit rester inchangee.

   IMPORTANT: La ligne 379 actuelle contient tout le HTML inline sur UNE seule ligne. Il faut remplacer EXACTEMENT cette ligne.
   Utiliser un script Node.js pour faire le remplacement:
   ```
   node -e "
   const fs = require('fs');
   const html = fs.readFileSync('C:/motokey-api/app.html', 'utf8');
   let api = fs.readFileSync('C:/motokey-api/motokey-api.js', 'utf8');
   const re = /^const _APP_HTML = [\s\S]*?;\s*$/m;
   api = api.replace(re, 'const _APP_HTML = ' + JSON.stringify(html) + ';');
   fs.writeFileSync('C:/motokey-api/motokey-api.js', api);
   console.log('OK: HTML embarque');
   "
   ```

3. Verifier que motokey-api.js contient le nouveau HTML
4. git add app.html motokey-api.js
5. git commit -m "Application MotoKey complete: 5 sections, theme clair, API connectee"
6. git push
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const api=fs.readFileSync('C:/motokey-api/motokey-api.js','utf8'); const has=api.includes('Tableau de bord')&&api.includes('auth/login')&&api.includes('f0f2f5'); console.log(has?'PASS: HTML embarque avec succes':'FAIL: HTML non embarque'); process.exit(has?0:1)"</automated>
  </verify>
  <done>motokey-api.js contient le HTML complet, commit pousse sur Railway, l'app est accessible via GET / ou GET /app</done>
</task>

</tasks>

<verification>
1. node -e "..." verifie que app.html contient toutes les sections et mots-cles
2. node -e "..." verifie que motokey-api.js a le HTML embarque
3. git log -1 montre le commit
4. Apres deploy Railway: ouvrir l'URL et tester login avec garage@motokey.fr / motokey2026
</verification>

<success_criteria>
- app.html est un fichier standalone avec 5 sections fonctionnelles
- Le login fonctionne avec les credentials de test
- Le dashboard charge et affiche les motos avec recherche
- saveMoto (POST /motos) et saveInter (POST /motos/:id/interventions) sont connectes
- Le HTML est embarque dans motokey-api.js (const _APP_HTML)
- Le code est pousse sur git (Railway deploie automatiquement)
</success_criteria>

<output>
After completion, create `.planning/quick/260330-i6w-remplace-app-html-par-l-application-moto/260330-i6w-SUMMARY.md`
</output>
