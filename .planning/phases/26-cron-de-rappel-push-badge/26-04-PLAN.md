---
phase: 26-cron-de-rappel-push-badge
plan: 04
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - tests/test-consommable-rappel-cron.js
autonomous: false
requirements: [GAUGE-03, GAUGE-04]
must_haves:
  truths:
    - "Les colonnes migration 24 existent réellement dans la base à laquelle le serveur local est branché (prod rzbqbaccjyxvtlnfitrr)"
    - "Les assertions d'intégration GAUGE-03/GAUGE-04 tournent RÉELLEMENT (ne sont plus skippées pour cause de colonnes absentes) et sortent vertes"
    - "La suite de régression racine reste verte (aucune régression introduite)"
  artifacts:
    - path: "tests/test-consommable-rappel-cron.js"
      provides: "suite d'intégration verte après migration"
      contains: "rappel_photo_en_retard"
  key_links:
    - from: "migration 24 appliquée en prod"
      to: "tests/test-consommable-rappel-cron.js assertions actives"
      via: "colonnes présentes → assertions non skippées"
      pattern: "km_a_la_photo"
---

<objective>
Gate de fin de phase : faire appliquer la migration 24 sur la base prod par Mehdi (via Supabase Dashboard, pas de CLI — pattern établi Phase 23/25), puis confirmer que la suite d'intégration GAUGE-03/GAUGE-04 tourne réellement au vert (assertions non skippées) et que la régression racine reste verte.

Purpose: Prouver le bout-en-bout réel (discipline v1.6 "gate = exécution réelle") — les plans précédents sont autonomes mais leurs assertions d'intégration ne peuvent passer du skip au vert qu'une fois les colonnes en base.
Output: migration 24 appliquée en prod ; suite verte confirmée ; note opérationnelle scheduler.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-cron-de-rappel-push-badge/26-CONTEXT.md
@.planning/phases/26-cron-de-rappel-push-badge/26-RESEARCH.md
@.planning/phases/26-cron-de-rappel-push-badge/26-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: appliquer la migration 24 sur la base prod (Supabase Dashboard)</name>
  <action>
    Action humaine requise (aucune CLI pour appliquer une migration sur le projet prod Supabase `rzbqbaccjyxvtlnfitrr` — pattern manuel établi Phase 23/25). Mehdi applique `sql/migrations/24_consommables_rappel_state.sql` via le SQL Editor du Dashboard.
  </action>
  <what-built>
    Migration `sql/migrations/24_consommables_rappel_state.sql` (créée plan 26-01) ajoute 3 colonnes :
    `consommables.dernier_rappel_envoye_at`, `consommables.dernier_rappel_km`, `photos_consommables.km_a_la_photo`.
    Il n'existe pas de CLI pour appliquer une migration sur le projet prod Supabase (`rzbqbaccjyxvtlnfitrr`) — l'application se fait manuellement au Dashboard, exactement comme les migrations 23/10/11/12.
  </what-built>
  <how-to-verify>
    1. Ouvrir Supabase Dashboard → projet prod `rzbqbaccjyxvtlnfitrr` → SQL Editor.
    2. Coller le contenu intégral de `sql/migrations/24_consommables_rappel_state.sql` et l'exécuter. Exécution attendue : propre, sans erreur (3 `ALTER TABLE ... ADD COLUMN` + 3 `COMMENT`).
    3. Vérifier côté Claude via sonde REST (service-role) que les colonnes répondent : un `select=dernier_rappel_envoye_at,dernier_rappel_km` sur `consommables` et `select=km_a_la_photo` sur `photos_consommables` renvoient 200 (pas d'erreur "column does not exist").
    4. Confirmer que le serveur local (`node motokey-api.js`) est branché sur cette même base prod (variables `.env` habituelles) avant de lancer les tests d'intégration.
  </how-to-verify>
  <resume-signal>Tape "migration appliquée" une fois l'exécution SQL confirmée sans erreur, ou décris l'erreur rencontrée.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Suite d'intégration réelle verte + régression</name>
  <files>tests/test-consommable-rappel-cron.js</files>
  <read_first>
    - tests/test-consommable-rappel-cron.js (assertions à faire passer réellement)
    - .planning/phases/26-cron-de-rappel-push-badge/26-VALIDATION.md (Sampling Rate + Validation Sign-Off)
  </read_first>
  <action>
    Colonnes migration 24 désormais en base. Exécuter la validation réelle (serveur local lancé dans un terminal, `PUSH_ENABLED` non défini → push dev console-log) :

    1. `node --check motokey-api.js` (convention repo CLAUDE.md) — doit passer.
    2. Lancer `node motokey-api.js` (avec `CRON_SECRET` défini dans l'env local pour que les assertions cron ne soient pas skippées) dans un terminal.
    3. `node tests/test-consommable-rappel-cron.js` dans un autre — doit sortir exit 0, et les assertions GAUGE-03/GAUGE-04 précédemment skippées pour cause de colonnes absentes doivent maintenant s'EXÉCUTER (vérifier dans la sortie qu'il n'y a plus de `⏭️  skip: ... colonne`/`CRON_SECRET`). Un skip résiduel est acceptable UNIQUEMENT pour Cloudinary 503 (credentials non provisionnés — known gap externe hérité de Phase 25) et pour l'absence de token device seed.
    4. Régression racine : `node test-api.js` — doit rester au baseline vert (9/9 par STATE.md).

    Si une assertion d'intégration échoue réellement (pas un skip), corriger la cause dans le code du plan concerné (26-02/26-03) — mais NE PAS relâcher une assertion en skip pour masquer un vrai échec.

    Note opérationnelle à consigner dans le SUMMARY (action humaine post-merge, non bloquante — miroir du flag scheduler RESEARCH.md / VALIDATION.md) : confirmer avec Mehdi si le scheduler externe (Railway cron ou équivalent) qui appelle déjà `/cron/maintenance-alerts` a besoin d'une seconde entrée planifiée pour `POST /cron/rappels-photo-consommables`. Ce n'est pas une tâche code.
  </action>
  <verify>
    <automated>node --check C:/motokey-api/motokey-api.js && echo "check OK (lancer ensuite: node motokey-api.js puis node tests/test-consommable-rappel-cron.js et node test-api.js)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check motokey-api.js` passe
    - `node tests/test-consommable-rappel-cron.js` sort exit 0 AVEC les assertions GAUGE-03 (401 mauvais secret, 200 + scanned/notified, idempotence, reset D-05) et GAUGE-04 (`rappel_photo_en_retard` présent) EXÉCUTÉES et vertes — aucun skip pour cause de colonne absente ou CRON_SECRET manquant dans la sortie
    - Skips résiduels acceptés uniquement pour Cloudinary 503 et absence de token device seed (documentés dans le SUMMARY)
    - `node test-api.js` reste au baseline (9/9, aucune régression)
    - Le SUMMARY consigne la note opérationnelle scheduler (action humaine post-merge, non bloquante)
  </acceptance_criteria>
  <done>Migration 24 en prod ; suite d'intégration GAUGE-03/GAUGE-04 réellement verte ; régression racine verte ; phase 26 prête pour /gsd:verify-phase.</done>
</task>

</tasks>

<verification>
- Migration 24 appliquée en prod (sonde REST 200 sur les 3 colonnes)
- `node tests/test-consommable-rappel-cron.js` exit 0 avec assertions actives (pas skippées) sur GAUGE-03/GAUGE-04
- `node test-api.js` baseline vert
</verification>

<success_criteria>
GAUGE-03 (push de rappel groupé, idempotent, réarmé à la photo) et GAUGE-04 (champ badge calculé pour motos garage/inconnu) vérifiés de bout en bout contre une base réelle. Seuls restent skippés les items dépendant de credentials externes non provisionnés (Cloudinary), documentés comme known gap hérité.
</success_criteria>

<output>
After completion, create `.planning/phases/26-cron-de-rappel-push-badge/26-04-SUMMARY.md`
</output>
