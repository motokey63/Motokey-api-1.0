---
phase: 25-endpoints-backend-km-photos-remplacement-compteur-cloudinary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - services/cloudinaryService.js
  - supabase.js
autonomous: true
requirements: [CLOUD-01, CONSO-01]
user_setup:
  - service: cloudinary
    why: "Stockage réel des photos compteur/consommables (CLOUD-01) — plus de placeholder (D-02)"
    env_vars:
      - name: CLOUDINARY_CLOUD_NAME
        source: "Cloudinary Dashboard → Settings → Account → Cloud name"
      - name: CLOUDINARY_API_KEY
        source: "Cloudinary Dashboard → Settings → API Keys → API Key"
      - name: CLOUDINARY_API_SECRET
        source: "Cloudinary Dashboard → Settings → API Keys → API Secret"
    dashboard_config:
      - task: "Créer un compte Cloudinary (free tier) si absent, récupérer les 3 credentials ci-dessus"
        location: "https://cloudinary.com — puis Railway env vars pour le service motokey1.1"

must_haves:
  truths:
    - "cloudinary@^2.x et multer@^2.x sont installés (multer >=2.1.1, jamais 1.x — CVE-2025-47944/CVE-2026-3520)"
    - "cloudinaryService.uploadPhoto() lève une erreur explicite statusCode=503 code=CLOUDINARY_NOT_CONFIGURED quand les credentials manquent — JAMAIS une URL placeholder (D-02)"
    - "cloudinaryService.uploadPhoto() renvoie { secure_url, public_id, ... } quand configuré"
    - "TYPES_CONSOMMABLES est un tableau unique exporté depuis supabase.js listant exactement les 9 types de la contrainte CHECK migration 23"
  artifacts:
    - path: "services/cloudinaryService.js"
      provides: "uploadPhoto(buffer,{folder}) + CLOUDINARY_READY flag, no silent fallback (D-02)"
      exports: ["uploadPhoto", "CLOUDINARY_READY"]
    - path: "supabase.js"
      provides: "TYPES_CONSOMMABLES constant (9 types) exporté"
      contains: "TYPES_CONSOMMABLES"
    - path: "package.json"
      provides: "cloudinary + multer dependencies"
      contains: "cloudinary"
  key_links:
    - from: "services/cloudinaryService.js"
      to: "process.env.CLOUDINARY_CLOUD_NAME/API_KEY/API_SECRET"
      via: "module-load config + CLOUDINARY_READY boolean"
      pattern: "CLOUDINARY_READY"
---

<objective>
Poser les fondations non-HTTP de la Phase 25 : installer les deux nouvelles dépendances (cloudinary, multer), créer le service `cloudinaryService.js` (obligatoire, sans fallback silencieux — D-02), et exposer la liste canonique des 9 types de consommables comme constante unique dans `supabase.js`.

Purpose: Tous les endpoints des plans 25-03/04/05 consomment ces trois artefacts. Les isoler ici (aucune modif de `motokey-api.js`) permet de les livrer en parallèle du harnais de test (25-02).
Output: `services/cloudinaryService.js`, `TYPES_CONSOMMABLES` dans `supabase.js`, `cloudinary`/`multer` dans `package.json`.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-CONTEXT.md
@.planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-RESEARCH.md

<interfaces>
<!-- Convention de service flag-gated à mirrorer (mais D-02: PAS de fallback silencieux) -->
Modèle de forme (services/visionAnalysisService.js) : flag calculé au chargement du module, warning au boot si non configuré.

Contrat cible de cloudinaryService (RESEARCH.md "Pattern 3", à reproduire) :
```javascript
// services/cloudinaryService.js
const cloudinary = require('cloudinary').v2;
// CLOUDINARY_READY = true seulement si CLOUD_NAME && API_KEY && API_SECRET présents
async function uploadPhoto(buffer, { folder = 'motokey' } = {}) {
  // si !CLOUDINARY_READY → throw Error avec err.statusCode=503, err.code='CLOUDINARY_NOT_CONFIGURED' AVANT tout appel SDK
  // sinon cloudinary.uploader.upload_stream({folder, resource_type:'image'}, cb).end(buffer)
  //   → resolve(result) { secure_url, public_id, ... }
  //   → sur erreur SDK: reject Error avec err.statusCode=502, err.code='CLOUDINARY_UPLOAD_FAILED'
}
module.exports = { uploadPhoto, CLOUDINARY_READY };
```

Les 9 types canoniques (sql/migrations/23_consommables_km.sql, CHECK constraint — AUTORITÉ) :
`pneu_av`, `pneu_ar`, `chaine`, `plaquettes_av`, `plaquettes_ar`, `disque_av`, `disque_ar`, `huile_moteur`, `liquide_frein`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer cloudinary + multer</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (voir les deps existantes, engines node>=20)
  </read_first>
  <action>
    Exécuter `npm install cloudinary@^2.10.0 multer@^2.2.0` depuis C:/motokey-api.
    CRITIQUE (RESEARCH.md Pitfall 3) : multer DOIT être en ^2.x (minimum 2.1.1) — multer 1.x a deux CVE non patchées (CVE-2025-47944, CVE-2026-3520). Après install, vérifier que package.json montre bien `"multer": "^2.x"` et non `^1.x`.
    Ne PAS modifier les autres dépendances. Ne PAS toucher à `express` (dépendance inutilisée, laissée en place).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const m=p.dependencies.multer||''; const c=p.dependencies.cloudinary||''; if(!c) throw new Error('cloudinary manquant'); if(!/^\^?2\./.test(m.replace('^',''))&&!/2\./.test(m)) throw new Error('multer doit etre 2.x, trouve: '+m); console.log('OK cloudinary='+c+' multer='+m);"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"cloudinary"`
    - package.json contains `"multer"` with a `2.` major version (NOT `1.`)
    - `node -e "require('cloudinary')"` exits 0
    - `node -e "require('multer')"` exits 0
  </acceptance_criteria>
  <done>Les deux packages sont dans package.json (multer en 2.x), résolus et requerables.</done>
</task>

<task type="auto">
  <name>Task 2: Créer services/cloudinaryService.js (obligatoire, sans fallback silencieux — D-02)</name>
  <files>services/cloudinaryService.js</files>
  <read_first>
    - services/visionAnalysisService.js (lignes 1-60 — convention de flag calculé au boot ; NOTE: cloudinary DIVERGE — pas de fallback silencieux, voir D-02)
    - .planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-RESEARCH.md ("Pattern 3" — code complet à reproduire)
  </read_first>
  <action>
    Créer `services/cloudinaryService.js` reproduisant RESEARCH.md "Pattern 3" :
    - `const cloudinary = require('cloudinary').v2;`
    - Lire `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET` depuis process.env (vars séparées — discrétion #12b tranchée : forme séparée, cohérente avec SUPABASE_URL/SUPABASE_SECRET_KEY du projet).
    - Calculer `CLOUDINARY_READY = !!(CLOUD_NAME && API_KEY && API_SECRET)` au chargement du module. Si prêt : `cloudinary.config({cloud_name, api_key, api_secret, secure:true})` + `console.log('✅ [25] Config Cloudinary détectée')`. Sinon : `console.warn('⚠️  [25] Cloudinary non configuré (CLOUDINARY_CLOUD_NAME/API_KEY/API_SECRET manquants)')`.
    - `async function uploadPhoto(buffer, { folder = 'motokey' } = {})` :
      - D-02 CRITIQUE : si `!CLOUDINARY_READY`, construire `const err = new Error('Cloudinary non configuré'); err.statusCode = 503; err.code = 'CLOUDINARY_NOT_CONFIGURED'; throw err;` AVANT tout appel au SDK. JAMAIS retourner d'URL placeholder (contrairement à VISION_ENABLED/EMAIL_ENABLED/PUSH_ENABLED). Un faux URL corromprait des données de preuve anti-fraude.
      - Sinon : retourner `new Promise((resolve,reject)=>{ const stream = cloudinary.uploader.upload_stream({folder, resource_type:'image'}, (error,result)=>{ if(error){ const e=new Error(error.message||'Échec upload Cloudinary'); e.statusCode=502; e.code='CLOUDINARY_UPLOAD_FAILED'; return reject(e);} resolve(result); }); stream.end(buffer); })`.
    - `module.exports = { uploadPhoto, CLOUDINARY_READY };`
    Commencer le fichier par `'use strict';`.
  </action>
  <verify>
    <automated>node -e "const s=require('./services/cloudinaryService.js'); if(typeof s.uploadPhoto!=='function') throw new Error('uploadPhoto absent'); if(!('CLOUDINARY_READY' in s)) throw new Error('CLOUDINARY_READY absent'); s.uploadPhoto(Buffer.from('x')).then(()=>{throw new Error('aurait du throw 503 sans creds')}).catch(e=>{ if(e.statusCode!==503||e.code!=='CLOUDINARY_NOT_CONFIGURED') throw new Error('mauvaise erreur: '+e.code+'/'+e.statusCode); console.log('OK D-02 respecté: '+e.code+' '+e.statusCode); });"</automated>
  </verify>
  <acceptance_criteria>
    - services/cloudinaryService.js contains `require('cloudinary').v2`
    - services/cloudinaryService.js contains `CLOUDINARY_NOT_CONFIGURED`
    - services/cloudinaryService.js contains `statusCode = 503` (ou `.statusCode=503`)
    - services/cloudinaryService.js contains `upload_stream`
    - services/cloudinaryService.js contains `module.exports = { uploadPhoto, CLOUDINARY_READY }`
    - Sans credentials, `uploadPhoto(buffer)` rejette avec code='CLOUDINARY_NOT_CONFIGURED' et statusCode=503 (jamais une URL)
  </acceptance_criteria>
  <done>Le service existe, expose uploadPhoto + CLOUDINARY_READY, et lève un 503 typé sans credentials (D-02 respecté).</done>
</task>

<task type="auto">
  <name>Task 3: Exposer TYPES_CONSOMMABLES (9 types) comme constante unique dans supabase.js</name>
  <files>supabase.js</files>
  <read_first>
    - supabase.js (lignes 1314-1345 — helper Consommables ; lignes 1637-1660 — module.exports)
    - sql/migrations/23_consommables_km.sql (contrainte CHECK sur consommables.type_consommable — source d'autorité des 9 types)
  </read_first>
  <action>
    Dans supabase.js, juste AVANT `const Consommables = {` (~ligne 1314), ajouter (édition directe via l'outil Edit, jamais PowerShell/sed — règle projet CLAUDE.md) :
    ```javascript
    // Les 9 types canoniques v1.6 — source d'autorité = contrainte CHECK migration 23.
    // Une seule copie JS pour valider côté endpoint (Pitfall 5 RESEARCH.md) ; extensible sans dupliquer.
    const TYPES_CONSOMMABLES = ['pneu_av','pneu_ar','chaine','plaquettes_av','plaquettes_ar','disque_av','disque_ar','huile_moteur','liquide_frein'];
    ```
    Puis ajouter `TYPES_CONSOMMABLES,` dans l'objet `module.exports = { ... }` (~ligne 1637), à côté de `Consommables,` / `PhotosConsommables,`.
    Ne PAS modifier la logique de `Consommables.upsert` (elle laisse la contrainte CHECK DB faire foi — la validation JS est côté endpoint, plans 25-04/05).
  </action>
  <verify>
    <automated>node -e "const sb=require('./supabase'); if(!sb) throw new Error('supabase.js a retourné null (env .env manquant?)'); const t=sb.TYPES_CONSOMMABLES; if(!Array.isArray(t)||t.length!==9) throw new Error('TYPES_CONSOMMABLES doit avoir 9 entrées, trouvé: '+(t&&t.length)); ['pneu_av','pneu_ar','chaine','plaquettes_av','plaquettes_ar','disque_av','disque_ar','huile_moteur','liquide_frein'].forEach(x=>{if(!t.includes(x))throw new Error('type manquant: '+x)}); console.log('OK 9 types');"</automated>
  </verify>
  <acceptance_criteria>
    - supabase.js contains `const TYPES_CONSOMMABLES = [`
    - supabase.js module.exports contains `TYPES_CONSOMMABLES`
    - `require('./supabase').TYPES_CONSOMMABLES` a exactement 9 éléments incluant les 9 types canoniques
    - `node --check supabase.js` exits 0
  </acceptance_criteria>
  <done>TYPES_CONSOMMABLES est exporté depuis supabase.js avec les 9 types exacts de la migration 23.</done>
</task>

</tasks>

<verification>
- `node -e "require('./services/cloudinaryService.js')"` charge sans crash (warning attendu si pas de creds).
- `node --check supabase.js` OK.
- package.json contient cloudinary + multer (2.x).
</verification>

<success_criteria>
- Les deux deps installées (multer 2.x).
- cloudinaryService lève 503 typé sans creds (D-02), renvoie secure_url si configuré.
- TYPES_CONSOMMABLES exporté (9 types).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/25-endpoints-backend-km-photos-remplacement-compteur-cloudinary/25-01-SUMMARY.md`
</output>
