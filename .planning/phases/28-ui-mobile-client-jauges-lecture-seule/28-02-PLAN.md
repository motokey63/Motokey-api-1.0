---
phase: 28-ui-mobile-client-jauges-lecture-seule
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - mobile-app/app/(app)/(tabs)/motos/[id].tsx
autonomous: false
requirements: [GAUGE-05, GAUGE-06]

must_haves:
  truths:
    - "The Fiche Moto screen fetches GET /motos/:id/consommables as a 4th parallel call in load()"
    - "The screen renders one GaugeBar per consommable item (9 types) in the received order"
    - "The screen renders a general gauge = the jauge_generale item (weakest link, never an average) near the header/score"
    - "The legacy Pneumatiques section (moto.pneu_av / moto.pneu_ar) no longer renders"
    - "Tapping a moto_entretien rappel notification opens motos/[id] where the gauges are visible (D-04, no code change)"
  artifacts:
    - path: "mobile-app/app/(app)/(tabs)/motos/[id].tsx"
      provides: "Wired consommables gauges section + general gauge, Pneumatiques removed"
      contains: "GaugeBar"
  key_links:
    - from: "mobile-app/app/(app)/(tabs)/motos/[id].tsx"
      to: "GET /motos/:id/consommables"
      via: "apiGet in load() Promise.all"
      pattern: "consommables"
    - from: "mobile-app/app/(app)/(tabs)/motos/[id].tsx"
      to: "mobile-app/components/GaugeBar.tsx"
      via: "import + map over items"
      pattern: "GaugeBar"
    - from: "mobile-app/hooks/useNotificationObserver.ts"
      to: "mobile-app/app/(app)/(tabs)/motos/[id].tsx"
      via: "mapNotificationDataToRoute moto_entretien → motos/[id]"
      pattern: "moto_entretien"
---

<objective>
Wire the consommables gauges into the existing Fiche Moto screen (`app/(app)/(tabs)/motos/[id].tsx`): add a 4th parallel fetch to `GET /motos/:id/consommables`, render a general (weakest-link) gauge near the score header, render one `GaugeBar` per consommable in a new "Usure des Consommables" section, and REMOVE the legacy Pneumatiques section (replaced by the pneu_av/pneu_ar gauges). Confirm the rappel-notification deep link lands here (D-04 — already implemented, no code change).

Purpose: This is the user-visible payoff of GAUGE-05/GAUGE-06 — the client sees the same gauges as web, read-only, and the push deep link routes to this screen.
Output: A wired `[id].tsx` consuming 28-01's `parseConsommables` + `GaugeBar` + `etatColor`/`ETAT_WORDING`.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-ui-mobile-client-jauges-lecture-seule/28-CONTEXT.md

Note: `mobile-app/CLAUDE.md` → `AGENTS.md` requires reading Expo v54 docs (https://docs.expo.dev/versions/v54.0.0/) before writing RN code. This screen uses only RN core + expo-router (already imported) — no new Expo SDK surface.

<interfaces>
<!-- Contracts produced by plan 28-01 (already on disk). Consume directly, do not reimplement. -->

From mobile-app/lib/motoParse.ts:
  export interface ConsommableJauge { type_consommable: string; km_montage?: number|null; date_montage?: string|null; reference?: string|null; pct_usure: number|null; etat: string|null; has_data: boolean; }
  export function parseConsommables(res: ApiResult): { items: ConsommableJauge[]; jaugeGenerale: ConsommableJauge | null }

From mobile-app/lib/motoDisplay.ts:
  export const CONSO_LABELS: Record<string,string>   // type_consommable → FR label
  export const ETAT_WORDING: Record<string,string>   // etat → public wording ('Très bon état'...)
  export function etatColor(etat?: string): string    // 'bon'/'moyen'/'usé'/'critique' → gn/bl/yw/rd, default bl

From mobile-app/components/GaugeBar.tsx:
  export interface GaugeBarProps { label: string; pctUsure: number|null; etat: string|null; hasData: boolean; }
  export function GaugeBar(props: GaugeBarProps)

<!-- Existing deep-link contract (Phase 16, already shipped + tested — D-04 says NO change). -->
mobile-app/hooks/useNotificationObserver.ts::mapNotificationDataToRoute
  { type:'moto_entretien', motoId } → { pathname:'/(app)/(tabs)/motos/[id]', params:{ id: String(motoId) } }
  (proven by hooks/__tests__/useNotificationObserver.test.ts Test 5 & Test 7)
consommableRappelService.js sends push data { type:'moto_entretien', motoId } (same type as MPUSH-04). No new type this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire consommables fetch + general gauge + gauges section into [id].tsx, remove Pneumatiques</name>
  <files>mobile-app/app/(app)/(tabs)/motos/[id].tsx</files>
  <read_first>
    - mobile-app/app/(app)/(tabs)/motos/[id].tsx (full file — current load()/state/JSX including the Pneumatiques block lines ~140-150 and the styles object)
    - mobile-app/lib/motoParse.ts (parseConsommables + ConsommableJauge — from 28-01)
    - mobile-app/lib/motoDisplay.ts (CONSO_LABELS, ETAT_WORDING, etatColor — from 28-01)
    - mobile-app/components/GaugeBar.tsx (props — from 28-01)
    - MotoKey_Client.html lines 636-647 (web jaugesSectionClient — general-gauge + section-title wording reference)
  </read_first>
  <action>
    Edit mobile-app/app/(app)/(tabs)/motos/[id].tsx:

    1. IMPORTS — add:
       - `import { parseInterventions, parseAlertes, fmtStatut, Moto, Intervention, Alerte, parseConsommables, ConsommableJauge } from '../../../../lib/motoParse';` (extend the existing motoParse import — parseConsommables/ConsommableJauge were added in 28-01).
       - `import { couleurColor, fmtDate, CONSO_LABELS, ETAT_WORDING, etatColor } from '../../../../lib/motoDisplay';` (extend existing motoDisplay import).
       - `import { GaugeBar } from '../../../../components/GaugeBar';`

    2. STATE — add after the `alertes` state:
       - `const [consommables, setConsommables] = useState<ConsommableJauge[]>([]);`
       - `const [jaugeGenerale, setJaugeGenerale] = useState<ConsommableJauge | null>(null);`

    3. FETCH — extend the `Promise.all` in load() to a 4th call:
       ```
       const [motoRes, ivRes, alRes, coRes] = await Promise.all([
         apiGet('/motos/' + id, token ?? undefined),
         apiGet('/motos/' + id + '/interventions', token ?? undefined),
         apiGet('/motos/' + id + '/entretien/alertes', token ?? undefined),
         apiGet('/motos/' + id + '/consommables', token ?? undefined),
       ]);
       ```
       After `setAlertes(parseAlertes(alRes));` add:
       ```
       const co = parseConsommables(coRes);
       setConsommables(co.items);
       setJaugeGenerale(co.jaugeGenerale);
       ```

    4. GENERAL GAUGE — directly under the existing `<View style={styles.headerRow}>...</View>` block (contiguous with the ScoreBadge, per D-07 intent / <specifics>), render a general-état pill:
       ```
       {jaugeGenerale ? (
         <View style={styles.generalRow}>
           <StatutBadge
             label={'État général : ' + (ETAT_WORDING[jaugeGenerale.etat ?? ''] ?? jaugeGenerale.etat ?? '')}
             color={etatColor(jaugeGenerale.etat ?? undefined)}
           />
         </View>
       ) : (
         <Text style={styles.generalEmpty}>Pas encore suivi</Text>
       )}
       ```
       NOTE the empty-state wording is `Pas encore suivi` WITHOUT the web's "— ajoutez une photo pour lancer le suivi d'usure" clause: mobile is read-only (D-04), so the "add a photo" call-to-action is intentionally dropped to avoid pointing the user at an action that does not exist on mobile. This is a deliberate, documented deviation from web wording parity.

    5. GAUGES SECTION — add a new section (place it AFTER the Historique section and BEFORE the Plan d'entretien `showAlertes` block, so wear state sits near the top of the maintenance info):
       ```
       <View style={styles.section}>
         <Text style={styles.sectionTitle}>Usure des Consommables</Text>
         {consommables.map((c) => (
           <GaugeBar
             key={c.type_consommable}
             label={CONSO_LABELS[c.type_consommable] ?? c.type_consommable}
             pctUsure={c.pct_usure}
             etat={c.etat}
             hasData={c.has_data}
           />
         ))}
       </View>
       ```
       The section title MUST be exactly `Usure des Consommables` (case-sensitive 'Consommables' marker, parity with web + Phase 27 note).

    6. REMOVE the legacy Pneumatiques block entirely: delete the `const showPneus = !!(moto.pneu_av || moto.pneu_ar);` line AND the whole `{showPneus ? ( ... Pneumatiques ... ) : null}` JSX block (lines ~140-150). The pneu_av/pneu_ar wear is now shown by the pneu_av/pneu_ar GaugeBar rows (D-02).

    7. STYLES — add to the StyleSheet.create object:
       - `generalRow: { marginTop: 8 },`
       - `generalEmpty: { marginTop: 8, fontSize: 13, color: colors.tx3 },`
       Keep the existing `section`/`sectionTitle` styles (reused by the new section).

    Do NOT touch useNotificationObserver.ts (D-04 — deep link already routes moto_entretien → motos/[id]).
  </action>
  <verify>
    <automated>cd mobile-app && npx tsc --noEmit && npx jest hooks/__tests__/useNotificationObserver.test.ts lib/__tests__/ficheMoto.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `[id].tsx` contains `/consommables'` (the 4th apiGet path)
    - `[id].tsx` contains `parseConsommables(coRes)`
    - `[id].tsx` contains `import { GaugeBar }`
    - `[id].tsx` contains `Usure des Consommables`
    - `[id].tsx` contains `État général :`
    - `[id].tsx` contains `Pas encore suivi`
    - `[id].tsx` does NOT contain `Pneumatiques` (grep returns no match)
    - `[id].tsx` does NOT contain `showPneus` (grep returns no match)
    - `cd mobile-app && npx tsc --noEmit` exits 0
    - `cd mobile-app && npx jest hooks/__tests__/useNotificationObserver.test.ts lib/__tests__/ficheMoto.test.ts` exits 0
  </acceptance_criteria>
  <done>The Fiche Moto screen fetches consommables, shows a general weakest-link gauge by the score and a 9-row gauges section, and the legacy Pneumatiques section is gone. Deep-link test still green. Typecheck clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — on-device gauges + rappel deep link</name>
  <files></files>
  <action>
    Checkpoint de vérification visuelle sur appareil — après que l'automatique (tsc + jest) est vert. Claude prépare les étapes ci-dessous ; Mehdi lance l'app et confirme le rendu des jauges + le deep link. Faire une pause pour cette revue avant de clore la phase. Aucun code à écrire dans cette tâche.
  </action>
  <what-built>
    The mobile Fiche Moto screen now shows: (1) a general "État général" gauge next to the score at the top, (2) an "Usure des Consommables" section with a horizontal gauge + wording per consommable (up to 9), (3) "Non renseigné" for consommables with no photo/analysis yet, and (4) NO more Pneumatiques section. The rappel push deep link (moto_entretien) opens this same screen (already implemented — D-04).
    Automated proof already green: `npx tsc --noEmit` (types), `npx jest` (parseConsommables, etatColor, and the moto_entretien → motos/[id] route test).
  </what-built>
  <how-to-verify>
    Run the app against prod (`cd mobile-app && npx expo start`, open on a device/simulator, log in as a CLIENT who owns at least one moto):
    1. Open a moto's Fiche. Confirm an "État général : …" pill appears next to / under the score at the top (or "Pas encore suivi" if the moto has no analysed photo yet).
    2. Scroll to the "Usure des Consommables" section. Confirm a row per consommable: label on the left, a coloured bar + wording pill (e.g. "Très bon état" / "À surveiller" / "À changer bientôt" / "À changer maintenant") on the right, or "Non renseigné" for untracked ones.
    3. Confirm there is NO "Pneumatiques" section anymore.
    4. Colour sanity: bon=vert, moyen=bleu, usé=jaune, critique=rouge — same reading as the web client.
    5. (If a rappel photo push can be triggered/received) tap the notification and confirm it opens the correct moto's Fiche where the gauges are visible. If no push is available, this is covered by the passing useNotificationObserver route test.
    Known-gap note: if prod migration `25_migrate_pneus_to_consommables.sql` is not yet applied, pneu_av/pneu_ar rows may legitimately show "Non renseigné" (D-03) — that is expected, not a bug.
  </how-to-verify>
  <verify>Manual on-device verification per the steps above; automated tsc + jest already green from Task 1.</verify>
  <done>Mehdi confirms the general gauge, the 9-row Usure des Consommables section, the absence of Pneumatiques, correct colour mapping, and (if testable) the rappel deep link landing on the moto Fiche.</done>
  <resume-signal>Type "approved" or describe what looks wrong.</resume-signal>
</task>

</tasks>

<verification>
- `cd mobile-app && npx tsc --noEmit` exits 0.
- `cd mobile-app && npx jest` (full suite) exits 0 — including the moto_entretien deep-link route test.
- grep confirms `[id].tsx` contains `GaugeBar`, `Usure des Consommables`, `/consommables'` and NO `Pneumatiques`/`showPneus`.
- Human verification of the on-device gauges + deep link (checkpoint).
</verification>

<success_criteria>
- GAUGE-05: client sees a % gauge per consommable (9 types) on the mobile Fiche Moto.
- GAUGE-06: client sees a general gauge = weakest-link item (never an average); tapping the rappel push opens this screen.
- Legacy Pneumatiques section removed (D-02), replaced by pneu_av/pneu_ar gauges.
- Read-only: no photo capture / upload added on mobile (D-04).
</success_criteria>

<output>
After completion, create `.planning/phases/28-ui-mobile-client-jauges-lecture-seule/28-02-SUMMARY.md`
</output>
