---
phase: 24-helpers-supabase-js-contrat-stub-vision
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase.js
  - scripts/test-consommables-crud.js
autonomous: true
requirements: [VISION-02]

must_haves:
  truths:
    - "Consommables.upsert() écrit une seule ligne par (moto_id, type_consommable) — une re-saisie du même type met à jour au lieu de lever un 23505"
    - "PhotosConsommables.insert() persiste à la fois consommable_id (FK) ET la colonne dénormalisée type_consommable, plus analyse_ia (JSONB) et analyse_status — les champs de sortie du contrat vision (VISION-02)"
    - "Consommables et PhotosConsommables sont exportés depuis supabase.js — unique frontière d'accès DB pour les 2 nouvelles tables"
    - "RelevesKm reste suffisant : enregistrer() existe déjà (Phase 23), aucun helper de lecture spéculatif ajouté (YAGNI, open question recherche #1 = defer)"
  artifacts:
    - path: "supabase.js"
      provides: "Helpers CRUD minces Consommables (upsert/listByMoto) + PhotosConsommables (insert/listByConsommable), ajoutés à module.exports"
      contains: "const Consommables"
    - path: "scripts/test-consommables-crud.js"
      provides: "Vérification structurelle (grep source supabase.js) + comportement pg-direct contre FRESH_DB_URL (upsert on conflict, dénormalisation)"
      contains: "Consommables"
  key_links:
    - from: "supabase.js Consommables.upsert"
      to: "consommables table"
      via: "upsert onConflict moto_id,type_consommable"
      pattern: "onConflict.*moto_id,type_consommable"
    - from: "supabase.js module.exports"
      to: "Consommables, PhotosConsommables"
      via: "ajout au bloc exports"
      pattern: "Consommables"
---

<objective>
Ajouter à `supabase.js` les helpers CRUD minces `Consommables` et `PhotosConsommables` pour les 2 tables créées en Phase 23, en suivant le style `CataloguePieces`. Confirmer que `RelevesKm` (déjà présent depuis Phase 23) suffit — aucun helper de lecture spéculatif. `supabase.js` reste l'unique frontière d'accès DB. Créer un script de vérification autonome (structurel + comportement pg-direct).

Purpose: Success criterion #4 du ROADMAP (Consommables/PhotosConsommables/RelevesKm existent comme helpers CRUD minces, seul point d'accès DB). VISION-02 : `PhotosConsommables.insert()` persiste les champs de sortie du contrat vision (analyse_ia JSONB + analyse_status), maillon de persistance du contrat consommé identiquement par les jauges.
Output: modifications additives dans `supabase.js` + `scripts/test-consommables-crud.js` (nouveau).
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-helpers-supabase-js-contrat-stub-vision/24-CONTEXT.md
@.planning/phases/24-helpers-supabase-js-contrat-stub-vision/24-RESEARCH.md
@.planning/phases/24-helpers-supabase-js-contrat-stub-vision/24-VALIDATION.md

<interfaces>
<!-- Extraits du codebase — l'exécuteur applique directement -->

Style de helper CRUD mince à suivre (supabase.js:1250 CataloguePieces) : objet littéral, méthodes async,
validation manuelle des champs requis, `throw new Error('[Contexte] message')` sur erreur Supabase,
`const { data, error } = await supabase.from('table')...select().single()`.

Client partagé déjà en portée dans supabase.js : `const supabase = createClient(SUPABASE_URL, SUPABASE_SERVICE_KEY, ...)`.

Bloc module.exports actuel (supabase.js:1565) — Consommables/PhotosConsommables ABSENTS, à ajouter :
```javascript
module.exports = {
  supabase, supabasePublic, Auth, Garages, Motos, RelevesKm, Interventions, Entretien,
  Devis, Transferts, Storage, Realtime, Fraude, OrdresReparation, OrTaches, OrPieces,
  CataloguePieces, GarageUsers, resolveProprietaire, checkLimiteMotosClient,
  cessionMoto, BillingEvents, PushSendLog
};
```

RelevesKm existe déjà (supabase.js:385) avec la seule méthode enregistrer() — NE PAS le recréer ni ajouter de méthode de lecture.

Schéma cible (migration 23, déjà en prod + dans schema.sql) :
- consommables : id, moto_id (FK NOT NULL), type_consommable (TEXT+CHECK 9 types), km_montage INT,
  date_montage DATE, reference TEXT, created_at, updated_at, UNIQUE(moto_id, type_consommable)
- photos_consommables : id, moto_id (FK NOT NULL), consommable_id (FK nullable), type_consommable (TEXT nullable, PAS de CHECK),
  photo_url (TEXT NOT NULL), analyse_ia (JSONB), analyse_status (TEXT), created_at
- 9 types : pneu_av, pneu_ar, chaine, plaquettes_av, plaquettes_ar, disque_av, disque_ar, huile_moteur, liquide_frein

Connexion pg de test (scripts/test-releves-km-trigger.js:91-115) : `process.env.FRESH_DB_URL`, refus si contient 'rzbqbaccjyxvtlnfitrr' (garde-fou anti-prod), ne logger que `new URL(connectionString).host`, `new Client({ connectionString })`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter Consommables + PhotosConsommables à supabase.js et les exporter</name>
  <files>supabase.js</files>
  <read_first>
    - supabase.js (lignes 1250-1309 CataloguePieces — style CRUD à répliquer ; lignes 385-417 RelevesKm — helper Phase 23 à NE PAS toucher ; lignes 1565-1589 module.exports — à étendre)
    - sql/migrations/23_consommables_km.sql (lignes 51-76 — schéma exact des 2 tables, UNIQUE constraint, colonne dénormalisée type_consommable sans CHECK)
    - .planning/phases/24-helpers-supabase-js-contrat-stub-vision/24-CONTEXT.md (Claude's Discretion — upsert pour Consommables, insert+list pour PhotosConsommables)
  </read_first>
  <action>
    Éditer `supabase.js` via `str_replace` natif UNIQUEMENT (fichier critique — jamais PowerShell/sed/awk/Python, cf CLAUDE.md). Insérer les 2 nouveaux objets juste APRÈS le bloc `CataloguePieces` (avant `GarageUsers`, autour de la ligne 1309), avec un bandeau de section cohérent avec le style existant.

    **Consommables** — write path en `upsert` (contrainte UNIQUE(moto_id, type_consommable) — un `insert` naïf lèverait 23505 sur re-saisie ; le mécano corrige/complète km_montage plus tard en Phase 25, pitfall recherche #1) :
    ```javascript
    // ══════════════════════════════════════════════════════════
    // CONSOMMABLES (v1.6 — Phase 24) — état courant d'un consommable par moto
    // ══════════════════════════════════════════════════════════
    const Consommables = {
      // upsert (pas insert) : UNIQUE(moto_id, type_consommable) — la table modélise
      // l'état courant (une ligne par type), pas un historique append-only.
      async upsert(moto_id, { type_consommable, km_montage, date_montage, reference }) {
        if (!moto_id) throw new Error('[Consommables.upsert] moto_id requis');
        if (!type_consommable) throw new Error('[Consommables.upsert] type_consommable requis');
        const payload = {
          moto_id,
          type_consommable,
          km_montage: (km_montage !== undefined && km_montage !== null && km_montage !== '') ? parseInt(km_montage) : null,
          date_montage: date_montage || null,
          reference: reference || null,
          updated_at: new Date().toISOString()
        };
        const { data, error } = await supabase
          .from('consommables')
          .upsert(payload, { onConflict: 'moto_id,type_consommable' })
          .select().single();
        if (error) throw new Error(`[Consommables.upsert] ${error.message}`);
        return data;
      },

      async listByMoto(moto_id) {
        if (!moto_id) throw new Error('[Consommables.listByMoto] moto_id requis');
        const { data, error } = await supabase
          .from('consommables').select('*')
          .eq('moto_id', moto_id)
          .order('type_consommable');
        if (error) throw new Error(`[Consommables.listByMoto] ${error.message}`);
        return data || [];
      }
    };
    ```

    **PhotosConsommables** — insert doit écrire À LA FOIS consommable_id ET la colonne dénormalisée type_consommable (pitfall recherche #5 : la colonne existe sans CHECK ni FK, un oubli fait diverger les jointures futures), plus les champs de sortie du contrat vision analyse_ia (objet JSONB complet) + analyse_status :
    ```javascript
    // ══════════════════════════════════════════════════════════
    // PHOTOS CONSOMMABLES (v1.6 — Phase 24) — historique photo + résultat analyse vision
    // ══════════════════════════════════════════════════════════
    const PhotosConsommables = {
      // analyse_ia = objet contrat complet renvoyé par visionAnalysisService.analyzePhoto()
      // (pct_usure/etat/confiance/analyse_status/engine) ; analyse_status dupliqué en colonne
      // dédiée pour requêtes rapides. type_consommable dénormalisé DOIT être fourni (pas de CHECK
      // sur cette colonne — pitfall dénormalisation).
      async insert({ moto_id, consommable_id, type_consommable, photo_url, analyse_ia, analyse_status }) {
        if (!moto_id) throw new Error('[PhotosConsommables.insert] moto_id requis');
        if (!photo_url) throw new Error('[PhotosConsommables.insert] photo_url requis');
        const payload = {
          moto_id,
          consommable_id: consommable_id || null,
          type_consommable: type_consommable || null,
          photo_url,
          analyse_ia: analyse_ia || null,
          analyse_status: analyse_status || null
        };
        const { data, error } = await supabase
          .from('photos_consommables').insert(payload).select().single();
        if (error) throw new Error(`[PhotosConsommables.insert] ${error.message}`);
        return data;
      },

      async listByConsommable(consommable_id) {
        if (!consommable_id) throw new Error('[PhotosConsommables.listByConsommable] consommable_id requis');
        const { data, error } = await supabase
          .from('photos_consommables').select('*')
          .eq('consommable_id', consommable_id)
          .order('created_at', { ascending: false });
        if (error) throw new Error(`[PhotosConsommables.listByConsommable] ${error.message}`);
        return data || [];
      }
    };
    ```

    **module.exports** (ligne ~1565) : ajouter `Consommables,` et `PhotosConsommables,` au bloc. Insérer par exemple juste après `CataloguePieces,`. NE PAS retirer ni réordonner les exports existants. `RelevesKm` est DÉJÀ dans les exports — le laisser tel quel (open question recherche #1 : aucun helper de lecture RelevesKm ajouté, enregistrer() suffit pour le success criterion #4).
  </action>
  <verify>
    <automated>node --check supabase.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check supabase.js` sort en code 0
    - `grep -c "const Consommables = {" supabase.js` == 1
    - `grep -c "const PhotosConsommables = {" supabase.js` == 1
    - `grep -E "onConflict:\s*'moto_id,type_consommable'" supabase.js` matche (upsert, pas insert naïf)
    - `PhotosConsommables.insert` référence `consommable_id`, `type_consommable`, `analyse_ia` ET `analyse_status` dans son payload (grep les 4)
    - Le bloc module.exports contient `Consommables` ET `PhotosConsommables` ET `RelevesKm` (grep les 3 dans les ~30 lignes suivant `module.exports = {`)
    - `grep -c "RelevesKm.list\|RelevesKm.history\|RelevesKm.getBy" supabase.js` == 0 (aucun helper de lecture spéculatif ajouté)
  </acceptance_criteria>
  <done>Consommables (upsert/listByMoto) et PhotosConsommables (insert/listByConsommable) existent dans supabase.js, exportés, RelevesKm inchangé, `node --check` vert.</done>
</task>

<task type="auto">
  <name>Task 2: Créer scripts/test-consommables-crud.js (structurel + comportement pg-direct)</name>
  <read_first>
  <files>scripts/test-consommables-crud.js</files>
    - scripts/test-releves-km-trigger.js (lignes 56-140 — style pg-direct, garde-fou anti-prod, host-only logging, PASS/FAIL, cleanup fixtures, --case)
    - supabase.js (les objets Consommables/PhotosConsommables ajoutés en Task 1 — cible des assertions structurelles)
    - .planning/STATE.md (décision KM-04 en Phase 23 : vérif par analyse statique grep + node --check plutôt que test live supabase.js→REST, pour éviter un 2e type de credential Supabase — même raisonnement ici)
  </read_first>
  <action>
    Créer `scripts/test-consommables-crud.js`. Deux familles d'assertions, style `assert(caseName, label, condition, detail)` / PASS-FAIL / exit 1 si KO, `--case=` comme `scripts/test-releves-km-trigger.js`.

    IMPORTANT — décision de vérification : les helpers `Consommables`/`PhotosConsommables` accèdent à la DB via l'API REST Supabase (SUPABASE_URL + service key = PROD dans .env). Les exécuter live pointerait sur la prod OU exigerait la clé service REST du projet jetable (2e credential, nouveau checkpoint). Suivant la décision documentée en Phase 23 pour KM-04 (STATE.md : analyse statique plutôt que test REST live), on vérifie :
    (a) la FORME des helpers par analyse statique du source supabase.js (grep/regex), et
    (b) le COMPORTEMENT SQL dont les helpers dépendent (upsert on conflict, dénormalisation) via pg-direct contre `FRESH_DB_URL` (déjà dans .env depuis Phase 23, garde-fou anti-prod — aucun nouveau credential).

    **`--case=structure` (aucune DB)** : lire `supabase.js` en texte (`fs.readFileSync`). Asserter par regex :
    - `const Consommables = {` présent ; méthodes `async upsert(` et `async listByMoto(` présentes
    - `onConflict: 'moto_id,type_consommable'` présent (upsert, pas insert naïf)
    - `const PhotosConsommables = {` présent ; `async insert(` et `async listByConsommable(` présentes
    - le payload de PhotosConsommables.insert mentionne `consommable_id`, `type_consommable`, `analyse_ia`, `analyse_status`
    - le bloc `module.exports` contient `Consommables`, `PhotosConsommables`, `RelevesKm`
    - AUCUNE méthode de lecture RelevesKm spéculative (`RelevesKm.list`/`.history`/`.getBy` absents)

    **`--case=upsert-behavior` (pg-direct FRESH_DB_URL)** : reproduire le préambule de connexion de `scripts/test-releves-km-trigger.js` (lire `process.env.FRESH_DB_URL`, refuser si absent, refuser si contient `rzbqbaccjyxvtlnfitrr`, ne logger que `new URL(cs).host`, `new Client`). D'abord sanity-check `SELECT to_regclass('public.consommables')` — si NULL, imprimer un message clair "lancer d'abord node scripts/bootstrap-fresh-schema.js" et exit 1. Créer une fixture garage + moto (UUID explicites, `proprietaire_type='garage'` + `proprietaire_garage_id` posés — la contrainte CHECK L8 moto_proprietaire_coherence l'exige, cf STATE.md fixtures Phase 23). Puis, en SQL brut reproduisant la sémantique du helper :
    - `INSERT INTO consommables (moto_id, type_consommable, km_montage) VALUES (...,'chaine',1000)` puis un `INSERT ... ON CONFLICT (moto_id, type_consommable) DO UPDATE SET km_montage = EXCLUDED.km_montage, updated_at = NOW()` avec km_montage=2000 — asserter qu'après les deux, `SELECT COUNT(*)` sur (moto_id,'chaine') == 1 (une seule ligne) ET km_montage == 2000 (upsert a mis à jour, pas dupliqué ni levé 23505).
    - `INSERT INTO photos_consommables (moto_id, consommable_id, type_consommable, photo_url, analyse_ia, analyse_status) VALUES (..., 'chaine', 'https://fake/p.jpg', '{"pct_usure":42,"etat":"moyen","engine":"stub"}'::jsonb, 'ok')` — asserter que la ligne relue a `type_consommable = 'chaine'` (dénormalisation stockée) ET `analyse_ia->>'engine' = 'stub'` ET `analyse_status = 'ok'`.
    - Nettoyage : `DELETE` fixtures dans un `finally`, `client.end()`.

    `main()` exécute les cas non filtrés, résumé `OK/(OK+KO)`, exit 1 si KO. Ne jamais logger de secret/connection string.
  </action>
  <verify>
    <automated>node --check scripts/test-consommables-crud.js && node scripts/test-consommables-crud.js --case=structure</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/test-consommables-crud.js` sort en code 0
    - `node scripts/test-consommables-crud.js --case=structure` sort en code 0 (KO === 0)
    - Le script contient le garde-fou anti-prod : `grep -c "rzbqbaccjyxvtlnfitrr" scripts/test-consommables-crud.js` >= 1
    - Le script contient `to_regclass('public.consommables')` (sanity-check schéma avant les assertions pg)
    - Le script contient `ON CONFLICT` et une assertion `COUNT` (comportement upsert)
    - Le script n'imprime jamais `FRESH_DB_URL` ni la connection string en clair (seulement `.host`) — `grep -c "console.*connectionString" scripts/test-consommables-crud.js` == 0
  </acceptance_criteria>
  <done>Le script existe ; `--case=structure` est vert (prouve la forme des helpers sans DB) ; `--case=upsert-behavior` est disponible et exécutable contre FRESH_DB_URL (prouve upsert-on-conflict + dénormalisation) après bootstrap.</done>
</task>

</tasks>

<verification>
- `node --check supabase.js` OK
- `node --check scripts/test-consommables-crud.js` OK
- `node scripts/test-consommables-crud.js --case=structure` → vert (forme des helpers)
- `node scripts/test-consommables-crud.js --case=upsert-behavior` → vert quand FRESH_DB_URL est bootstrappé (upsert + dénormalisation)
- Aucun helper de lecture RelevesKm ajouté (grep vide)
</verification>

<success_criteria>
- Success criterion #4 : Consommables, PhotosConsommables, RelevesKm existent comme helpers CRUD minces dans supabase.js, unique point d'accès DB pour les nouvelles tables ; Consommables/PhotosConsommables exportés.
- VISION-02 (persistance) : PhotosConsommables.insert() stocke l'objet contrat vision (analyse_ia) + analyse_status — le maillon de persistance du contrat consommé identiquement par les jauges (Phase 27/28).
- RelevesKm confirmé suffisant (enregistrer() Phase 23), pas de helper de lecture spéculatif (open question recherche #1 = defer).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/24-helpers-supabase-js-contrat-stub-vision/24-02-SUMMARY.md`
</output>
