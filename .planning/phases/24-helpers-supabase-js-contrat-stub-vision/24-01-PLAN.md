---
phase: 24-helpers-supabase-js-contrat-stub-vision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/visionAnalysisService.js
  - scripts/test-vision-stub.js
autonomous: true
requirements: [VISION-01, VISION-02]

must_haves:
  truths:
    - "analyzePhoto() renvoie une analyse structurée sans jamais appeler Anthropic ce milestone"
    - "Un même input (photoUrl ou consommableId) redonne toujours exactement le même résultat (déterminisme D-04)"
    - "analyse_status n'est JAMAIS 'echec' depuis le stub (D-05) — seulement 'ok' ou 'incertain'"
    - "VISION_ENABLED=true sans ANTHROPIC_API_KEY = fallback silencieux vers le stub + warning loggé, jamais de crash (D-06)"
    - "Le contrat renvoyé a une forme fixe : pct_usure (int 0-100), etat (bon/moyen/usé/critique), confiance (int 0-100), analyse_status (ok/incertain), engine ('stub')"
  artifacts:
    - path: "services/visionAnalysisService.js"
      provides: "Service vision flag-gated + stub déterministe + dérivations etat/analyse_status"
      exports: ["analyzePhoto", "deriveEtat", "deriveAnalyseStatus"]
      min_lines: 60
    - path: "scripts/test-vision-stub.js"
      provides: "Harnais de test autonome (PASS/FAIL, --case=) pour le contrat vision, zéro DB"
      contains: "analyzePhoto"
  key_links:
    - from: "scripts/test-vision-stub.js"
      to: "services/visionAnalysisService.js"
      via: "require('../services/visionAnalysisService')"
      pattern: "require\\(.*visionAnalysisService"
    - from: "services/visionAnalysisService.js"
      to: "process.env.VISION_ENABLED"
      via: "flag-gate au chargement du module (convention EMAIL_ENABLED/PUSH_ENABLED)"
      pattern: "VISION_ENABLED"
---

<objective>
Créer `services/visionAnalysisService.js` : le service d'analyse vision flag-gated dont le CONTRAT de réponse est verrouillé dès maintenant (stub aujourd'hui, moteur réel Anthropic dans un futur milestone), consommé identiquement par tous les futurs endpoints/jauges (Phase 25/27/28). Créer aussi le harnais de test autonome qui prouve le contrat en isolation, sans HTTP ni DB.

Purpose: VISION-01 (service flag-gated qui renvoie une fausse analyse tant que la clé Anthropic est absente) + VISION-02 (contrat fixe : % usure, état, confiance, statut, moteur). Le point même de cette phase est de figer la FORME du contrat avant tout consommateur, pour qu'un branchement IA réel futur ne casse aucun call site.
Output: `services/visionAnalysisService.js` (nouveau) + `scripts/test-vision-stub.js` (nouveau harnais Wave 0).
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-helpers-supabase-js-contrat-stub-vision/24-CONTEXT.md
@.planning/phases/24-helpers-supabase-js-contrat-stub-vision/24-RESEARCH.md
@.planning/phases/24-helpers-supabase-js-contrat-stub-vision/24-VALIDATION.md

<interfaces>
<!-- Conventions extraites du codebase — l'exécuteur les applique directement, pas d'exploration -->

Pattern flag-gate à répliquer À L'IDENTIQUE (services/emailService.js:14-32) :
```javascript
const EMAIL_ENABLED = process.env.EMAIL_ENABLED === 'true';
let resendClient = null;
if (EMAIL_ENABLED) {
  try {
    const { Resend } = require('resend');
    if (!process.env.RESEND_API_KEY) {
      console.warn('⚠️  [7b] EMAIL_ENABLED=true mais RESEND_API_KEY manquant — fallback console');
    } else { resendClient = new Resend(process.env.RESEND_API_KEY); console.log('✅ [7b] Resend initialisé'); }
  } catch (e) { console.warn('⚠️  [7b] Module resend non disponible — fallback console:', e.message); }
} else { console.log('📧 [7b] Email en mode dev (EMAIL_ENABLED=false) — console.log uniquement'); }
```

Convention `crypto` déterministe déjà utilisée (scripts/test-releves-km-trigger.js:61) : `const crypto = require('crypto');`

Colonnes DB cibles du contrat (photos_consommables, migration 23, déjà en prod) :
- `analyse_ia JSONB` — reçoit l'objet contrat complet
- `analyse_status TEXT` — reçoit 'ok' / 'incertain' / 'echec' (echec réservé au futur moteur réel)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer le harnais de test autonome scripts/test-vision-stub.js</name>
  <files>scripts/test-vision-stub.js</files>
  <read_first>
    - scripts/test-releves-km-trigger.js (style PASS/FAIL, `assert()`, `--case=<nom>`, `shouldRun()`, exit code 1 si un FAIL — à répliquer)
    - .planning/phases/24-helpers-supabase-js-contrat-stub-vision/24-VALIDATION.md (liste exacte des cas et commandes)
  </read_first>
  <action>
    Créer `scripts/test-vision-stub.js` — script Node autonome, AUCUNE dépendance DB (le service vision est une fonction pure). Répliquer le style de `scripts/test-releves-km-trigger.js` : `'use strict';`, helper `assert(caseName, label, condition, detail)` qui incrémente OK/KO et imprime `PASS`/`FAIL`, parsing `--case=<nom>` via `shouldRun(caseName)`, résumé `OK/(OK+KO) assertions passées` + `process.exit(1)` si KO > 0.

    Le script `require('../services/visionAnalysisService')` en haut. Il importe `{ analyzePhoto, deriveEtat, deriveAnalyseStatus }`.

    Cas à implémenter (chacun gardé par `shouldRun`) :

    1. `--case=contract-shape` : appeler `await analyzePhoto({ photoUrl: 'https://fake/x.jpg', consommableId: 'c-1', typeConsommable: 'chaine', kmActuel: 20000, kmMontage: 5000 })`. Asserter : `typeof r.pct_usure === 'number'` ET entier 0-100 ; `['bon','moyen','usé','critique'].includes(r.etat)` ; `typeof r.confiance === 'number'` ET entier 0-100 ; `['ok','incertain'].includes(r.analyse_status)` ; `r.analyse_status !== 'echec'` ; `r.engine === 'stub'`. Asserter aussi que les clés de l'objet sont EXACTEMENT `['pct_usure','etat','confiance','analyse_status','engine']` (snake_case ASCII verrouillé).

    2. `--case=deterministic-seed` : appeler `analyzePhoto` DEUX fois avec le MÊME input `{ photoUrl:'https://fake/deterministic.jpg', consommableId:'c-9', typeConsommable:'pneu_av', kmActuel:12000, kmMontage:2000 }`. Asserter `JSON.stringify(r1) === JSON.stringify(r2)` (déterminisme D-04). Puis appeler avec une photoUrl DIFFÉRENTE et asserter que le résultat diffère au moins parfois (au moins pct_usure OU confiance change sur un petit lot de 5 URLs distinctes — pseudo-aléatoire, pas figé).

    3. `--case=derivation-thresholds` : appels DIRECTS aux fonctions pures (indépendant du seed). Table `deriveEtat` : 0→'bon', 29→'bon', 30→'moyen', 59→'moyen', 60→'usé', 84→'usé', 85→'critique', 100→'critique'. Table `deriveAnalyseStatus` : 0→'incertain', 49→'incertain', 50→'ok', 99→'ok'. Asserter chaque paire.

    4. `--case=never-echec` : boucler sur 200 seeds distincts (photoUrl `https://fake/${i}.jpg`, km variés) et asserter que `r.analyse_status` vaut TOUJOURS 'ok' ou 'incertain', JAMAIS 'echec' (D-05). Asserter aussi qu'AU MOINS un 'incertain' apparaît sur le lot (prouve que la branche incertain est réellement atteignable en dev/démo, pas juste théorique).

    5. `--case=inconsistent-config-fallback` : via `child_process.spawnSync('node', ['-e', "require('./services/visionAnalysisService.js'); const {analyzePhoto}=require('./services/visionAnalysisService.js'); analyzePhoto({photoUrl:'https://fake/z.jpg',consommableId:'c',typeConsommable:'chaine',kmActuel:1,kmMontage:0}).then(r=>{ if(r.engine!=='stub'){process.exit(2)}; process.exit(0)})"], { env: { ...process.env, VISION_ENABLED: 'true', ANTHROPIC_API_KEY: '' }, encoding: 'utf8', cwd: <racine repo> })`. Asserter que la sortie combinée (stdout+stderr) CONTIENT la sous-chaîne `VISION_ENABLED=true` ET `fallback` (le warning D-06), ET que `status === 0` (pas de crash, engine reste 'stub'). Calculer la racine repo via `require('path').join(__dirname, '..')`.

    6. `--case=isolated-call` : prouver le success criterion #3 — un seul appel `await analyzePhoto({ photoUrl:'https://fake/iso.jpg' })` (params km/consommable absents) renvoie un objet conforme au contrat SANS lancer de serveur HTTP ni toucher la DB. Asserter que kmActuel/kmMontage absents ne font pas crasher (pct_usure calculé avec kmParcouru=0) et que la forme reste conforme.

    Le `main()` exécute tous les cas non filtrés puis imprime le résumé. Ne PAS logger de secret. Comme le service n'existe pas encore au moment de créer ce fichier, la vérification à cette étape est purement syntaxique.
  </action>
  <verify>
    <automated>node --check scripts/test-vision-stub.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/test-vision-stub.js` sort en code 0 (syntaxe valide)
    - `grep -c "require('../services/visionAnalysisService')" scripts/test-vision-stub.js` >= 1
    - Le fichier contient les 6 tokens de cas : `contract-shape`, `deterministic-seed`, `derivation-thresholds`, `never-echec`, `inconsistent-config-fallback`, `isolated-call` (grep chacun)
    - Le fichier contient `spawnSync` (cas fallback D-06) et `VISION_ENABLED` et `ANTHROPIC_API_KEY`
    - Le fichier contient une assertion sur `engine` égal à `'stub'` et une assertion `!== 'echec'`
  </acceptance_criteria>
  <done>Le harnais existe, syntaxe valide, couvre les 6 cas du 24-VALIDATION.md. Il échouera au run tant que Task 2 n'a pas livré le service — attendu.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Créer services/visionAnalysisService.js (flag-gate + stub déterministe + dérivations)</name>
  <files>services/visionAnalysisService.js</files>
  <read_first>
    - services/emailService.js (lignes 12-32 — convention exacte flag-gate + fallback silencieux à répliquer)
    - services/pushService.js (lignes 12-27 — deuxième référence du même pattern ENABLED)
    - scripts/test-vision-stub.js (créé en Task 1 — c'est la spécification exécutable du contrat)
    - sql/migrations/23_consommables_km.sql (lignes 67-76 — colonnes photos_consommables : analyse_ia JSONB, analyse_status TEXT)
  </read_first>
  <behavior>
    - Contrat fixe renvoyé par analyzePhoto() : { pct_usure:int 0-100, etat:'bon'|'moyen'|'usé'|'critique', confiance:int 0-100, analyse_status:'ok'|'incertain', engine:'stub' } (D-01, clés snake_case ASCII)
    - deriveEtat(pct) : <30 bon, 30-59 moyen, 60-84 usé, >=85 critique (D-02)
    - deriveAnalyseStatus(confiance) : <50 incertain, >=50 ok (D-03)
    - Même input → même output (déterminisme via seed hash SHA-256 de photoUrl||consommableId) (D-04)
    - analyse_status jamais 'echec' (D-05)
    - VISION_ENABLED=true sans ANTHROPIC_API_KEY → warning + fallback stub, jamais de throw (D-06)
    - confiance tirée dans 35-99 pour que 'incertain' (<50) soit réellement atteignable en dev
  </behavior>
  <action>
    Créer `services/visionAnalysisService.js`. En-tête de bloc commentaire documentant le contrat verrouillé (référence canonique pour Phase 25/27/28). `'use strict';` puis `const crypto = require('crypto');`. NE PAS `require('./supabase')` ni faire d'accès DB — service pur (pitfall recherche #2, success criterion #3).

    **Flag-gate (répliquer emailService.js:14-32, adapté)** :
    ```javascript
    const VISION_ENABLED = process.env.VISION_ENABLED === 'true';
    const ANTHROPIC_API_KEY = process.env.ANTHROPIC_API_KEY || null;
    let visionReady = false;
    if (VISION_ENABLED) {
      if (!ANTHROPIC_API_KEY) {
        console.warn('⚠️  [24] VISION_ENABLED=true mais ANTHROPIC_API_KEY manquant — fallback stub');
      } else {
        visionReady = true; // réservé au futur moteur réel — non branché ce milestone
        console.log('✅ [24] Config Vision détectée (moteur réel non branché ce milestone)');
      }
    } else {
      console.log('🔎 [24] Vision en mode stub (VISION_ENABLED=false) — analyse simulée uniquement');
    }
    ```

    **Dérivations (fonctions pures, exportées)** :
    ```javascript
    function deriveEtat(pctUsure) {
      if (pctUsure >= 85) return 'critique';
      if (pctUsure >= 60) return 'usé';
      if (pctUsure >= 30) return 'moyen';
      return 'bon';
    }
    function deriveAnalyseStatus(confiance) {
      return confiance < 50 ? 'incertain' : 'ok';
    }
    ```

    **Seed déterministe + PRNG (mulberry32)** :
    ```javascript
    function seedFromInput(photoUrl, consommableId) {
      const raw = photoUrl || consommableId || 'no-input';
      return crypto.createHash('sha256').update(String(raw)).digest().readUInt32BE(0);
    }
    function mulberry32(seed) {
      return function () {
        seed |= 0; seed = (seed + 0x6D2B79F5) | 0;
        let t = Math.imul(seed ^ (seed >>> 15), 1 | seed);
        t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
        return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
      };
    }
    ```

    **Cœur stub** `buildStubAnalysis({ photoUrl, consommableId, typeConsommable, kmActuel, kmMontage })` :
    - `const seed = seedFromInput(photoUrl, consommableId); const rand = mulberry32(seed);`
    - `const kmParcouru = Math.max(0, (Number(kmActuel)||0) - (Number(kmMontage)||0));`
    - `const dureeVieKm = 15000;` (valeur unique simple tous types ce milestone)
    - `const usureBase = Math.min(95, Math.round((kmParcouru / dureeVieKm) * 100));`
    - `const bruit = Math.round((rand() - 0.5) * 30);` (± 15 points déterministes)
    - `const pct_usure = Math.max(0, Math.min(100, usureBase + bruit));`
    - `const etat = deriveEtat(pct_usure);`
    - `const confiance = Math.max(35, Math.min(99, Math.round(35 + rand() * 64)));` (plage 35-99 → 'incertain' atteignable, pitfall recherche #4)
    - `const analyse_status = deriveAnalyseStatus(confiance);`
    - retourner `{ pct_usure, etat, confiance, analyse_status, engine: 'stub' }` (clés dans cet ordre exact, snake_case ASCII).

    **Fonction publique async** `analyzePhoto({ photoUrl, consommableId, typeConsommable, kmActuel, kmMontage })` :
    - Emplacement futur du vrai moteur : `if (VISION_ENABLED && visionReady) { /* futur appel Anthropic — non implémenté ce milestone */ }`
    - Toujours retourner `buildStubAnalysis(...)` ce milestone.
    - async pour que la signature soit stable quand le vrai appel (réseau) sera branché.

    `module.exports = { analyzePhoto, deriveEtat, deriveAnalyseStatus };`

    NE JAMAIS produire `analyse_status: 'echec'` (D-05 — aucune branche "simuler un échec").
  </action>
  <verify>
    <automated>node --check services/visionAnalysisService.js && node scripts/test-vision-stub.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/test-vision-stub.js` sort en code 0 avec `KO === 0` (tous les cas PASS)
    - `grep -c "require('./supabase" services/visionAnalysisService.js` == 0 (aucun couplage DB — pitfall #2)
    - `grep -c "echec" services/visionAnalysisService.js` == 0 (D-05 — le stub ne produit jamais echec)
    - `grep -E "module.exports\s*=\s*\{[^}]*analyzePhoto[^}]*deriveEtat[^}]*deriveAnalyseStatus" services/visionAnalysisService.js` matche (les 3 exports présents)
    - `grep -c "VISION_ENABLED === 'true'" services/visionAnalysisService.js` >= 1
    - `grep -c "mulberry32\|createHash('sha256')" services/visionAnalysisService.js` >= 1 (seed déterministe)
    - Les seuils exacts présents : `deriveEtat` contient `>= 85`, `>= 60`, `>= 30` ; `deriveAnalyseStatus` contient `< 50`
    - `node scripts/test-vision-stub.js --case=inconsistent-config-fallback` PASS (D-06, warning + engine stub, pas de crash)
  </acceptance_criteria>
  <done>Le service existe, `node scripts/test-vision-stub.js` est vert sur les 6 cas, aucun couplage DB, aucun 'echec' possible depuis le stub, contrat snake_case verrouillé.</done>
</task>

</tasks>

<verification>
- `node --check services/visionAnalysisService.js` OK
- `node --check scripts/test-vision-stub.js` OK
- `node scripts/test-vision-stub.js` → toutes assertions vertes, exit 0
- Aucun `require('./supabase')` dans le service (grep vide)
- Aucun `'echec'` dans le service (grep vide)
</verification>

<success_criteria>
- VISION-01 : `analyzePhoto()` existe, flag-gated `VISION_ENABLED` (convention EMAIL_ENABLED/PUSH_ENABLED), renvoie une fausse analyse structurée sans clé Anthropic ; VISION_ENABLED=true sans clé = fallback stub + warning (D-06), pas de crash.
- VISION-02 : contrat fixe pct_usure/etat/confiance/analyse_status/engine, dérivations D-02/D-03 correctes, déterminisme D-04, jamais 'echec' D-05.
- Success criterion #3 : appel direct avec URL factice vérifiable hors de tout endpoint HTTP (`--case=isolated-call` vert).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/24-helpers-supabase-js-contrat-stub-vision/24-01-SUMMARY.md`
</output>
