---
phase: 23-sch-ma-anti-fraude-km-au-niveau-db
plan: 04
type: execute
wave: 3
depends_on: ["23-01", "23-02", "23-03"]
files_modified: []
autonomous: false
requirements: [KM-01, KM-04, CONSO-02]

must_haves:
  truths:
    - "node scripts/bootstrap-fresh-schema.js imprime SCHEMA_BOOTSTRAP_OK contre un projet jetable, incluant les 4 nouvelles tables"
    - "node scripts/test-releves-km-trigger.js est vert (les 4 cas trigger + CHECK 9-types passent) — TDD passe de RED à GREEN"
    - "pg_policies montre 0 policy sur les 4 nouvelles tables (default-deny intentionnel, D-01)"
    - "schema.sql et la migration 23 sont byte-équivalents sur les sections concernées (parité same-commit, Anti-Pattern 2)"
  artifacts: []
  key_links:
    - from: "scripts/bootstrap-fresh-schema.js"
      to: "schema.sql (avec les 4 nouvelles tables)"
      via: "exécution contre FRESH_DB_URL (projet jetable)"
      pattern: "SCHEMA_BOOTSTRAP_OK"
    - from: "scripts/test-releves-km-trigger.js"
      to: "trigger verifier_km_monotone (DB bootstrappée)"
      via: "inserts pg directs asserant rejet+log+sync"
      pattern: "PASS"
---

<objective>
Gate de fin de phase : prouver que le schéma bootstrappe proprement avec les 4 nouvelles tables, que le trigger anti-fraude se comporte correctement (script de test vert), et que le RLS default-deny est bien en place (0 policy). C'est la discipline v1.5 non négociable — une phase schéma n'est "terminée" qu'après un bootstrap propre vérifié.

Purpose: garantir zéro dérive schema.sql dès le premier jour de v1.6 (la leçon de 3 phases entières en v1.5) et valider le comportement DB réel du trigger, pas seulement le texte SQL.
Output: gate vert (aucun fichier produit hors SUMMARY) + revue humaine pg_policies.
</objective>

<context>
@.planning/phases/23-sch-ma-anti-fraude-km-au-niveau-db/23-VALIDATION.md

<interfaces>
- `node scripts/bootstrap-fresh-schema.js` — imprime `SCHEMA_BOOTSTRAP_OK` (succès) ou `SCHEMA_BOOTSTRAP_FAILED: <msg>` (exit 1). Nécessite FRESH_DB_URL (posé en 23-02).
- `node scripts/test-releves-km-trigger.js` — créé en 23-02, exerce le trigger. Doit être exécuté APRÈS le bootstrap (la DB jetable doit contenir le schéma).
- Ordre obligatoire : bootstrap (applique schema.sql) → test-trigger (exerce la DB bootstrappée).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap propre + script de test vert (RED → GREEN)</name>
  <files></files>
  <read_first>
    - scripts/bootstrap-fresh-schema.js (comportement, sortie attendue SCHEMA_BOOTSTRAP_OK)
    - scripts/test-releves-km-trigger.js (les cas à faire passer)
    - schema.sql (vérifier que les objets de 23-01 y sont bien — sinon le bootstrap ne les créera pas)
  </read_first>
  <action>
Pré-requis : FRESH_DB_URL en place (23-02) et supabase.js/schema.sql modifiés (23-01, 23-03). Si le projet jetable a déjà été bootstrappé lors d'un run précédent, le re-créer/reset n'est pas nécessaire tant que bootstrap-fresh-schema.js commence par DROP (schema.sql a un DROP block en tête — le re-run est idempotent).

1. Lancer `node scripts/bootstrap-fresh-schema.js`. Attendu : `SCHEMA_BOOTSTRAP_OK`. Si `SCHEMA_BOOTSTRAP_FAILED`, lire le message, corriger le DDL fautif dans `schema.sql` (et refléter dans la migration pour garder la parité), relancer jusqu'au vert. Ne PAS considérer la phase faite tant que ce n'est pas OK.
2. Lancer `node scripts/test-releves-km-trigger.js`. Attendu : tous les cas `PASS` (accept, reject-regression, null-safe-baseline, counter-replacement-bypass, conso-check-violation), exit 0. Si un cas `FAIL`, diagnostiquer : soit le trigger (schema.sql/migration, corriger en 23-01 puis re-bootstrap), soit une erreur d'assertion du script (corriger le script). Le cas `null-safe-baseline` et `counter-replacement-bypass` sont les plus discriminants — ne pas les affaiblir pour "faire passer".
3. Ne jamais pointer FRESH_DB_URL vers la prod (le script refuse `rzbqbaccjyxvtlnfitrr`, mais vérifier).

Note : la migration `sql/migrations/23_consommables_km.sql` sera appliquée à la PROD manuellement par Mehdi via Supabase Dashboard SQL Editor (convention du projet, aucun runner automatisé) — cela relève de l'exécution/déploiement, hors de ce gate qui prouve la correction sur le projet jetable.
  </action>
  <verify>
    <automated>node scripts/bootstrap-fresh-schema.js && node scripts/test-releves-km-trigger.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/bootstrap-fresh-schema.js` imprime `SCHEMA_BOOTSTRAP_OK` (exit 0)
    - `node scripts/test-releves-km-trigger.js` imprime `PASS` pour les 5 cas et exit 0 (aucun `FAIL`)
    - Le cas `null-safe-baseline` passe : une moto avec motos.km peuplé et zéro releves_km rejette bien un premier relevé inférieur
    - Le cas `counter-replacement-bypass` passe : après remplacement_compteur, la chaîne repart de la nouvelle valeur (pas de comparaison au max pré-reset)
  </acceptance_criteria>
  <done>Bootstrap propre (SCHEMA_BOOTSTRAP_OK) incluant les 4 nouvelles tables + script de test 100% vert. Le TDD est passé de RED (23-02) à GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Revue humaine — pg_policies (0 policy) + parité schema.sql/migration</name>
  <files></files>
  <action>
Checkpoint de revue humaine — deux vérifications à jugement (RLS default-deny intentionnel + parité migration/schema.sql). Claude prépare les requêtes/diff ci-dessous (how-to-verify) ; Mehdi confirme le verdict. Faire une pause pour cette revue avant de clore la phase.
  </action>
  <what-built>Bootstrap propre + trigger vérifié (Task 1). Restent deux vérifications à jugement humain : (a) confirmer que les 4 nouvelles tables ont RLS activé SANS policy (default-deny intentionnel D-01, pas un oubli), (b) confirmer la parité byte-équivalente entre le fichier de migration 23 et les sections correspondantes de schema.sql (discipline Anti-Pattern 2, la leçon centrale de v1.5).</what-built>
  <how-to-verify>
1. Contre le projet jetable (après bootstrap), exécuter dans le SQL Editor Supabase (ou psql) :
   `SELECT tablename, policyname FROM pg_policies WHERE tablename IN ('consommables','photos_consommables','releves_km','releves_km_rejets');`
   Attendu : 0 ligne. Puis :
   `SELECT relname, relrowsecurity FROM pg_class WHERE relname IN ('consommables','photos_consommables','releves_km','releves_km_rejets');`
   Attendu : relrowsecurity = true (t) pour les 4. => RLS ON + 0 policy = default-deny intentionnel.
2. Diffuser visuellement (ou `git diff` / lecture) les blocs CREATE TABLE + triggers + RLS de `sql/migrations/23_consommables_km.sql` contre les sections hand-appended de `schema.sql` : confirmer qu'ils sont équivalents (mêmes colonnes, mêmes CHECK, même logique de trigger). Confirmer aussi que le commentaire "INTENTIONNEL" RLS est présent dans les deux.
3. Confirmer que `trg_update_km` / `update_moto_km` n'apparaît plus dans schema.sql.
Répondre "approved" si les 3 points sont OK, sinon décrire l'écart.
  </how-to-verify>
  <verify>Manuel : pg_policies retourne 0 ligne pour les 4 tables ; relrowsecurity = true ; migration ↔ schema.sql équivalents ; aucun trg_update_km résiduel.</verify>
  <done>RLS default-deny confirmé (0 policy, RLS on) + parité migration/schema.sql confirmée + trg_update_km absent.</done>
  <resume-signal>Taper "approved" ou décrire les écarts (policies inattendues, dérive migration/schema.sql, trg_update_km résiduel)</resume-signal>
</task>

</tasks>

<verification>
- bootstrap-fresh-schema.js → SCHEMA_BOOTSTRAP_OK.
- test-releves-km-trigger.js → tous PASS.
- pg_policies : 0 policy sur les 4 tables ; relrowsecurity = true.
- Parité migration ↔ schema.sql confirmée en revue.
</verification>

<success_criteria>
- Schéma bootstrappe proprement avec les 4 nouvelles tables (discipline v1.5 respectée).
- Comportement du trigger anti-fraude prouvé (KM-01, y compris NULL-safe et reset compteur).
- RLS default-deny en place et documenté (D-01).
- Aucun bypass km applicatif restant (KM-04, vérifié par grep en 23-03 + comportement DB ici).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/23-sch-ma-anti-fraude-km-au-niveau-db/23-04-SUMMARY.md`.
</output>
