---
phase: 23-sch-ma-anti-fraude-km-au-niveau-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/migrations/23_consommables_km.sql
  - schema.sql
autonomous: true
requirements: [KM-01, KM-04, CONSO-02]

must_haves:
  truths:
    - "Insérer un relevé km < max historique de la moto est annulé par un trigger BEFORE INSERT et journalisé dans une table de rejet"
    - "motos.km est recalculé automatiquement depuis releves_km à chaque relevé accepté (trigger AFTER INSERT)"
    - "Un remplacement de compteur démarre une chaîne monotone fraîche (relevés ultérieurs comparés seulement aux relevés postérieurs au remplacement)"
    - "type_consommable n'accepte que les 9 types v1, extensible par migration légère (TEXT+CHECK)"
    - "trg_update_km (ancien clamp GREATEST sur interventions) est supprimé — plus de second writer non coordonné vers motos.km"
  artifacts:
    - path: "sql/migrations/23_consommables_km.sql"
      provides: "4 CREATE TABLE + 2 trigger functions/triggers + DROP de trg_update_km + RLS-no-policy documenté"
      contains: "CREATE TABLE releves_km"
    - path: "schema.sql"
      provides: "Mêmes objets hand-appended dans le même commit (DROP block, CREATE TABLE, triggers, RLS)"
      contains: "verifier_km_monotone"
  key_links:
    - from: "trigger BEFORE INSERT verifier_km_monotone"
      to: "releves_km_rejets"
      via: "INSERT du log puis RETURN NULL (jamais RAISE EXCEPTION)"
      pattern: "RETURN NULL"
    - from: "trigger AFTER INSERT trg_sync_moto_km"
      to: "motos.km"
      via: "UPDATE motos SET km = NEW.km (assignation directe, pas de GREATEST)"
      pattern: "UPDATE motos SET km = NEW.km"
---

<objective>
Poser le schéma DB de l'anti-fraude km et des consommables : 4 nouvelles tables (`releves_km`, `releves_km_rejets`, `consommables`, `photos_consommables`), le trigger monotone `BEFORE INSERT` (rejet + log via RETURN NULL, NULL-safe, chaîne fraîche après remplacement compteur), le trigger de sync `AFTER INSERT` (`motos.km`), la suppression du clamp legacy `trg_update_km`, et le RLS default-deny documenté — le tout écrit dans le même commit dans le fichier de migration ET dans `schema.sql`.

Purpose: `releves_km` devient la source de vérité du km, protégée au niveau DB contre toute régression (KM-01), avec un schéma consommables extensible (CONSO-02). C'est la fondation dont dépend la fermeture des chemins d'écriture (plan 23-03).
Output: `sql/migrations/23_consommables_km.sql` + `schema.sql` mis à jour dans le même commit.
</objective>

<context>
@.planning/phases/23-sch-ma-anti-fraude-km-au-niveau-db/23-CONTEXT.md
@.planning/phases/23-sch-ma-anti-fraude-km-au-niveau-db/23-RESEARCH.md

<discipline>
DISCIPLINE NON NÉGOCIABLE (leçon centrale de v1.5, 3 phases entières) :
le DDL du fichier de migration DOIT être hand-appended dans `schema.sql` dans le
MÊME commit. Une migration sans mise à jour schema.sql same-commit = échec du plan.
Le plan 23-04 re-lancera `node scripts/bootstrap-fresh-schema.js` pour prouver que
`schema.sql` bootstrappe proprement avec les nouvelles tables.
</discipline>

<interfaces>
<!-- Emplacements exacts dans schema.sql (vérifiés par lecture directe cette passe) -->
- DROP block (nettoyage) : schema.sql L41-60 — ajouter les 4 nouvelles tables en tête, ordre inverse de dépendance.
- Dernier CREATE TABLE existant : `reclamations_moto` à L534 — appender les 4 nouvelles tables juste après le dernier bloc CREATE TABLE.
- Trigger legacy à SUPPRIMER : `update_moto_km()` + `CREATE TRIGGER trg_update_km` à schema.sql L631-643 — retirer ce bloc, ne PAS le ré-ajouter.
- Triggers existants conservés : `trg_recalc_score` (L624-626) reste intact.
- Pattern TEXT+CHECK de référence (à copier pour type_consommable) : `interventions.niveau_preuve` schema.sql L302.
- Convention commentaire RLS-no-policy (à reproduire mot pour mot) : schema.sql L689-702.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer les 4 tables (migration + schema.sql même commit)</name>
  <files>sql/migrations/23_consommables_km.sql, schema.sql</files>
  <read_first>
    - schema.sql L41-60 (DROP block — où insérer les DROP des nouvelles tables)
    - schema.sql L281-308 (table interventions + pattern niveau_preuve TEXT+CHECK L302 à copier)
    - schema.sql L494-560 (tables Gap B billing_events/motos_proprietaires_historique — style CREATE TABLE + emplacement d'append après L534)
    - sql/migrations/22_devis_undocumented_columns.sql (style d'en-tête et de commentaires des fichiers de migration)
  </read_first>
  <action>
Créer `sql/migrations/23_consommables_km.sql` avec un en-tête commenté (style migration 22) expliquant : "Phase 23 (v1.6) — schéma consommables + source de vérité km (releves_km) + anti-fraude km au niveau DB". Y déclarer les 4 tables ci-dessous. Puis hand-append EXACTEMENT le même DDL dans `schema.sql` : (a) ajouter les 4 DROP TABLE en tête du DROP block (L41+, ordre inverse de dépendance : releves_km_rejets, releves_km, photos_consommables, consommables), (b) ajouter les 4 blocs CREATE TABLE juste après le dernier CREATE TABLE existant (reclamations_moto, ~L560).

DDL exact des 4 tables (identique dans les deux fichiers) :

```sql
-- ══════════════════════════════════════════════════════════
-- Phase 23 (v1.6) — Consommables + source de vérité km
-- ══════════════════════════════════════════════════════════

CREATE TABLE consommables (
  id                UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  moto_id           UUID NOT NULL REFERENCES motos(id) ON DELETE CASCADE,
  -- Extensibilité CONSO-02 : TEXT + CHECK (même pattern que interventions.niveau_preuve).
  -- Ajouter un type plus tard = ALTER TABLE ... DROP CONSTRAINT ... ADD CONSTRAINT (migration légère).
  type_consommable  TEXT NOT NULL CHECK (type_consommable IN (
                      'pneu_av','pneu_ar','chaine','plaquettes_av','plaquettes_ar',
                      'disque_av','disque_ar','huile_moteur','liquide_frein')),
  km_montage        INTEGER,
  date_montage      DATE,
  reference         TEXT,
  created_at        TIMESTAMPTZ DEFAULT NOW(),
  updated_at        TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE (moto_id, type_consommable)
);

CREATE TABLE photos_consommables (
  id                UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  moto_id           UUID NOT NULL REFERENCES motos(id) ON DELETE CASCADE,
  consommable_id    UUID REFERENCES consommables(id) ON DELETE CASCADE,
  type_consommable  TEXT,
  photo_url         TEXT NOT NULL,
  analyse           JSONB,           -- rempli par le stub vision (Phase 24/25)
  analyse_status    TEXT,            -- ok / incertain / echec (Phase 24)
  created_at        TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE releves_km (
  id                UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  moto_id           UUID NOT NULL REFERENCES motos(id) ON DELETE CASCADE,
  garage_id         UUID,            -- nullable : moto client sans garage (modèle L8) ; renseigné côté app
  km                INTEGER NOT NULL CHECK (km >= 0),
  type_evenement    TEXT NOT NULL DEFAULT 'lecture'
                      CHECK (type_evenement IN ('lecture','lecture_initiale','remplacement_compteur')),
  acteur_type       TEXT NOT NULL CHECK (acteur_type IN ('client','garage')),  -- D-04 : jamais anonyme
  acteur_id         UUID NOT NULL,
  note              TEXT,            -- obligatoire côté app pour remplacement_compteur (D-03)
  photo_url         TEXT,           -- photo compteur optionnelle (Phase 25)
  created_at        TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE releves_km_rejets (
  id                UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  moto_id           UUID NOT NULL,
  garage_id         UUID,
  acteur_type       TEXT,
  acteur_id         UUID,
  km_tente          INTEGER NOT NULL,
  km_actuel         INTEGER NOT NULL,  -- vrai max historique au moment de la tentative (D-04)
  created_at        TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_releves_km_moto        ON releves_km(moto_id, created_at DESC);
CREATE INDEX idx_releves_km_rejets_moto ON releves_km_rejets(moto_id, created_at DESC);
CREATE INDEX idx_consommables_moto      ON consommables(moto_id);
CREATE INDEX idx_photos_conso_moto      ON photos_consommables(moto_id);
```

Ne PAS ajouter de FK vers `client_id` (modèle de propriété polymorphe L8 : la propriété se résout via moto_id → motos). Ne PAS créer de table de référence `types_consommables` (rejeté, D-02).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('schema.sql','utf8'); const m=require('fs').readFileSync('sql/migrations/23_consommables_km.sql','utf8'); const t=['consommables','photos_consommables','releves_km','releves_km_rejets']; for(const x of t){if(!s.includes('CREATE TABLE '+x+' '))throw new Error('schema.sql manque CREATE TABLE '+x); if(!m.includes('CREATE TABLE '+x+' '))throw new Error('migration manque '+x);} if(!s.includes('DROP TABLE IF EXISTS releves_km_rejets'))throw new Error('DROP block schema.sql manque releves_km_rejets'); console.log('OK tables')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE consommables " schema.sql` == 1 ET `grep -c "CREATE TABLE consommables " sql/migrations/23_consommables_km.sql` == 1
    - Les 4 tables (consommables, photos_consommables, releves_km, releves_km_rejets) ont un `CREATE TABLE` présent dans les DEUX fichiers
    - schema.sql DROP block contient `DROP TABLE IF EXISTS releves_km_rejets` `DROP TABLE IF EXISTS releves_km` `DROP TABLE IF EXISTS photos_consommables` `DROP TABLE IF EXISTS consommables` (ordre inverse de dépendance, avant les tables qu'elles référencent)
    - `type_consommable` déclare exactement les 9 valeurs : `grep "pneu_av','pneu_ar','chaine','plaquettes_av','plaquettes_ar'` présent dans les deux fichiers
    - `grep "REFERENCES motos(id)" ` présent pour consommables/photos_consommables/releves_km ; AUCUN `REFERENCES clients` sur ces 4 tables (`grep -c "REFERENCES clients" sql/migrations/23_consommables_km.sql` == 0)
    - `acteur_type TEXT NOT NULL CHECK (acteur_type IN ('client','garage'))` présent (D-04, jamais anonyme)
  </acceptance_criteria>
  <done>Les 4 tables existent, identiques, dans la migration ET schema.sql (même commit), avec le CHECK 9-types, les FK moto_id, la table de rejet D-04 complète, et les DROP en ordre inverse.</done>
</task>

<task type="auto">
  <name>Task 2: Triggers km (monotone + sync) + suppression trg_update_km + RLS documenté</name>
  <files>sql/migrations/23_consommables_km.sql, schema.sql</files>
  <read_first>
    - schema.sql L624-643 (trg_recalc_score conservé + update_moto_km/trg_update_km à SUPPRIMER)
    - schema.sql L686-702 (convention commentaire RLS-no-policy à reproduire mot pour mot)
    - .planning/phases/23-sch-ma-anti-fraude-km-au-niveau-db/23-RESEARCH.md Pattern 1 et Pattern 2 (logique trigger) + Pitfall A (NULL-safe) + Pitfall B (RETURN NULL vs RAISE EXCEPTION)
    - sql/migrations/23_consommables_km.sql (état après Task 1)
  </read_first>
  <action>
Ajouter au fichier de migration `sql/migrations/23_consommables_km.sql` ET hand-append dans `schema.sql` (même commit) : (A) suppression du clamp legacy, (B) le trigger monotone BEFORE INSERT, (C) le trigger de sync AFTER INSERT, (D) le bloc RLS-no-policy documenté.

(A) Suppression de trg_update_km (résout la question ouverte n°2 du research, conséquence logique de D-05 : le km d'intervention est un historique découplé qui ne doit JAMAIS bumper motos.km).
- Dans la MIGRATION (appliquée à une prod existante), ajouter :
```sql
-- D-05 / KM-04 : le km d'intervention est un historique découplé — il ne doit plus
-- toucher motos.km. On retire l'ancien clamp GREATEST (second writer non coordonné,
-- incompatible avec "releves_km = source de vérité unique"). Désormais motos.km
-- n'est écrit QUE par trg_sync_moto_km ci-dessous.
DROP TRIGGER IF EXISTS trg_update_km ON interventions;
DROP FUNCTION IF EXISTS update_moto_km();
```
- Dans `schema.sql`, RETIRER purement le bloc `CREATE OR REPLACE FUNCTION update_moto_km()` + `CREATE TRIGGER trg_update_km` (L631-643) et le remplacer par les nouveaux triggers ci-dessous. Conserver `trg_recalc_score` (L624-626) intact.

(B) Trigger monotone (KM-01) — copier ce DDL EXACT dans les deux fichiers :
```sql
CREATE OR REPLACE FUNCTION verifier_km_monotone()
RETURNS TRIGGER AS $$
DECLARE
  v_moto_km    INTEGER;
  v_last_reset TIMESTAMPTZ;
  v_max_releve INTEGER;
  v_km_actuel  INTEGER;
BEGIN
  -- Bypass total pour un changement de compteur explicite (gate PRO+ fait côté app).
  -- Démarre une chaîne monotone FRAÎCHE : les relevés suivants ne seront comparés
  -- qu'aux relevés postérieurs à ce remplacement.
  IF NEW.type_evenement = 'remplacement_compteur' THEN
    RETURN NEW;
  END IF;

  SELECT km INTO v_moto_km FROM motos WHERE id = NEW.moto_id;

  SELECT MAX(created_at) INTO v_last_reset
    FROM releves_km
    WHERE moto_id = NEW.moto_id AND type_evenement = 'remplacement_compteur';

  SELECT MAX(km) INTO v_max_releve
    FROM releves_km
    WHERE moto_id = NEW.moto_id
      AND (v_last_reset IS NULL OR created_at >= v_last_reset);

  -- NULL-safe baseline (Pitfall A) : sans GREATEST(v_moto_km, ...), le tout premier
  -- releve de CHAQUE moto prod existante (releves_km vide mais motos.km déjà peuplé)
  -- passerait quelle que soit sa valeur, car MAX sur 0 ligne = NULL et "NEW.km < NULL"
  -- vaut NULL (faux) en PL/pgSQL. Après un remplacement, v_moto_km reflète déjà le
  -- nouveau compteur bas (posé par trg_sync_moto_km), donc GREATEST reste correct.
  v_km_actuel := GREATEST(COALESCE(v_moto_km, 0), COALESCE(v_max_releve, 0));

  IF NEW.km < v_km_actuel THEN
    INSERT INTO releves_km_rejets
      (moto_id, garage_id, acteur_type, acteur_id, km_tente, km_actuel, created_at)
    VALUES
      (NEW.moto_id, NEW.garage_id, NEW.acteur_type, NEW.acteur_id, NEW.km, v_km_actuel, NOW());
    RETURN NULL;  -- annule UNIQUEMENT cet insert — pas d'exception, le log survit (Pitfall B)
  END IF;

  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_verifier_km_monotone
  BEFORE INSERT ON releves_km
  FOR EACH ROW EXECUTE FUNCTION verifier_km_monotone();
```

(C) Trigger de sync (KM-04) — assignation directe, PAS de GREATEST (le BEFORE trigger garantit déjà la sûreté ; un clamp masquerait un bug et casserait le reset après remplacement compteur) :
```sql
CREATE OR REPLACE FUNCTION sync_moto_km_depuis_releve()
RETURNS TRIGGER AS $$
BEGIN
  UPDATE motos SET km = NEW.km, updated_at = NOW() WHERE id = NEW.moto_id;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_sync_moto_km
  AFTER INSERT ON releves_km
  FOR EACH ROW EXECUTE FUNCTION sync_moto_km_depuis_releve();
```

(D) RLS default-deny documenté (D-01) — dans les deux fichiers, en reproduisant la convention de commentaire de schema.sql L689-702 :
```sql
ALTER TABLE consommables         ENABLE ROW LEVEL SECURITY;
ALTER TABLE photos_consommables  ENABLE ROW LEVEL SECURITY;
ALTER TABLE releves_km           ENABLE ROW LEVEL SECURITY;
ALTER TABLE releves_km_rejets    ENABLE ROW LEVEL SECURITY;
-- Phase 23 (v1.6) : RLS activé SANS policy explicite, INTENTIONNEL — même pattern que
-- garage_users/client_device_tokens/push_send_log (Phase 19) et les 4 tables Gap B (Phase 21).
-- Default-deny pour anon/authenticated ; seul service_role (utilisé par supabase.js) lit/écrit.
-- Toute l'autorisation réelle (requireRole() + ownership via moto_id/garage_id) vit dans
-- motokey-api.js, ajoutée en Phase 25 avec les endpoints HTTP — Phase 23 livre le schéma seul,
-- aucun chemin client-atteignable vers ces tables n'existe encore.
```
Dans `schema.sql`, placer ces ALTER dans la section RLS existante (après L688, avec les autres tables RLS-no-policy). Ne créer AUCUNE CREATE POLICY pour ces 4 tables.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('schema.sql','utf8'); const m=require('fs').readFileSync('sql/migrations/23_consommables_km.sql','utf8'); if(s.includes('CREATE TRIGGER trg_update_km'))throw new Error('trg_update_km encore dans schema.sql'); if(s.includes('FUNCTION update_moto_km'))throw new Error('update_moto_km encore dans schema.sql'); for(const f of [['schema.sql',s],['migration',m]]){const x=f[1]; if(!x.includes('verifier_km_monotone'))throw new Error(f[0]+' manque verifier_km_monotone'); if(!x.includes('RETURN NULL'))throw new Error(f[0]+' manque RETURN NULL'); if(x.includes('RAISE EXCEPTION')&&x.indexOf('verifier_km_monotone')<x.indexOf('RAISE EXCEPTION')&&x.indexOf('RAISE EXCEPTION')<x.indexOf('trg_verifier_km_monotone'))throw new Error(f[0]+' RAISE EXCEPTION dans le trigger monotone'); if(!x.includes('AFTER INSERT ON releves_km'))throw new Error(f[0]+' manque trg_sync'); if(!x.includes('ENABLE ROW LEVEL SECURITY')||!x.includes('releves_km_rejets'))throw new Error(f[0]+' RLS incomplet');} if(m.includes('DROP TRIGGER IF EXISTS trg_update_km')===false)throw new Error('migration manque DROP trg_update_km'); console.log('OK triggers+rls')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TRIGGER trg_update_km" schema.sql` == 0 ET `grep -c "FUNCTION update_moto_km" schema.sql` == 0 (clamp legacy retiré)
    - `grep "DROP TRIGGER IF EXISTS trg_update_km ON interventions" sql/migrations/23_consommables_km.sql` présent ET `grep "DROP FUNCTION IF EXISTS update_moto_km" sql/migrations/23_consommables_km.sql` présent
    - `grep -c "trg_recalc_score" schema.sql` >= 1 (trigger score conservé, non touché)
    - `verifier_km_monotone` présent dans les deux fichiers, contient `GREATEST(COALESCE(v_moto_km, 0)` et `RETURN NULL` (pas de `RAISE EXCEPTION` dans le corps de cette fonction)
    - `BEFORE INSERT ON releves_km` et `AFTER INSERT ON releves_km` présents dans les deux fichiers
    - `sync_moto_km_depuis_releve` contient `SET km = NEW.km` (assignation directe) et NE contient PAS `GREATEST` sur son UPDATE
    - Les 4 tables ont `ENABLE ROW LEVEL SECURITY` + le commentaire "INTENTIONNEL" ; `grep -c "CREATE POLICY" ` ne référence AUCUNE des 4 nouvelles tables
    - `node --check` non applicable (SQL) ; le bootstrap réel est vérifié en 23-04
  </acceptance_criteria>
  <done>Le trigger monotone (rejet+log via RETURN NULL, NULL-safe, chaîne fraîche post-remplacement), le trigger de sync (assignation directe), la suppression de trg_update_km, et le RLS-no-policy documenté existent identiques dans la migration et schema.sql, dans le même commit.</done>
</task>

</tasks>

<verification>
- `node -e` d'assertions ci-dessus verts pour les deux tâches.
- Parité migration ↔ schema.sql : les blocs CREATE TABLE / trigger / RLS sont byte-équivalents dans les sections concernées (revue diff manuelle avant commit — Anti-Pattern 2).
- Le bootstrap réel contre un projet neuf est délégué au plan 23-04 (dépend de FRESH_DB_URL, checkpoint plan 23-02).
</verification>

<success_criteria>
- 4 tables + 2 triggers + suppression trg_update_km + RLS documenté présents dans la migration ET schema.sql (même commit).
- CHECK 9-types sur type_consommable (CONSO-02).
- Trigger monotone NULL-safe avec RETURN NULL et chaîne fraîche post-remplacement (KM-01).
- motos.km synchronisé uniquement via trg_sync_moto_km (KM-04, plus de clamp legacy).
</success_criteria>

<output>
Après complétion, créer `.planning/phases/23-sch-ma-anti-fraude-km-au-niveau-db/23-01-SUMMARY.md`.
</output>
