---
phase: 23-sch-ma-anti-fraude-km-au-niveau-db
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - supabase.js
autonomous: true
requirements: [KM-04]

must_haves:
  truths:
    - "RelevesKm.enregistrer() est l'unique fonction partagée qui insère dans releves_km et normalise succès/rejet"
    - "Motos.update() ne peut plus écrire km (retiré de la liste allowed) — pneu_av/ar/km_montage intacts"
    - "OrdresReparation.cloturer() route km_sortie via RelevesKm.enregistrer() au lieu d'un UPDATE motos.km direct, et surface le rejet au lieu du skip silencieux"
    - "Interventions.create() reste explicitement découplé (D-05) : commentaire documentant que son km est un historique qui ne touche jamais motos.km — AUCUN changement de comportement"
  artifacts:
    - path: "supabase.js"
      provides: "Objet RelevesKm + 2 chemins fermés + 1 chemin documenté"
      contains: "RelevesKm"
  key_links:
    - from: "OrdresReparation.cloturer()"
      to: "RelevesKm.enregistrer()"
      via: "remplacement du bloc UPDATE motos.km direct"
      pattern: "RelevesKm.enregistrer"
    - from: "Motos.update()"
      to: "liste allowed sans 'km'"
      via: "retrait de 'km' du tableau allowed"
      pattern: "allowed = \\['pneu_av'"
---

<objective>
Fermer les chemins d'écriture km dans `supabase.js` (KM-04) : créer la fonction de validation partagée `RelevesKm.enregistrer()`, retirer `km` de `Motos.update()`, router `OrdresReparation.cloturer()` via la fonction partagée (rejet surfacé, plus de skip silencieux), et documenter explicitement le découplage de `Interventions.create()` (D-05, sans changer son comportement).

Purpose: après cette phase, `motos.km` n'est plus jamais écrit directement — tout passe par `releves_km` (source de vérité) + le trigger de sync. Plus aucun bypass applicatif.
Output: `supabase.js` modifié.
</objective>

<context>
@.planning/phases/23-sch-ma-anti-fraude-km-au-niveau-db/23-CONTEXT.md
@.planning/phases/23-sch-ma-anti-fraude-km-au-niveau-db/23-RESEARCH.md

<interfaces>
<!-- Code actuel vérifié cette passe (line numbers à re-confirmer à l'implémentation) -->
Helper insert partagé — supabase.js L78-82 :
  `async function insert(table, payload)` fait `.insert(payload).select().single()` et THROW sur erreur
  (y compris PGRST116 "no rows"). => RelevesKm.enregistrer NE DOIT PAS utiliser ce helper pour
  releves_km : il doit appeler supabase.from('releves_km').insert(...).select().single() en direct
  et inspecter error.code === 'PGRST116' (ligne annulée par le trigger BEFORE INSERT).
  Précédent de gestion PGRST116 : loginGarage (hardening L4 v2, 07/05/2026).

Motos.update() — supabase.js ~L350-356 :
  `const allowed = ['km','pneu_av','pneu_ar','pneu_km_montage','couleur','photo_url'];`

Interventions.create() — supabase.js ~L397-412 :
  insert('interventions', { ..., km: payload.km, ... }) — km jamais comparé/propagé à motos.km.

OrdresReparation.cloturer() — supabase.js ~L893-923 :
  garde interne conservée : `if (km_sortie < (or.km_entree||0)) throw` (invariant OR, orthogonal).
  À REMPLACER : le bloc final
    `const { data: moto } = await supabase.from('motos').select('km')...`
    `if (moto && km_sortie > moto.km) { await supabase.from('motos').update({ km: km_sortie })... }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter l'objet RelevesKm + enregistrer() (validation partagée)</name>
  <files>supabase.js</files>
  <read_first>
    - supabase.js L78-92 (helpers insert/update partagés — comprendre pourquoi ne PAS réutiliser insert() pour releves_km)
    - supabase.js L384-425 (structure d'un objet entité : Interventions — style à imiter)
    - .planning/phases/23-sch-ma-anti-fraude-km-au-niveau-db/23-RESEARCH.md Pattern 3 (forme de retour {accepted, ...})
  </read_first>
  <action>
Ajouter dans `supabase.js` un nouvel objet entité `RelevesKm` (exporté au même endroit que les autres entités — vérifier le bloc `module.exports` et l'ajouter à la liste exportée). Style cohérent avec les objets existants (`Motos`, `Interventions`, `OrdresReparation`).

Fonction principale :
```javascript
const RelevesKm = {
  // Unique point d'écriture km. Le trigger DB BEFORE INSERT est le vrai gate :
  // si le km régresse, le trigger annule la ligne (0 row => PGRST116) et journalise
  // dans releves_km_rejets. On normalise succès/rejet en un retour stable.
  async enregistrer(garage_id, moto_id, { km, type_evenement = 'lecture', acteur_type, acteur_id, note = null, photo_url = null }) {
    if (!moto_id) throw new Error('[RelevesKm.enregistrer] moto_id requis');
    if (km === undefined || km === null || isNaN(parseInt(km))) throw new Error('[RelevesKm.enregistrer] km numérique requis');
    if (!acteur_type || !acteur_id) throw new Error('[RelevesKm.enregistrer] acteur_type + acteur_id requis (jamais anonyme)');

    const payload = {
      moto_id, garage_id: garage_id || null,
      km: parseInt(km), type_evenement,
      acteur_type, acteur_id, note, photo_url
    };

    const { data, error } = await supabase.from('releves_km').insert(payload).select().single();

    if (error) {
      // PGRST116 = 0 ligne renvoyée = ligne annulée par le trigger monotone (rejet).
      if (error.code === 'PGRST116') {
        // Récupérer le rejet fraîchement journalisé pour renvoyer km_actuel/km_tente.
        const { data: rejet } = await supabase.from('releves_km_rejets')
          .select('km_tente, km_actuel')
          .eq('moto_id', moto_id)
          .order('created_at', { ascending: false })
          .limit(1).maybeSingle();
        return { accepted: false, km_tente: rejet?.km_tente ?? parseInt(km), km_actuel: rejet?.km_actuel ?? null };
      }
      throw new Error(`[RelevesKm.enregistrer] ${error.message}`);
    }
    return { accepted: true, releve: data };
  }
};
```
Ne PAS utiliser le helper module-level `insert()` (il throw sur PGRST116, ce qui masquerait le rejet). Ajouter `RelevesKm` à l'objet exporté par `module.exports`.
  </action>
  <verify>
    <automated>node --check supabase.js && node -e "const db=require('./supabase.js'); if(!db.RelevesKm||typeof db.RelevesKm.enregistrer!=='function')throw new Error('RelevesKm.enregistrer non exporté'); console.log('OK RelevesKm exporté')"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check supabase.js` passe
    - `require('./supabase.js').RelevesKm.enregistrer` est une fonction (exportée)
    - `grep "error.code === 'PGRST116'" supabase.js` présent (gestion de la ligne annulée par le trigger)
    - `grep "accepted: false" supabase.js` ET `grep "accepted: true" supabase.js` présents (retour normalisé)
    - `enregistrer` insère via `supabase.from('releves_km').insert` en direct (PAS via le helper `insert(` qui throw) — `grep "from('releves_km').insert" supabase.js` présent
    - Validation "jamais anonyme" : `grep "acteur_type" supabase.js` présent avec un throw si absent
  </acceptance_criteria>
  <done>RelevesKm.enregistrer() existe, exporté, gère succès et rejet (PGRST116) en un retour stable {accepted,...}, sans réutiliser le helper insert() qui masquerait le rejet.</done>
</task>

<task type="auto">
  <name>Task 2: Fermer les 3 chemins (Motos.update strip km, cloturer route, Interventions.create documenté)</name>
  <files>supabase.js</files>
  <read_first>
    - supabase.js L350-356 (Motos.update, liste allowed)
    - supabase.js L397-412 (Interventions.create, champ km)
    - supabase.js L893-923 (OrdresReparation.cloturer, bloc UPDATE motos.km direct à remplacer)
    - .planning/phases/23-sch-ma-anti-fraude-km-au-niveau-db/23-CONTEXT.md (D-05 : Interventions.create NE route PAS vers la validation monotone)
  </read_first>
  <action>
Trois modifications ciblées dans `supabase.js` (éditions directes, jamais via PowerShell/sed/awk — règle CLAUDE.md) :

(1) `Motos.update()` — retirer `'km'` de `allowed`. Nouvelle ligne EXACTE :
```javascript
    const allowed = ['pneu_av','pneu_ar','pneu_km_montage','couleur','photo_url'];
```
Ajouter juste au-dessus un commentaire : `// km retiré (KM-04) : le km ne s'écrit plus que via RelevesKm.enregistrer() → releves_km → trigger de sync. pneu_* restent (retrait = Phase 27).`

(2) `OrdresReparation.cloturer()` — CONSERVER la garde interne `if (km_sortie < (or.km_entree||0)) throw` (invariant OR, orthogonal à l'anti-fraude moto). REMPLACER uniquement le bloc final :
```javascript
    const { data: moto } = await supabase.from('motos').select('km').eq('id', or.moto_id).single();
    if (moto && km_sortie > moto.km) {
      await supabase.from('motos').update({ km: km_sortie }).eq('id', or.moto_id);
    }
```
par :
```javascript
    // KM-04 : le km de sortie représente un relevé compteur "live" — il passe par la
    // validation partagée (releves_km + trigger). Un rejet est surfacé, plus de skip silencieux.
    const releveKm = await RelevesKm.enregistrer(garage_id, or.moto_id, {
      km: km_sortie, type_evenement: 'lecture', acteur_type: 'garage', acteur_id: garage_id
    });
    // releveKm.accepted === false => km_sortie régresse vs historique ; on l'expose dans la réponse.
```
Puis inclure `km_releve: releveKm` (ou `km_rejete: !releveKm.accepted` + `km_actuel: releveKm.km_actuel`) dans l'objet de réponse retourné par `cloturer()`, pour que l'appelant voie le rejet. NE PAS throw sur un rejet (la clôture de l'OR reste valide ; seule la propagation km est refusée + journalisée).

(3) `Interventions.create()` — AUCUN changement de comportement (D-05). Ajouter au-dessus du champ `km: payload.km,` un commentaire explicite :
```javascript
      // D-05 : le km d'intervention est un HISTORIQUE découplé — métadonnée volontairement
      // NON routée vers RelevesKm/le ratchet monotone (permet la saisie d'entretien passé,
      // km < km actuel de la moto). Ne touche JAMAIS motos.km (trg_update_km supprimé en 23-01).
      km:              payload.km,
```
Ne PAS appeler RelevesKm.enregistrer() ici, ne PAS ajouter de rejet sur km inférieur — ce serait une violation de D-05 qui casserait la saisie d'historique légitime.

Vérifier `node --check supabase.js` avant fin.
  </action>
  <verify>
    <automated>node --check supabase.js && node -e "const c=require('fs').readFileSync('supabase.js','utf8'); if(/allowed\s*=\s*\['km'/.test(c))throw new Error('km encore dans allowed de Motos.update'); if(!/allowed\s*=\s*\['pneu_av'/.test(c))throw new Error('allowed Motos.update mal formé'); if(!c.includes('RelevesKm.enregistrer(garage_id, or.moto_id'))throw new Error('cloturer ne route pas via RelevesKm'); if(c.includes(\"update({ km: km_sortie }).eq('id', or.moto_id)\"))throw new Error('cloturer écrit encore motos.km en direct'); if(!/D-05/.test(c))throw new Error('Interventions.create manque commentaire D-05'); console.log('OK 3 chemins')"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check supabase.js` passe
    - `grep -c "allowed = \['km'" supabase.js` == 0 ET `grep "allowed = \['pneu_av','pneu_ar','pneu_km_montage','couleur','photo_url'\]" supabase.js` présent (Motos.update ne peut plus écrire km ; pneu_* intacts)
    - `grep "RelevesKm.enregistrer(garage_id, or.moto_id" supabase.js` présent (cloturer route via la fonction partagée)
    - `grep -c "update({ km: km_sortie }).eq('id', or.moto_id)" supabase.js` == 0 (plus d'UPDATE motos.km direct dans cloturer)
    - La garde OR interne est conservée : `grep "km_sortie < (or.km_entree" supabase.js` présent
    - `grep "D-05" supabase.js` présent au-dessus du champ km d'Interventions.create ; `Interventions.create` n'appelle PAS `RelevesKm.enregistrer` (grep : aucune occurrence de RelevesKm dans le corps de Interventions.create)
  </acceptance_criteria>
  <done>Motos.update() ne peut plus écrire km, cloturer() route km_sortie via RelevesKm.enregistrer() (rejet surfacé, plus de skip silencieux), Interventions.create() est documenté découplé sans changement de comportement (D-05). node --check vert.</done>
</task>

</tasks>

<verification>
- `node --check supabase.js` vert.
- Grep confirme : km absent de allowed, cloturer route via RelevesKm, Interventions.create inchangé/documenté, garde OR conservée.
- Vérification comportementale DB (trigger) déléguée au plan 23-04 (script de test + bootstrap).
</verification>

<success_criteria>
- Un seul chemin d'écriture km applicatif : RelevesKm.enregistrer() → releves_km → trigger de sync.
- Motos.update() et OrdresReparation.cloturer() fermés ; Interventions.create() explicitement découplé (D-05).
- Aucun UPDATE motos.km direct restant dans les 3 chemins.
</success_criteria>

<output>
Après complétion, créer `.planning/phases/23-sch-ma-anti-fraude-km-au-niveau-db/23-03-SUMMARY.md`.
</output>
