---
phase: 30-audit-sch-ma-or-statut
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/introspect-or-statut.js
  - .planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md
autonomous: false
requirements: [MIGR-02]
user_setup: []

must_haves:
  truths:
    - "Exécuter `node scripts/introspect-or-statut.js` imprime la liste réelle et vivante des valeurs de l'enum or_statut de la prod (issue de PostgREST OpenAPI, pas inférée de la migration 08 ni du code)"
    - "Le script imprime aussi les valeurs de or_tache_statut (side-finding, Open Question 5 de la recherche) sans faire échouer la phase"
    - "30-FINDINGS.md liste chaque valeur réelle de or_statut avec son mapping vers l'une des 7 cibles (brouillon, envoye, accepte, en_cours, termine, facture, refuse) OU un marqueur explicite de question ouverte"
    - "30-FINDINGS.md documente un plan de réconciliation ordonné : recommandation ENUM-patché vs TEXT+CHECK avec justification, contraintes transactionnelles ALTER TYPE ADD VALUE (erreur 55P04), impossibilité de DROP VALUE"
    - "30-FINDINGS.md flague explicitement pour Mehdi les questions produit non résolues (mapping de valide_client, attente, annule) au lieu de les trancher silencieusement"
    - "Aucun fichier DDL touchant or_statut n'est écrit ni committé pendant cette phase (git log reste vide sur ce motif)"
  artifacts:
    - path: "scripts/introspect-or-statut.js"
      provides: "Requête live PostgREST-OpenAPI des valeurs enum or_statut/or_tache_statut de la prod"
      min_lines: 40
      contains: "statut.enum"
    - path: ".planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md"
      provides: "Table de réconciliation + plan ordonné + questions ouvertes produit"
      contains: "Reconciliation"
  key_links:
    - from: "scripts/introspect-or-statut.js"
      to: "prod Supabase PostgREST OpenAPI"
      via: "fetch(SUPABASE_URL + '/rest/v1/?apikey=' + key)"
      pattern: "rest/v1"
    - from: "scripts/introspect-or-statut.js"
      to: "definitions.ordres_reparation.properties.statut.enum"
      via: "parse OpenAPI spec.definitions"
      pattern: "ordres_reparation"
    - from: ".planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md"
      to: "sortie live du script"
      via: "valeurs collées depuis l'exécution réelle"
      pattern: "or_statut"
---

<objective>
Auditer l'état RÉEL de l'enum Postgres `or_statut` en prod (requête live, pas inférence) et produire un plan de réconciliation documenté vers les 7 statuts cibles du cycle unifié (`brouillon, envoye, accepte, en_cours, termine, facture, refuse`), AVANT que la Phase 31 n'écrive la moindre ligne de DDL.

Cette phase est READ-ONLY par conception : ZÉRO changement de code ou de schéma. Elle ne fait que lire la prod via l'endpoint PostgREST OpenAPI existant (aucun nouveau credential, réutilise `SUPABASE_URL`/`SUPABASE_SECRET_KEY` de `.env`) et produire deux livrables : un script d'introspection versionné et un document de constats/réconciliation.

Contexte de la dérive (STATE.md, 2026-07-17) : le fichier tracké `migrations/08-livraison-3a-ordres-reparation.sql:26-28` déclare `or_statut` avec 4 valeurs seulement (`brouillon, en_cours, termine, annule`), alors que le code backend (`supabase.js:913-921` `_OR_TRANS`, mirroir RAM `motokey-api.js:2982-2990`) utilise déjà 7 valeurs (`brouillon, valide_client, en_cours, attente, termine, facture, annule`). Le code prouve une borne INFÉRIEURE des valeurs écrites, pas le contenu exact du catalogue — d'où l'obligation de la requête live (une valeur fantôme ajoutée manuellement au Dashboard ne se verrait pas dans le code, exactement le pattern des colonnes fantômes du Gap A, migrations 20/21).

Purpose: Fermer la dernière incertitude schéma avant la migration DDL de la Phase 31, en suivant le playbook déjà validé 3 fois sur ce projet (Gap A colonnes, Gap B tables, introspection live Phase 19/22). Le mapping de `valide_client`/`attente`/`annule` vers les 7 cibles n'a PAS d'équivalent 1:1 propre — ce sont des décisions PRODUIT à remonter à Mehdi, pas à trancher par l'exécutant.
Output: `scripts/introspect-or-statut.js` + `.planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md`.
</objective>

<execution_context>
@C:/motokey-api/.claude/get-shit-done/workflows/execute-plan.md
@C:/motokey-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-audit-sch-ma-or-statut/30-RESEARCH.md
@.planning/phases/30-audit-sch-ma-or-statut/30-VALIDATION.md

<interfaces>
<!-- Contrats extraits de la codebase. L'exécutant les utilise directement — pas d'exploration nécessaire. -->

Pattern d'introspection à réutiliser — scripts/introspect-schema.js (existant, Phase 19) :
```javascript
// Charge .env pour SUPABASE_URL / SUPABASE_SECRET_KEY
require('dotenv').config({ path: require('path').join(__dirname, '..', '.env') });

async function introspect(url, key) {
  // ne JAMAIS logger la cle — seulement le host via new URL(url).host
  const res = await fetch(url + '/rest/v1/?apikey=' + key, {
    headers: { Authorization: 'Bearer ' + key }
  });
  if (!res.ok) throw new Error(`PostgREST OpenAPI request failed: HTTP ${res.status} ${res.statusText}`);
  const spec = await res.json();
  const definitions = spec.definitions || {};
  // ... chaque def a def.properties[col].enum (array) pour les colonnes enum-typees
}
// cle lue ainsi : process.env.SUPABASE_SECRET_KEY || process.env.SUPABASE_SERVICE_KEY
```

Emplacement des valeurs enum dans le spec OpenAPI PostgREST :
- `spec.definitions.ordres_reparation.properties.statut.enum` -> array des valeurs live de or_statut
- `spec.definitions.ordres_reparation.properties.statut.format` -> chaine type "public.or_statut"
- `spec.definitions.or_taches.properties.statut.enum` -> array des valeurs live de or_tache_statut

Source de verite "code" (borne INFERIEURE, a confronter au live) — supabase.js:913-921 :
```javascript
const _OR_TRANS = {
  brouillon:     ['valide_client', 'annule'],
  valide_client: ['brouillon', 'en_cours', 'annule'],
  en_cours:      ['attente', 'annule'],          // termine via /cloturer
  attente:       ['en_cours', 'annule'],
  termine:       ['en_cours', 'annule'],          // facture via /facturer
  facture:       ['annule'],
  annule:        []
};
```
-> 7 valeurs prouvees writables : brouillon, valide_client, en_cours, attente, termine, facture, annule.

Declaration TRACKEE mais PERIMEE — migrations/08-livraison-3a-ordres-reparation.sql:26-28 :
```sql
CREATE TYPE or_statut AS ENUM ('brouillon','en_cours','termine','annule');  -- 4 valeurs
CREATE TYPE or_tache_statut AS ENUM ('a_faire','en_cours','fait');
```

7 statuts CIBLES du cycle unifie v1.8 (REQUIREMENTS MIGR-02) :
`brouillon, envoye, accepte, en_cours, termine, facture, refuse`

Cross-check SQL (Pattern 1 de la recherche) a faire executer par Mehdi au Dashboard :
```sql
SELECT enumlabel, enumsortorder
FROM pg_enum
WHERE enumtypid = 'or_statut'::regtype
ORDER BY enumsortorder;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer scripts/introspect-or-statut.js (requete live des valeurs enum)</name>
  <files>scripts/introspect-or-statut.js</files>
  <read_first>
    - scripts/introspect-schema.js (pattern introspect() a copier : auth, host-masking, parse spec.definitions, ne jamais logger la cle)
    - .planning/phases/30-audit-sch-ma-or-statut/30-RESEARCH.md (Pattern 1 : Live PostgREST-OpenAPI Enum Introspection)
  </read_first>
  <action>
    Creer un NOUVEAU fichier `scripts/introspect-or-statut.js` (ne PAS modifier introspect-schema.js), single-purpose, phase-scoped. Il doit :

    1. Charger `.env` : `require('dotenv').config({ path: require('path').join(__dirname, '..', '.env') });`
    2. Lire `const url = process.env.SUPABASE_URL;` et `const key = process.env.SUPABASE_SECRET_KEY || process.env.SUPABASE_SERVICE_KEY;`. Si absents : `console.error` en francais et `process.exitCode = 1; return;`.
    3. Ne JAMAIS imprimer la cle. Logger uniquement le host via `new URL(url).host` (copier le garde-fou de introspect-schema.js).
    4. `fetch(url + '/rest/v1/?apikey=' + key, { headers: { Authorization: 'Bearer ' + key } })`, verifier `res.ok`, sinon throw `PostgREST OpenAPI request failed: HTTP ${res.status} ${res.statusText}`.
    5. `const spec = await res.json();` puis `const defs = spec.definitions || {};`.
    6. Extraire et imprimer :
       - `defs.ordres_reparation.properties.statut.enum` (array) et `.format` -> label "or_statut live values:" et "format:"
       - `defs.or_taches.properties.statut.enum` (array) et `.format` -> label "or_tache_statut live values:" (side-finding, Open Question 5 — NON bloquant)
    7. Garder chaque acces defensif (`?.`) : si `defs.ordres_reparation` est absent, imprimer un message explicite "definition ordres_reparation introuvable dans le spec OpenAPI" plutot que de crasher sur un TypeError.
    8. Calculer et imprimer la comparaison avec les 7 cibles : pour chaque valeur cible de `['brouillon','envoye','accepte','en_cours','termine','facture','refuse']`, imprimer `[PRESENT]` ou `[MANQUANT]` selon sa presence dans le tableau live. Et pour chaque valeur live NON dans les 7 cibles, imprimer `[HORS-CIBLE] <valeur>` (ex. valide_client, attente, annule).
    9. Assertion de sortie : si le tableau live de or_statut contient >= 7 valeurs, `process.exitCode = 0` ; sinon `process.exitCode = 1` avec un message expliquant que le spec renvoie moins que les 7 valeurs prouvees par le code (indice de cache PostgREST perime — Pitfall 1 de la recherche).
    10. Wrapper `main().catch(err => { console.error('Erreur fatale :', err.message); process.exitCode = 1; });`.

    Messages utilisateur en francais (convention projet). Commentaires de code OK en francais.
  </action>
  <verify>
    <automated>node --check scripts/introspect-or-statut.js && node scripts/introspect-or-statut.js</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/introspect-or-statut.js` retourne vrai
    - `node --check scripts/introspect-or-statut.js` passe (syntaxe valide)
    - `grep -q "statut.enum" scripts/introspect-or-statut.js` (le script lit bien le champ enum)
    - `grep -q "ordres_reparation" scripts/introspect-or-statut.js`
    - `grep -q "or_taches" scripts/introspect-or-statut.js` (side-finding or_tache_statut couvert)
    - `grep -q "rest/v1" scripts/introspect-or-statut.js`
    - Le script n'imprime JAMAIS la cle : `grep -nE "console\.(log|error).*(key|SECRET|apikey)" scripts/introspect-or-statut.js` ne retourne aucune ligne qui imprime la valeur de la cle
    - `node scripts/introspect-or-statut.js` imprime une ligne contenant "or_statut live values:" suivie d'un tableau non vide de >= 7 valeurs
  </acceptance_criteria>
  <done>Le script existe, passe node --check, s'execute contre la prod et imprime la liste live reelle de or_statut (>=7 valeurs) + or_tache_statut + le diff PRESENT/MANQUANT/HORS-CIBLE vs les 7 cibles, sans jamais fuiter la cle.</done>
</task>

<task type="auto">
  <name>Task 2: Rediger 30-FINDINGS.md (valeurs live + table de reconciliation + questions ouvertes)</name>
  <files>.planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md</files>
  <read_first>
    - .planning/phases/30-audit-sch-ma-or-statut/30-RESEARCH.md (Code Examples : Reconciliation Table Template ; Common Pitfalls 2/3/4 ; Open Questions 2/3/4/5)
    - sql/migrations/20_garages_undocumented_columns.sql (format de doc de derive : item / origine / verdict-action — a imiter)
    - migrations/08-livraison-3a-ordres-reparation.sql (declaration tracke perimee, lignes 26-35)
  </read_first>
  <action>
    Creer `.planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md` (redige en francais, c'est un document destine a Mehdi). Utiliser la SORTIE REELLE de `node scripts/introspect-or-statut.js` (Task 1) — ne PAS recopier les valeurs inferees du code comme si elles etaient verifiees.

    Structure obligatoire (titres de section exacts) :

    1. `## Valeurs live de or_statut (source : PostgREST OpenAPI, <date>)` — coller le tableau exact renvoye par le script, avec le host prod confirme. Mentionner la methode (requete live, pas inference). Inclure aussi les valeurs live de `or_tache_statut` en side-finding (Open Question 5), en precisant qu'il est hors scope MIGR-02 mais audite gratuitement dans la meme passe.

    2. `## Ecart vs les 7 statuts cibles` — les 7 cibles sont `brouillon, envoye, accepte, en_cours, termine, facture, refuse`. Lister explicitement, a partir de la sortie du script :
       - Valeurs cibles MANQUANTES dans l'enum live (candidates a `ADD VALUE` ou a couvrir par TEXT+CHECK)
       - Valeurs live HORS-CIBLE (ex. `valide_client`, `attente`, `annule`) sans equivalent propre dans les 7
       - Toute valeur fantome (presente en live mais jamais referencee par `_OR_TRANS`) — si detectee, la traiter comme les colonnes fantomes du Gap A (verdict "origine indeterminee, non utilisee par le code").

    3. `## Reconciliation Table` — reproduire le squelette du template de la recherche (Code Examples), UNE LIGNE par valeur reelle live, colonnes : `Valeur live | Utilisee par le code ? | Statut cible | Action`. Remplacer les valeurs UNVERIFIED par les valeurs reelles du script. Pour chaque ligne sans mapping 1:1 propre, mettre l'action `DECISION PRODUIT REQUISE (voir Questions ouvertes)`.

    4. `## Plan de reconciliation ordonne` — documenter les DEUX options avec recommandation explicite :
       - Option A (ENUM patche) : ordre des `ALTER TYPE or_statut ADD VALUE ...`, avec la contrainte Postgres que la nouvelle valeur N'EST PAS utilisable dans la MEME transaction que son ajout (erreur 55P04) -> chaque `ADD VALUE` doit etre dans une migration/transaction separee AVANT toute migration qui ecrit des lignes avec cette valeur. Rappeler qu'il n'existe pas de `DROP VALUE` (impossible de retirer proprement `attente`/`annule` si l'ENUM est conserve).
       - Option B (RECOMMANDEE) : remplacer l'ENUM `or_statut` par une colonne `TEXT + CHECK`, coherent avec `devis.statut` (deja `TEXT NOT NULL CHECK`, schema.sql:344) que la Phase 31 fusionne de toute facon. Justifier : au volume actuel (6 OR, 8 devis, STATE.md 2026-07-17) la difference storage/perf ENUM vs TEXT est immaterielle ; le facteur decisif est la flexibilite operationnelle (`ALTER TABLE ... DROP/ADD CONSTRAINT` en une transaction ordinaire, pas de dance ADD VALUE/rename/recreate-type).
       - Marquer clairement la recommandation comme le point de depart de la Phase 31, sujette a l'arbitrage produit de la Task 3.

    5. `## Questions ouvertes — DECISIONS PRODUIT POUR MEHDI` — lister explicitement, sans les trancher :
       - `valide_client` -> `envoye` OU `accepte` ? (le code montre `brouillon -> valide_client -> en_cours` ; le nom suggere une validation/acceptation, pas un envoi ; `devis.statut` a deja `envoye` ET `accepte` separes -> hypothese : `valide_client` s'effondre sur `accepte`, `envoye` est un etat neuf).
       - `attente` (on-hold, detour depuis en_cours avec `attente_motif`) : replier en flag sur `en_cours`, ou garder comme statut supplementaire hors des 7 ?
       - `annule` (annulation garage) vs `refuse` (refus client) : semantiquement distincts — mapper `annule -> refuse`, ou garder `annule` comme 8e statut ? Ne PAS presupposer.
       - Formuler chaque question de facon qu'une reponse binaire/courte de Mehdi suffise.

    6. `## Gate Phase 31` — noter explicitement : aucune ecriture DDL sur `or_statut` ne doit avoir lieu avant que ce document existe et que les questions ci-dessus soient tranchees ; ce document est l'entree de la Phase 31.

    NE PAS creer de fichier `.sql` ni de migration dans cette phase.
  </action>
  <verify>
    <automated>test -f .planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md && grep -q "Reconciliation Table" .planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md && grep -qi "DECISIONS PRODUIT\|Questions ouvertes" .planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md` retourne vrai
    - Le doc contient les 6 sections exactes : "Valeurs live de or_statut", "Ecart vs les 7 statuts cibles", "Reconciliation Table", "Plan de reconciliation ordonne", "Questions ouvertes", "Gate Phase 31" (verifiable via grep de chaque titre)
    - La section Reconciliation Table contient UNE ligne par valeur live reellement renvoyee par le script (pas les valeurs UNVERIFIED du template) — verifiable en croisant avec la sortie de `node scripts/introspect-or-statut.js`
    - `grep -q "55P04\|ADD VALUE" .planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md` (contrainte transactionnelle documentee)
    - `grep -q "TEXT + CHECK\|TEXT+CHECK\|TEXT NOT NULL CHECK" .planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md` (option B documentee)
    - `grep -qi "valide_client" .planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md && grep -qi "attente" .planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md && grep -qi "annule" .planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md` (les 3 questions produit flaguees)
    - Aucun fichier `.sql`/migration cree : `git status --porcelain sql/migrations migrations | grep -E '\.sql$'` reste vide
  </acceptance_criteria>
  <done>Le doc de constats existe avec ses 6 sections, la table de reconciliation reflete les valeurs live reelles (pas le template), le plan ordonne documente les contraintes ENUM + la recommandation TEXT+CHECK, et les 3 questions produit (valide_client/attente/annule) sont flaguees pour Mehdi sans etre tranchees.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cross-check Dashboard + arbitrage des questions produit (Mehdi)</name>
  <files>.planning/phases/30-audit-sch-ma-or-statut/30-FINDINGS.md</files>
  <action>
    Checkpoint bloquant : Mehdi execute le cross-check pg_enum au Supabase Dashboard et tranche les 3 questions produit. L'executant met a jour 30-FINDINGS.md avec les resultats du cross-check et les decisions tranchees. Voir <what-built>, <how-to-verify> et <resume-signal> ci-dessous pour le detail des etapes.
  </action>
  <what-built>
    Un script d'introspection live (`scripts/introspect-or-statut.js`) qui a interroge la prod via PostgREST OpenAPI et imprime les valeurs reelles de l'enum `or_statut`, et un document `30-FINDINGS.md` contenant la table de reconciliation vers les 7 statuts cibles + un plan de migration ordonne + 3 questions produit non tranchees.
  </what-built>
  <how-to-verify>
    1. CROSS-CHECK (Pitfall 1 de la recherche — cache PostgREST potentiellement perime). Ouvrir Supabase Dashboard -> SQL Editor et executer :
       ```sql
       SELECT enumlabel, enumsortorder
       FROM pg_enum
       WHERE enumtypid = 'or_statut'::regtype
       ORDER BY enumsortorder;
       ```
       Comparer la liste retournee avec le tableau "or_statut live values" imprime par `node scripts/introspect-or-statut.js` et avec la section "Valeurs live de or_statut" de 30-FINDINGS.md. Elles doivent etre identiques. Si elles different : la source pg_enum fait foi, le noter dans le doc (cache PostgREST a rafraichir via `NOTIFY pgrst, 'reload schema';`).

    2. Relire 30-FINDINGS.md, section "Questions ouvertes — DECISIONS PRODUIT POUR MEHDI", et repondre aux 3 arbitrages (reponses courtes suffisent) :
       - `valide_client` devient `accepte` (hypothese recherche) ou `envoye` ?
       - `attente` : flag sur `en_cours`, ou statut supplementaire conserve hors des 7 ?
       - `annule` : mappe sur `refuse`, ou conserve comme statut distinct (annulation garage vs refus client) ?

    3. Confirmer qu'aucune ecriture DDL sur `or_statut` n'a eu lieu dans cette phase (la Phase 31 fera cela, sur la base de ce doc et de tes reponses).
  </how-to-verify>
  <verify>
    <automated>git log --oneline -- 'sql/migrations/*or_statut*' 'migrations/*or_statut*' | wc -l | grep -q '^0*$' || echo "ATTENTION: DDL or_statut detecte"</automated>
  </verify>
  <acceptance_criteria>
    - Mehdi a colle la sortie du cross-check pg_enum et confirme (ou signale) la concordance avec la sortie du script
    - Les 3 questions produit ont recu une reponse de Mehdi, reportee dans 30-FINDINGS.md (l'executant met a jour la section "Reconciliation Table" + "Questions ouvertes" avec les decisions tranchees apres reponse)
    - `git log --oneline -- 'sql/migrations/*or_statut*' 'migrations/*or_statut*'` reste vide (gate Phase 31 respecte : aucun DDL ecrit)
  </acceptance_criteria>
  <done>Le cross-check pg_enum concorde (ou l'ecart est note, pg_enum faisant foi), les 3 decisions produit sont tranchees et reportees dans 30-FINDINGS.md, et aucun DDL or_statut n'a ete ecrit.</done>
  <resume-signal>Colle la sortie du SQL pg_enum et reponds aux 3 questions produit (valide_client / attente / annule), ou signale un ecart entre pg_enum et le script.</resume-signal>
</task>

</tasks>

<verification>
- `node scripts/introspect-or-statut.js` s'execute contre la prod et imprime la liste live de `or_statut` (>= 7 valeurs), `or_tache_statut`, et le diff PRESENT/MANQUANT/HORS-CIBLE vs les 7 cibles.
- `30-FINDINGS.md` existe avec ses 6 sections, une table de reconciliation basee sur les valeurs live reelles, un plan de migration ordonne (contraintes ENUM + recommandation TEXT+CHECK), et les 3 questions produit flaguees.
- Cross-check pg_enum (Mehdi, Dashboard) concorde avec la sortie du script — sinon ecart note, pg_enum faisant foi.
- Aucun fichier DDL/`.sql` touchant `or_statut` cree pendant la phase (gate Phase 31).
</verification>

<success_criteria>
Les 4 criteres de succes de la Phase 30 (ROADMAP.md) sont TOUS vrais :
1. Les valeurs reelles de l'enum `or_statut` en prod sont listees via une requete live (`node scripts/introspect-or-statut.js`, cross-checkee pg_enum) et documentees dans 30-FINDINGS.md — pas une inference du code ou des migrations.
2. L'ecart entre valeurs reelles et les 7 statuts cibles (`brouillon, envoye, accepte, en_cours, termine, facture, refuse`) est explicite : chaque valeur manquante, hors-cible ou fantome est listee.
3. Un plan de reconciliation ordonne est documente (ordre des `ALTER TYPE ... ADD VALUE`, contrainte 55P04, absence de `DROP VALUE`, OU recommandation TEXT+CHECK avec justification).
4. Ce plan est la base de la Phase 31 — aucune ecriture DDL sur `or_statut` n'a eu lieu, les questions produit sont tranchees par Mehdi.
</success_criteria>

<output>
Apres completion, creer `.planning/phases/30-audit-sch-ma-or-statut/30-01-SUMMARY.md`.
</output>
